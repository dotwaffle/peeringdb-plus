// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/peeringdb-plus/ent/network"
	"github.com/dotwaffle/peeringdb-plus/ent/poc"
)

// Poc is the model entity for the Poc schema.
type Poc struct {
	config `json:"-"`
	// ID of the ent.
	// PeeringDB poc ID
	ID int `json:"id,omitempty"`
	// FK to network
	NetID *int `json:"net_id"`
	// Email address
	Email string `json:"email"`
	// Contact name
	Name string `json:"name"`
	// Phone number
	Phone string `json:"phone"`
	// Contact role
	Role string `json:"role"`
	// URL
	URL string `json:"url"`
	// Visibility level
	Visible string `json:"visible"`
	// PeeringDB creation timestamp
	Created time.Time `json:"created"`
	// PeeringDB last update timestamp
	Updated time.Time `json:"updated"`
	// Record status
	Status string `json:"status"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PocQuery when eager-loading is set.
	Edges        PocEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PocEdges holds the relations/edges for other nodes in the graph.
type PocEdges struct {
	// Network holds the value of the network edge.
	Network *Network `json:"network,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// NetworkOrErr returns the Network value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PocEdges) NetworkOrErr() (*Network, error) {
	if e.Network != nil {
		return e.Network, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: network.Label}
	}
	return nil, &NotLoadedError{edge: "network"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Poc) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case poc.FieldID, poc.FieldNetID:
			values[i] = new(sql.NullInt64)
		case poc.FieldEmail, poc.FieldName, poc.FieldPhone, poc.FieldRole, poc.FieldURL, poc.FieldVisible, poc.FieldStatus:
			values[i] = new(sql.NullString)
		case poc.FieldCreated, poc.FieldUpdated:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Poc fields.
func (_m *Poc) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case poc.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case poc.FieldNetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field net_id", values[i])
			} else if value.Valid {
				_m.NetID = new(int)
				*_m.NetID = int(value.Int64)
			}
		case poc.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case poc.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case poc.FieldPhone:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field phone", values[i])
			} else if value.Valid {
				_m.Phone = value.String
			}
		case poc.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = value.String
			}
		case poc.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case poc.FieldVisible:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field visible", values[i])
			} else if value.Valid {
				_m.Visible = value.String
			}
		case poc.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				_m.Created = value.Time
			}
		case poc.FieldUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated", values[i])
			} else if value.Valid {
				_m.Updated = value.Time
			}
		case poc.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Poc.
// This includes values selected through modifiers, order, etc.
func (_m *Poc) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryNetwork queries the "network" edge of the Poc entity.
func (_m *Poc) QueryNetwork() *NetworkQuery {
	return NewPocClient(_m.config).QueryNetwork(_m)
}

// Update returns a builder for updating this Poc.
// Note that you need to call Poc.Unwrap() before calling this method if this Poc
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Poc) Update() *PocUpdateOne {
	return NewPocClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Poc entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Poc) Unwrap() *Poc {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Poc is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Poc) String() string {
	var builder strings.Builder
	builder.WriteString("Poc(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.NetID; v != nil {
		builder.WriteString("net_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("phone=")
	builder.WriteString(_m.Phone)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(_m.Role)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("visible=")
	builder.WriteString(_m.Visible)
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(_m.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated=")
	builder.WriteString(_m.Updated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// Pocs is a parsable slice of Poc.
type Pocs []*Poc
