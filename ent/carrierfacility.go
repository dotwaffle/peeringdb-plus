// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/peeringdb-plus/ent/carrier"
	"github.com/dotwaffle/peeringdb-plus/ent/carrierfacility"
	"github.com/dotwaffle/peeringdb-plus/ent/facility"
)

// CarrierFacility is the model entity for the CarrierFacility schema.
type CarrierFacility struct {
	config `json:"-"`
	// ID of the ent.
	// PeeringDB carrier-facility ID
	ID int `json:"id,omitempty"`
	// FK to carrier
	CarrierID *int `json:"carrier_id"`
	// FK to facility
	FacID *int `json:"fac_id"`
	// Facility name (computed)
	Name string `json:"name"`
	// PeeringDB creation timestamp
	Created time.Time `json:"created"`
	// PeeringDB last update timestamp
	Updated time.Time `json:"updated"`
	// Record status
	Status string `json:"status"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierFacilityQuery when eager-loading is set.
	Edges        CarrierFacilityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CarrierFacilityEdges holds the relations/edges for other nodes in the graph.
type CarrierFacilityEdges struct {
	// Carrier holds the value of the carrier edge.
	Carrier *Carrier `json:"carrier,omitempty"`
	// Facility holds the value of the facility edge.
	Facility *Facility `json:"facility,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// CarrierOrErr returns the Carrier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierFacilityEdges) CarrierOrErr() (*Carrier, error) {
	if e.Carrier != nil {
		return e.Carrier, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: carrier.Label}
	}
	return nil, &NotLoadedError{edge: "carrier"}
}

// FacilityOrErr returns the Facility value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierFacilityEdges) FacilityOrErr() (*Facility, error) {
	if e.Facility != nil {
		return e.Facility, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: facility.Label}
	}
	return nil, &NotLoadedError{edge: "facility"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierFacility) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierfacility.FieldID, carrierfacility.FieldCarrierID, carrierfacility.FieldFacID:
			values[i] = new(sql.NullInt64)
		case carrierfacility.FieldName, carrierfacility.FieldStatus:
			values[i] = new(sql.NullString)
		case carrierfacility.FieldCreated, carrierfacility.FieldUpdated:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierFacility fields.
func (_m *CarrierFacility) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierfacility.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case carrierfacility.FieldCarrierID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_id", values[i])
			} else if value.Valid {
				_m.CarrierID = new(int)
				*_m.CarrierID = int(value.Int64)
			}
		case carrierfacility.FieldFacID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field fac_id", values[i])
			} else if value.Valid {
				_m.FacID = new(int)
				*_m.FacID = int(value.Int64)
			}
		case carrierfacility.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case carrierfacility.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				_m.Created = value.Time
			}
		case carrierfacility.FieldUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated", values[i])
			} else if value.Valid {
				_m.Updated = value.Time
			}
		case carrierfacility.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierFacility.
// This includes values selected through modifiers, order, etc.
func (_m *CarrierFacility) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCarrier queries the "carrier" edge of the CarrierFacility entity.
func (_m *CarrierFacility) QueryCarrier() *CarrierQuery {
	return NewCarrierFacilityClient(_m.config).QueryCarrier(_m)
}

// QueryFacility queries the "facility" edge of the CarrierFacility entity.
func (_m *CarrierFacility) QueryFacility() *FacilityQuery {
	return NewCarrierFacilityClient(_m.config).QueryFacility(_m)
}

// Update returns a builder for updating this CarrierFacility.
// Note that you need to call CarrierFacility.Unwrap() before calling this method if this CarrierFacility
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CarrierFacility) Update() *CarrierFacilityUpdateOne {
	return NewCarrierFacilityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CarrierFacility entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CarrierFacility) Unwrap() *CarrierFacility {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierFacility is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CarrierFacility) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierFacility(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.CarrierID; v != nil {
		builder.WriteString("carrier_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.FacID; v != nil {
		builder.WriteString("fac_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(_m.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated=")
	builder.WriteString(_m.Updated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// CarrierFacilities is a parsable slice of CarrierFacility.
type CarrierFacilities []*CarrierFacility
