// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dotwaffle/peeringdb-plus/ent/carrier"
	"github.com/dotwaffle/peeringdb-plus/ent/carrierfacility"
	"github.com/dotwaffle/peeringdb-plus/ent/facility"
	"github.com/dotwaffle/peeringdb-plus/ent/predicate"
)

// CarrierFacilityQuery is the builder for querying CarrierFacility entities.
type CarrierFacilityQuery struct {
	config
	ctx          *QueryContext
	order        []carrierfacility.OrderOption
	inters       []Interceptor
	predicates   []predicate.CarrierFacility
	withCarrier  *CarrierQuery
	withFacility *FacilityQuery
	modifiers    []func(*sql.Selector)
	loadTotal    []func(context.Context, []*CarrierFacility) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarrierFacilityQuery builder.
func (_q *CarrierFacilityQuery) Where(ps ...predicate.CarrierFacility) *CarrierFacilityQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CarrierFacilityQuery) Limit(limit int) *CarrierFacilityQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CarrierFacilityQuery) Offset(offset int) *CarrierFacilityQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CarrierFacilityQuery) Unique(unique bool) *CarrierFacilityQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CarrierFacilityQuery) Order(o ...carrierfacility.OrderOption) *CarrierFacilityQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCarrier chains the current query on the "carrier" edge.
func (_q *CarrierFacilityQuery) QueryCarrier() *CarrierQuery {
	query := (&CarrierClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierfacility.Table, carrierfacility.FieldID, selector),
			sqlgraph.To(carrier.Table, carrier.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, carrierfacility.CarrierTable, carrierfacility.CarrierColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFacility chains the current query on the "facility" edge.
func (_q *CarrierFacilityQuery) QueryFacility() *FacilityQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierfacility.Table, carrierfacility.FieldID, selector),
			sqlgraph.To(facility.Table, facility.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, carrierfacility.FacilityTable, carrierfacility.FacilityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarrierFacility entity from the query.
// Returns a *NotFoundError when no CarrierFacility was found.
func (_q *CarrierFacilityQuery) First(ctx context.Context) (*CarrierFacility, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carrierfacility.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CarrierFacilityQuery) FirstX(ctx context.Context) *CarrierFacility {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarrierFacility ID from the query.
// Returns a *NotFoundError when no CarrierFacility ID was found.
func (_q *CarrierFacilityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carrierfacility.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CarrierFacilityQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarrierFacility entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarrierFacility entity is found.
// Returns a *NotFoundError when no CarrierFacility entities are found.
func (_q *CarrierFacilityQuery) Only(ctx context.Context) (*CarrierFacility, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carrierfacility.Label}
	default:
		return nil, &NotSingularError{carrierfacility.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CarrierFacilityQuery) OnlyX(ctx context.Context) *CarrierFacility {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarrierFacility ID in the query.
// Returns a *NotSingularError when more than one CarrierFacility ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CarrierFacilityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carrierfacility.Label}
	default:
		err = &NotSingularError{carrierfacility.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CarrierFacilityQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarrierFacilities.
func (_q *CarrierFacilityQuery) All(ctx context.Context) ([]*CarrierFacility, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarrierFacility, *CarrierFacilityQuery]()
	return withInterceptors[[]*CarrierFacility](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CarrierFacilityQuery) AllX(ctx context.Context) []*CarrierFacility {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarrierFacility IDs.
func (_q *CarrierFacilityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(carrierfacility.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CarrierFacilityQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CarrierFacilityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CarrierFacilityQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CarrierFacilityQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CarrierFacilityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CarrierFacilityQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarrierFacilityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CarrierFacilityQuery) Clone() *CarrierFacilityQuery {
	if _q == nil {
		return nil
	}
	return &CarrierFacilityQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]carrierfacility.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.CarrierFacility{}, _q.predicates...),
		withCarrier:  _q.withCarrier.Clone(),
		withFacility: _q.withFacility.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCarrier tells the query-builder to eager-load the nodes that are connected to
// the "carrier" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CarrierFacilityQuery) WithCarrier(opts ...func(*CarrierQuery)) *CarrierFacilityQuery {
	query := (&CarrierClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCarrier = query
	return _q
}

// WithFacility tells the query-builder to eager-load the nodes that are connected to
// the "facility" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CarrierFacilityQuery) WithFacility(opts ...func(*FacilityQuery)) *CarrierFacilityQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFacility = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CarrierID int `json:"carrier_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarrierFacility.Query().
//		GroupBy(carrierfacility.FieldCarrierID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CarrierFacilityQuery) GroupBy(field string, fields ...string) *CarrierFacilityGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarrierFacilityGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = carrierfacility.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CarrierID int `json:"carrier_id,omitempty"`
//	}
//
//	client.CarrierFacility.Query().
//		Select(carrierfacility.FieldCarrierID).
//		Scan(ctx, &v)
func (_q *CarrierFacilityQuery) Select(fields ...string) *CarrierFacilitySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CarrierFacilitySelect{CarrierFacilityQuery: _q}
	sbuild.label = carrierfacility.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarrierFacilitySelect configured with the given aggregations.
func (_q *CarrierFacilityQuery) Aggregate(fns ...AggregateFunc) *CarrierFacilitySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CarrierFacilityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !carrierfacility.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CarrierFacilityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarrierFacility, error) {
	var (
		nodes       = []*CarrierFacility{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withCarrier != nil,
			_q.withFacility != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarrierFacility).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarrierFacility{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCarrier; query != nil {
		if err := _q.loadCarrier(ctx, query, nodes, nil,
			func(n *CarrierFacility, e *Carrier) { n.Edges.Carrier = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFacility; query != nil {
		if err := _q.loadFacility(ctx, query, nodes, nil,
			func(n *CarrierFacility, e *Facility) { n.Edges.Facility = e }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CarrierFacilityQuery) loadCarrier(ctx context.Context, query *CarrierQuery, nodes []*CarrierFacility, init func(*CarrierFacility), assign func(*CarrierFacility, *Carrier)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CarrierFacility)
	for i := range nodes {
		if nodes[i].CarrierID == nil {
			continue
		}
		fk := *nodes[i].CarrierID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carrier.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "carrier_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CarrierFacilityQuery) loadFacility(ctx context.Context, query *FacilityQuery, nodes []*CarrierFacility, init func(*CarrierFacility), assign func(*CarrierFacility, *Facility)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CarrierFacility)
	for i := range nodes {
		if nodes[i].FacID == nil {
			continue
		}
		fk := *nodes[i].FacID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(facility.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fac_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *CarrierFacilityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CarrierFacilityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carrierfacility.Table, carrierfacility.Columns, sqlgraph.NewFieldSpec(carrierfacility.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carrierfacility.FieldID)
		for i := range fields {
			if fields[i] != carrierfacility.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCarrier != nil {
			_spec.Node.AddColumnOnce(carrierfacility.FieldCarrierID)
		}
		if _q.withFacility != nil {
			_spec.Node.AddColumnOnce(carrierfacility.FieldFacID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CarrierFacilityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(carrierfacility.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = carrierfacility.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarrierFacilityGroupBy is the group-by builder for CarrierFacility entities.
type CarrierFacilityGroupBy struct {
	selector
	build *CarrierFacilityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CarrierFacilityGroupBy) Aggregate(fns ...AggregateFunc) *CarrierFacilityGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CarrierFacilityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierFacilityQuery, *CarrierFacilityGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CarrierFacilityGroupBy) sqlScan(ctx context.Context, root *CarrierFacilityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarrierFacilitySelect is the builder for selecting fields of CarrierFacility entities.
type CarrierFacilitySelect struct {
	*CarrierFacilityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CarrierFacilitySelect) Aggregate(fns ...AggregateFunc) *CarrierFacilitySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CarrierFacilitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierFacilityQuery, *CarrierFacilitySelect](ctx, _s.CarrierFacilityQuery, _s, _s.inters, v)
}

func (_s *CarrierFacilitySelect) sqlScan(ctx context.Context, root *CarrierFacilityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
