// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/peeringdb-plus/ent/ixlan"
	"github.com/dotwaffle/peeringdb-plus/ent/ixprefix"
)

// IxPrefix is the model entity for the IxPrefix schema.
type IxPrefix struct {
	config `json:"-"`
	// ID of the ent.
	// PeeringDB ixprefix ID
	ID int `json:"id,omitempty"`
	// FK to IX LAN
	IxlanID *int `json:"ixlan_id"`
	// In default-free zone
	InDfz bool `json:"in_dfz"`
	// Notes
	Notes string `json:"notes"`
	// IP prefix
	Prefix string `json:"prefix"`
	// Protocol (IPv4/IPv6)
	Protocol string `json:"protocol"`
	// PeeringDB creation timestamp
	Created time.Time `json:"created"`
	// PeeringDB last update timestamp
	Updated time.Time `json:"updated"`
	// Record status
	Status string `json:"status"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IxPrefixQuery when eager-loading is set.
	Edges        IxPrefixEdges `json:"edges"`
	selectValues sql.SelectValues
}

// IxPrefixEdges holds the relations/edges for other nodes in the graph.
type IxPrefixEdges struct {
	// IxLan holds the value of the ix_lan edge.
	IxLan *IxLan `json:"ix_lan,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// IxLanOrErr returns the IxLan value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e IxPrefixEdges) IxLanOrErr() (*IxLan, error) {
	if e.IxLan != nil {
		return e.IxLan, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: ixlan.Label}
	}
	return nil, &NotLoadedError{edge: "ix_lan"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*IxPrefix) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ixprefix.FieldInDfz:
			values[i] = new(sql.NullBool)
		case ixprefix.FieldID, ixprefix.FieldIxlanID:
			values[i] = new(sql.NullInt64)
		case ixprefix.FieldNotes, ixprefix.FieldPrefix, ixprefix.FieldProtocol, ixprefix.FieldStatus:
			values[i] = new(sql.NullString)
		case ixprefix.FieldCreated, ixprefix.FieldUpdated:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the IxPrefix fields.
func (_m *IxPrefix) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ixprefix.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case ixprefix.FieldIxlanID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ixlan_id", values[i])
			} else if value.Valid {
				_m.IxlanID = new(int)
				*_m.IxlanID = int(value.Int64)
			}
		case ixprefix.FieldInDfz:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field in_dfz", values[i])
			} else if value.Valid {
				_m.InDfz = value.Bool
			}
		case ixprefix.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case ixprefix.FieldPrefix:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field prefix", values[i])
			} else if value.Valid {
				_m.Prefix = value.String
			}
		case ixprefix.FieldProtocol:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field protocol", values[i])
			} else if value.Valid {
				_m.Protocol = value.String
			}
		case ixprefix.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				_m.Created = value.Time
			}
		case ixprefix.FieldUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated", values[i])
			} else if value.Valid {
				_m.Updated = value.Time
			}
		case ixprefix.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the IxPrefix.
// This includes values selected through modifiers, order, etc.
func (_m *IxPrefix) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryIxLan queries the "ix_lan" edge of the IxPrefix entity.
func (_m *IxPrefix) QueryIxLan() *IxLanQuery {
	return NewIxPrefixClient(_m.config).QueryIxLan(_m)
}

// Update returns a builder for updating this IxPrefix.
// Note that you need to call IxPrefix.Unwrap() before calling this method if this IxPrefix
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *IxPrefix) Update() *IxPrefixUpdateOne {
	return NewIxPrefixClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the IxPrefix entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *IxPrefix) Unwrap() *IxPrefix {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: IxPrefix is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *IxPrefix) String() string {
	var builder strings.Builder
	builder.WriteString("IxPrefix(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.IxlanID; v != nil {
		builder.WriteString("ixlan_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("in_dfz=")
	builder.WriteString(fmt.Sprintf("%v", _m.InDfz))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	builder.WriteString("prefix=")
	builder.WriteString(_m.Prefix)
	builder.WriteString(", ")
	builder.WriteString("protocol=")
	builder.WriteString(_m.Protocol)
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(_m.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated=")
	builder.WriteString(_m.Updated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// IxPrefixes is a parsable slice of IxPrefix.
type IxPrefixes []*IxPrefix
