// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dotwaffle/peeringdb-plus/ent/facility"
	"github.com/dotwaffle/peeringdb-plus/ent/network"
	"github.com/dotwaffle/peeringdb-plus/ent/networkfacility"
	"github.com/dotwaffle/peeringdb-plus/ent/predicate"
)

// NetworkFacilityQuery is the builder for querying NetworkFacility entities.
type NetworkFacilityQuery struct {
	config
	ctx          *QueryContext
	order        []networkfacility.OrderOption
	inters       []Interceptor
	predicates   []predicate.NetworkFacility
	withNetwork  *NetworkQuery
	withFacility *FacilityQuery
	modifiers    []func(*sql.Selector)
	loadTotal    []func(context.Context, []*NetworkFacility) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NetworkFacilityQuery builder.
func (_q *NetworkFacilityQuery) Where(ps ...predicate.NetworkFacility) *NetworkFacilityQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *NetworkFacilityQuery) Limit(limit int) *NetworkFacilityQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *NetworkFacilityQuery) Offset(offset int) *NetworkFacilityQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *NetworkFacilityQuery) Unique(unique bool) *NetworkFacilityQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *NetworkFacilityQuery) Order(o ...networkfacility.OrderOption) *NetworkFacilityQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryNetwork chains the current query on the "network" edge.
func (_q *NetworkFacilityQuery) QueryNetwork() *NetworkQuery {
	query := (&NetworkClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(networkfacility.Table, networkfacility.FieldID, selector),
			sqlgraph.To(network.Table, network.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, networkfacility.NetworkTable, networkfacility.NetworkColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFacility chains the current query on the "facility" edge.
func (_q *NetworkFacilityQuery) QueryFacility() *FacilityQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(networkfacility.Table, networkfacility.FieldID, selector),
			sqlgraph.To(facility.Table, facility.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, networkfacility.FacilityTable, networkfacility.FacilityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NetworkFacility entity from the query.
// Returns a *NotFoundError when no NetworkFacility was found.
func (_q *NetworkFacilityQuery) First(ctx context.Context) (*NetworkFacility, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{networkfacility.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *NetworkFacilityQuery) FirstX(ctx context.Context) *NetworkFacility {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NetworkFacility ID from the query.
// Returns a *NotFoundError when no NetworkFacility ID was found.
func (_q *NetworkFacilityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{networkfacility.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *NetworkFacilityQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NetworkFacility entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NetworkFacility entity is found.
// Returns a *NotFoundError when no NetworkFacility entities are found.
func (_q *NetworkFacilityQuery) Only(ctx context.Context) (*NetworkFacility, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{networkfacility.Label}
	default:
		return nil, &NotSingularError{networkfacility.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *NetworkFacilityQuery) OnlyX(ctx context.Context) *NetworkFacility {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NetworkFacility ID in the query.
// Returns a *NotSingularError when more than one NetworkFacility ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *NetworkFacilityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{networkfacility.Label}
	default:
		err = &NotSingularError{networkfacility.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *NetworkFacilityQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NetworkFacilities.
func (_q *NetworkFacilityQuery) All(ctx context.Context) ([]*NetworkFacility, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NetworkFacility, *NetworkFacilityQuery]()
	return withInterceptors[[]*NetworkFacility](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *NetworkFacilityQuery) AllX(ctx context.Context) []*NetworkFacility {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NetworkFacility IDs.
func (_q *NetworkFacilityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(networkfacility.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *NetworkFacilityQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *NetworkFacilityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*NetworkFacilityQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *NetworkFacilityQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *NetworkFacilityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *NetworkFacilityQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NetworkFacilityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *NetworkFacilityQuery) Clone() *NetworkFacilityQuery {
	if _q == nil {
		return nil
	}
	return &NetworkFacilityQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]networkfacility.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.NetworkFacility{}, _q.predicates...),
		withNetwork:  _q.withNetwork.Clone(),
		withFacility: _q.withFacility.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithNetwork tells the query-builder to eager-load the nodes that are connected to
// the "network" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NetworkFacilityQuery) WithNetwork(opts ...func(*NetworkQuery)) *NetworkFacilityQuery {
	query := (&NetworkClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNetwork = query
	return _q
}

// WithFacility tells the query-builder to eager-load the nodes that are connected to
// the "facility" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NetworkFacilityQuery) WithFacility(opts ...func(*FacilityQuery)) *NetworkFacilityQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFacility = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NetID int `json:"net_id"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NetworkFacility.Query().
//		GroupBy(networkfacility.FieldNetID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *NetworkFacilityQuery) GroupBy(field string, fields ...string) *NetworkFacilityGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NetworkFacilityGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = networkfacility.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NetID int `json:"net_id"`
//	}
//
//	client.NetworkFacility.Query().
//		Select(networkfacility.FieldNetID).
//		Scan(ctx, &v)
func (_q *NetworkFacilityQuery) Select(fields ...string) *NetworkFacilitySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &NetworkFacilitySelect{NetworkFacilityQuery: _q}
	sbuild.label = networkfacility.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NetworkFacilitySelect configured with the given aggregations.
func (_q *NetworkFacilityQuery) Aggregate(fns ...AggregateFunc) *NetworkFacilitySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *NetworkFacilityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !networkfacility.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *NetworkFacilityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NetworkFacility, error) {
	var (
		nodes       = []*NetworkFacility{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withNetwork != nil,
			_q.withFacility != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NetworkFacility).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NetworkFacility{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withNetwork; query != nil {
		if err := _q.loadNetwork(ctx, query, nodes, nil,
			func(n *NetworkFacility, e *Network) { n.Edges.Network = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFacility; query != nil {
		if err := _q.loadFacility(ctx, query, nodes, nil,
			func(n *NetworkFacility, e *Facility) { n.Edges.Facility = e }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *NetworkFacilityQuery) loadNetwork(ctx context.Context, query *NetworkQuery, nodes []*NetworkFacility, init func(*NetworkFacility), assign func(*NetworkFacility, *Network)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NetworkFacility)
	for i := range nodes {
		if nodes[i].NetID == nil {
			continue
		}
		fk := *nodes[i].NetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(network.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "net_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *NetworkFacilityQuery) loadFacility(ctx context.Context, query *FacilityQuery, nodes []*NetworkFacility, init func(*NetworkFacility), assign func(*NetworkFacility, *Facility)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NetworkFacility)
	for i := range nodes {
		if nodes[i].FacID == nil {
			continue
		}
		fk := *nodes[i].FacID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(facility.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fac_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *NetworkFacilityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *NetworkFacilityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(networkfacility.Table, networkfacility.Columns, sqlgraph.NewFieldSpec(networkfacility.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, networkfacility.FieldID)
		for i := range fields {
			if fields[i] != networkfacility.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withNetwork != nil {
			_spec.Node.AddColumnOnce(networkfacility.FieldNetID)
		}
		if _q.withFacility != nil {
			_spec.Node.AddColumnOnce(networkfacility.FieldFacID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *NetworkFacilityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(networkfacility.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = networkfacility.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NetworkFacilityGroupBy is the group-by builder for NetworkFacility entities.
type NetworkFacilityGroupBy struct {
	selector
	build *NetworkFacilityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *NetworkFacilityGroupBy) Aggregate(fns ...AggregateFunc) *NetworkFacilityGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *NetworkFacilityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NetworkFacilityQuery, *NetworkFacilityGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *NetworkFacilityGroupBy) sqlScan(ctx context.Context, root *NetworkFacilityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NetworkFacilitySelect is the builder for selecting fields of NetworkFacility entities.
type NetworkFacilitySelect struct {
	*NetworkFacilityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *NetworkFacilitySelect) Aggregate(fns ...AggregateFunc) *NetworkFacilitySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *NetworkFacilitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NetworkFacilityQuery, *NetworkFacilitySelect](ctx, _s.NetworkFacilityQuery, _s, _s.inters, v)
}

func (_s *NetworkFacilitySelect) sqlScan(ctx context.Context, root *NetworkFacilityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
