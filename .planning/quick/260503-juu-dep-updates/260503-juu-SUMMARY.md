---
quick_id: 260503-juu
slug: dep-updates
status: complete
completed_at: "2026-05-03T14:29:33Z"
commits:
  - hash: 6d4ce49
    subject: "deps: bump direct module deps to latest"
    files:
      - go.mod
      - go.sum
      - graph/custom.resolvers.go
      - graph/generated.go
      - graph/schema.resolvers.go
files_modified:
  - go.mod
  - go.sum
  - graph/custom.resolvers.go
  - graph/generated.go
  - graph/schema.resolvers.go
gates:
  build: PASS
  vet: PASS
  test_race: PASS
  golangci_lint: FAIL_PRE_EXISTING
  govulncheck: PASS
  drift_check: PASS
deviations:
  - description: "golangci-lint reports 6 nolintlint findings; pre-existing on main, not caused by this bump"
    rule: "Out of scope (not in plan's allowed file set; main CI is already failing on the same 6 issues)"
conditional_commit_2_fired: false
---

# Quick Task 260503-juu: Module Dep Updates Summary

## One-liner

Bumped 6 direct Go module deps (5 patch + 1 minor for `modernc.org/sqlite`),
ran the full codegen pipeline (graph/ regenerated by gqlgen v0.17.90),
landed as a single atomic commit; lint pre-existing breakage on main
(unrelated to bump) documented for follow-up.

## Direct-dep version table

| Module                               | Before    | After     | Notes                          |
| ------------------------------------ | --------- | --------- | ------------------------------ |
| `charm.land/lipgloss/v2`             | `v2.0.2`  | `v2.0.3`  | patch                          |
| `connectrpc.com/connect`             | `v1.19.1` | `v1.19.2` | patch                          |
| `github.com/99designs/gqlgen`        | `v0.17.89`| `v0.17.90`| patch (codegen drift in graph/)|
| `github.com/klauspost/compress`      | `v1.18.5` | `v1.18.6` | patch                          |
| `github.com/vektah/gqlparser/v2`     | `v2.5.32` | `v2.5.33` | patch                          |
| `modernc.org/sqlite`                 | `v1.49.1` | `v1.50.0` | minor (prod-critical, no SKIP) |

No SKIP rules fired. No deps reverted or pinned. No `go` or `toolchain`
directive change (the project has no `toolchain` directive; `go 1.26.1`
preserved).

Pre-flight `go list -m -u all` confirmed all 6 audit-table targets
unchanged since the 2026-05-03 14:20Z audit; no newer versions had
landed by execute time (2026-05-03 14:23Z).

## Indirect-dep churn (transitive bumps)

`go mod tidy` after `go get -u ./...` produced these indirect-dep
moves alongside the 6 direct ones:

| Indirect module                                      | Before                                  | After                                   |
| ---------------------------------------------------- | --------------------------------------- | --------------------------------------- |
| `github.com/charmbracelet/ultraviolet`               | `v0.0.0-20260330092749-0f94982c930b`    | `v0.0.0-20260428153724-66037269d7be`    |
| `github.com/charmbracelet/x/ansi`                    | `v0.11.6`                               | `v0.11.7`                               |
| `github.com/dlclark/regexp2`                         | `v1.11.5`                               | `v1.12.0`                               |
| `github.com/grpc-ecosystem/grpc-gateway/v2`          | `v2.28.0`                               | `v2.29.0`                               |
| `github.com/mattn/go-isatty`                         | `v0.0.21`                               | `v0.0.22`                               |
| `github.com/urfave/cli/v3`                           | `v3.7.0`                                | `v3.8.0`                                |
| `github.com/zclconf/go-cty`                          | `v1.18.0`                               | `v1.18.1`                               |
| `go.uber.org/zap`                                    | `v1.27.1`                               | `v1.28.0`                               |
| `google.golang.org/genproto/googleapis/api`          | `v0.0.0-20260414002931-afd174a4e478`    | `v0.0.0-20260427160629-7cedc36a6bc4`    |
| `google.golang.org/genproto/googleapis/rpc`          | `v0.0.0-20260414002931-afd174a4e478`    | `v0.0.0-20260427160629-7cedc36a6bc4`    |
| `modernc.org/libc`                                   | `v1.72.0`                               | `v1.72.1`                               |

All within-major; no unexpected major-version jumps.

## Codegen drift

`go generate ./...` produced drift in `graph/` only — exactly as the
plan predicted (gqlgen patch bump):

- `graph/custom.resolvers.go` — generator version stamp `v0.17.89` → `v0.17.90`
- `graph/schema.resolvers.go` — generator version stamp `v0.17.89` → `v0.17.90`
- `graph/generated.go` — substantial regen (4568 insertions / 8343 deletions)
  reflecting upstream simplification of emitted code in gqlgen v0.17.90;
  no behavioural difference at the resolver surface

`ent/`, `gen/`, `internal/web/templates/` — no drift.

All regenerated files are in the same commit, satisfying the CI
drift-check gate.

## Gate results (post-commit)

| Gate                | Status   | Notes                                                  |
| ------------------- | -------- | ------------------------------------------------------ |
| `go build ./...`    | PASS     | Clean compile across all packages                      |
| `go vet ./...`      | PASS     | No findings                                            |
| `go test -race -count=1 ./...` | PASS  | All 44 testable packages green; full suite ~3 min     |
| `golangci-lint run ./...`      | FAIL  | 6 `nolintlint` findings — pre-existing on main         |
| `govulncheck ./...` | PASS     | "No vulnerabilities found."                            |
| `go generate ./...` drift gate | PASS  | Clean tree post-commit                                 |

### Lint failure (pre-existing, out of scope)

golangci-lint reports 6 `nolintlint` findings about unused
`//nolint:gosec` directives:

- `internal/config/config.go:270:139` — `slog.Warn` deprecation message
- `internal/web/detail.go:60:82, 72:90` — `slog.Error` ent query / template render
- `internal/web/handler.go:293:116, 306:114` — `slog.Error` compare networks
- `internal/web/query_network.go:33:96` — `slog.Error` count network contacts

**These are pre-existing, not caused by this bump.** Verified by:

1. Stashing the bump and re-running `golangci-lint run ./...` on the
   pre-bump tree — same 6 findings appear.
2. The latest CI run on `main` (`run id: 25280024764`, head SHA
   `61eb564`, 2026-05-03 13:09Z) is already failing on the **exact
   same 6 findings** in the Lint job log.

The plan's allowed file set is `go.mod, go.sum, plus any regenerated
files under ent/, gen/, graph/, internal/web/templates/`. Editing
`internal/config/config.go` and `internal/web/*.go` to drop the
unused directives is outside this task's scope. The dep bump
introduces zero lint regression. Cleanup is queued for a follow-up
quick task.

## GH Actions verification (Step B)

Re-checked all 6 actions pinned in `.github/workflows/ci.yml`
against `gh api repos/<org>/<repo>/releases/latest --jq .tag_name`
at execute time (2026-05-03 14:21Z):

| Action                             | Pinned at | Latest tag | Edit needed? |
| ---------------------------------- | --------- | ---------- | ------------ |
| `actions/checkout`                 | `v6`      | `v6.0.2`   | No           |
| `actions/setup-go`                 | `v6`      | `v6.4.0`   | No           |
| `docker/build-push-action`         | `v7`      | `v7.1.0`   | No           |
| `docker/setup-buildx-action`       | `v4`      | `v4.0.0`   | No           |
| `golangci/golangci-lint-action`    | `v9`      | `v9.2.0`   | No           |
| `k1LoW/octocov-action`             | `v1`      | `v1.5.1`   | No           |

All 6 actions are at their current latest major. **Conditional
commit 2 did NOT fire.**

## Deviations

1. **golangci-lint pre-existing breakage (out of scope).** The plan
   gate "golangci-lint run passes" is technically failed, but the
   failure is unrelated to the dep bump and the affected files
   (`internal/config/config.go`, `internal/web/*.go`) are outside the
   plan's allowed file set. Per the user's CLAUDE.md "Frustrations"
   directive ("If a change request is specific, only modify what was
   requested -- do not make adjacent 'improvements' without asking"),
   the cleanup is left to a follow-up task. Main's CI is already
   failing on the same 6 issues, so this commit cannot make CI more
   red.

   Suggested follow-up: a one-line quick task to delete the 6 unused
   `//nolint:gosec` directives.

2. **No SKIP rules fired.** sqlite v1.50.0 builds, tests, and passes
   govulncheck cleanly. gqlgen v0.17.90 produced clean codegen drift
   (no resolver-signature breakage). All bumps land as the plan
   anticipated.

3. **No `go`/`toolchain` directive perturbation.** `go get -u ./...`
   did not modify the `go 1.26.1` directive; no `toolchain` directive
   exists in this `go.mod` to preserve.

## Self-Check: PASSED

Verifications:

- File `.planning/quick/260503-juu-dep-updates/260503-juu-SUMMARY.md` written by this executor (this file).
- Commit `6d4ce49` exists in `git log --oneline`.
- `go build`, `go vet`, `go test -race`, `govulncheck` all PASS post-commit.
- Drift-check gate clean post-commit (`go generate ./...` produces zero diff).
- `git status` clean post-commit (no stray untracked or modified files).
- No edits outside go.mod / go.sum / regenerated graph/ files.
