---
phase: 77-telemetry-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/77-telemetry-audit/AUDIT.md
  - internal/sync/worker.go
  - cmd/peeringdb-plus/main.go
  - internal/health/handler.go
  - internal/middleware/logging.go
autonomous: false
requirements:
  - OBS-06
tags:
  - observability
  - logging
  - slog

must_haves:
  truths:
    - "AUDIT.md exists at .planning/phases/77-telemetry-audit/AUDIT.md and lists every reclassified log line with before/after slog level + rationale"
    - "Every reclassification cites the source file:line, current level, recommended level, and whether the log fires under non-error conditions"
    - "Production Loki INFO/WARN volume is qualitatively reduced — chatty INFOs that fire per-step or per-row are demoted to DEBUG; WARNs that fire under non-error conditions are demoted to INFO or DEBUG"
    - "No security-relevant signal is silenced — auth-failure / privacy-tier-mismatch / rollback / incremental-fallback / heap-threshold / fk-orphan-summary WARNs are explicitly retained"
    - "go test -race ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/... passes"
    - "golangci-lint run ./... is clean"
  artifacts:
    - path: ".planning/phases/77-telemetry-audit/AUDIT.md"
      provides: "Loki log-level audit document with per-line before/after table"
      contains: "| File:Line | Current | Recommended |"
    - path: "internal/sync/worker.go"
      provides: "Updated slog levels per AUDIT.md (e.g. per-step `fetching` INFO→DEBUG, `upserted` per-type INFO→DEBUG)"
    - path: "cmd/peeringdb-plus/main.go"
      provides: "Verified or adjusted slog level on logStartupClassification etc."
  key_links:
    - from: "AUDIT.md row"
      to: "exact slog.LevelXxx call site in source"
      via: "explicit file:line reference + commit-after-merge pattern"
      pattern: "internal/.*\\.go:[0-9]+"
    - from: "internal/sync/worker.go heap-threshold WARN"
      to: "PRESERVED (security/operator signal)"
      via: "AUDIT.md row marked Keep with rationale"
      pattern: "heap threshold crossed"
---

<objective>
Audit production Loki log levels via Grafana MCP, produce `.planning/phases/77-telemetry-audit/AUDIT.md` documenting every reclassification with before/after rationale, and apply the slog level changes inline to source files. Implements OBS-06 per CONTEXT.md D-01 (audit + fix in same phase).

Purpose: Production Loki log volume is dominated by per-step and per-row INFOs that should be DEBUG (e.g. per-type `fetching` and `upserted` logs in `internal/sync/worker.go` fire 13× per sync cycle × ~96 cycles/day = 1248 lines/day per machine × 8 machines). WARN currently includes some non-error events (sync rate-limit deferral, incremental fallback) that operators expect to fire routinely under normal traffic. The audit narrows WARN to error-genuine events so operators can grep for "WARN" in a 24h window without per-redaction or per-step-progress noise.

Output: AUDIT.md committed alongside slog source changes; production log volume measurably down post-merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-telemetry-audit/CONTEXT.md
@./CLAUDE.md
@internal/sync/worker.go
@internal/health/handler.go
@internal/middleware/logging.go
@internal/pdbcompat/handler.go
@cmd/peeringdb-plus/main.go

<interfaces>
<!-- Inventory of slog call sites the auditor MUST consider. Extracted from the codebase. -->

Production slog calls in scope (from `grep -rn "slog\.\(Info\|Warn\|Debug\|Error\)" internal/sync internal/health internal/middleware internal/pdbcompat cmd/peeringdb-plus | grep -v _test.go`):

internal/sync/worker.go:
- L198 — slog.LevelDebug "fk orphan" (per-row, already DEBUG — verify)
- L217 — slog.LevelDebug "fk orphans summary" total=0 (clean cycle, already DEBUG)
- L246 — slog.LevelWarn "fk orphans summary" total>0 (per-cycle aggregate, KEEP — addresses 7.5MB Tempo regression)
- L323 — slog.LevelError "unknown type for DB record check" (KEEP — programmer error)
- L327 — slog.LevelError "failed to check DB for record" (KEEP)
- L446 — slog.LevelError "failed to record sync start" (KEEP)
- L569 — slog.LevelWarn "sync aborted: memory limit exceeded" (KEEP — operator-actionable)
- L643 — slog.LevelWarn "heap threshold crossed" (KEEP — SEED-001 escalation)
- L698 — slog.LevelError "rollback failed" (KEEP)
- L721 — slog.LevelError "failed to update cursor" (KEEP)
- L736 — slog.LevelInfo "sync complete" (per-cycle, every ~15 min — review: KEEP or DEBUG?)
- L815 — slog.LevelInfo "fetching" (per-type per-cycle, 13× per cycle — DEMOTE candidate INFO→DEBUG)
- L824 — slog.LevelWarn "failed to get cursor, using full sync" (review: routine on first sync after restart — DEMOTE candidate WARN→INFO)
- L869 — slog.LevelWarn "incremental sync failed, falling back to full" (KEEP as WARN — operator-actionable; SEED-001 trigger)
- L971 — slog.LevelInfo "upserted" (per-type per-cycle, 13× per cycle — DEMOTE candidate INFO→DEBUG)
- L1401 — slog.LevelInfo "marked stale deleted" (per-type per-cycle, only when marked>0 — review)
- L1452 — slog.LevelWarn "sync rate-limited, deferring to next scheduled tick" (review: routine on Fly cold-boot — DEMOTE candidate WARN→INFO)
- L1462 — slog.LevelWarn "sync failed, retrying" (KEEP as WARN — operator wants visibility on retry storms)
- L1484 — slog.LevelWarn "sync rate-limited during retry, deferring" (review)
- L1492 — slog.LevelError "sync failed after all retries" (KEEP)
- L1533 — slog.LevelWarn "demoted during sync, aborting cycle" (KEEP — operator signal for LiteFS demotion)
- L1542 — slog.LevelError "sync cycle failed" (KEEP)
- L1577 — slog.LevelWarn "failed to get last sync time" (review: routine on first deploy — DEMOTE candidate)
- L1612 — slog.LevelDebug "starting scheduler as replica" (already DEBUG)
- L1633 — slog.LevelInfo "promoted to primary, checking sync status" (KEEP — operator signal, infrequent)
- L1650 — slog.LevelInfo "demoted to replica" (KEEP — operator signal, infrequent)
- L1662 — slog.LevelDebug "not primary, skipping sync" (already DEBUG)

internal/sync/scratch.go:
- L194 — slog.LevelWarn "close scratch db" (KEEP — error-conditional close failure)
- L202 — slog.LevelWarn "unlink scratch db" (KEEP)

internal/middleware/logging.go:
- L74 — slog.LevelInfo "http request" (per-request access log; /healthz and /readyz already skipped via accessLogSkipPaths — KEEP at INFO)

internal/health/handler.go:
- L90 — slog.LevelError "readyz db probe failed" (KEEP)
- L114 — slog.LevelError "readyz sync lookup failed" (KEEP)
- L123 — slog.LevelWarn "readyz no sync completed" (review: fires on every /readyz hit during pre-first-sync window — DEMOTE candidate WARN→DEBUG since /readyz is hit every ~15s by Fly)
- L140 — slog.LevelError "readyz sync lookup failed" (running branch) (KEEP)
- L148 — slog.LevelWarn "readyz no sync completed" (running branch) (review: same as L123)
- L157 — slog.LevelWarn "readyz sync marked failed" (KEEP — operator-actionable)
- L166 — slog.LevelWarn "readyz unknown sync status" (KEEP — programmer error escape)
- L181 — slog.LevelWarn "readyz sync stale" (KEEP — operator-actionable)

internal/pdbcompat/handler.go:
- L165 — slog.LevelDebug "pdbcompat list: ignoring unsupported ?depth= param" (already DEBUG)
- L195 — slog.LevelDebug "pdbcompat: unknown filter fields silently ignored" (already DEBUG)
- L274 — slog.LevelWarn "pdbcompat: response budget exceeded" (KEEP — operator-actionable, fires on 413 responses)
- L314 — slog.LevelError "pdbcompat: stream encode failed mid-response" (KEEP)

cmd/peeringdb-plus/main.go:
- L73 — slog.Error "failed to load config" (KEEP)
- L86 — slog.Error "failed to init otel" (KEEP)
- L493 — slog.Error "render terminal help" (KEEP)
- L894 — logger.Info "sync mode" (startup classification, fires once at boot — KEEP at INFO)
- L899 — logger.Warn "public tier override active" (KEEP — security-relevant config signal)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <files>.planning/phases/77-telemetry-audit/AUDIT.md (new), grafana-cloud MCP datasource (read-only)</files>
  <name>Task 1: Audit production Loki log volume via Grafana MCP and produce AUDIT.md</name>
  <read_first>
    - .planning/phases/77-telemetry-audit/CONTEXT.md (D-01)
    - internal/sync/worker.go (lines noted in interfaces block)
    - internal/health/handler.go (readyz log volume on Fly health probes)
    - internal/middleware/logging.go (accessLogSkipPaths already drops /healthz + /readyz)
  </read_first>
  <what-built>
    The auditor will use Grafana MCP (datasource: Grafana Cloud Loki) to sample 30 minutes of production logs at INFO, WARN, and DEBUG levels for the `peeringdb-plus` service (label: `service_name="peeringdb-plus"`). Sample queries the operator should run via the `grafana-cloud` MCP `loki_query_range` (or equivalent) tool:

    1. INFO volume by message: `sum by (msg) (count_over_time({service_name="peeringdb-plus"} | json | level="INFO" [30m]))` — identify chatty INFO lines (top 10 by volume).
    2. WARN volume by message: `sum by (msg) (count_over_time({service_name="peeringdb-plus"} | json | level="WARN" [30m]))` — identify WARNs that fire under non-error conditions.
    3. DEBUG sample: `{service_name="peeringdb-plus"} | json | level="DEBUG" | line_format "{{.msg}}"` (last 1000 lines) — verify DEBUG logs are still useful (not dead instrumentation accumulated since v1.6).
    4. Per-machine breakdown: `sum by (service_instance_id) (count_over_time({service_name="peeringdb-plus"} | json [30m]))` — sanity-check log volume per Fly machine.

    Cross-reference findings with the code inventory in the plan's `<interfaces>` block. For each chatty / mis-classified log, decide a recommended level:
    - INFO firing >10 times per sync cycle (per-type, per-row) → DEBUG.
    - WARN firing on routine non-error conditions (cursor missing on first sync, /readyz before first sync) → INFO or DEBUG.
    - DEBUG firing in production despite being meant for local debugging → DEBUG REMOVED entirely.
    - Security-relevant signals (auth failures, privacy-tier mismatches, rollback, demotion, heap threshold, fk-orphan summary, response budget exceeded) → KEEP regardless of volume.

    Produce `.planning/phases/77-telemetry-audit/AUDIT.md` with this exact structure:

    ```markdown
    # Phase 77 Loki Log-Level Audit (OBS-06)

    **Sampled:** YYYY-MM-DDTHH:MM:SSZ → +30m, Grafana Cloud Loki
    **Service:** peeringdb-plus (8 machines: 1 primary lhr + 7 replicas)
    **Scope:** Per CONTEXT.md D-01 — level audit only, not structured-attribute audit.

    ## Top-N volume findings

    | Rank | Level | Message | 30m count | Per-machine/min |
    |------|-------|---------|-----------|-----------------|
    | 1    | INFO  | "fetching"        | <N> | <N> |
    | ...  | ...   | ...               | ... | ... |

    ## Reclassification table

    | File:Line | Current | Recommended | Rationale | Security signal? |
    |-----------|---------|-------------|-----------|------------------|
    | internal/sync/worker.go:815 | INFO  | DEBUG | Per-type per-cycle (13× per ~15min cycle = ~1.4k/day/machine); operator can enable DEBUG when investigating fetch latency. | No |
    | internal/sync/worker.go:971 | INFO  | DEBUG | Per-type per-cycle; same volume profile. | No |
    | internal/sync/worker.go:824 | WARN  | INFO  | Fires on first sync after restart when cursor table is empty — routine, not an error condition. | No |
    | internal/sync/worker.go:643 | WARN  | KEEP  | SEED-001 escalation signal — must remain WARN for operator escalation. | Yes (memory exhaustion) |
    | internal/sync/worker.go:246 | WARN  | KEEP  | Per-cycle FK-orphan aggregate; replaces the per-row WARN-spam that breached Tempo's 7.5MB cap pre-Phase-68. | Yes (data integrity) |
    | internal/sync/worker.go:869 | WARN  | KEEP  | Incremental fallback is operator-actionable (SEED-001 escalation trigger). | Yes (sync correctness) |
    | internal/sync/worker.go:1462 | WARN | KEEP  | Retry storm visibility. | Yes |
    | internal/sync/worker.go:1492 | ERROR | KEEP  | Sync failure after all retries. | Yes |
    | internal/sync/worker.go:1533 | WARN | KEEP  | LiteFS demotion mid-sync — operator must see this. | Yes |
    | internal/health/handler.go:123 | WARN | DEBUG | Fires on every /readyz hit during pre-first-sync window (Fly hits /readyz every ~15s × 8 machines = high volume during 5-15min cold start); not actionable — Fly proxy handles failover via the 503 response. | No |
    | internal/health/handler.go:148 | WARN | DEBUG | Same rationale as L123 (running-branch path). | No |
    | internal/health/handler.go:157 | WARN | KEEP  | sync_status="failed" is operator-actionable. | Yes |
    | internal/health/handler.go:181 | WARN | KEEP  | Sync stale beyond threshold is operator-actionable. | Yes |
    | cmd/peeringdb-plus/main.go:894 | INFO | KEEP  | Startup-only classification (fires once at boot). | Yes (security config) |
    | cmd/peeringdb-plus/main.go:899 | WARN | KEEP  | Public tier override is a security-relevant config signal. | Yes |
    | <add-others-as-discovered> | ... | ... | ... | ... |

    ## Items reviewed and KEPT (no change)

    | File:Line | Level | Rationale |
    |-----------|-------|-----------|
    | <list of KEEP rows> | ... | ... |

    ## OTEL_BSP_* and sampling note (cross-reference to OBS-07)

    Current: `OTEL_BSP_SCHEDULE_DELAY=5s`, `OTEL_BSP_MAX_EXPORT_BATCH_SIZE=512`, sampling=1.0. Per OBS-07 (separate plan 77-02) — verified appropriate post-audit.
    ```

    The auditor MUST also confirm or refute the FK-orphan WARN-spam regression mode by running:
    `count_over_time({service_name="peeringdb-plus"} | json | msg="fk orphan" [30m])` — must return 0 (per-row was demoted to DEBUG in Phase 68; only the per-cycle aggregate fires at WARN).
  </what-built>
  <how-to-verify>
    The operator (using Grafana MCP) should:

    1. Run the four LogQL queries above against `peeringdb-plus.fly.dev` Loki.
    2. Cross-reference findings against the code inventory in this plan's `<interfaces>` block.
    3. Write `.planning/phases/77-telemetry-audit/AUDIT.md` matching the template.
    4. Confirm: `grep -c "^| .* | .* | .* | .* | .* |" .planning/phases/77-telemetry-audit/AUDIT.md` returns at least 10 (reclassification + KEEP rows combined).
    5. Confirm: `grep -c "fk orphan" .planning/phases/77-telemetry-audit/AUDIT.md` ≥ 1 (FK-orphan regression mode addressed).
    6. Confirm AUDIT.md addresses ALL high-volume INFO and WARN messages observed in the production sample, not just the candidates pre-listed in `<interfaces>`. New findings get new rows.
  </how-to-verify>
  <resume-signal>Type "audit-approved" when AUDIT.md is written and reflects production findings, or describe missing rows/findings to revise.</resume-signal>
  <action>
    Operator-driven (autonomous: false). Use grafana-cloud MCP tools to run the four LogQL queries above against the production Loki tenant. Cross-reference with the code inventory in this plan's `<interfaces>` block. Write `.planning/phases/77-telemetry-audit/AUDIT.md` per the template, populating concrete row counts and per-machine/min figures from the live sample. Add new reclassification rows for any chatty INFO/WARN observed in production that is not pre-listed in `<interfaces>`. Confirm or refute the FK-orphan WARN-spam regression mode by running the `msg="fk orphan"` count query — must return 0. Mark all security-relevant signals (auth failure, privacy-tier mismatch, rollback, demotion, heap threshold, fk-orphan summary, response budget exceeded) as `Recommended=KEEP` regardless of volume.
  </action>
  <verify>
    <automated>test -f .planning/phases/77-telemetry-audit/AUDIT.md</automated>
    <automated>grep -F "Reclassification table" .planning/phases/77-telemetry-audit/AUDIT.md</automated>
    <automated>grep -F "fk orphan" .planning/phases/77-telemetry-audit/AUDIT.md</automated>
    <automated>grep -F "OTEL_BSP_SCHEDULE_DELAY" .planning/phases/77-telemetry-audit/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/77-telemetry-audit/AUDIT.md`
    - Reclassification table populated: `test "$(grep -cE '^\| (internal|cmd)/.+\.go:[0-9]+ \|' .planning/phases/77-telemetry-audit/AUDIT.md)" -ge 8`
    - All "Security signal? Yes" rows are marked Recommended=KEEP: `test "$(grep -E '\| Yes \|?$' .planning/phases/77-telemetry-audit/AUDIT.md | grep -cv 'KEEP')" -eq 0`
    - FK-orphan regression mode addressed: `grep -F "fk orphan" .planning/phases/77-telemetry-audit/AUDIT.md`
    - OTEL_BSP_* cross-reference present: `grep -F "OTEL_BSP_SCHEDULE_DELAY" .planning/phases/77-telemetry-audit/AUDIT.md`
  </acceptance_criteria>
  <done>AUDIT.md is committed-ready (text-complete, accurate cross-reference between Loki sample and source inventory, all security signals retained).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply slog level changes per AUDIT.md (RED → GREEN → REFACTOR)</name>
  <files>internal/sync/worker.go, internal/health/handler.go, cmd/peeringdb-plus/main.go, internal/sync/worker_log_levels_test.go (new)</files>
  <read_first>
    - .planning/phases/77-telemetry-audit/AUDIT.md (the source of truth produced by Task 1)
    - internal/sync/worker.go (full read — multiple log call sites)
    - internal/health/handler.go (readyz log levels)
    - internal/sync/worker_test.go (existing test patterns: search for `slog.NewTextHandler` and `bytes.Buffer` to match the existing log-capture style)
  </read_first>
  <behavior>
    For each "Recommended != Current" row in AUDIT.md:
    - INFO → DEBUG: change `slog.LevelInfo` to `slog.LevelDebug` at the cited file:line.
    - WARN → INFO: change `slog.LevelWarn` to `slog.LevelInfo` at the cited file:line.
    - WARN → DEBUG: change `slog.LevelWarn` to `slog.LevelDebug` at the cited file:line.
    - DEBUG → REMOVED (if any such row exists): delete the entire `LogAttrs` call block.

    Do NOT touch any row marked "Recommended = KEEP". Do NOT touch any row marked "Security signal? Yes" without an explicit "KEEP" recommendation in AUDIT.md (this is the security-signal preservation invariant).

    Tests (RED phase) — create `internal/sync/worker_log_levels_test.go` to lock the post-audit levels:
    - Test 1: Capture slog output via `slog.NewTextHandler(buf, &slog.HandlerOptions{Level: slog.LevelDebug})` during a single fake sync cycle (use `httptest` fake-upstream returning empty result for all 13 types). Assert that "fetching" and "upserted" lines appear at level=DEBUG, NOT level=INFO.
    - Test 2: Same harness, assert "fk orphans summary" with total>0 still fires at level=WARN (security signal preserved). Use `seed.Full` then mark a parent row deleted to synthesise an orphan.
    - Test 3: Same harness with `slog.HandlerOptions{Level: slog.LevelInfo}`, assert per-step "fetching" and "upserted" do NOT appear in output (level=DEBUG correctly filtered).

    For the health package, add equivalent assertions in a sibling `internal/health/handler_log_levels_test.go` only if the audit demoted any health-handler log (per AUDIT.md rows for `internal/health/handler.go:123` and `:148` — likely DEBUG demotions).
  </behavior>
  <action>
    1. RED: write the three sync-package tests above; they MUST fail on current main (current INFO levels do not match expected DEBUG). Commit `test(77-01): RED — lock post-audit slog levels per AUDIT.md`.

    2. GREEN: apply the per-row changes from AUDIT.md. For each reclassification row in AUDIT.md, edit the cited file:line. Concrete pattern (example for sync/worker.go:815):
       - Before: `w.logger.LogAttrs(ctx, slog.LevelInfo, "fetching",`
       - After: `w.logger.LogAttrs(ctx, slog.LevelDebug, "fetching",`

       The exact set of edits is data-driven by AUDIT.md (Task 1 output) — do not pre-decide reclassifications here. Apply only what AUDIT.md prescribes.

       Re-run tests; all three sync-package tests + any new health-package tests MUST pass. Commit `feat(77-01): GREEN — apply slog level changes per AUDIT.md`.

    3. REFACTOR (if needed): if AUDIT.md identifies a "DEBUG → REMOVED" row, delete the dead instrumentation. If no such rows exist, skip this step. Commit `refactor(77-01): remove dead-instrumentation DEBUG logs per AUDIT.md` only if changes apply.

    4. Run `go test -race ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/...` — must pass.
    5. Run `golangci-lint run ./...` — must pass.
    6. Run `go generate ./...` — must produce zero drift (no generated files affected by log-level changes; this is a sanity gate).

    Per CLAUDE.md GO-OBS-1 (structured logging) and GO-OBS-5 (typed slog setters): preserve `slog.String()`/`slog.Int()` etc. attribute setters; only the level constant changes.

    Per CLAUDE.md "Shell environment" rule: avoid `if ! cmd; then` and `! cmd1 | cmd2` constructs; use `test "$(... | grep -c ...)" -eq 0` for negation.
  </action>
  <verify>
    <automated>go test -race ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/... 2>&amp;1 | tail -30</automated>
    <automated>golangci-lint run ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/...</automated>
    <automated>go generate ./... &amp;&amp; test -z "$(git status --porcelain ent/ gen/ graph/ internal/web/templates/)" || (echo "codegen drift" &amp;&amp; exit 1)</automated>
    <automated>test "$(grep -c 'slog\.LevelInfo, "fetching"' internal/sync/worker.go)" -eq 0</automated>
    <automated>test "$(grep -c 'slog\.LevelWarn, "heap threshold crossed"' internal/sync/worker.go)" -eq 1</automated>
    <automated>test "$(grep -c 'slog\.LevelWarn, "fk orphans summary"' internal/sync/worker.go)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c 'slog\.LevelInfo, "fetching"' internal/sync/worker.go)" -eq 0` (per-step INFO demoted)
    - `test "$(grep -c 'slog\.LevelInfo, "upserted"' internal/sync/worker.go)" -eq 0` (per-step INFO demoted)
    - `test "$(grep -c 'slog\.LevelWarn, "heap threshold crossed"' internal/sync/worker.go)" -eq 1` (security signal preserved)
    - `test "$(grep -c 'slog\.LevelWarn, "fk orphans summary"' internal/sync/worker.go)" -eq 1` (security signal preserved)
    - `test "$(grep -c 'slog\.LevelWarn, "incremental sync failed' internal/sync/worker.go)" -eq 1` (operator-actionable signal preserved)
    - `test "$(grep -c 'slog\.LevelWarn, "demoted during sync' internal/sync/worker.go)" -eq 1` (LiteFS-demotion signal preserved)
    - `test "$(grep -c 'slog\.LevelError, "sync failed after all retries' internal/sync/worker.go)" -eq 1` (final-failure signal preserved)
    - `test -f internal/sync/worker_log_levels_test.go` (RED tests committed)
    - `go test -race ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/...` exits 0
    - `golangci-lint run ./internal/sync/... ./internal/health/... ./internal/middleware/... ./cmd/peeringdb-plus/...` exits 0
    - `go generate ./...` produces zero drift in `ent/ gen/ graph/ internal/web/templates/`
  </acceptance_criteria>
  <done>All AUDIT.md "Recommended != Current" rows applied; tests lock the new levels; security-relevant WARNs preserved; lint + race + drift gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Loki | slog records (with attrs) cross from process to Grafana Cloud Loki. Demoting a log level keeps the record from being EMITTED to Loki at default INFO; security-relevant signals must remain at INFO or higher. |
| App → Tempo | Span attributes accompany traces; this plan does not modify span attribute emission, only slog levels. Cross-reference to OBS-07 plan (77-02) for trace-side concerns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information disclosure | slog records | accept | Existing logs do not contain auth tokens or PII (verified via CLAUDE.md memory `feedback_no_pii_in_repo.md` and prior OTel attribute audit 2026-04-26 quick task `260426-lod`). This plan does not add new attrs; the level demotion only reduces emission frequency. |
| T-77-02 | Repudiation (silenced security signal) | sync/worker.go heap-threshold WARN | mitigate | AUDIT.md row for `internal/sync/worker.go:643` MUST be marked `Recommended=KEEP` and `Security signal? Yes`. Acceptance criterion `grep -c 'slog\.LevelWarn, "heap threshold crossed"' internal/sync/worker.go` must equal 1. |
| T-77-03 | Repudiation (silenced FK-orphan signal) | sync/worker.go fk-orphan summary WARN | mitigate | The per-cycle aggregate at L246 was specifically introduced to fix the 7.5MB Tempo regression; demoting it would re-introduce the data-integrity blind spot. Acceptance criterion locks `grep -c 'slog\.LevelWarn, "fk orphans summary"' internal/sync/worker.go` == 1. |
| T-77-04 | Repudiation (silenced privacy-tier override) | cmd/peeringdb-plus/main.go logStartupClassification WARN | mitigate | Public-tier override (`logger.Warn("public tier override active", ...)`) is a security-relevant config signal. AUDIT.md row marks it Recommended=KEEP; main.go:899 left untouched. |
| T-77-05 | Repudiation (silenced auth/rollback failure) | sync/worker.go ERROR-level emissions | mitigate | All ERROR-level emissions (sync failure, rollback, cursor update, DB record check) are explicitly KEPT per AUDIT.md. Demoting any ERROR is out of scope for OBS-06. |
| T-77-06 | Information disclosure (DEBUG-level logs leaking sensitive data) | newly-demoted INFO/WARN → DEBUG records | accept | DEBUG-level records are not emitted to production Loki at default level (slog default = INFO). Only operators with explicit `LOG_LEVEL=debug` in env see them; that is itself an authorized debugging mode. No new attrs introduced. |
</threat_model>

<verification>
- AUDIT.md exists at `.planning/phases/77-telemetry-audit/AUDIT.md` with reclassification + KEEP tables.
- All "Security signal? Yes" rows are KEEP.
- Source-side: `grep -c 'slog\.LevelInfo, "fetching"' internal/sync/worker.go` = 0; `grep -c 'slog\.LevelWarn, "heap threshold crossed"' internal/sync/worker.go` = 1.
- New `internal/sync/worker_log_levels_test.go` exists with RED-then-GREEN test commits.
- `go test -race ./...` and `golangci-lint run ./...` clean.
- `go generate ./...` zero drift.
- (Post-merge, post-deploy, manual) Loki INFO+WARN volume in 24h window measurably down vs pre-merge.
</verification>

<success_criteria>
1. AUDIT.md committed at `.planning/phases/77-telemetry-audit/AUDIT.md` matching the template structure (reclassification table with file:line, current, recommended, rationale, security-signal columns).
2. All slog level changes prescribed by AUDIT.md applied to source files; no security-relevant signals demoted.
3. RED → GREEN test scaffolding in `internal/sync/worker_log_levels_test.go` (and `internal/health/handler_log_levels_test.go` if needed) locks the post-audit levels.
4. `go test -race ./...`, `golangci-lint run ./...`, `go generate ./...` all clean.
5. (Post-deploy verification, recorded in 77-VERIFICATION.md) Production Loki INFO+WARN log volume measurably down — operator can grep "WARN" in a 24h window without per-redaction or per-step-progress noise.
</success_criteria>

<output>
After completion, create `.planning/phases/77-telemetry-audit/77-01-SUMMARY.md` capturing:
- AUDIT.md row count by category (Recommended=DEBUG/INFO/KEEP/REMOVE)
- Files modified with specific line ranges
- Pre-merge baseline log volume (from Loki sample) and expected post-merge reduction
- Any AUDIT.md rows deferred (with rationale) for future phases
</output>
