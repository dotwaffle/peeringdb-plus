---
phase: 25-streaming-rpcs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/peeringdb/v1/services.proto
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/grpcserver/pagination.go
  - internal/grpcserver/campus.go
  - internal/grpcserver/carrier.go
  - internal/grpcserver/carrierfacility.go
  - internal/grpcserver/facility.go
  - internal/grpcserver/internetexchange.go
  - internal/grpcserver/ixfacility.go
  - internal/grpcserver/ixlan.go
  - internal/grpcserver/ixprefix.go
  - internal/grpcserver/network.go
  - internal/grpcserver/networkfacility.go
  - internal/grpcserver/networkixlan.go
  - internal/grpcserver/organization.go
  - internal/grpcserver/poc.go
  - cmd/peeringdb-plus/main.go
autonomous: true
requirements: [STRM-01, STRM-06, STRM-07]

must_haves:
  truths:
    - "All 13 Stream* RPCs are defined in services.proto with correct request messages and streaming return types"
    - "buf generate succeeds and produces updated handler interfaces with Stream* methods"
    - "All 13 handler files compile with stub Stream* implementations returning Unimplemented"
    - "OTel interceptor suppresses per-message trace events for streaming RPCs"
    - "StreamTimeout is configurable via PDBPLUS_STREAM_TIMEOUT env var with 60s default"
    - "Service structs accept StreamTimeout field for use by streaming handlers"
  artifacts:
    - path: "proto/peeringdb/v1/services.proto"
      provides: "13 Stream* RPC definitions and 13 Stream*Request messages"
      contains: "rpc StreamNetworks"
    - path: "gen/peeringdb/v1/peeringdbv1connect/services.connect.go"
      provides: "Generated handler interfaces with Stream* methods"
      contains: "StreamNetworks"
    - path: "internal/config/config.go"
      provides: "StreamTimeout configuration field"
      contains: "StreamTimeout"
    - path: "internal/grpcserver/pagination.go"
      provides: "streamBatchSize constant"
      contains: "streamBatchSize = 500"
    - path: "cmd/peeringdb-plus/main.go"
      provides: "OTel WithoutTraceEvents and StreamTimeout wiring"
      contains: "WithoutTraceEvents"
  key_links:
    - from: "proto/peeringdb/v1/services.proto"
      to: "gen/peeringdb/v1/peeringdbv1connect/services.connect.go"
      via: "buf generate"
      pattern: "StreamNetworks"
    - from: "internal/grpcserver/network.go"
      to: "gen/peeringdb/v1/peeringdbv1connect/services.connect.go"
      via: "interface implementation"
      pattern: "StreamNetworks"
    - from: "cmd/peeringdb-plus/main.go"
      to: "internal/config/config.go"
      via: "cfg.StreamTimeout passed to service structs"
      pattern: "StreamTimeout"
---

<objective>
Add 13 streaming RPC definitions to the proto schema, regenerate code, add stub implementations to all handlers, configure StreamTimeout, and update OTel interceptor.

Purpose: This plan restores compilation after the proto interface change and establishes the foundation (config, constants, stubs) that Plans 02 and 03 build real handler logic on top of.

Output: Compilable project with all 13 Stream* stubs, StreamTimeout config, and OTel trace event suppression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-streaming-rpcs/25-CONTEXT.md
@.planning/phases/25-streaming-rpcs/25-RESEARCH.md
@proto/peeringdb/v1/services.proto
@internal/config/config.go
@internal/config/config_test.go
@internal/grpcserver/pagination.go
@internal/grpcserver/network.go
@cmd/peeringdb-plus/main.go
@buf.gen.yaml

<interfaces>
<!-- Key types and contracts the executor needs. -->

From gen/peeringdb/v1/peeringdbv1connect/services.connect.go (current, pre-streaming):
The generated handler interfaces currently have Get* and List* methods.
After adding Stream* RPCs, each interface will gain a new method like:
  StreamNetworks(context.Context, *pb.StreamNetworksRequest, *connect.ServerStream[pb.Network]) error

From internal/config/config.go:
  type Config struct { ... }  // Add StreamTimeout time.Duration field
  func Load() (*Config, error)  // Add parseDuration call for PDBPLUS_STREAM_TIMEOUT

From internal/grpcserver/network.go (example service struct):
  type NetworkService struct { Client *ent.Client }
  // Must add StreamTimeout time.Duration field

From cmd/peeringdb-plus/main.go:
  otelInterceptor, err := otelconnect.NewInterceptor(
      otelconnect.WithoutServerPeerAttributes(),
  )
  // Must add otelconnect.WithoutTraceEvents()
  // Service registration uses: &grpcserver.NetworkService{Client: entClient}
  // Must change to: &grpcserver.NetworkService{Client: entClient, StreamTimeout: cfg.StreamTimeout}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proto schema + codegen + config + constants</name>
  <read_first>
    - proto/peeringdb/v1/services.proto
    - buf.gen.yaml
    - internal/config/config.go
    - internal/config/config_test.go
    - internal/grpcserver/pagination.go
  </read_first>
  <files>
    proto/peeringdb/v1/services.proto,
    internal/config/config.go,
    internal/config/config_test.go,
    internal/grpcserver/pagination.go
  </files>
  <action>
    **1. Proto schema (services.proto):**

    Add a `rpc Stream*` and a `message Stream*Request` for each of the 13 services. The RPC returns `stream EntityType` (not a response wrapper). The request message contains only filter fields (no pagination fields), all `optional`. Proto file comments document format negotiation per STRM-07.

    For each service, add after the existing `ListX` RPC:

    ```
    // StreamX streams all matching rows one message at a time.
    // Supports proto (application/proto) and JSON (application/json) via
    // Content-Type header. ConnectRPC handles format negotiation automatically.
    rpc StreamX(StreamXRequest) returns (stream EntityType);
    ```

    The 13 Stream* RPCs and their request messages (filter fields match the existing List request, minus pagination):

    - CampusService: `rpc StreamCampuses(StreamCampusesRequest) returns (stream Campus);`
      StreamCampusesRequest: optional string name=1, optional string country=2, optional string city=3, optional string status=4, optional int64 org_id=5

    - CarrierService: `rpc StreamCarriers(StreamCarriersRequest) returns (stream Carrier);`
      StreamCarriersRequest: optional string name=1, optional string status=2, optional int64 org_id=3

    - CarrierFacilityService: `rpc StreamCarrierFacilities(StreamCarrierFacilitiesRequest) returns (stream CarrierFacility);`
      StreamCarrierFacilitiesRequest: optional int64 carrier_id=1, optional int64 fac_id=2, optional string status=3

    - FacilityService: `rpc StreamFacilities(StreamFacilitiesRequest) returns (stream Facility);`
      StreamFacilitiesRequest: optional string name=1, optional string country=2, optional string city=3, optional string status=4, optional int64 org_id=5

    - InternetExchangeService: `rpc StreamInternetExchanges(StreamInternetExchangesRequest) returns (stream InternetExchange);`
      StreamInternetExchangesRequest: optional string name=1, optional string country=2, optional string city=3, optional string status=4, optional int64 org_id=5

    - IxFacilityService: `rpc StreamIxFacilities(StreamIxFacilitiesRequest) returns (stream IxFacility);`
      StreamIxFacilitiesRequest: optional int64 ix_id=1, optional int64 fac_id=2, optional string country=3, optional string city=4, optional string status=5

    - IxLanService: `rpc StreamIxLans(StreamIxLansRequest) returns (stream IxLan);`
      StreamIxLansRequest: optional int64 ix_id=1, optional string name=2, optional string status=3

    - IxPrefixService: `rpc StreamIxPrefixes(StreamIxPrefixesRequest) returns (stream IxPrefix);`
      StreamIxPrefixesRequest: optional int64 ixlan_id=1, optional string protocol=2, optional string status=3

    - NetworkService: `rpc StreamNetworks(StreamNetworksRequest) returns (stream Network);`
      StreamNetworksRequest: optional int64 asn=1, optional string name=2, optional string status=3, optional int64 org_id=4

    - NetworkFacilityService: `rpc StreamNetworkFacilities(StreamNetworkFacilitiesRequest) returns (stream NetworkFacility);`
      StreamNetworkFacilitiesRequest: optional int64 net_id=1, optional int64 fac_id=2, optional string country=3, optional string city=4, optional string status=5

    - NetworkIxLanService: `rpc StreamNetworkIxLans(StreamNetworkIxLansRequest) returns (stream NetworkIxLan);`
      StreamNetworkIxLansRequest: optional int64 net_id=1, optional int64 ixlan_id=2, optional int64 asn=3, optional string name=4, optional string status=5

    - OrganizationService: `rpc StreamOrganizations(StreamOrganizationsRequest) returns (stream Organization);`
      StreamOrganizationsRequest: optional string name=1, optional string country=2, optional string city=3, optional string status=4

    - PocService: `rpc StreamPocs(StreamPocsRequest) returns (stream Poc);`
      StreamPocsRequest: optional int64 net_id=1, optional string role=2, optional string name=3, optional string status=4

    Place each Stream*Request message immediately after the corresponding List*Response message.

    **2. Run buf generate:**
    ```bash
    TMPDIR=/tmp/claude-1000 go run github.com/bufbuild/buf/cmd/buf@latest generate
    ```

    **3. Config (internal/config/config.go):**

    Add `StreamTimeout time.Duration` field to the Config struct with comment:
    ```go
    // StreamTimeout is the maximum duration for a single streaming RPC.
    // Configured via PDBPLUS_STREAM_TIMEOUT. Default is 60 seconds.
    StreamTimeout time.Duration
    ```

    Add parsing in Load() after the syncStaleThreshold block:
    ```go
    streamTimeout, err := parseDuration("PDBPLUS_STREAM_TIMEOUT", 60*time.Second)
    if err != nil {
        return nil, fmt.Errorf("parsing PDBPLUS_STREAM_TIMEOUT: %w", err)
    }
    cfg.StreamTimeout = streamTimeout
    ```

    **4. Config test (internal/config/config_test.go):**

    Add TestLoad_StreamTimeout with table-driven tests:
    - "default is 60s" (envVal="", want=60*time.Second)
    - "explicit 30s" (envVal="30s", want=30*time.Second)
    - "explicit 2m" (envVal="2m", want=2*time.Minute)
    - "invalid duration" (envVal="not-a-duration", wantErr=true)

    Follow the exact pattern of TestLoad_SyncStaleThreshold.

    **5. Streaming batch size constant (internal/grpcserver/pagination.go):**

    Add after the existing maxPageSize constant:
    ```go
    // streamBatchSize is the number of rows fetched per database round-trip
    // during streaming RPCs. Hardcoded at 500 per user decision.
    streamBatchSize = 500
    ```
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go run github.com/bufbuild/buf/cmd/buf@latest generate && TMPDIR=/tmp/claude-1000 go test ./internal/config/... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - proto/peeringdb/v1/services.proto contains `rpc StreamNetworks(StreamNetworksRequest) returns (stream Network);`
    - proto/peeringdb/v1/services.proto contains `rpc StreamCampuses(StreamCampusesRequest) returns (stream Campus);`
    - proto/peeringdb/v1/services.proto contains all 13 `rpc Stream*` definitions
    - proto/peeringdb/v1/services.proto contains all 13 `message Stream*Request` messages
    - proto/peeringdb/v1/services.proto contains `// Supports proto (application/proto) and JSON (application/json)` comment on each Stream RPC
    - gen/peeringdb/v1/peeringdbv1connect/services.connect.go contains `StreamNetworks`
    - internal/config/config.go contains `StreamTimeout time.Duration`
    - internal/config/config.go contains `parseDuration("PDBPLUS_STREAM_TIMEOUT", 60*time.Second)`
    - internal/config/config_test.go contains `func TestLoad_StreamTimeout`
    - internal/grpcserver/pagination.go contains `streamBatchSize = 500`
    - `go test ./internal/config/... -count=1 -race` exits 0
  </acceptance_criteria>
  <done>Proto schema has 13 Stream* RPCs, codegen succeeds, StreamTimeout config field with test, streamBatchSize constant defined</done>
</task>

<task type="auto">
  <name>Task 2: Stub handlers + OTel update + service struct wiring</name>
  <read_first>
    - gen/peeringdb/v1/peeringdbv1connect/services.connect.go
    - internal/grpcserver/network.go
    - internal/grpcserver/facility.go
    - internal/grpcserver/campus.go
    - internal/grpcserver/carrier.go
    - internal/grpcserver/carrierfacility.go
    - internal/grpcserver/internetexchange.go
    - internal/grpcserver/ixfacility.go
    - internal/grpcserver/ixlan.go
    - internal/grpcserver/ixprefix.go
    - internal/grpcserver/networkfacility.go
    - internal/grpcserver/networkixlan.go
    - internal/grpcserver/organization.go
    - internal/grpcserver/poc.go
    - cmd/peeringdb-plus/main.go
  </read_first>
  <files>
    internal/grpcserver/network.go,
    internal/grpcserver/facility.go,
    internal/grpcserver/campus.go,
    internal/grpcserver/carrier.go,
    internal/grpcserver/carrierfacility.go,
    internal/grpcserver/internetexchange.go,
    internal/grpcserver/ixfacility.go,
    internal/grpcserver/ixlan.go,
    internal/grpcserver/ixprefix.go,
    internal/grpcserver/networkfacility.go,
    internal/grpcserver/networkixlan.go,
    internal/grpcserver/organization.go,
    internal/grpcserver/poc.go,
    cmd/peeringdb-plus/main.go
  </files>
  <action>
    **1. Add StreamTimeout field to all 13 service structs:**

    Each service struct currently has `Client *ent.Client`. Add a second field:
    ```go
    type NetworkService struct {
        Client        *ent.Client
        StreamTimeout time.Duration
    }
    ```
    Add `"time"` to the import block of each handler file. Do this for all 13 service structs:
    CampusService, CarrierService, CarrierFacilityService, FacilityService, InternetExchangeService,
    IxFacilityService, IxLanService, IxPrefixService, NetworkService, NetworkFacilityService,
    NetworkIxLanService, OrganizationService, PocService.

    **2. Add stub Stream* method to each of the 13 handler files:**

    Each stub follows this exact pattern (using Network as example -- adapt entity name and types for each):
    ```go
    // StreamNetworks streams all matching networks one message at a time using
    // batched keyset pagination. Returns Unimplemented until handler logic is added.
    func (s *NetworkService) StreamNetworks(ctx context.Context, req *pb.StreamNetworksRequest, stream *connect.ServerStream[pb.Network]) error {
        return connect.NewError(connect.CodeUnimplemented, fmt.Errorf("StreamNetworks not yet implemented"))
    }
    ```

    The `connect` import is already present in all handler files. Add `"time"` import.

    The 13 stub method signatures (derived from the simple codegen flag, verified in research):

    - `(s *CampusService) StreamCampuses(ctx context.Context, req *pb.StreamCampusesRequest, stream *connect.ServerStream[pb.Campus]) error`
    - `(s *CarrierService) StreamCarriers(ctx context.Context, req *pb.StreamCarriersRequest, stream *connect.ServerStream[pb.Carrier]) error`
    - `(s *CarrierFacilityService) StreamCarrierFacilities(ctx context.Context, req *pb.StreamCarrierFacilitiesRequest, stream *connect.ServerStream[pb.CarrierFacility]) error`
    - `(s *FacilityService) StreamFacilities(ctx context.Context, req *pb.StreamFacilitiesRequest, stream *connect.ServerStream[pb.Facility]) error`
    - `(s *InternetExchangeService) StreamInternetExchanges(ctx context.Context, req *pb.StreamInternetExchangesRequest, stream *connect.ServerStream[pb.InternetExchange]) error`
    - `(s *IxFacilityService) StreamIxFacilities(ctx context.Context, req *pb.StreamIxFacilitiesRequest, stream *connect.ServerStream[pb.IxFacility]) error`
    - `(s *IxLanService) StreamIxLans(ctx context.Context, req *pb.StreamIxLansRequest, stream *connect.ServerStream[pb.IxLan]) error`
    - `(s *IxPrefixService) StreamIxPrefixes(ctx context.Context, req *pb.StreamIxPrefixesRequest, stream *connect.ServerStream[pb.IxPrefix]) error`
    - `(s *NetworkService) StreamNetworks(ctx context.Context, req *pb.StreamNetworksRequest, stream *connect.ServerStream[pb.Network]) error`
    - `(s *NetworkFacilityService) StreamNetworkFacilities(ctx context.Context, req *pb.StreamNetworkFacilitiesRequest, stream *connect.ServerStream[pb.NetworkFacility]) error`
    - `(s *NetworkIxLanService) StreamNetworkIxLans(ctx context.Context, req *pb.StreamNetworkIxLansRequest, stream *connect.ServerStream[pb.NetworkIxLan]) error`
    - `(s *OrganizationService) StreamOrganizations(ctx context.Context, req *pb.StreamOrganizationsRequest, stream *connect.ServerStream[pb.Organization]) error`
    - `(s *PocService) StreamPocs(ctx context.Context, req *pb.StreamPocsRequest, stream *connect.ServerStream[pb.Poc]) error`

    **3. Update OTel interceptor in cmd/peeringdb-plus/main.go:**

    Change the otelconnect.NewInterceptor call from:
    ```go
    otelInterceptor, err := otelconnect.NewInterceptor(
        otelconnect.WithoutServerPeerAttributes(),
    )
    ```
    to:
    ```go
    otelInterceptor, err := otelconnect.NewInterceptor(
        otelconnect.WithoutServerPeerAttributes(),
        otelconnect.WithoutTraceEvents(), // Suppress per-message events (critical for streaming RPCs per STRM-06).
    )
    ```

    **4. Update service registration in main.go to pass StreamTimeout:**

    Change all 13 service registrations from:
    ```go
    registerService(peeringdbv1connect.NewCampusServiceHandler(&grpcserver.CampusService{Client: entClient}, handlerOpts))
    ```
    to:
    ```go
    registerService(peeringdbv1connect.NewCampusServiceHandler(&grpcserver.CampusService{Client: entClient, StreamTimeout: cfg.StreamTimeout}, handlerOpts))
    ```

    Do this for all 13 registerService calls.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go build ./... && TMPDIR=/tmp/claude-1000 go test ./internal/grpcserver/... -count=1 -race && TMPDIR=/tmp/claude-1000 go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (all 13 handler interfaces satisfied)
    - `go test ./internal/grpcserver/... -count=1 -race` exits 0
    - `go vet ./...` exits 0
    - internal/grpcserver/network.go contains `StreamTimeout time.Duration` in NetworkService struct
    - internal/grpcserver/network.go contains `func (s *NetworkService) StreamNetworks(`
    - internal/grpcserver/facility.go contains `func (s *FacilityService) StreamFacilities(`
    - internal/grpcserver/poc.go contains `func (s *PocService) StreamPocs(`
    - All 13 handler files contain a `Stream*` method
    - cmd/peeringdb-plus/main.go contains `otelconnect.WithoutTraceEvents()`
    - cmd/peeringdb-plus/main.go contains `StreamTimeout: cfg.StreamTimeout` (13 occurrences)
  </acceptance_criteria>
  <done>Project compiles with all 13 Stream* stubs returning Unimplemented, OTel trace events suppressed, StreamTimeout wired from config to all service structs</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds (all interfaces satisfied)
- `go test ./... -count=1 -race` succeeds (existing tests unbroken, new config test passes)
- `go vet ./...` clean
- Proto file has 13 Stream* RPCs with documented format negotiation
- All 13 handler files have Stream* stub methods
- OTel interceptor has WithoutTraceEvents()
- StreamTimeout configurable via PDBPLUS_STREAM_TIMEOUT with 60s default
</verification>

<success_criteria>
- Project compiles cleanly with all 13 streaming RPC interfaces implemented as stubs
- StreamTimeout config field added with test coverage
- OTel interceptor updated to suppress per-message trace events
- streamBatchSize constant (500) defined in pagination.go
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/25-streaming-rpcs/25-01-SUMMARY.md`
</output>
