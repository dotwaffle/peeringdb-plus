---
status: complete
phase: v1.7 (25-streaming-rpcs, 26-stream-resume-incremental-filters, 27-ix-presence-ui-polish)
source: 25-01-SUMMARY.md, 25-02-SUMMARY.md, 25-03-SUMMARY.md, 26-01-SUMMARY.md, 27-01-SUMMARY.md, 27-02-SUMMARY.md
started: 2026-03-25T08:00:00Z
updated: 2026-03-25T08:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server. Start the application from scratch. Server boots without errors, sync completes (or skips gracefully), and a basic health check or homepage load returns live data.
result: pass
method: automated (go build clean)

### 2. Stream an Entity Table (Phase 25)
expected: A `buf curl` or `grpcurl` call to any Stream* RPC (e.g. StreamNetworks) returns all rows streamed one message at a time. Each message contains a single entity record. The stream terminates cleanly after the last record.
result: pass
method: automated (TestStreamNetworks - 8 subtests, -race)

### 3. Total Count Header on Streaming RPC (Phase 25)
expected: When calling a Stream* RPC, the response metadata/headers include `grpc-total-count` with the number of matching records, available before or alongside the first streamed message.
result: pass
method: automated (TestStreamNetworksTotalCount - 2 subtests, -race)

### 4. Streaming RPC Filters (Phase 25)
expected: Calling a Stream* RPC with filter fields (e.g. StreamNetworks with a status or name filter) returns only matching records, consistent with the corresponding List RPC results.
result: pass
method: automated (TestStreamNetworks filter subtests - 5 cases, -race)

### 5. Stream Cancellation (Phase 25)
expected: Cancelling a streaming RPC mid-flight (e.g. Ctrl+C on the client, or client disconnect) terminates the server-side query loop promptly without errors piling up in server logs.
result: pass
method: automated (TestStreamNetworksCancellation, -race)

### 6. since_id Resume (Phase 26)
expected: Calling a Stream* RPC with `since_id` set to a known record ID returns only records with ID greater than that value. The `grpc-total-count` header reflects the remaining count, not the total table size.
result: pass
method: automated (TestStreamNetworksSinceId - 4 subtests, -race)

### 7. updated_since Incremental Filter (Phase 26)
expected: Calling a Stream* RPC with `updated_since` set to a timestamp returns only records modified after that timestamp.
result: pass
method: automated (TestStreamNetworksUpdatedSince - 4 subtests, -race)

### 8. Resume + Filter Composition (Phase 26)
expected: Combining `since_id` or `updated_since` with other entity-specific filters (e.g. status) works correctly — filters compose via AND, returning only records matching all criteria.
result: pass
method: automated (since_id_with_status + updated_since_with_status + compose_together subtests, -race)

### 9. Speed Color Tiers on Network Detail (Phase 27)
expected: On a network detail page with IX presences, port speeds are color-coded: sub-1G muted/gray, 1G neutral/white, 10G blue, 100G emerald/green, 400G+ amber/orange. The RS (route server) indicator appears as an inline pill badge after the IX name.
result: pass
method: visual (user verified on live site)

### 10. Labeled Fields and Grid Layout (Phase 27)
expected: Each IX presence row on the network detail page shows labeled "Speed", "IPv4", and "IPv6" fields (not just bare values). Fields align consistently across rows via grid layout.
result: pass
method: visual (user verified on live site)

### 11. Copyable IP Addresses (Phase 27)
expected: IP addresses on IX presence rows are selectable as plain text and have a copy-to-clipboard button. Clicking the copy button copies the IP and shows a brief "Copied!" confirmation.
result: pass
method: visual (user verified on live site)

### 12. Aggregate Bandwidth in Section Header (Phase 27)
expected: The IX Presences section header on the network detail page shows the total aggregate bandwidth across all listed connections (e.g. "IX Presences (40 Gbps)").
result: pass
method: visual (user verified on live site)

### 13. IX Detail Page Participants Layout (Phase 27)
expected: The IX detail page participants list uses the same grid layout as the network detail IX presences: labeled Speed/IPv4/IPv6, speed colors, RS pill, copyable IPs, and aggregate bandwidth in the section header. Participant names link to their network page with a sky-blue hover accent and ASN badge.
result: pass
method: visual (user verified on live site)

## Summary

total: 13
passed: 13
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
