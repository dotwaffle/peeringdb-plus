---
phase: 15-record-detail
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - internal/web/detail.go
  - internal/web/templates/detail_ix.templ
  - internal/web/templates/detail_fac.templ
  - internal/web/templates/detail_org.templ
  - internal/web/templates/detail_campus.templ
  - internal/web/templates/detail_carrier.templ
  - internal/web/detail_test.go
autonomous: true
requirements: [DETL-01, DETL-02, DETL-03, DETL-04, DETL-05]

must_haves:
  truths:
    - "User can view a full detail page for any IXP at /ui/ix/{id}"
    - "User can view a full detail page for any Facility at /ui/fac/{id}"
    - "User can view a full detail page for any Organization at /ui/org/{id}"
    - "User can view a full detail page for any Campus at /ui/campus/{id}"
    - "User can view a full detail page for any Carrier at /ui/carrier/{id}"
    - "IXP detail shows participants, facilities, and prefixes in collapsible lazy-loaded sections"
    - "Facility detail shows networks, IXPs, and carriers in collapsible lazy-loaded sections"
    - "Organization detail shows networks, IXPs, facilities, campuses, and carriers in collapsible lazy-loaded sections"
    - "Campus detail shows facilities in a collapsible lazy-loaded section"
    - "Carrier detail shows facilities in a collapsible lazy-loaded section"
    - "All related record rows cross-link to their own detail pages"
    - "Non-existent entity IDs return 404"
  artifacts:
    - path: "internal/web/detail.go"
      provides: "Fully implemented handlers for all 6 entity types + all fragment endpoints"
      contains: "handleIXDetail"
    - path: "internal/web/templates/detail_ix.templ"
      provides: "IXP detail page template with participant, facility, prefix sections"
      contains: "IXDetailPage"
    - path: "internal/web/templates/detail_fac.templ"
      provides: "Facility detail page template with network, IXP, carrier sections"
      contains: "FacilityDetailPage"
    - path: "internal/web/templates/detail_org.templ"
      provides: "Organization detail page with network, IXP, facility, campus, carrier sections"
      contains: "OrgDetailPage"
    - path: "internal/web/templates/detail_campus.templ"
      provides: "Campus detail page with facility section"
      contains: "CampusDetailPage"
    - path: "internal/web/templates/detail_carrier.templ"
      provides: "Carrier detail page with facility section"
      contains: "CarrierDetailPage"
    - path: "internal/web/detail_test.go"
      provides: "Tests for all 6 entity types and their fragment endpoints"
      contains: "TestIXDetail"
  key_links:
    - from: "internal/web/detail.go"
      to: "internal/web/templates/detail_ix.templ"
      via: "templates.IXDetailPage(data)"
      pattern: "templates\\.IXDetailPage"
    - from: "internal/web/templates/detail_ix.templ"
      to: "/ui/fragment/ix/"
      via: "hx-get attribute for lazy-loaded sections"
      pattern: "hx-get.*fragment/ix"
    - from: "internal/web/templates/detail_fac.templ"
      to: "/ui/fragment/fac/"
      via: "hx-get attribute for lazy-loaded sections"
      pattern: "hx-get.*fragment/fac"
    - from: "internal/web/templates/detail_org.templ"
      to: "/ui/fragment/org/"
      via: "hx-get attribute for lazy-loaded sections"
      pattern: "hx-get.*fragment/org"
---

<objective>
Implement detail pages for the remaining 5 entity types (IXP, Facility, Organization, Campus, Carrier) following the Network pattern established in Plan 01. Replace all stub handlers with full implementations and add all remaining fragment endpoints.

Purpose: Completes Phase 15 by providing detail pages for every PeeringDB entity type. Users can navigate from search results to any entity's detail page and explore related records via lazy-loaded collapsible sections.
Output: All 6 entity types have working detail pages with header stats, collapsible lazy-loaded related record sections, and cross-links between entities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-record-detail/CONTEXT.md
@.planning/phases/15-record-detail/15-RESEARCH.md
@.planning/phases/15-record-detail/15-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01. Executor should read the actual files. -->

From internal/web/templates/detailtypes.go (created by Plan 01):
- NetworkDetail, IXDetail, FacilityDetail, OrgDetail, CampusDetail, CarrierDetail structs
- Row types: IXParticipantRow, IXFacilityRow, IXPrefixRow, FacNetworkRow, FacIXRow, FacCarrierRow, OrgNetworkRow, OrgIXRow, OrgFacilityRow, OrgCampusRow, OrgCarrierRow, CampusFacilityRow, CarrierFacilityRow

From internal/web/templates/detail_shared.templ (created by Plan 01):
- CollapsibleSection(title string, count int, loadURL string)
- DetailHeader(typeBadge string, accentColor string, name string, subtitle string)
- StatBadge(label string, count int)
- DetailField(label string, value string)
- DetailLink(label string, url string)
- formatSpeed(mbps int) string

From internal/web/detail.go (created by Plan 01 -- stubs to replace):
- handleIXDetail(w, r, idStr) -- currently calls handleNotFound
- handleFacilityDetail(w, r, idStr) -- currently calls handleNotFound
- handleOrgDetail(w, r, idStr) -- currently calls handleNotFound
- handleCampusDetail(w, r, idStr) -- currently calls handleNotFound
- handleCarrierDetail(w, r, idStr) -- currently calls handleNotFound
- handleFragment(w, r, path) -- currently handles "net" type, stubs for others

From internal/web/detail.go (created by Plan 01 -- patterns to follow):
- handleNetworkDetail: parse ID/ASN, query entity WithOrganization, count stats, build template data, renderPage
- handleNetIXLansFragment: query related records, convert to row types, render fragment directly (no renderPage)

From ent schema -- query paths for IXP:
- Participants: client.IxLan.Query().Where(ixlan.IxID(ixID)).QueryNetworkIxLans().All(ctx)
  (CRITICAL: go through IxLan, NOT directly from InternetExchange per Research Pitfall 1)
- Facilities: client.IxFacility.Query().Where(ixfacility.HasInternetExchangeWith(internetexchange.ID(ixID))).All(ctx)
- Prefixes: client.IxLan.Query().Where(ixlan.IxID(ixID)).QueryIxPrefixes().All(ctx)
  (CRITICAL: go through IxLan per Research Pitfall 2)

From ent schema -- query paths for Facility:
- Networks: client.NetworkFacility.Query().Where(networkfacility.HasFacilityWith(facility.ID(facID))).All(ctx)
- IXPs: client.IxFacility.Query().Where(ixfacility.HasFacilityWith(facility.ID(facID))).All(ctx)
- Carriers: client.CarrierFacility.Query().Where(carrierfacility.HasFacilityWith(facility.ID(facID))).All(ctx)

From ent schema -- query paths for Organization:
- Networks: client.Network.Query().Where(network.HasOrganizationWith(organization.ID(orgID))).All(ctx)
- IXPs: client.InternetExchange.Query().Where(internetexchange.HasOrganizationWith(organization.ID(orgID))).All(ctx)
- Facilities: client.Facility.Query().Where(facility.HasOrganizationWith(organization.ID(orgID))).All(ctx)
- Campuses: client.Campus.Query().Where(campus.HasOrganizationWith(organization.ID(orgID))).All(ctx)
- Carriers: client.Carrier.Query().Where(carrier.HasOrganizationWith(organization.ID(orgID))).All(ctx)

From ent schema -- query paths for Campus:
- Facilities: client.Facility.Query().Where(facility.HasCampusWith(campus.ID(campusID))).All(ctx)

From ent schema -- query paths for Carrier:
- Facilities: client.CarrierFacility.Query().Where(carrierfacility.HasCarrierWith(carrier.ID(carrierID))).All(ctx)

From ent schema -- FK field notes (all Optional+Nillable):
- IxFacility: fac_id (*int), ix_id (*int)
- NetworkFacility: fac_id (*int), net_id (*int)
- CarrierFacility: carrier_id (*int), fac_id (*int)
- Dereference with nil checks when building row types

From ent schema -- Pre-computed count fields:
- InternetExchange: net_count, fac_count
- Facility: net_count, ix_count, carrier_count
- Organization: net_count, fac_count (ix_count, campus_count, carrier_count need count queries)
- Campus: no pre-computed counts (need count query for facilities)
- Carrier: fac_count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement IXP, Facility, and Organization detail handlers, templates, and fragments</name>
  <files>
    internal/web/detail.go
    internal/web/templates/detail_ix.templ
    internal/web/templates/detail_fac.templ
    internal/web/templates/detail_org.templ
  </files>
  <read_first>
    internal/web/detail.go
    internal/web/templates/detailtypes.go
    internal/web/templates/detail_shared.templ
    internal/web/templates/detail_net.templ
    internal/web/render.go
    ent/schema/internetexchange.go
    ent/schema/ixlan.go
    ent/schema/ixfacility.go
    ent/schema/ixprefix.go
    ent/schema/facility.go
    ent/schema/networkfacility.go
    ent/schema/carrierfacility.go
    ent/schema/organization.go
    .planning/phases/15-record-detail/15-RESEARCH.md
  </read_first>
  <action>
    **1. Replace IXP stub in `internal/web/detail.go`** with full implementation:

    **handleIXDetail(w, r, idStr):**
    - Parse idStr to int. On error -> handleNotFound.
    - Query: `h.client.InternetExchange.Query().Where(internetexchange.ID(id)).WithOrganization().Only(r.Context())`
    - On NotFound -> handleNotFound. On other error -> slog.Error + 500.
    - Pre-computed counts: `ix.NetCount`, `ix.FacCount`
    - Build `templates.IXDetail` with all fields: Name, NameLong, AKA, Website, City, Country, RegionContinent, Media, ProtoUnicast, ProtoMulticast, ProtoIPv6, Notes, Status
    - Org from edges: check `ix.Edges.Organization != nil`
    - Render: `renderPage(r.Context(), w, r, PageContent{Title: ix.Name, Content: templates.IXDetailPage(data)})`

    **IXP fragment handlers:**

    `handleIXParticipantsFragment(w, r, ixID)`:
    - CRITICAL QUERY PATH (Research Pitfall 1): `h.client.IxLan.Query().Where(ixlan.HasInternetExchangeWith(internetexchange.ID(ixID))).QueryNetworkIxLans().All(r.Context())`
    - Convert to `[]templates.IXParticipantRow`:
      - NetName: computed `name` field on NetworkIxLan (this is the IX name, not the network name -- see below)
      - IMPORTANT: NetworkIxLan.name is the IX name (computed from serializer). For the network name, need to check if there's a way to get it. The `asn` field is available on NetworkIxLan. Use ASN as the primary identifier: NetName can show "AS{asn}" and the ASN links to `/ui/asn/{asn}`.
      - ASN: direct int field from NetworkIxLan
      - Speed, IPAddr4 (dereference *string), IPAddr6 (dereference *string), IsRSPeer
    - Render fragment directly (no renderPage): `templates.IXParticipantsList(rows).Render(r.Context(), w)`

    `handleIXFacilitiesFragment(w, r, ixID)`:
    - Query: `h.client.IxFacility.Query().Where(ixfacility.HasInternetExchangeWith(internetexchange.ID(ixID))).All(r.Context())`
    - Convert to `[]templates.IXFacilityRow`:
      - FacName: computed `name` field on IxFacility
      - FacID: dereference nillable `fac_id` (*int), skip row if nil
      - City/Country: computed fields on IxFacility
    - Render fragment directly

    `handleIXPrefixesFragment(w, r, ixID)`:
    - CRITICAL QUERY PATH (Research Pitfall 2): `h.client.IxLan.Query().Where(ixlan.HasInternetExchangeWith(internetexchange.ID(ixID))).QueryIxPrefixes().All(r.Context())`
    - Convert to `[]templates.IXPrefixRow`: Prefix, Protocol, InDFZ
    - Render fragment directly

    **2. Replace Facility stub** with full implementation:

    **handleFacilityDetail(w, r, idStr):**
    - Parse, query `h.client.Facility.Query().Where(facility.ID(id)).WithOrganization().WithCampus().Only(r.Context())`
    - Pre-computed: `fac.NetCount`, `fac.IxCount`, `fac.CarrierCount`
    - Build `templates.FacilityDetail` with: Name, NameLong, AKA, Website, Address1, Address2, City, State, Country, Zipcode, RegionContinent, CLLI, Notes, Status
    - Campus from edges: check `fac.Edges.Campus != nil` for CampusName/CampusID
    - Org from edges: check `fac.Edges.Organization != nil`

    **Facility fragment handlers:**

    `handleFacNetworksFragment(w, r, facID)`:
    - Query: `h.client.NetworkFacility.Query().Where(networkfacility.HasFacilityWith(facility.ID(facID))).All(r.Context())`
    - Convert to `[]templates.FacNetworkRow`: NetName (computed `name`), ASN (use `local_asn` field)
    - Cross-link: `/ui/asn/{ASN}`

    `handleFacIXPsFragment(w, r, facID)`:
    - Query: `h.client.IxFacility.Query().Where(ixfacility.HasFacilityWith(facility.ID(facID))).All(r.Context())`
    - Convert to `[]templates.FacIXRow`: IXName (computed `name`), IXID (dereference `ix_id`)
    - Cross-link: `/ui/ix/{IXID}`

    `handleFacCarriersFragment(w, r, facID)`:
    - Query: `h.client.CarrierFacility.Query().Where(carrierfacility.HasFacilityWith(facility.ID(facID))).All(r.Context())`
    - Convert to `[]templates.FacCarrierRow`: CarrierName (computed `name`), CarrierID (dereference `carrier_id`)
    - Cross-link: `/ui/carrier/{CarrierID}`

    **3. Replace Organization stub** with full implementation:

    **handleOrgDetail(w, r, idStr):**
    - Parse, query `h.client.Organization.Query().Where(organization.ID(id)).Only(r.Context())`
    - Pre-computed: `org.NetCount`, `org.FacCount`
    - Count queries for non-pre-computed counts:
      - IXCount: `h.client.InternetExchange.Query().Where(internetexchange.HasOrganizationWith(organization.ID(id))).Count(r.Context())`
      - CampusCount: `h.client.Campus.Query().Where(campus.HasOrganizationWith(organization.ID(id))).Count(r.Context())`
      - CarrierCount: `h.client.Carrier.Query().Where(carrier.HasOrganizationWith(organization.ID(id))).Count(r.Context())`
    - Build `templates.OrgDetail` with all fields

    **Organization fragment handlers:**

    `handleOrgNetworksFragment(w, r, orgID)`:
    - Query: `h.client.Network.Query().Where(network.HasOrganizationWith(organization.ID(orgID))).All(r.Context())`
    - Convert to `[]templates.OrgNetworkRow`: NetName (name field), ASN (asn field)
    - Cross-link: `/ui/asn/{ASN}`

    `handleOrgIXPsFragment(w, r, orgID)`:
    - Query: `h.client.InternetExchange.Query().Where(internetexchange.HasOrganizationWith(organization.ID(orgID))).All(r.Context())`
    - Convert to `[]templates.OrgIXRow`: IXName, IXID
    - Cross-link: `/ui/ix/{IXID}`

    `handleOrgFacilitiesFragment(w, r, orgID)`:
    - Query: `h.client.Facility.Query().Where(facility.HasOrganizationWith(organization.ID(orgID))).All(r.Context())`
    - Convert to `[]templates.OrgFacilityRow`: FacName, FacID, City, Country
    - Cross-link: `/ui/fac/{FacID}`

    `handleOrgCampusesFragment(w, r, orgID)`:
    - Query: `h.client.Campus.Query().Where(campus.HasOrganizationWith(organization.ID(orgID))).All(r.Context())`
    - Convert to `[]templates.OrgCampusRow`: CampusName, CampusID
    - Cross-link: `/ui/campus/{CampusID}`

    `handleOrgCarriersFragment(w, r, orgID)`:
    - Query: `h.client.Carrier.Query().Where(carrier.HasOrganizationWith(organization.ID(orgID))).All(r.Context())`
    - Convert to `[]templates.OrgCarrierRow`: CarrierName, CarrierID
    - Cross-link: `/ui/carrier/{CarrierID}`

    **4. Update fragment dispatcher** in handleFragment to wire up all IX, Fac, and Org fragment routes (replace stubs).

    **5. Create templ files:**

    `detail_ix.templ`:
    - `IXDetailPage(data IXDetail)`: DetailHeader("ix", "sky", name, formatLocation(city, country)), stats (NetCount, FacCount), org link, detail fields (NameLong, AKA, Website, RegionContinent, Media, protocol booleans, Notes), 3 CollapsibleSections:
      - "Participants" -> /ui/fragment/ix/{ID}/participants (count: NetCount)
      - "Facilities" -> /ui/fragment/ix/{ID}/facilities (count: FacCount)
      - "Prefixes" -> /ui/fragment/ix/{ID}/prefixes (count: 0 -- no pre-computed, or omit count)
    - `IXParticipantsList(rows []IXParticipantRow)`: each row links to `/ui/asn/{ASN}`, shows AS{ASN}, speed (formatSpeed), IPv4, IPv6, RS peer badge
    - `IXFacilitiesList(rows []IXFacilityRow)`: each row links to `/ui/fac/{FacID}`, shows name, city/country
    - `IXPrefixesList(rows []IXPrefixRow)`: shows prefix, protocol, DFZ badge (no cross-link for prefixes)

    `detail_fac.templ`:
    - `FacilityDetailPage(data FacilityDetail)`: DetailHeader("fac", "violet", name, formatLocation(city, country)), stats (NetCount, IXCount, CarrierCount), org link, campus link (if CampusID > 0), detail fields (NameLong, AKA, Website, Address, CLLI, RegionContinent, Notes), 3 CollapsibleSections:
      - "Networks" -> /ui/fragment/fac/{ID}/networks (count: NetCount)
      - "IXPs" -> /ui/fragment/fac/{ID}/ixps (count: IXCount)
      - "Carriers" -> /ui/fragment/fac/{ID}/carriers (count: CarrierCount)
    - `FacNetworksList(rows []FacNetworkRow)`: each row links to `/ui/asn/{ASN}`, shows name and AS{ASN}
    - `FacIXPsList(rows []FacIXRow)`: each row links to `/ui/ix/{IXID}`
    - `FacCarriersList(rows []FacCarrierRow)`: each row links to `/ui/carrier/{CarrierID}`

    `detail_org.templ`:
    - `OrgDetailPage(data OrgDetail)`: DetailHeader("org", "amber", name, ""), stats (NetCount, FacCount, IXCount, CampusCount, CarrierCount), detail fields (NameLong, AKA, Website, Address, City, State, Country, Zipcode, Notes), 5 CollapsibleSections:
      - "Networks" -> /ui/fragment/org/{ID}/networks (count: NetCount)
      - "IXPs" -> /ui/fragment/org/{ID}/ixps (count: IXCount)
      - "Facilities" -> /ui/fragment/org/{ID}/facilities (count: FacCount)
      - "Campuses" -> /ui/fragment/org/{ID}/campuses (count: CampusCount)
      - "Carriers" -> /ui/fragment/org/{ID}/carriers (count: CarrierCount)
    - Fragment list templates for each relation (similar pattern to network fragments)

    Use `formatLocation` from search.go (it's in the web package, accessible to detail.go). For templ files that need it, pass city/country as a single string from the handler.

    **IMPORTANT anti-patterns to avoid:**
    - Do NOT use WithNetworkIxLans() on InternetExchange -- go through IxLan (Research Pitfall 1)
    - Do NOT use renderPage for fragment endpoints -- write directly (Research Pitfall 4)
    - Do NOT eager-load related records in detail handlers -- only WithOrganization and WithCampus (Research anti-pattern)
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && templ generate && go build ./internal/web/... 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "handleIXDetail" internal/web/detail.go | grep -v "NotFound"` confirms IXP handler is fully implemented (no longer stub)
    - `grep "handleFacilityDetail" internal/web/detail.go | grep -v "NotFound"` confirms facility handler is fully implemented
    - `grep "handleOrgDetail" internal/web/detail.go | grep -v "NotFound"` confirms org handler is fully implemented
    - `grep "IXDetailPage" internal/web/templates/detail_ix.templ` finds IXP detail template
    - `grep "FacilityDetailPage" internal/web/templates/detail_fac.templ` finds facility detail template
    - `grep "OrgDetailPage" internal/web/templates/detail_org.templ` finds org detail template
    - `grep "hx-get.*fragment/ix" internal/web/templates/detail_ix.templ` finds lazy-load attributes
    - `grep "hx-get.*fragment/fac" internal/web/templates/detail_fac.templ` finds lazy-load attributes
    - `grep "hx-get.*fragment/org" internal/web/templates/detail_org.templ` finds lazy-load attributes
    - `grep "/ui/asn/" internal/web/templates/detail_ix.templ` finds participant cross-links to network pages (DETL-05)
    - `grep "/ui/ix/" internal/web/templates/detail_fac.templ` finds IXP cross-links from facility (DETL-05)
    - `grep "QueryNetworkIxLans" internal/web/detail.go` confirms IXP participants go through IxLan edge
    - `go build ./internal/web/...` compiles without errors
  </acceptance_criteria>
  <done>IXP, Facility, and Organization detail pages fully working with headers, stats, org links, detail fields, and collapsible lazy-loaded sections. All fragment endpoints for these 3 types return bare HTML with cross-links. Code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Campus and Carrier detail pages, complete fragment wiring, and add all tests</name>
  <files>
    internal/web/detail.go
    internal/web/templates/detail_campus.templ
    internal/web/templates/detail_carrier.templ
    internal/web/detail_test.go
  </files>
  <read_first>
    internal/web/detail.go
    internal/web/templates/detailtypes.go
    internal/web/templates/detail_shared.templ
    internal/web/templates/detail_net.templ
    internal/web/templates/detail_ix.templ
    internal/web/templates/detail_fac.templ
    internal/web/templates/detail_org.templ
    internal/web/detail_test.go
    ent/schema/campus.go
    ent/schema/carrier.go
    ent/schema/carrierfacility.go
    internal/testutil/testutil.go
    .planning/phases/15-record-detail/15-RESEARCH.md
  </read_first>
  <action>
    **1. Replace Campus and Carrier stubs in `internal/web/detail.go`:**

    **handleCampusDetail(w, r, idStr):**
    - Parse idStr to int. On error -> handleNotFound.
    - Query: `h.client.Campus.Query().Where(campus.ID(id)).WithOrganization().Only(r.Context())`
    - On NotFound -> handleNotFound. On other error -> slog.Error + 500.
    - Count query for FacCount: `h.client.Facility.Query().Where(facility.HasCampusWith(campus.ID(id))).Count(r.Context())` (no pre-computed count on Campus)
    - Build `templates.CampusDetail`: Name, NameLong, AKA, Website, City, State, Country, Zipcode, Notes, Status, OrgName/OrgID from edges, FacCount
    - Render: `renderPage(r.Context(), w, r, PageContent{Title: campus.Name, Content: templates.CampusDetailPage(data)})`

    **handleCarrierDetail(w, r, idStr):**
    - Parse idStr to int. On error -> handleNotFound.
    - Query: `h.client.Carrier.Query().Where(carrier.ID(id)).WithOrganization().Only(r.Context())`
    - Pre-computed: `carrier.FacCount`
    - Build `templates.CarrierDetail`: Name, NameLong, AKA, Website, Notes, Status, OrgName/OrgID from edges, FacCount
    - Render: `renderPage(r.Context(), w, r, PageContent{Title: carrier.Name, Content: templates.CarrierDetailPage(data)})`

    **Campus fragment handler:**

    `handleCampusFacilitiesFragment(w, r, campusID)`:
    - Query: `h.client.Facility.Query().Where(facility.HasCampusWith(campus.ID(campusID))).All(r.Context())`
    - Convert to `[]templates.CampusFacilityRow`: FacName (name), FacID (ID), City, Country
    - Cross-link: `/ui/fac/{FacID}`
    - Render fragment directly

    **Carrier fragment handler:**

    `handleCarrierFacilitiesFragment(w, r, carrierID)`:
    - Query: `h.client.CarrierFacility.Query().Where(carrierfacility.HasCarrierWith(carrier.ID(carrierID))).All(r.Context())`
    - Convert to `[]templates.CarrierFacilityRow`: FacName (computed `name`), FacID (dereference `fac_id`)
    - Cross-link: `/ui/fac/{FacID}`
    - Render fragment directly

    **Update fragment dispatcher** to wire up campus and carrier fragment routes (replace remaining stubs).

    **2. Create templ files:**

    `detail_campus.templ`:
    - `CampusDetailPage(data CampusDetail)`: DetailHeader("campus", "rose", name, formatLocation(city, country)), stats (FacCount), org link, detail fields (NameLong, AKA, Website, City, State, Country, Zipcode, Notes), 1 CollapsibleSection:
      - "Facilities" -> /ui/fragment/campus/{ID}/facilities (count: FacCount)
    - `CampusFacilitiesList(rows []CampusFacilityRow)`: each row links to `/ui/fac/{FacID}`, shows name, city/country

    `detail_carrier.templ`:
    - `CarrierDetailPage(data CarrierDetail)`: DetailHeader("carrier", "cyan", name, ""), stats (FacCount), org link, detail fields (NameLong, AKA, Website, Notes), 1 CollapsibleSection:
      - "Facilities" -> /ui/fragment/carrier/{ID}/facilities (count: FacCount)
    - `CarrierFacilitiesList(rows []CarrierFacilityRow)`: each row links to `/ui/fac/{FacID}`, shows name

    **3. Add comprehensive tests to `internal/web/detail_test.go`:**

    All tests use `t.Parallel()`, table-driven where appropriate (per T-1, T-3).

    **Shared test data seeder helper** (reduces boilerplate across tests):
    ```go
    func seedTestData(t *testing.T, client *ent.Client) {
        // Create org, network, IX, facility, campus, carrier, and junction records
        // Returns nothing -- seed data has well-known IDs used in test assertions
    }
    ```

    Seed data with well-known IDs:
    - Organization: ID=1, Name="TestOrg"
    - Network: ID=10, ASN=13335, Name="Cloudflare", OrgID=1, IxCount=1, FacCount=1
    - InternetExchange: ID=20, Name="DE-CIX Frankfurt", OrgID=1, City="Frankfurt", Country="DE", RegionContinent="Europe", Media="Ethernet", NetCount=1, FacCount=1
    - Facility: ID=30, Name="Equinix FR5", OrgID=1, City="Frankfurt", Country="DE", NetCount=1, IxCount=1, CarrierCount=1
    - Campus: ID=40, Name="Test Campus", OrgID=1, City="Berlin", Country="DE"
    - Carrier: ID=50, Name="Test Carrier", OrgID=1, FacCount=1
    - IxLan: ID=100, IxID=20
    - NetworkIxLan: ID=200, NetID=10, IxlanID=100, ASN=13335, Speed=10000, Name="DE-CIX Frankfurt", IxID=20
    - NetworkFacility: ID=300, NetID=10, FacID=30, LocalASN=13335, Name="Equinix FR5"
    - IxFacility: ID=400, FacID=30, IxID=20, Name="DE-CIX Frankfurt", City="Frankfurt", Country="DE"
    - Poc: ID=500, NetID=10, Name="NOC Contact", Role="NOC"
    - CarrierFacility: ID=600, CarrierID=50, FacID=30, Name="Equinix FR5"

    **Test cases** (table-driven, testing all 6 types in a loop where possible):

    `TestDetailPages_AllTypes` -- table-driven test for all 6 entity types:
    ```go
    tests := []struct {
        name     string
        url      string
        wantCode int
        wantBody []string
    }{
        {"network by ASN", "/ui/asn/13335", 200, []string{"Cloudflare", "AS13335"}},
        {"IXP by ID", "/ui/ix/20", 200, []string{"DE-CIX Frankfurt", "Frankfurt"}},
        {"facility by ID", "/ui/fac/30", 200, []string{"Equinix FR5"}},
        {"org by ID", "/ui/org/1", 200, []string{"TestOrg"}},
        {"campus by ID", "/ui/campus/40", 200, []string{"Test Campus"}},
        {"carrier by ID", "/ui/carrier/50", 200, []string{"Test Carrier"}},
    }
    ```

    `TestDetailPages_NotFound` -- table-driven 404 tests:
    ```go
    tests := []struct {
        name string
        url  string
    }{
        {"network not found", "/ui/asn/99999"},
        {"ix not found", "/ui/ix/99999"},
        {"fac not found", "/ui/fac/99999"},
        {"org not found", "/ui/org/99999"},
        {"campus not found", "/ui/campus/99999"},
        {"carrier not found", "/ui/carrier/99999"},
        {"invalid asn", "/ui/asn/abc"},
        {"invalid ix id", "/ui/ix/abc"},
    }
    ```

    `TestDetailPages_HtmxFragment` -- verify htmx requests get fragment (no DOCTYPE):
    - Pick one type (network), request with HX-Request: true, verify no DOCTYPE, verify body has content

    `TestDetailPages_Stats` -- verify summary stats appear:
    - Network: body contains the IX count and fac count values
    - IXP: body contains net_count value

    `TestDetailPages_OrgLink` -- verify org cross-link present on entity detail pages:
    - Network, IXP, Facility: body contains `/ui/org/1`

    `TestDetailPages_CollapsibleSections` -- verify details/summary structure:
    - Network: body contains `<details` and `hx-trigger` and `fragment/net`
    - IXP: body contains `fragment/ix`
    - Facility: body contains `fragment/fac`

    `TestFragments_AllTypes` -- table-driven fragment endpoint tests:
    ```go
    tests := []struct {
        name     string
        url      string
        wantCode int
        wantBody []string
        noBody   []string
    }{
        {"net ixlans", "/ui/fragment/net/10/ixlans", 200, []string{"DE-CIX"}, []string{"<!doctype"}},
        {"net facilities", "/ui/fragment/net/10/facilities", 200, []string{"Equinix"}, []string{"<!doctype"}},
        {"net contacts", "/ui/fragment/net/10/contacts", 200, []string{"NOC"}, []string{"<!doctype"}},
        {"ix participants", "/ui/fragment/ix/20/participants", 200, []string{"13335"}, []string{"<!doctype"}},
        {"ix facilities", "/ui/fragment/ix/20/facilities", 200, []string{"Equinix"}, []string{"<!doctype"}},
        {"ix prefixes", "/ui/fragment/ix/20/prefixes", 200, []string{}, []string{"<!doctype"}},
        {"fac networks", "/ui/fragment/fac/30/networks", 200, []string{"Cloudflare"}, []string{"<!doctype"}},
        {"fac ixps", "/ui/fragment/fac/30/ixps", 200, []string{"DE-CIX"}, []string{"<!doctype"}},
        {"fac carriers", "/ui/fragment/fac/30/carriers", 200, []string{"Test Carrier"}, []string{"<!doctype"}},
        {"org networks", "/ui/fragment/org/1/networks", 200, []string{"Cloudflare"}, []string{"<!doctype"}},
        {"org ixps", "/ui/fragment/org/1/ixps", 200, []string{"DE-CIX"}, []string{"<!doctype"}},
        {"org facilities", "/ui/fragment/org/1/facilities", 200, []string{"Equinix"}, []string{"<!doctype"}},
        {"campus facilities", "/ui/fragment/campus/40/facilities", 200, []string{}, []string{"<!doctype"}},
        {"carrier facilities", "/ui/fragment/carrier/50/facilities", 200, []string{"Equinix"}, []string{"<!doctype"}},
    }
    ```

    `TestFragments_CrossLinks` -- verify fragment responses contain cross-links:
    - /ui/fragment/net/10/ixlans contains `/ui/ix/` (DETL-05)
    - /ui/fragment/net/10/facilities contains `/ui/fac/` (DETL-05)
    - /ui/fragment/fac/30/networks contains `/ui/asn/` (DETL-05)
    - /ui/fragment/ix/20/participants contains `/ui/asn/` (DETL-05)

    `TestFragment_InvalidPath` -- verify bad fragment URLs return 404:
    - /ui/fragment/invalid/1/test -> 404
    - /ui/fragment/net/abc/ixlans -> 404
    - /ui/fragment/net/10/invalid -> 404

    NOTE: If Plan 01 already created some tests, merge new tests into the same file. Do not duplicate existing tests. Expand the existing test file with new test functions.

    Run `templ generate` before running tests.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && templ generate && go test -race -count=1 -v ./internal/web/... 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep "handleCampusDetail" internal/web/detail.go | grep -v "NotFound"` confirms campus handler is fully implemented
    - `grep "handleCarrierDetail" internal/web/detail.go | grep -v "NotFound"` confirms carrier handler is fully implemented
    - `grep "CampusDetailPage" internal/web/templates/detail_campus.templ` finds campus template
    - `grep "CarrierDetailPage" internal/web/templates/detail_carrier.templ` finds carrier template
    - `grep "hx-get.*fragment/campus" internal/web/templates/detail_campus.templ` finds lazy-load attributes
    - `grep "hx-get.*fragment/carrier" internal/web/templates/detail_carrier.templ` finds lazy-load attributes
    - `grep "/ui/fac/" internal/web/templates/detail_campus.templ` finds facility cross-links from campus (DETL-05)
    - `grep "/ui/fac/" internal/web/templates/detail_carrier.templ` finds facility cross-links from carrier (DETL-05)
    - `grep "TestDetailPages_AllTypes" internal/web/detail_test.go` finds table-driven all-types test
    - `grep "TestDetailPages_NotFound" internal/web/detail_test.go` finds 404 tests
    - `grep "TestFragments_AllTypes" internal/web/detail_test.go` finds fragment tests
    - `grep "TestFragments_CrossLinks" internal/web/detail_test.go` finds cross-link tests (DETL-05)
    - `go test -race -count=1 ./internal/web/...` passes with all tests green
    - No stub handlers remain (all 6 types fully implemented, no handleNotFound calls in detail handlers)
  </acceptance_criteria>
  <done>All 6 entity types have fully working detail pages. All fragment endpoints return lazy-loaded HTML. All related record rows cross-link to their own detail pages. Comprehensive tests cover all types, 404s, fragment endpoints, cross-links, and htmx fragment behavior. All tests pass with -race flag.</done>
</task>

</tasks>

<verification>
1. `go test -race -count=1 ./internal/web/...` -- all tests pass (existing + new)
2. `go test -race -count=1 ./...` -- full suite passes
3. All 6 detail pages render: /ui/asn/{asn}, /ui/ix/{id}, /ui/fac/{id}, /ui/org/{id}, /ui/campus/{id}, /ui/carrier/{id}
4. All fragment endpoints return bare HTML without layout wrapper
5. Every related record section uses hx-trigger="toggle once from:closest details" for lazy loading
6. Every related record row cross-links to its own detail page
7. Non-existent entities return 404
8. Summary stats appear in header area of each detail page
</verification>

<success_criteria>
- GET /ui/ix/{valid_id} returns 200 with full detail page (DETL-01)
- GET /ui/fac/{valid_id} returns 200 with full detail page (DETL-01)
- GET /ui/org/{valid_id} returns 200 with full detail page (DETL-01)
- GET /ui/campus/{valid_id} returns 200 with full detail page (DETL-01)
- GET /ui/carrier/{valid_id} returns 200 with full detail page (DETL-01)
- All detail pages have collapsible details/summary sections (DETL-02)
- All sections lazy-load via htmx fragments on first toggle (DETL-03)
- IXP page shows participant count and facility count in header (DETL-04)
- Facility page shows network count, IXP count, carrier count in header (DETL-04)
- Org page shows counts for all 5 child entity types (DETL-04)
- Fragment responses contain cross-links: /ui/asn/, /ui/ix/, /ui/fac/, /ui/org/, /ui/campus/, /ui/carrier/ (DETL-05)
- All tests pass with `go test -race -count=1 ./internal/web/...`
</success_criteria>

<output>
After completion, create `.planning/phases/15-record-detail/15-02-SUMMARY.md`
</output>
