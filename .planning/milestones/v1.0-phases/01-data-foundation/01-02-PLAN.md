---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - ent/entc.go
  - ent/schema/organization.go
  - ent/schema/network.go
  - ent/schema/facility.go
  - ent/schema/internetexchange.go
  - ent/schema/poc.go
  - ent/schema/ixlan.go
  - ent/schema/ixprefix.go
  - ent/schema/networkixlan.go
  - ent/schema/networkfacility.go
  - ent/schema/ixfacility.go
  - ent/schema/carrier.go
  - ent/schema/carrierfacility.go
  - ent/schema/campus.go
  - ent/schema/organization_test.go
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "All 13 PeeringDB object types exist as entgo schemas"
    - "Every field from the PeeringDB API field inventory is present in the corresponding schema"
    - "FK relationships are modeled as entgo edges with nullable FK fields"
    - "Junction tables (netixlan, netfac, carrierfac, ixfac) are modeled with edges to both sides"
    - "Computed/serializer fields are stored as regular fields"
    - "Code generation succeeds with all 13 schemas"
    - "CRUD operations work for all 13 types in-memory"
    - "Mutation hooks for OTel tracing are configured on write operations per D-46"
  artifacts:
    - path: "ent/entc.go"
      provides: "entgo code generation config with mutation hooks for OTel tracing"
      contains: "entgql.NewExtension"
    - path: "ent/schema/network.go"
      provides: "Network schema with all fields and edges"
      contains: "func (Network) Fields()"
    - path: "ent/schema/facility.go"
      provides: "Facility schema with AddressModel fields"
      contains: "func (Facility) Fields()"
    - path: "ent/schema/internetexchange.go"
      provides: "Internet Exchange schema"
      contains: "func (InternetExchange) Fields()"
    - path: "ent/schema/poc.go"
      provides: "Point of Contact schema"
      contains: "func (Poc) Fields()"
    - path: "ent/schema/ixlan.go"
      provides: "IXLan schema"
      contains: "func (IxLan) Fields()"
    - path: "ent/schema/ixprefix.go"
      provides: "IX Prefix schema"
      contains: "func (IxPrefix) Fields()"
    - path: "ent/schema/networkixlan.go"
      provides: "Network-IXLan junction schema"
      contains: "func (NetworkIxLan) Fields()"
    - path: "ent/schema/networkfacility.go"
      provides: "Network-Facility junction schema"
      contains: "func (NetworkFacility) Fields()"
    - path: "ent/schema/ixfacility.go"
      provides: "IX-Facility junction schema"
      contains: "func (IxFacility) Fields()"
    - path: "ent/schema/carrier.go"
      provides: "Carrier schema"
      contains: "func (Carrier) Fields()"
    - path: "ent/schema/carrierfacility.go"
      provides: "Carrier-Facility junction schema"
      contains: "func (CarrierFacility) Fields()"
    - path: "ent/schema/campus.go"
      provides: "Campus schema"
      contains: "func (Campus) Fields()"
  key_links:
    - from: "ent/schema/network.go"
      to: "ent/schema/organization.go"
      via: "edge.From org, field org_id"
      pattern: 'edge\.From\("organization"'
    - from: "ent/schema/networkixlan.go"
      to: "ent/schema/network.go"
      via: "edge.From network, field net_id"
      pattern: 'edge\.From\("network"'
    - from: "ent/schema/networkixlan.go"
      to: "ent/schema/ixlan.go"
      via: "edge.From ixlan, field ixlan_id"
      pattern: 'edge\.From\("ix_lan"'
    - from: "ent/schema/facility.go"
      to: "ent/schema/campus.go"
      via: "edge.From campus, field campus_id"
      pattern: 'edge\.From\("campus"'
    - from: "ent/entc.go"
      to: "ent/schema/*.go"
      via: "Hooks() method on schemas for OTel mutation tracing"
      pattern: 'func.*Hooks\(\)'
---

<objective>
Define all 13 PeeringDB object types as entgo schemas with complete field inventories, edges (relationships), indexes, annotations, and OTel mutation hooks. Update the Organization schema from Plan 01 to include its edges to other types. Configure entgo mutation hooks for OTel tracing on writes per D-46.

Purpose: DATA-01 requires all 13 types modeled. DATA-02 requires fields match actual API responses. D-46 requires mutation hooks for OTel tracing. This plan delivers the complete data model that the sync worker and all API surfaces depend on.
Output: 13 compilable entgo schema files with OTel mutation hooks, generated ent client code, passing CRUD tests for all types.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01, the following are established: -->
<!-- ent/entc.go: entgql extension, sql/upsert feature flag -->
<!-- ent/generate.go: //go:generate go run -mod=mod entc.go -->
<!-- ent/schema/organization.go: Organization schema (will be updated with edges) -->
<!-- internal/testutil/testutil.go: SetupClient(t) returns *ent.Client -->
<!-- SocialMedia struct defined in organization.go: { Service string, Identifier string } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 13 entgo schemas with fields, edges, indexes, annotations, and OTel mutation hooks</name>
  <files>
    ent/entc.go,
    ent/schema/organization.go,
    ent/schema/network.go,
    ent/schema/facility.go,
    ent/schema/internetexchange.go,
    ent/schema/poc.go,
    ent/schema/ixlan.go,
    ent/schema/ixprefix.go,
    ent/schema/networkixlan.go,
    ent/schema/networkfacility.go,
    ent/schema/ixfacility.go,
    ent/schema/carrier.go,
    ent/schema/carrierfacility.go,
    ent/schema/campus.go
  </files>
  <read_first>
    ent/schema/organization.go
    ent/entc.go
    .planning/phases/01-data-foundation/01-RESEARCH.md (lines 412-705 for complete field inventory, lines 222-254 for edge pattern, lines 189-220 for ID pattern)
    .planning/phases/01-data-foundation/01-CONTEXT.md (decisions D-20, D-36-D-47)
    CLAUDE.md (CS-2 for no name stutter, API-1 for documented exports)
  </read_first>
  <action>
    Create 12 new schema files, update Organization with edges, and configure OTel mutation hooks per D-46. Each schema follows these consistent patterns:

    **Common patterns for ALL schemas (per HandleRefModel):**
    - `field.Int("id").Positive().Immutable().Comment("PeeringDB {type} ID")` per D-37
    - `field.Time("created").Immutable().Comment("PeeringDB creation timestamp")` per D-41, D-47
    - `field.Time("updated").Comment("PeeringDB last update timestamp")` per D-41, D-47
    - `field.String("status").MaxLen(255).Default("ok").Comment("Record status")`
    - ALL FK fields: `field.Int("{fk}_id").Optional().Nillable().Comment("FK to {target}")` per D-20
    - ALL edges: `edge.From("{name}", {Type}.Type).Ref("{back_ref}").Field("{fk}_id").Unique()` per D-38
    - Choice fields: `field.String` NOT `field.Enum` (per Pitfall 6 in research)
    - JSON array fields: `field.JSON("{name}", []{GoType}{}).Optional()`
    - Annotations: `entgql.QueryField()` on every schema per D-42

    **SocialMedia type** should be moved to a shared file `ent/schema/types.go` since it is used by org, net, fac, ix, carrier, campus.

    Create `ent/schema/types.go`:
    ```go
    package schema

    // SocialMedia represents a social media link from PeeringDB.
    type SocialMedia struct {
        Service    string `json:"service"`
        Identifier string `json:"identifier"`
    }
    ```

    **OTel Mutation Hooks per D-46:**

    D-46 requires "basic mutation hooks for OTel tracing on writes." Implement this by adding a `Hooks()` method on each schema that creates an OTel span for every mutation (create, update, delete). Use a shared hook helper to avoid code duplication.

    Create a shared hook helper in `ent/schema/hooks.go`:
    ```go
    package schema

    import (
        "context"
        "fmt"

        "entgo.io/ent"
        "go.opentelemetry.io/otel"
        "go.opentelemetry.io/otel/attribute"
    )

    // otelMutationHook returns an ent.Hook that creates an OTel span around mutations.
    // The span name follows the pattern "ent.{Type}.{Op}" (e.g., "ent.Organization.Create").
    func otelMutationHook(typeName string) ent.Hook {
        return func(next ent.Mutator) ent.Mutator {
            return ent.MutateFunc(func(ctx context.Context, m ent.Mutation) (ent.Value, error) {
                spanName := fmt.Sprintf("ent.%s.%s", typeName, m.Op().String())
                ctx, span := otel.Tracer("ent").Start(ctx, spanName)
                defer span.End()

                span.SetAttributes(
                    attribute.String("ent.type", typeName),
                    attribute.String("ent.op", m.Op().String()),
                )

                v, err := next.Mutate(ctx, m)
                if err != nil {
                    span.RecordError(err)
                }
                return v, err
            })
        }
    }
    ```

    Then on each schema, add a `Hooks()` method:
    ```go
    // Hooks returns Organization's mutation hooks for OTel tracing per D-46.
    func (Organization) Hooks() []ent.Hook {
        return []ent.Hook{
            otelMutationHook("Organization"),
        }
    }
    ```

    Add this `Hooks()` method to ALL 13 schemas, each passing its own type name.

    **Schema definitions (fields from research field inventory):**

    **Organization (`organization.go`)** -- UPDATE existing file to add edges and Hooks:
    - Add `Edges()`: `edge.To("networks", Network.Type)`, `edge.To("facilities", Facility.Type)`, `edge.To("internet_exchanges", InternetExchange.Type)`, `edge.To("carriers", Carrier.Type)`, `edge.To("campuses", Campus.Type)`
    - Add `Hooks()` with `otelMutationHook("Organization")` per D-46
    - Remove SocialMedia struct (moved to types.go)

    **Campus (`campus.go`):**
    - Fields: id, org_id (FK, Optional Nillable), org_name (String Optional), name (String 255 NotEmpty Unique), name_long (String 255 Optional Nillable), aka (String 255 Optional Nillable), website (String Optional), social_media (JSON []SocialMedia Optional), notes (String Optional), country (String Optional), city (String Optional), zipcode (String Optional), state (String Optional), logo (String Optional Nillable), created, updated, status
    - Edges: From organization via org_id; To facilities
    - Indexes: name, status, org_id
    - Hooks: `otelMutationHook("Campus")` per D-46

    **Facility (`facility.go`):**
    - Fields: id, org_id (FK), org_name (String Optional computed), campus_id (FK Optional Nillable), name (String 255 NotEmpty Unique), aka (String 255 Optional), name_long (String 255 Optional), website (String Optional), social_media (JSON), clli (String 18 Optional), rencode (String 18 Optional), npanxx (String 21 Optional), tech_email (String 254 Optional), tech_phone (String 192 Optional), sales_email (String 254 Optional), sales_phone (String 192 Optional), property (String 27 Optional Nillable), diverse_serving_substations (Bool Optional Nillable), available_voltage_services (JSON []string Optional), notes (String Optional), region_continent (String Optional Nillable), status_dashboard (String Optional Nillable), logo (String Optional Nillable), net_count (Int Optional computed), ix_count (Int Optional computed), carrier_count (Int Optional computed), address1 (String Optional), address2 (String Optional), city (String Optional), state (String Optional), country (String Optional), zipcode (String 48 Optional), suite (String 255 Optional), floor (String 255 Optional), latitude (Float Optional Nillable), longitude (Float Optional Nillable), created, updated, status
    - Edges: From organization via org_id; From campus via campus_id; To network_facilities, ix_facilities, carrier_facilities
    - Indexes: name, status, org_id, campus_id
    - Hooks: `otelMutationHook("Facility")` per D-46

    **Network (`network.go`):**
    - Fields: id, org_id (FK), name (String 255 NotEmpty Unique), aka (String 255 Optional), name_long (String 255 Optional), website (String Optional), social_media (JSON), asn (Int Unique Positive), looking_glass (String Optional), route_server (String Optional), irr_as_set (String 255 Optional), info_type (String 60 Optional), info_types (JSON []string Optional), info_prefixes4 (Int Optional Nillable), info_prefixes6 (Int Optional Nillable), info_traffic (String 39 Optional), info_ratio (String 45 Optional), info_scope (String 39 Optional), info_unicast (Bool Default false), info_multicast (Bool Default false), info_ipv6 (Bool Default false), info_never_via_route_servers (Bool Default false), notes (String Optional), policy_url (String Optional), policy_general (String 72 Optional), policy_locations (String 72 Optional), policy_ratio (Bool Default false), policy_contracts (String 36 Optional), allow_ixp_update (Bool Default false), status_dashboard (String Optional Nillable), rir_status (String 255 Optional Nillable), rir_status_updated (Time Optional Nillable), logo (String Optional Nillable), ix_count (Int Optional computed), fac_count (Int Optional computed), netixlan_updated (Time Optional Nillable computed), netfac_updated (Time Optional Nillable computed), poc_updated (Time Optional Nillable computed), created, updated, status
    - Edges: From organization via org_id; To pocs, network_facilities, network_ix_lans
    - Indexes: name, status, asn, org_id
    - Hooks: `otelMutationHook("Network")` per D-46

    **InternetExchange (`internetexchange.go`):**
    - Fields: id, org_id (FK), name (String 64 NotEmpty Unique), aka (String 255 Optional), name_long (String 255 Optional), city (String 192), country (String), region_continent (String 255), media (String 128 Default "Ethernet"), notes (String Optional), proto_unicast (Bool Default false), proto_multicast (Bool Default false), proto_ipv6 (Bool Default false), website (String Optional), social_media (JSON), url_stats (String Optional), tech_email (String 254 Optional), tech_phone (String 192 Optional), policy_email (String 254 Optional), policy_phone (String 192 Optional), sales_email (String 254 Optional), sales_phone (String 192 Optional), net_count (Int Optional computed), fac_count (Int Optional computed), ixf_net_count (Int Default 0), ixf_last_import (Time Optional Nillable), ixf_import_request (String Optional Nillable), ixf_import_request_status (String Optional), service_level (String 60 Optional), terms (String 60 Optional), status_dashboard (String Optional Nillable), logo (String Optional Nillable), created, updated, status
    - Edges: From organization via org_id; To ix_lans, ix_facilities
    - Indexes: name, status, org_id
    - Hooks: `otelMutationHook("InternetExchange")` per D-46

    **Poc (`poc.go`):**
    - Fields: id, net_id (FK Optional Nillable), role (String 27), visible (String 64 Default "Public"), name (String 254 Optional), phone (String 100 Optional), email (String 254 Optional), url (String Optional), created, updated, status
    - Edges: From network via net_id
    - Indexes: status, net_id, role
    - Hooks: `otelMutationHook("Poc")` per D-46

    **IxLan (`ixlan.go`):**
    - Fields: id, ix_id (FK Optional Nillable), name (String 255 Optional), descr (String Optional), mtu (Int Default 1500), dot1q_support (Bool Default false), rs_asn (Int Optional Nillable Default 0), arp_sponge (String Optional Nillable), ixf_ixp_member_list_url_visible (String 64 Default "Private"), ixf_ixp_import_enabled (Bool Default false), created, updated, status
    - Edges: From internet_exchange via ix_id; To ix_prefixes, network_ix_lans
    - Indexes: status, ix_id
    - Hooks: `otelMutationHook("IxLan")` per D-46

    **IxPrefix (`ixprefix.go`):**
    - Fields: id, ixlan_id (FK Optional Nillable), protocol (String 64), prefix (String Unique), in_dfz (Bool Default false), notes (String 255 Optional), created, updated, status
    - Edges: From ix_lan via ixlan_id
    - Indexes: status, ixlan_id, protocol
    - Hooks: `otelMutationHook("IxPrefix")` per D-46

    **IxFacility (`ixfacility.go`):**
    - Fields: id, ix_id (FK Optional Nillable), fac_id (FK Optional Nillable), name (String Optional computed), city (String Optional computed), country (String Optional computed), created, updated, status
    - Edges: From internet_exchange via ix_id; From facility via fac_id
    - Indexes: status, ix_id, fac_id
    - Hooks: `otelMutationHook("IxFacility")` per D-46

    **NetworkIxLan (`networkixlan.go`):**
    - Fields: id, net_id (FK Optional Nillable), ix_id (Int Optional computed -- NOT an edge, just stored), ixlan_id (FK Optional Nillable), name (String Optional computed), notes (String 255 Optional), speed (Int), asn (Int), ipaddr4 (String Optional Nillable), ipaddr6 (String Optional Nillable), is_rs_peer (Bool Default false), bfd_support (Bool Default false), operational (Bool Default true), net_side_id (Int Optional Nillable), ix_side_id (Int Optional Nillable), created, updated, status
    - Edges: From network via net_id; From ix_lan via ixlan_id
    - Indexes: status, net_id, ixlan_id, asn, ipaddr4, ipaddr6
    - Hooks: `otelMutationHook("NetworkIxLan")` per D-46

    **NetworkFacility (`networkfacility.go`):**
    - Fields: id, net_id (FK Optional Nillable), fac_id (FK Optional Nillable), name (String Optional computed), city (String Optional computed), country (String Optional computed), local_asn (Int), created, updated, status
    - Edges: From network via net_id; From facility via fac_id
    - Indexes: status, net_id, fac_id, local_asn
    - Hooks: `otelMutationHook("NetworkFacility")` per D-46

    **Carrier (`carrier.go`):**
    - Fields: id, org_id (FK Optional Nillable), org_name (String Optional computed), name (String 255 NotEmpty Unique), aka (String 255 Optional), name_long (String 255 Optional), website (String Optional), social_media (JSON), notes (String Optional), fac_count (Int Optional computed), logo (String Optional Nillable), created, updated, status
    - Edges: From organization via org_id; To carrier_facilities
    - Indexes: name, status, org_id
    - Hooks: `otelMutationHook("Carrier")` per D-46

    **CarrierFacility (`carrierfacility.go`):**
    - Fields: id, carrier_id (FK Optional Nillable), fac_id (FK Optional Nillable), name (String Optional computed), created, updated, status
    - Edges: From carrier via carrier_id; From facility via fac_id
    - Indexes: status, carrier_id, fac_id
    - Hooks: `otelMutationHook("CarrierFacility")` per D-46

    **Edge naming conventions:**
    - Parent-to-child: `edge.To("{children}", {Child}.Type)` on parent
    - Child-to-parent: `edge.From("{parent}", {Parent}.Type).Ref("{back_ref}").Field("{fk}_id").Unique()` on child
    - Back-ref names must match the `edge.To` name on the other side

    After creating all schemas, run `go generate ./ent` to regenerate the full ent client.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go generate ./ent && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - ent/schema/types.go contains `type SocialMedia struct`
    - ent/schema/hooks.go contains `func otelMutationHook(`
    - ent/schema/hooks.go contains `otel.Tracer("ent")`
    - ent/schema/hooks.go contains `span.RecordError`
    - ent/schema/organization.go contains `edge.To("networks"`
    - ent/schema/organization.go contains `edge.To("facilities"`
    - ent/schema/organization.go contains `edge.To("campuses"`
    - ent/schema/organization.go contains `func (Organization) Hooks()`
    - ent/schema/network.go contains `field.Int("asn").Unique().Positive()`
    - ent/schema/network.go contains `field.Int("org_id").Optional().Nillable()`
    - ent/schema/network.go contains `edge.From("organization"`
    - ent/schema/network.go contains `func (Network) Hooks()`
    - ent/schema/facility.go contains `field.String("clli")`
    - ent/schema/facility.go contains `edge.From("campus"`
    - ent/schema/facility.go contains `func (Facility) Hooks()`
    - ent/schema/internetexchange.go contains `field.String("media")`
    - ent/schema/poc.go contains `field.String("role")`
    - ent/schema/ixlan.go contains `field.Int("mtu")`
    - ent/schema/ixprefix.go contains `field.String("prefix")`
    - ent/schema/networkixlan.go contains `field.String("ipaddr4")`
    - ent/schema/networkixlan.go contains `field.String("ipaddr6")`
    - ent/schema/networkixlan.go contains `field.Int("net_side_id")`
    - ent/schema/networkfacility.go contains `field.Int("local_asn")`
    - ent/schema/ixfacility.go contains `field.Int("ix_id")`
    - ent/schema/carrier.go contains `edge.To("carrier_facilities"`
    - ent/schema/carrierfacility.go contains `edge.From("carrier"`
    - ent/schema/campus.go contains `field.Int("org_id")`
    - 13 schema files exist in ent/schema/ (excluding types.go, hooks.go, and test files)
    - `go generate ./ent` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>All 13 PeeringDB schemas defined with complete field inventories, edges, indexes, annotations, and OTel mutation hooks per D-46. Code generation succeeds. All packages compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema CRUD verification tests for all 13 types</name>
  <files>
    ent/schema/schema_test.go
  </files>
  <read_first>
    ent/schema/organization.go
    ent/schema/network.go
    ent/schema/facility.go
    ent/schema/internetexchange.go
    ent/schema/networkixlan.go
    internal/testutil/testutil.go
    .planning/phases/01-data-foundation/01-REFERENCE-SQLITE-ENTGO.md
  </read_first>
  <behavior>
    - Test 1: Create and query Organization with all required fields
    - Test 2: Create and query Network with FK to Organization (edge traversal)
    - Test 3: Create and query Facility with FK to Organization
    - Test 4: Create and query InternetExchange with FK to Organization
    - Test 5: Create and query Campus with FK to Organization
    - Test 6: Create and query Carrier with FK to Organization
    - Test 7: Create and query IxLan with FK to InternetExchange
    - Test 8: Create and query IxPrefix with FK to IxLan
    - Test 9: Create and query Poc with FK to Network
    - Test 10: Create and query NetworkFacility with FKs to Network + Facility
    - Test 11: Create and query NetworkIxLan with FKs to Network + IxLan
    - Test 12: Create and query IxFacility with FKs to InternetExchange + Facility
    - Test 13: Create and query CarrierFacility with FKs to Carrier + Facility
    - Test 14: Verify nullable FK fields accept nil values (D-20)
    - Test 15: Verify edge traversal works (e.g., org -> networks -> network_ix_lans)
  </behavior>
  <action>
    Create `ent/schema/schema_test.go` with table-driven tests per T-1.

    Use `testutil.SetupClient(t)` for each test. Tests should be parallel where possible (T-3).

    The test file structure:
    ```go
    package schema_test

    import (
        "context"
        "testing"
        "time"

        "github.com/dotwaffle/peeringdb-plus/internal/testutil"
    )

    func TestAllSchemasCRUD(t *testing.T) {
        // Table-driven: for each of the 13 types, create a record with required fields,
        // query it back, verify fields match.
    }

    func TestNullableFK(t *testing.T) {
        // Create a NetworkFacility with nil net_id and nil fac_id
        // Verify it succeeds (D-20)
    }

    func TestEdgeTraversal(t *testing.T) {
        // Create org -> network -> network_ix_lan chain
        // Query org, traverse .QueryNetworks(), traverse .QueryNetworkIxLans()
        // Verify the chain returns correct data
    }
    ```

    Each type test creates a minimal valid record (ID + required fields + created/updated timestamps), queries by ID, and asserts field values match. Use `time.Date(2024, 1, 1, 0, 0, 0, 0, time.UTC)` for consistent timestamps.

    For junction table tests (netfac, netixlan, ixfac, carrierfac), create parent records first, then create the junction record with FK IDs set.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go test -race -count=1 -v ./ent/schema/...</automated>
  </verify>
  <acceptance_criteria>
    - ent/schema/schema_test.go contains `func TestAllSchemasCRUD`
    - ent/schema/schema_test.go contains `func TestNullableFK`
    - ent/schema/schema_test.go contains `func TestEdgeTraversal`
    - ent/schema/schema_test.go contains `testutil.SetupClient`
    - ent/schema/schema_test.go contains `t.Parallel()` on at least one subtest
    - `go test -race ./ent/schema/...` exits 0
    - Test output shows all 13 types tested
  </acceptance_criteria>
  <done>All 13 schema types pass CRUD tests. Nullable FK fields verified. Edge traversal works across entity relationships. Schema correctness validated against in-memory SQLite.</done>
</task>

</tasks>

<verification>
1. `go generate ./ent` succeeds with all 13 schemas
2. `go build ./...` compiles without errors
3. `go test -race ./ent/schema/...` passes all CRUD and edge traversal tests
4. Each schema file contains the fields documented in the research field inventory
5. All FK fields are Optional().Nillable() per D-20
6. No field.Enum used anywhere (field.String for all choices)
7. All 13 schemas have Hooks() method returning otelMutationHook per D-46
</verification>

<success_criteria>
- 13 entgo schema files exist with fields matching the PeeringDB API field inventory
- All FK relationships modeled as entgo edges with nullable FK integer fields
- Computed/serializer fields (org_name, net_count, ix_count, etc.) stored as regular fields
- IP addresses stored as strings per research Pattern 6
- social_media stored as JSON per research Pattern 7
- Choice fields use field.String, not field.Enum
- All schemas have indexes on commonly-queried fields per D-45
- All schemas have entgql annotations per D-42
- All schemas have OTel mutation hooks per D-46
- Code generation produces working ent client
- CRUD + edge traversal tests pass for all 13 types
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
