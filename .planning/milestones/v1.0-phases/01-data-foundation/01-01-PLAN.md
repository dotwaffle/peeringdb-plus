---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - LICENSE
  - Dockerfile
  - ent/entc.go
  - ent/generate.go
  - ent/schema/organization.go
  - internal/config/config.go
  - internal/database/database.go
  - internal/otel/provider.go
  - internal/testutil/testutil.go
autonomous: true
requirements: [STOR-01]

must_haves:
  truths:
    - "Go module initializes and all dependencies resolve"
    - "entgo code generation runs successfully against a sample schema"
    - "SQLite database opens with WAL mode and FK constraints enabled"
    - "OTel trace provider initializes without error"
    - "In-memory SQLite test helper works for enttest"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module github.com/dotwaffle/peeringdb-plus"
    - path: "ent/entc.go"
      provides: "entgo code generation configuration with extensions"
      contains: "entgql.NewExtension"
    - path: "ent/generate.go"
      provides: "go:generate directive for entgo"
      contains: "go:generate"
    - path: "ent/schema/organization.go"
      provides: "Proof-of-concept entgo schema"
      contains: "func (Organization) Fields()"
    - path: "internal/config/config.go"
      provides: "Environment variable configuration"
      contains: "PDBPLUS_DB_PATH"
    - path: "internal/database/database.go"
      provides: "SQLite client setup with modernc driver"
      contains: "sql.Register"
    - path: "internal/otel/provider.go"
      provides: "OpenTelemetry trace provider initialization"
      contains: "otel.SetTracerProvider"
    - path: "internal/testutil/testutil.go"
      provides: "Shared test helper for in-memory entgo client"
      contains: "enttest.Open"
    - path: "LICENSE"
      provides: "BSD 3-Clause license"
      contains: "BSD 3-Clause"
    - path: "Dockerfile"
      provides: "Multi-stage Docker build"
      contains: "FROM golang"
  key_links:
    - from: "internal/database/database.go"
      to: "ent/client.go"
      via: "ent.NewClient with entsql.OpenDB"
      pattern: "ent\\.NewClient"
    - from: "ent/entc.go"
      to: "ent/schema/organization.go"
      via: "entc.Generate reads schema directory"
      pattern: "entc\\.Generate"
---

<objective>
Bootstrap the Go project with module initialization, all dependencies, entgo code generation infrastructure, SQLite database setup, OpenTelemetry provider, configuration loading, and a proof-of-concept Organization schema that validates the entire toolchain end-to-end.

Purpose: Establishes the project foundation that all subsequent plans build upon. Without a working go.mod, entc.go, and SQLite driver setup, no other work can proceed.
Output: A compilable Go project with `go generate ./ent` producing a working entgo client that can open an SQLite database with WAL mode.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-REFERENCE-SQLITE-ENTGO.md

<interfaces>
<!-- No existing code -- this is the first plan in a greenfield project. -->
<!-- Key patterns to follow are in 01-REFERENCE-SQLITE-ENTGO.md and 01-RESEARCH.md architecture patterns. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module, install dependencies, create project structure and infrastructure</name>
  <files>
    go.mod, go.sum, LICENSE, Dockerfile,
    internal/config/config.go,
    internal/database/database.go,
    internal/otel/provider.go,
    internal/testutil/testutil.go
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-REFERENCE-SQLITE-ENTGO.md
    .planning/phases/01-data-foundation/01-RESEARCH.md (lines 100-170 for project structure, lines 786-865 for code examples)
    .planning/phases/01-data-foundation/01-CONTEXT.md (decisions D-28, D-33, D-34, D-35, D-48-D-54)
    CLAUDE.md (coding conventions)
  </read_first>
  <action>
    1. Initialize Go module per D-48:
       ```
       go mod init github.com/dotwaffle/peeringdb-plus
       ```

    2. Install all Phase 1 dependencies (per Standard Stack in research):
       ```
       go get entgo.io/ent@v0.14.5
       go get entgo.io/contrib@v0.7.0
       go get github.com/lrstanley/entrest@v1.0.2
       go get github.com/99designs/gqlgen@latest
       go get google.golang.org/protobuf@latest
       go get modernc.org/sqlite@latest
       go get golang.org/x/time@latest
       go get go.opentelemetry.io/otel@latest
       go get go.opentelemetry.io/otel/sdk@latest
       go get go.opentelemetry.io/otel/exporters/stdout/stdouttrace@latest
       go get go.opentelemetry.io/contrib/bridges/otelslog@latest
       go get github.com/KimMachineGun/automemlimit@latest
       ```

    3. Create `LICENSE` with BSD 3-Clause text per D-53. Use standard BSD 3-Clause template with:
       - Copyright (c) 2026, dotwaffle
       - The standard 3 clauses about redistribution

    4. Create `internal/config/config.go` per D-33:
       - Package `config`
       - Type `Config` struct with fields:
         - `DBPath string` (from env `PDBPLUS_DB_PATH`, default `"./peeringdb-plus.db"`)
         - `PeeringDBBaseURL string` (from env `PDBPLUS_PEERINGDB_URL`, default `"https://api.peeringdb.com"`) per D-09
         - `SyncToken string` (from env `PDBPLUS_SYNC_TOKEN`, default `""`) per D-23
         - `SyncInterval time.Duration` (from env `PDBPLUS_SYNC_INTERVAL`, default `1*time.Hour`) per D-22
         - `IncludeDeleted bool` (from env `PDBPLUS_INCLUDE_DELETED`, default `false`) per D-32
         - `OTelEndpoint string` (from env `OTEL_EXPORTER_OTLP_ENDPOINT`, default `""`)
         - `ListenAddr string` (from env `PDBPLUS_LISTEN_ADDR`, default `":8080"`)
         - `IsPrimary bool` (from env `PDBPLUS_IS_PRIMARY`, default `true`) per D-29
       - `func Load() (*Config, error)` that reads from environment, validates (fail fast per CFG-1), returns immutable config
       - Use `os.Getenv` with explicit defaults -- no config library
       - Validate: DBPath must not be empty, SyncInterval must be > 0

    5. Create `internal/database/database.go` per D-28, D-34, D-51:
       - Package `database`
       - `func init()` that registers modernc.org/sqlite driver under name `"sqlite3"`:
         ```go
         sql.Register("sqlite3", &sqlite.Driver{})
         ```
       - `func Open(dbPath string) (*ent.Client, error)` that:
         - Constructs DSN: `file:{dbPath}?_pragma=foreign_keys(1)&_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)`
         - Opens via `sql.Open("sqlite3", dsn)` (note: uses the registered "sqlite3" name per REFERENCE doc)
         - Wraps with `entsql.OpenDB(dialect.SQLite, db)`
         - Returns `ent.NewClient(ent.Driver(drv))`
       - Import paths: `database/sql`, `fmt`, `entgo.io/ent/dialect`, `entsql "entgo.io/ent/dialect/sql"`, `modernc.org/sqlite`
       - NOTE: The ent import will fail until schemas are generated. Add a comment: `// import "github.com/dotwaffle/peeringdb-plus/ent"` and use a build tag `//go:build ignore` temporarily. This file will be updated after schema generation in Plan 02.

    6. Create `internal/otel/provider.go` per D-35:
       - Package `otel`
       - `func InitProvider(ctx context.Context, serviceName string) (shutdown func(context.Context) error, err error)` that:
         - Creates a stdout trace exporter (for development; production will use OTLP)
         - Creates a TracerProvider with the exporter and resource (service.name=serviceName)
         - Sets as global via `otel.SetTracerProvider(tp)`
         - Returns a shutdown function that calls `tp.Shutdown(ctx)`
       - Use `go.opentelemetry.io/otel`, `go.opentelemetry.io/otel/sdk/trace`, `go.opentelemetry.io/otel/sdk/resource`, `go.opentelemetry.io/otel/exporters/stdout/stdouttrace`

    7. Create `internal/testutil/testutil.go`:
       - Package `testutil`
       - `func SetupClient(t *testing.T) *ent.Client` that:
         - Opens in-memory SQLite: `enttest.Open(t, dialect.SQLite, "file:ent?mode=memory&cache=shared&_pragma=foreign_keys(1)")`
         - Registers cleanup via `t.Cleanup`
         - Returns the client
       - NOTE: Same build-tag caveat as database.go until schemas exist. Use `//go:build ignore` temporarily.

    8. Create `Dockerfile` per D-54 -- multi-stage build:
       ```dockerfile
       # Build stage
       FROM golang:1.26-alpine AS builder
       WORKDIR /build
       COPY go.mod go.sum ./
       RUN go mod download
       COPY . .
       RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /peeringdb-plus ./cmd/peeringdb-plus

       # Runtime stage
       FROM alpine:3.21
       RUN apk add --no-cache ca-certificates
       COPY --from=builder /peeringdb-plus /usr/local/bin/peeringdb-plus
       EXPOSE 8080
       ENTRYPOINT ["/usr/local/bin/peeringdb-plus"]
       ```

    9. Create directory structure (empty .gitkeep files where needed):
       - `cmd/peeringdb-plus/` (main binary, will be populated in Plan 04)
       - `cmd/pdb-schema-extract/` (extraction tool, Plan 05)
       - `cmd/pdb-schema-generate/` (generation tool, Plan 05)
       - `ent/schema/` (schemas, Task 2 + Plan 02)
       - `schema/` (intermediate JSON, Plan 05)
       - `testdata/fixtures/` (test fixtures, Plan 06)
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go mod tidy && go vet ./internal/config/... ./internal/otel/...</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/dotwaffle/peeringdb-plus`
    - go.mod contains `entgo.io/ent v0.14`
    - go.mod contains `modernc.org/sqlite`
    - go.mod contains `go.opentelemetry.io/otel`
    - go.mod contains `golang.org/x/time`
    - LICENSE contains "BSD 3-Clause"
    - internal/config/config.go contains `PDBPLUS_DB_PATH`
    - internal/config/config.go contains `PDBPLUS_PEERINGDB_URL`
    - internal/config/config.go contains `PDBPLUS_SYNC_TOKEN`
    - internal/config/config.go contains `PDBPLUS_INCLUDE_DELETED`
    - internal/config/config.go contains `func Load()`
    - internal/database/database.go contains `sql.Register("sqlite3"`
    - internal/database/database.go contains `_pragma=journal_mode(WAL)`
    - internal/database/database.go contains `_pragma=foreign_keys(1)`
    - internal/otel/provider.go contains `otel.SetTracerProvider`
    - internal/otel/provider.go contains `func InitProvider`
    - Dockerfile contains `FROM golang:1.26`
    - Dockerfile contains `-trimpath`
    - `go vet ./internal/config/... ./internal/otel/...` passes
  </acceptance_criteria>
  <done>Go module initialized with all dependencies. Config, database, OTel, and test utility packages created. Dockerfile and LICENSE in place. Project directory structure established.</done>
</task>

<task type="auto">
  <name>Task 2: Create proof-of-concept Organization schema and validate entgo code generation pipeline</name>
  <files>
    ent/entc.go,
    ent/generate.go,
    ent/schema/organization.go
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (lines 189-220 for Pattern 2, lines 335-390 for Pattern 7-8, lines 412-445 for org fields)
    .planning/phases/01-data-foundation/01-CONTEXT.md (decisions D-36 through D-47)
    .planning/phases/01-data-foundation/01-REFERENCE-SQLITE-ENTGO.md
    go.mod
  </read_first>
  <action>
    1. Create `ent/generate.go` per D-18 and D-52:
       ```go
       package ent

       //go:generate go run -mod=mod entc.go
       ```

    2. Create `ent/entc.go` per Pattern 8 in research:
       ```go
       //go:build ignore

       package main

       import (
           "log"

           "entgo.io/contrib/entgql"
           "entgo.io/ent/entc"
           "entgo.io/ent/entc/gen"
       )

       func main() {
           gqlExt, err := entgql.NewExtension(
               entgql.WithSchemaGenerator(),
               entgql.WithSchemaPath("graph/schema.graphqls"),
               entgql.WithWhereInputs(true),
           )
           if err != nil {
               log.Fatalf("creating entgql extension: %v", err)
           }

           opts := []entc.Option{
               entc.Extensions(gqlExt),
               entc.FeatureNames("sql/upsert"),
           }

           if err := entc.Generate("./schema", &gen.Config{}, opts...); err != nil {
               log.Fatalf("running ent codegen: %v", err)
           }
       }
       ```
       NOTE: entproto and entrest extensions to be added once their annotation patterns are validated. Start with entgql only (highest confidence per research).

    3. Create `ent/schema/organization.go` -- the Organization schema per the field inventory in research (lines 425-445):
       - Package `schema`
       - Type `Organization` struct with `ent.Schema` embed
       - `func (Organization) Fields() []ent.Field` returning:
         - `field.Int("id").Positive().Immutable().Comment("PeeringDB organization ID")` per D-37
         - `field.String("name").MaxLen(255).NotEmpty().Annotations(entgql.OrderField("NAME")).Comment("Organization name")`
         - `field.String("aka").Optional().MaxLen(255).Default("").Comment("Also known as")`
         - `field.String("name_long").Optional().MaxLen(255).Default("").Comment("Long name")`
         - `field.String("website").Optional().Default("").Comment("Organization website URL")`
         - `field.JSON("social_media", []SocialMedia{}).Optional().Comment("Social media links")` per Pattern 7
         - `field.String("notes").Optional().Default("").Comment("Notes")`
         - `field.String("logo").Optional().Nillable().Comment("Logo URL")`
         - Address fields per AddressModel: `address1`, `address2`, `city`, `state`, `country`, `zipcode`, `suite`, `floor` -- all `field.String().Optional().Default("")`
         - `field.Float("latitude").Optional().Nillable().Comment("Latitude")`
         - `field.Float("longitude").Optional().Nillable().Comment("Longitude")`
         - `field.Time("created").Immutable().Comment("PeeringDB creation timestamp")` per D-41, D-47
         - `field.Time("updated").Comment("PeeringDB last update timestamp")` per D-41, D-47
         - `field.String("status").MaxLen(255).Default("ok").Comment("Record status")` per HandleRefModel
       - Define `SocialMedia` struct before the schema: `type SocialMedia struct { Service string json:"service"; Identifier string json:"identifier" }`
       - `func (Organization) Edges() []ent.Edge` returning edges To:
         - `edge.To("networks", Network.Type)` -- this will fail until Network schema exists, so comment it out for now
         - Add a comment: `// Edges will be populated in Plan 02 when all schemas exist`
         - Return empty slice for now
       - `func (Organization) Indexes() []ent.Index` per D-45:
         - `index.Fields("name")`
         - `index.Fields("status")`
       - `func (Organization) Annotations() []schema.Annotation` per D-42:
         - `entgql.QueryField()`
         - `entgql.Mutations(entgql.MutationCreate(), entgql.MutationUpdate())` -- needed for entgql generation even though we're read-only; mutations won't be exposed in Phase 2

    4. Run code generation:
       ```
       cd ent && go generate .
       ```
       This must succeed, producing the full `ent/` generated code including `ent/client.go`, `ent/organization.go`, etc.

    5. After generation succeeds, remove the `//go:build ignore` tags from `internal/database/database.go` and `internal/testutil/testutil.go` (Task 1), update their imports to reference the generated `ent` package, and verify compilation:
       ```
       go build ./...
       ```

    6. Write a basic test in `ent/schema/organization_test.go` that uses the testutil helper to create an in-memory client, creates an Organization, and queries it back:
       ```go
       func TestOrganizationCRUD(t *testing.T) {
           client := testutil.SetupClient(t)
           ctx := context.Background()
           org, err := client.Organization.Create().
               SetID(1).
               SetName("Test Org").
               SetCreated(time.Now()).
               SetUpdated(time.Now()).
               Exec(ctx)
           // assert no error, query back, verify fields
       }
       ```

    7. Run the test to validate the full stack:
       ```
       go test -race ./ent/schema/...
       ```
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go generate ./ent && go build ./... && go test -race -count=1 ./ent/schema/...</automated>
  </verify>
  <acceptance_criteria>
    - ent/generate.go contains `//go:generate go run -mod=mod entc.go`
    - ent/entc.go contains `entgql.NewExtension`
    - ent/entc.go contains `entc.FeatureNames("sql/upsert")`
    - ent/entc.go contains `entc.Generate("./schema"`
    - ent/schema/organization.go contains `func (Organization) Fields()`
    - ent/schema/organization.go contains `field.Int("id").Positive().Immutable()`
    - ent/schema/organization.go contains `field.JSON("social_media"`
    - ent/schema/organization.go contains `field.Float("latitude").Optional().Nillable()`
    - ent/schema/organization.go contains `func (Organization) Indexes()`
    - ent/schema/organization.go contains `func (Organization) Annotations()`
    - ent/schema/organization.go contains `type SocialMedia struct`
    - ent/client.go exists (generated)
    - ent/organization.go exists (generated)
    - internal/database/database.go does NOT contain `//go:build ignore`
    - internal/testutil/testutil.go does NOT contain `//go:build ignore`
    - `go build ./...` exits 0
    - `go test -race ./ent/schema/...` exits 0
  </acceptance_criteria>
  <done>Entgo code generation pipeline works end-to-end. Organization schema compiles, generates code, and passes CRUD test with in-memory SQLite. The toolchain is validated for Plan 02 to add remaining 12 schemas.</done>
</task>

</tasks>

<verification>
1. `go mod tidy && go vet ./...` passes
2. `go generate ./ent` produces generated code without errors
3. `go build ./...` compiles all packages
4. `go test -race ./ent/schema/...` passes Organization CRUD test
5. Project structure matches recommended layout from research
</verification>

<success_criteria>
- Go module with all Phase 1 dependencies resolves and compiles
- entgo code generation produces working client from Organization schema
- SQLite opens with WAL mode, FK constraints, and busy_timeout pragmas
- Organization CRUD works in-memory via enttest
- Config package loads all environment variables per D-33
- OTel provider initializes trace pipeline
- Dockerfile builds (multi-stage, -trimpath)
- BSD 3-Clause LICENSE file present
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
