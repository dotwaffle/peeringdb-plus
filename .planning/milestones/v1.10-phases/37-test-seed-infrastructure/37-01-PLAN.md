---
phase: 37-test-seed-infrastructure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/testutil/seed/seed.go
  - internal/testutil/seed/seed_test.go
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "seed.Full(t, client) creates exactly one entity of each of the 13 PeeringDB types"
    - "seed.Minimal(t, client) creates Org + Network + IX + Facility (4 entities)"
    - "seed.Networks(t, client, 2) creates exactly 2 networks with their Org dependency"
    - "Result struct provides typed, non-nil references to every created entity"
    - "Seed package importable from graph, grpcserver, and web without import cycles"
  artifacts:
    - path: "internal/testutil/seed/seed.go"
      provides: "Full, Minimal, Networks functions + Result struct"
      exports: ["Result", "Full", "Minimal", "Networks", "Timestamp"]
      min_lines: 150
    - path: "internal/testutil/seed/seed_test.go"
      provides: "Tests for all 3 seed functions + entity count + relationship validation"
      min_lines: 100
  key_links:
    - from: "internal/testutil/seed/seed.go"
      to: "ent.Client"
      via: "ent Create builders"
      pattern: "client\\.(Organization|Network|InternetExchange|Facility|Campus|Carrier|IxLan|IxPrefix|NetworkIxLan|NetworkFacility|IxFacility|CarrierFacility|Poc)\\.Create\\(\\)"
    - from: "internal/testutil/seed/seed_test.go"
      to: "internal/testutil"
      via: "testutil.SetupClient for isolated test DBs"
      pattern: "testutil\\.SetupClient"
---

<objective>
Create a shared deterministic test seed package that any test file can import to populate a database with all 13 PeeringDB entity types via a single function call, eliminating ~840 lines of duplicated entity creation code across 4 packages.

Purpose: Foundation for Phases 38-42 test coverage work. Every future test that needs seeded data will use this package instead of writing bespoke entity creation boilerplate.
Output: `internal/testutil/seed/` package with `Full()`, `Minimal()`, `Networks()` functions and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-test-seed-infrastructure/37-RESEARCH.md

@internal/testutil/testutil.go
@internal/web/detail_test.go
@ent/schema/organization.go
@ent/schema/network.go
@ent/schema/internetexchange.go
@ent/schema/facility.go
@ent/schema/campus.go
@ent/schema/carrier.go
@ent/schema/ixlan.go
@ent/schema/ixprefix.go
@ent/schema/networkixlan.go
@ent/schema/networkfacility.go
@ent/schema/ixfacility.go
@ent/schema/carrierfacility.go
@ent/schema/poc.go

<interfaces>
<!-- Existing testutil package that seed sub-package builds on -->

From internal/testutil/testutil.go:
```go
package testutil

func SetupClient(t *testing.T) *ent.Client
func SetupClientWithDB(t *testing.T) (*ent.Client, *sql.DB)
```

<!-- Ent entity types the seed package will create (all 13) -->
From ent/:
```go
type Organization struct { ... }  // ent/organization.go
type Network struct { ... }       // ent/network.go
type InternetExchange struct { ... } // ent/internetexchange.go
type Facility struct { ... }      // ent/facility.go
type Campus struct { ... }        // ent/campus.go
type Carrier struct { ... }       // ent/carrier.go
type IxLan struct { ... }         // ent/ixlan.go
type IxPrefix struct { ... }      // ent/ixprefix.go
type NetworkIxLan struct { ... }  // ent/networkixlan.go
type NetworkFacility struct { ... } // ent/networkfacility.go
type IxFacility struct { ... }    // ent/ixfacility.go
type CarrierFacility struct { ... } // ent/carrierfacility.go
type Poc struct { ... }           // ent/poc.go
```

<!-- Existing seed pattern from detail_test.go (reference implementation) -->
Entity creation uses both FK int field AND edge setter:
```go
net, err := client.Network.Create().
    SetID(10).SetName("Cloudflare").SetAsn(13335).
    SetOrgID(1).SetOrganization(org).  // BOTH FK + edge
    SetIxCount(1).SetFacCount(1).
    SetCreated(timestamp).SetUpdated(timestamp).
    Save(ctx)
```

<!-- Fixed IDs from existing seedAllTestData (preserve for compatibility) -->
Org=1, Network=10, IX=20, Facility=30, Campus=40, Carrier=50, IxLan=100,
NetworkIxLan=200, NetworkFacility=300, IxFacility=400, Poc=500,
CarrierFacility=600, IxPrefix=700
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement seed package with Full, Minimal, and Networks functions</name>
  <files>internal/testutil/seed/seed.go, internal/testutil/seed/seed_test.go</files>
  <read_first>
    internal/web/detail_test.go (seedAllTestData is the reference implementation for Full -- uses all 13 types with IDs: Org=1, Net=10, IX=20, Fac=30, Campus=40, Carrier=50, IxLan=100, NetIxLan=200, NetFac=300, IxFac=400, Poc=500, CarrierFac=600, IxPrefix=700)
    internal/testutil/testutil.go (SetupClient pattern -- the test DB factory)
    ent/schema/organization.go (required fields: id, name [NotEmpty+Unique], created, updated; optional with defaults: city, country, status)
    ent/schema/network.go (required fields: id, asn [Unique+Positive], name [NotEmpty+Unique], created, updated; FK: org_id + Organization edge)
    ent/schema/internetexchange.go (required fields: id, name [NotEmpty+Unique], created, updated; FK: org_id)
    ent/schema/facility.go (required fields: id, name [NotEmpty+Unique], created, updated; FK: org_id, campus_id [optional])
    ent/schema/campus.go (required fields: id, name [NotEmpty+Unique], created, updated; FK: org_id)
    ent/schema/carrier.go (required fields: id, name [NotEmpty+Unique], created, updated; FK: org_id)
    ent/schema/ixlan.go (required fields: id, created, updated; FK: ix_id)
    ent/schema/ixprefix.go (required fields: id, prefix [NotEmpty+Unique], created, updated; FK: ixlan_id)
    ent/schema/networkixlan.go (required fields: id, asn [Positive], speed, created, updated; FK: net_id, ixlan_id)
    ent/schema/networkfacility.go (required fields: id, local_asn [Positive], created, updated; FK: net_id, fac_id)
    ent/schema/ixfacility.go (required fields: id, created, updated; FK: ix_id, fac_id)
    ent/schema/carrierfacility.go (required fields: id, created, updated; FK: carrier_id, fac_id)
    ent/schema/poc.go (required fields: id, created, updated; FK: net_id)
  </read_first>
  <behavior>
    - TestFull_CreatesAllThirteenTypes: seed.Full(t, client) returns non-nil Result where every field (Org, Network, IX, Facility, Campus, Carrier, IxLan, IxPrefix, NetworkIxLan, NetworkFacility, IxFacility, CarrierFacility, Poc) is non-nil
    - TestFull_EntityCount: After seed.Full, querying each entity type returns count=1 (except Network which returns 2 in Full for the Network2 field, and Facility which returns 2 for the campus-assigned facility)
    - TestFull_RelationshipIntegrity: result.NetworkIxLan's IxLan edge points to result.IxLan; result.NetworkFacility's Network edge points to result.Network; result.IxFacility's InternetExchange edge points to result.IX; result.CarrierFacility's Carrier edge points to result.Carrier; result.Poc's Network edge points to result.Network; result.IxPrefix's IxLan edge points to result.IxLan
    - TestFull_DeterministicIDs: result.Org.ID==1, result.Network.ID==10, result.IX.ID==20, result.Facility.ID==30, result.Campus.ID==40, result.Carrier.ID==50, result.IxLan.ID==100, result.NetworkIxLan.ID==200, result.NetworkFacility.ID==300, result.IxFacility.ID==400, result.Poc.ID==500, result.CarrierFacility.ID==600, result.IxPrefix.ID==700
    - TestMinimal_CreatesFourTypes: seed.Minimal(t, client) returns Result with non-nil Org, Network, IX, Facility; all other fields are nil
    - TestMinimal_EntityCount: After seed.Minimal, Organization count=1, Network count=1, InternetExchange count=1, Facility count=1, all junction types count=0
    - TestNetworks_CreatesExactCount: seed.Networks(t, client, 2) returns Result with non-nil Org, non-nil Network (first), len(result.AllNetworks)==2; seed.Networks(t, client, 5) returns len(result.AllNetworks)==5
    - TestNetworks_UniqueASNs: Each network created by Networks() has a unique ASN (65001, 65002, ..., 65000+n)
    - TestFull_RealisticFieldValues: result.Network.Name is non-empty, result.IX.Name is non-empty, result.Facility.Name is non-empty, result.Org.Name is non-empty
  </behavior>
  <action>
    Create `internal/testutil/seed/seed.go` with package `seed`:

    1. **Exported constants/vars:**
       - `var Timestamp = time.Date(2024, 1, 1, 0, 0, 0, 0, time.UTC)` -- shared deterministic timestamp for all seed data

    2. **Result struct:**
       ```go
       // Result holds typed references to all entities created by seed functions.
       type Result struct {
           Org             *ent.Organization
           Network         *ent.Network
           Network2        *ent.Network         // second network, only in Full
           IX              *ent.InternetExchange
           Facility        *ent.Facility
           Facility2       *ent.Facility         // campus-assigned facility, only in Full
           Campus          *ent.Campus
           Carrier         *ent.Carrier
           IxLan           *ent.IxLan
           IxPrefix        *ent.IxPrefix
           NetworkIxLan    *ent.NetworkIxLan
           NetworkFacility *ent.NetworkFacility
           IxFacility      *ent.IxFacility
           CarrierFacility *ent.CarrierFacility
           Poc             *ent.Poc
           AllNetworks     []*ent.Network        // all created networks (for Networks())
       }
       ```

    3. **Full(tb testing.TB, client *ent.Client) *Result:**
       Create entities in topological FK order, using the same IDs as existing `seedAllTestData` in `internal/web/detail_test.go` for backward compatibility:
       - Organization: ID=1, Name="Test Organization", City="Frankfurt", Country="DE"
       - Campus: ID=40, Name="Test Campus", OrgID=1, City="Berlin", Country="DE"
       - Carrier: ID=50, Name="Test Carrier", OrgID=1, FacCount=1
       - Facility: ID=30, Name="Equinix FR5", OrgID=1, City="Frankfurt", Country="DE", NetCount=1, IxCount=1, CarrierCount=1
       - Facility2: ID=31, Name="Campus Facility", OrgID=1, CampusID=40, City="Berlin", Country="DE"
       - InternetExchange: ID=20, Name="DE-CIX Frankfurt", OrgID=1, City="Frankfurt", Country="DE", RegionContinent="Europe", Media="Ethernet", NetCount=1, FacCount=1
       - IxLan: ID=100, IxID=20
       - Network: ID=10, Name="Cloudflare", ASN=13335, OrgID=1, IxCount=1, FacCount=1
       - Network2: ID=11, Name="Hurricane Electric", ASN=6939, OrgID=1
       - IxPrefix: ID=700, IxlanID=100, Prefix="80.81.192.0/22", Protocol="IPv4", InDfz=true
       - NetworkIxLan: ID=200, NetID=10, IxlanID=100, ASN=13335, Speed=10000, Name="DE-CIX Frankfurt", IxID=20
       - NetworkFacility: ID=300, NetID=10, FacID=30, LocalAsn=13335, Name="Equinix FR5"
       - IxFacility: ID=400, FacID=30, IxID=20, Name="DE-CIX Frankfurt", City="Frankfurt", Country="DE"
       - CarrierFacility: ID=600, CarrierID=50, FacID=30, Name="Equinix FR5"
       - Poc: ID=500, NetID=10, Name="NOC Contact", Role="NOC"

       Every entity creation uses BOTH the FK int setter AND the edge object setter (e.g., `.SetOrgID(org.ID).SetOrganization(org)`). Every entity sets `.SetCreated(Timestamp).SetUpdated(Timestamp)`. On error, call `tb.Fatalf("seed: create <type>: %v", err)`.

       Set `AllNetworks = []*ent.Network{r.Network, r.Network2}`.

    4. **Minimal(tb testing.TB, client *ent.Client) *Result:**
       Create only 4 entities needed for basic relationship traversal:
       - Organization: ID=1, Name="Test Organization", City="Frankfurt", Country="DE"
       - InternetExchange: ID=20, Name="DE-CIX Frankfurt", OrgID=1, City="Frankfurt", Country="DE", RegionContinent="Europe", Media="Ethernet"
       - Facility: ID=30, Name="Equinix FR5", OrgID=1, City="Frankfurt", Country="DE"
       - Network: ID=10, Name="Cloudflare", ASN=13335, OrgID=1
       Junction types (IxLan, NetworkIxLan, etc.) NOT created. Result fields for uncreated entities are nil.
       Set `AllNetworks = []*ent.Network{r.Network}`.

    5. **Networks(tb testing.TB, client *ent.Client, n int) *Result:**
       Create 1 Organization (ID=1, Name="Test Organization") + n Networks.
       Each network: ID=10+i, Name=fmt.Sprintf("Network %d", i+1), ASN=65001+i.
       Result.Network = first network. Result.AllNetworks = all n networks.
       All other Result fields nil.

    6. **Imports:** Only `context`, `fmt`, `testing`, `time`, and `github.com/dotwaffle/peeringdb-plus/ent`. Do NOT import `graph`, `internal/web`, `internal/grpcserver`, or `internal/pdbcompat`.

    Create `internal/testutil/seed/seed_test.go` with package `seed_test` (external test package):

    Tests use `testutil.SetupClient(t)` for isolated DBs. Each test calls `t.Parallel()`.

    Table-driven tests per T-1 where appropriate. Use stdlib assertions (no testify).

    Test functions:
    - `TestFull` -- calls Full, asserts all 13 Result fields non-nil, checks deterministic IDs, checks realistic names non-empty
    - `TestFull_EntityCounts` -- after Full, queries each of the 13 entity types via `client.<Type>.Query().Count(ctx)`, asserts Organization=1, Network=2 (Network + Network2), InternetExchange=1, Facility=2 (Facility + Facility2), Campus=1, Carrier=1, IxLan=1, IxPrefix=1, NetworkIxLan=1, NetworkFacility=1, IxFacility=1, CarrierFacility=1, Poc=1
    - `TestFull_Relationships` -- eager-loads edges on junction entities, asserts FK relationships point to correct parent entities by ID
    - `TestMinimal` -- calls Minimal, asserts Org/Network/IX/Facility non-nil, all junction type fields nil
    - `TestMinimal_EntityCounts` -- after Minimal, asserts Organization=1, Network=1, InternetExchange=1, Facility=1, all junction types=0
    - `TestNetworks` -- table-driven with n=1, n=2, n=5; asserts len(result.AllNetworks)==n, each network has unique ASN, Result.Org non-nil
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -v -count=1 ./internal/testutil/seed/</automated>
  </verify>
  <acceptance_criteria>
    - internal/testutil/seed/seed.go exists and contains `package seed`
    - seed.go contains `func Full(tb testing.TB, client *ent.Client) *Result`
    - seed.go contains `func Minimal(tb testing.TB, client *ent.Client) *Result`
    - seed.go contains `func Networks(tb testing.TB, client *ent.Client, n int) *Result`
    - seed.go contains `type Result struct` with fields: Org, Network, Network2, IX, Facility, Facility2, Campus, Carrier, IxLan, IxPrefix, NetworkIxLan, NetworkFacility, IxFacility, CarrierFacility, Poc, AllNetworks
    - seed.go contains `var Timestamp = time.Date(2024, 1, 1, 0, 0, 0, 0, time.UTC)`
    - seed.go does NOT contain imports of "graph", "internal/web", "internal/grpcserver", or "internal/pdbcompat"
    - seed.go contains both `.SetOrgID(` and `.SetOrganization(` (dual FK + edge setter pattern)
    - seed_test.go exists and contains `package seed_test`
    - seed_test.go contains `testutil.SetupClient`
    - `go test -race -v -count=1 ./internal/testutil/seed/` exits 0 with all tests passing
    - `go vet ./internal/testutil/seed/...` exits 0
  </acceptance_criteria>
  <done>All three seed functions (Full, Minimal, Networks) create entities with correct FK relationships and deterministic IDs, all tests pass with -race flag, and the package compiles without import cycles</done>
</task>

<task type="auto">
  <name>Task 2: Validate import-cycle freedom from 3 consumer packages</name>
  <files>internal/testutil/seed/seed.go</files>
  <read_first>
    internal/testutil/seed/seed.go (just created -- verify it exists and exports are correct)
    graph/resolver_test.go (existing test file that will import seed in future phases -- verify no cycle)
    internal/grpcserver/grpcserver_test.go (existing test file -- verify no cycle)
    internal/web/detail_test.go (existing test file -- verify no cycle)
  </read_first>
  <action>
    Validate that the seed package can be imported from the 3 consumer packages without import cycles by running build checks. This is a validation-only task -- no file modifications unless a cycle is discovered.

    Run the following commands to prove import-cycle freedom:

    1. `go build ./internal/testutil/seed/` -- the seed package itself compiles
    2. `go build ./graph/...` -- graph package (which already imports testutil) can coexist with the seed package
    3. `go build ./internal/grpcserver/...` -- grpcserver package compiles
    4. `go build ./internal/web/...` -- web package compiles
    5. `go test -race -count=1 ./internal/testutil/seed/` -- seed tests pass (uses testutil.SetupClient which is a consumer-like import)
    6. `go vet ./internal/testutil/seed/...` -- no vet issues

    If any import cycle is detected, the error message will indicate the cycle path. Fix by ensuring seed.go only imports `ent` and standard library packages.

    Additionally, verify the seed package is usable from downstream by confirming the `go build` commands succeed -- this proves that adding `import "github.com/dotwaffle/peeringdb-plus/internal/testutil/seed"` to any of those test files would not cause a cycle.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go build ./internal/testutil/seed/ && go build ./graph/... && go build ./internal/grpcserver/... && go build ./internal/web/... && go vet ./internal/testutil/seed/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/testutil/seed/` exits 0 (no compilation errors, no import cycles)
    - `go build ./graph/...` exits 0
    - `go build ./internal/grpcserver/...` exits 0
    - `go build ./internal/web/...` exits 0
    - `go vet ./internal/testutil/seed/...` exits 0
    - seed.go import block contains only: "context", "fmt", "testing", "time", and paths under "github.com/dotwaffle/peeringdb-plus/ent"
  </acceptance_criteria>
  <done>The seed package compiles and all 3 consumer packages (graph, grpcserver, web) build successfully, proving import-cycle freedom per INFRA-01 success criteria 3</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `go test -race -v -count=1 ./internal/testutil/seed/` -- all seed tests pass
2. `go build ./graph/... ./internal/grpcserver/... ./internal/web/...` -- no import cycles
3. `go vet ./...` -- no vet issues introduced
4. Verify seed.Full creates 13+ entities: TestFull test asserts all Result fields non-nil
5. Verify seed.Minimal creates 4 entities: TestMinimal test asserts Org/Network/IX/Facility non-nil, junction types nil
6. Verify seed.Networks creates exactly n networks: TestNetworks test with table cases n=1,2,5
</verification>

<success_criteria>
1. `internal/testutil/seed/seed.go` exists with exported Full, Minimal, Networks functions and Result struct
2. `internal/testutil/seed/seed_test.go` exists with tests covering all 3 functions, entity counts, and FK relationships
3. `go test -race -v -count=1 ./internal/testutil/seed/` passes with 0 failures
4. `go build ./graph/... ./internal/grpcserver/... ./internal/web/...` succeeds (no import cycles)
5. Result struct provides typed references to all 13 entity types plus AllNetworks slice
</success_criteria>

<output>
After completion, create `.planning/phases/37-test-seed-infrastructure/37-01-SUMMARY.md`
</output>
