---
phase: 42-test-quality-audit-coverage-hygiene
plan: 04
type: execute
wave: 1
depends_on: ["42-03"]
files_modified:
  - internal/sync/status_test.go
  - internal/web/compare_test.go
  - internal/web/search_test.go
  - internal/web/handler_test.go
autonomous: true
gap_closure: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "sync status.go DB error paths (GetCursor, UpsertCursor, RecordSyncStart, RecordSyncComplete, GetLastSuccessfulSyncTime, GetLastStatus) each have a test that closes the DB and asserts the error wraps the expected context string"
    - "web CompareService DB error path (Compare with closed client) has a test that asserts error propagation"
    - "web SearchService DB error path (Search with closed client) has a test that asserts error propagation"
    - "web handleServerError is exercised by a test that asserts 500 status and body content"
  artifacts:
    - path: "internal/sync/status_test.go"
      provides: "DB error path tests for all status.go exported functions"
      contains: "TestGetCursor_DBError"
    - path: "internal/web/compare_test.go"
      provides: "DB error path test for CompareService"
      contains: "TestCompareService_DBError"
    - path: "internal/web/search_test.go"
      provides: "DB error path test for SearchService"
      contains: "TestSearchService_DBError"
    - path: "internal/web/handler_test.go"
      provides: "handleServerError coverage"
      contains: "TestHandleServerError"
  key_links:
    - from: "internal/sync/status_test.go"
      to: "internal/sync/status.go"
      via: "closed DB triggers error paths"
      pattern: "db\\.Close.*Get(Cursor|LastStatus)"
    - from: "internal/web/compare_test.go"
      to: "internal/web/compare.go"
      via: "closed ent client triggers query error"
      pattern: "client\\.Close.*Compare"
---

<objective>
Close QUAL-02 gap for internal/sync and internal/web error paths by adding DB-error tests.

Purpose: The 42-03 VERIFICATION found that sync status.go, web compare.go, web search.go, and web handler.go have untested error paths. These are all "DB operation fails" patterns testable by closing the database connection before exercising the function.

Output: Error path tests in 4 files, improving sync and web coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-test-quality-audit-coverage-hygiene/42-03-SUMMARY.md
@internal/sync/status.go
@internal/sync/status_test.go
@internal/web/compare.go
@internal/web/compare_test.go
@internal/web/search.go
@internal/web/search_test.go
@internal/web/handler.go
@internal/web/handler_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync status.go DB error path tests</name>
  <files>internal/sync/status_test.go</files>
  <read_first>
    - internal/sync/status.go (all exported functions and their fmt.Errorf wrapping)
    - internal/sync/status_test.go (existing test patterns, imports, setupClientWithDB usage)
    - internal/testutil/testutil.go (SetupClientWithDB signature)
  </read_first>
  <action>
Add table-driven test functions to internal/sync/status_test.go that exercise the DB error path of each exported status.go function. The pattern is: call InitStatusTable to create tables, then close the DB, then call the function and assert the returned error contains the expected context string.

Add these test functions (all in package sync_test since status_test.go is external):

1. TestGetCursor_DBError: Init tables, close DB, call GetCursor. Assert error contains "get cursor for".

2. TestUpsertCursor_DBError: Init tables, close DB, call UpsertCursor. Assert error contains "upsert cursor for".

3. TestRecordSyncStart_DBError: Init tables, close DB, call RecordSyncStart. Assert error contains "record sync start".

4. TestRecordSyncComplete_DBError: Init tables, record a start row first (to get an ID), close DB, call RecordSyncComplete. Assert error contains "record sync complete".

5. TestGetLastSuccessfulSyncTime_DBError: Init tables, close DB, call GetLastSuccessfulSyncTime. Assert error contains "get last successful sync time".

6. TestGetLastStatus_DBError: Init tables, close DB, call GetLastStatus. Assert error contains "get last sync status".

7. TestInitStatusTable_SecondCreateFails: This is harder since both CREATE TABLE statements need to run. Instead, close the DB before calling InitStatusTable and assert error contains "create sync_status table".

Each test MUST:
- Use t.Parallel()
- Use testutil.SetupClientWithDB to get _, db
- Call InitStatusTable(ctx, db) first (except test 7)
- Close db explicitly (do NOT rely on t.Cleanup -- call db.Close() directly before the operation under test)
- Assert err != nil
- Assert strings.Contains(err.Error(), expectedSubstring) for the error wrapping context
- Use the existing import pattern from status_test.go (package sync_test, importing sync and testutil)

Note: Since testutil.SetupClientWithDB registers a t.Cleanup that also closes the DB, and closing an already-closed DB is fine for SQLite, this is safe.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -count=1 -run='TestGetCursor_DBError|TestUpsertCursor_DBError|TestRecordSyncStart_DBError|TestRecordSyncComplete_DBError|TestGetLastSuccessfulSyncTime_DBError|TestGetLastStatus_DBError|TestInitStatusTable_SecondCreateFails' ./internal/sync/</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new test functions pass with -race
    - Each test asserts both err != nil and the error message substring
    - go test -cover ./internal/sync/ shows status.go functions improved from their current levels (GetCursor 85.7->100%, UpsertCursor 75->100%, RecordSyncStart 71.4->100%, GetLastSuccessfulSyncTime 85.7->100%, GetLastStatus 84.2->~95%, InitStatusTable 71.4->~85%)
  </acceptance_criteria>
  <done>All status.go exported functions have their DB error path exercised by a test that closes the DB first and asserts the error wrapping string</done>
</task>

<task type="auto">
  <name>Task 2: Web compare/search/handler error path tests</name>
  <files>internal/web/compare_test.go, internal/web/search_test.go, internal/web/handler_test.go</files>
  <read_first>
    - internal/web/compare.go (Compare method error paths -- line 48, 53, 73, 84, 95, 106, 112, 124)
    - internal/web/compare_test.go (existing TestCompareService_InvalidASN pattern, seedCompareTestData helper)
    - internal/web/search.go (Search method, typeQueryFunc, per-type query functions and their error returns)
    - internal/web/search_test.go (existing seedSearchData pattern, SearchService creation)
    - internal/web/handler.go (handleServerError at line 161 -- 0% coverage, handleCompare error path)
    - internal/web/handler_test.go (existing test patterns, setupTestHandler helper)
  </read_first>
  <action>
Add error path tests to the three web test files. All tests are in package web (internal tests) so unexported functions and fields are accessible.

**compare_test.go** -- Add TestCompareService_DBError:
- Create client via testutil.SetupClient, seed two networks (reuse seedCompareTestData or create minimal: org + 2 networks)
- Close the client: client.Close()
- Call svc.Compare(ctx, CompareInput{ASN1: 13335, ASN2: 15169, ViewMode: "shared"})
- Assert err != nil
- Assert error contains "network ASN" (the first query to fail)
- Use t.Parallel()

**search_test.go** -- Add TestSearchService_DBError:
- Create client via testutil.SetupClient (no need to seed -- query will fail before results)
- Create SearchService from client
- Close client: client.Close()
- Call svc.Search(ctx, "test")
- Assert err != nil
- Assert error contains "search" (from the Search method's errgroup error wrapping)
- Use t.Parallel()

**handler_test.go** -- Add TestHandleServerError:
- Look at the existing setupTestHandler helper pattern to understand how Handler is created
- Create a Handler with a valid client
- Create an httptest.NewRecorder and http.NewRequest("GET", "/500", nil)
- Call h.handleServerError(rec, req)
- Assert rec.Code == http.StatusInternalServerError
- Assert rec.Body.String() contains "Server Error" (the page title from templates.ServerErrorPage())
- Use t.Parallel()

Each test MUST follow project conventions:
- t.Parallel()
- Table-driven where multiple cases exist (T-1)
- Assert error content, not just err != nil (per QUAL-02 requirement and Pitfall 5 from RESEARCH)
- Use strings.Contains for error message checking
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -count=1 -run='TestCompareService_DBError|TestSearchService_DBError|TestHandleServerError' ./internal/web/</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new test functions pass with -race
    - TestCompareService_DBError asserts error wrapping string from compare.go
    - TestSearchService_DBError asserts error wrapping string from search.go
    - TestHandleServerError asserts 500 status code and "Server Error" in body
    - handleServerError moves from 0% to 100% coverage
    - go test -cover ./internal/web/ shows improvement from 78.6% baseline
  </acceptance_criteria>
  <done>Web compare, search, and handler error paths each have at least one test exercising the DB/render failure path with content assertions</done>
</task>

</tasks>

<verification>
Run the full test suite for both packages to confirm no regressions:
```
TMPDIR=/tmp/claude-1000 go test -race -count=1 -cover ./internal/sync/ ./internal/web/
```
Expected: All tests pass, sync coverage improves from 87.3%, web coverage improves from 78.6%.
</verification>

<success_criteria>
- internal/sync/status.go has all exported function DB error paths tested
- internal/web handleServerError reaches >0% coverage (currently 0%)
- internal/web compare and search DB error paths each have at least one test
- All new tests pass with -race flag
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/42-test-quality-audit-coverage-hygiene/42-04-SUMMARY.md`
</output>
