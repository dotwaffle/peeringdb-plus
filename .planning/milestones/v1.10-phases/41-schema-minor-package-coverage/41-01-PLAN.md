---
phase: 41-schema-minor-package-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ent/schema/schema_test.go
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03

must_haves:
  truths:
    - "otelMutationHook records error on span when mutation fails, and error propagates to caller"
    - "Creating an entity with a non-existent FK reference returns an error when foreign_keys pragma is enabled"
    - "All 13 schema types have their Edges, Indexes, and Annotations methods exercised in tests"
  artifacts:
    - path: "ent/schema/schema_test.go"
      provides: "Schema hook error path, FK constraint, and config method coverage tests"
      contains: "TestOtelMutationHook"
  key_links:
    - from: "ent/schema/schema_test.go"
      to: "ent/schema/hooks.go"
      via: "otelMutationHook triggered by duplicate ID mutation"
      pattern: "span\\.RecordError"
    - from: "ent/schema/schema_test.go"
      to: "ent/schema/*.go"
      via: "direct calls to Edges(), Indexes(), Annotations()"
      pattern: "\\.(Edges|Indexes|Annotations)\\(\\)"
---

<objective>
Test ent/schema hand-written code: the otelMutationHook error path, FK constraint violation edge cases, and all 39 uncovered Edges/Indexes/Annotations configuration methods to reach 65%+ coverage.

Purpose: SCHEMA-01, SCHEMA-02, SCHEMA-03 require the hook error path, FK edge cases, and 65%+ coverage on hand-written schema code. Current baseline is 47.4%.
Output: Updated ent/schema/schema_test.go with three new test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-schema-minor-package-coverage/41-RESEARCH.md
@ent/schema/hooks.go
@ent/schema/schema_test.go
@ent/schema/organization_test.go

<interfaces>
<!-- Key types and contracts from ent/schema package -->

From ent/schema/hooks.go:
```go
func otelMutationHook(typeName string) ent.Hook
// Creates span "ent.{Type}.{Op}", records error on span if mutation fails (line 28)
```

From ent/schema/organization.go (pattern for all 13 types):
```go
func (Organization) Edges() []ent.Edge
func (Organization) Indexes() []ent.Index
func (Organization) Annotations() []schema.Annotation
func (Organization) Hooks() []ent.Hook
func (Organization) Fields() []ent.Field
```

All 13 schema types follow this pattern:
Organization, Network, Facility, InternetExchange, Campus, Carrier, CarrierFacility, IxFacility, IxLan, IxPrefix, NetworkFacility, NetworkIxLan, Poc

From internal/testutil/testutil.go:
```go
func SetupClient(t *testing.T) *ent.Client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema hook error path and FK constraint tests</name>
  <files>ent/schema/schema_test.go</files>
  <read_first>
    - ent/schema/hooks.go
    - ent/schema/schema_test.go
    - ent/schema/organization.go
  </read_first>
  <action>
Add two new test functions to ent/schema/schema_test.go (SCHEMA-01, SCHEMA-02):

**TestOtelMutationHook_ErrorPath (SCHEMA-01):**
- Import `go.opentelemetry.io/otel`, `go.opentelemetry.io/otel/sdk/trace` as sdktrace, and `go.opentelemetry.io/otel/sdk/trace/tracetest`
- Create an in-memory span exporter: `exporter := tracetest.NewInMemoryExporter()`
- Create a TracerProvider with the syncer: `tp := sdktrace.NewTracerProvider(sdktrace.WithSyncer(exporter))`
- Set as global: `otel.SetTracerProvider(tp)`
- Add t.Cleanup to shutdown tp
- Use `testutil.SetupClient(t)` to get an ent client with hooks enabled
- Create an Organization with ID=1, Name="First", Created/Updated timestamps
- Attempt to create another Organization with the same ID=1 (duplicate primary key)
- Assert that err != nil (the mutation failed)
- Flush the exporter: `tp.ForceFlush(context.Background())`
- Get spans from exporter.GetSpans()
- Find a span named "ent.Organization.Create" that has an event named "exception" (this is what span.RecordError produces)
- Assert that such a span+event exists. If not found, t.Error("expected span with RecordError event")
- This exercises the `if err != nil { span.RecordError(err) }` branch at hooks.go line 27-29

**TestFKConstraintViolation (SCHEMA-02):**
- t.Parallel()
- Use testutil.SetupClient(t) -- this already sets `_pragma=foreign_keys(1)`
- Table-driven tests per T-1 with cases:
  1. "Network with non-existent org_id": Create Network with SetOrgID(99999) and SetOrganization edge to a non-existent org. Since ent requires the edge object for SetOrganization, instead use the raw approach: create Network with SetID(1), SetName("Test"), SetAsn(65000), SetOrgID(99999) -- without SetOrganization. The OrgID field is nullable and set directly, but with foreign_keys pragma ON, SQLite should reject the non-existent FK.
  2. "IxLan with non-existent ix_id": Create IxLan with SetIxID(99999) without the IX edge.
  3. "Poc with non-existent net_id": Create Poc with SetNetID(99999) without the Network edge.
- For each case, assert err != nil (FK constraint error returned)
- Note: Some FK fields in this schema are nullable and use Optional(). When the FK value is explicitly set to a non-existent ID, SQLite with foreign_keys(1) should reject it. If it doesn't reject (because ent generates nullable FKs without ON constraint), document that finding and still assert the behavior observed -- the test still exercises the code path.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -run "TestOtelMutationHook_ErrorPath|TestFKConstraintViolation" ./ent/schema/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `TestOtelMutationHook_ErrorPath` passes and exercises hooks.go line 28 (span.RecordError)
    - `TestFKConstraintViolation` passes with at least one case demonstrating FK rejection or documents nullable FK behavior
    - No data races reported
  </acceptance_criteria>
  <done>The otelMutationHook error branch is covered by a test that triggers a real mutation failure and verifies the span records the error. FK constraint edge cases are tested with non-existent FK references.</done>
</task>

<task type="auto">
  <name>Task 2: Schema Edges/Indexes/Annotations coverage for all 13 types</name>
  <files>ent/schema/schema_test.go</files>
  <read_first>
    - ent/schema/schema_test.go
    - ent/schema/organization.go
    - ent/schema/network.go
    - ent/schema/facility.go
  </read_first>
  <action>
Add a test function to ent/schema/schema_test.go that exercises all 39 uncovered Edges/Indexes/Annotations methods (SCHEMA-03):

**TestSchemaEdges:**
- t.Parallel()
- Table-driven with all 13 types. Each entry has name string and edgesFn func() []ent.Edge
- Schema types to test: Organization{}, Network{}, Facility{}, InternetExchange{}, Campus{}, Carrier{}, CarrierFacility{}, IxFacility{}, IxLan{}, IxPrefix{}, NetworkFacility{}, NetworkIxLan{}, Poc{}
- Import `ent/schema` package types (already imported as `schema` in organization_test.go -- but since this is package `schema_test`, use the schema types directly via the local package import)
- Wait -- the file is `package schema_test` (external test), so it needs `"github.com/dotwaffle/peeringdb-plus/ent/schema"` import. Organization is already imported via `schema.SocialMedia`. Access types as `schema.Organization{}`.
- For each type, call `.Edges()` and assert `len(edges) >= 0` (some types like IxPrefix may have zero edges, so just assert it doesn't panic). For types known to have edges (Organization has 5, Network has several), assert specific minimum counts.
- Run subtest per type for clear reporting.

**TestSchemaIndexes:**
- Same pattern as TestSchemaEdges but calling `.Indexes()`.
- All 13 types. Each has at least status+updated+created indexes.
- Assert `len(indexes) >= 1` for all types.

**TestSchemaAnnotations:**
- Same pattern calling `.Annotations()`.
- All 13 types have annotations (entgql, entrest, entproto).
- Assert `len(annotations) >= 1` for all types.

These three functions directly call the 39 zero-coverage methods (13 Edges + 13 Indexes + 13 Annotations), raising coverage from 47.4% to well above 65%.

After adding all tests, run `go test -race -cover ./ent/schema/ -count=1` and verify coverage >= 65%.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -cover ./ent/schema/ -count=1 2>&1 | grep -E "^ok|coverage"</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -cover ./ent/schema/` reports coverage >= 65.0%
    - All 13 types appear in TestSchemaEdges, TestSchemaIndexes, TestSchemaAnnotations
    - grep for `\.Edges\(\)` in schema_test.go returns 13+ matches
    - grep for `\.Indexes\(\)` in schema_test.go returns 13+ matches
    - grep for `\.Annotations\(\)` in schema_test.go returns 13+ matches
  </acceptance_criteria>
  <done>ent/schema hand-written code reaches 65%+ coverage. All 39 previously-uncovered Edges/Indexes/Annotations methods are exercised.</done>
</task>

</tasks>

<verification>
1. `go test -race -run "TestOtelMutationHook_ErrorPath" ./ent/schema/ -count=1 -v` -- passes, exercises hook error branch
2. `go test -race -run "TestFKConstraintViolation" ./ent/schema/ -count=1 -v` -- passes, exercises FK edge cases
3. `go test -race -run "TestSchemaEdges|TestSchemaIndexes|TestSchemaAnnotations" ./ent/schema/ -count=1 -v` -- passes for all 13 types
4. `go test -race -cover ./ent/schema/ -count=1` -- reports >= 65% coverage
5. `go test -race ./...` -- full suite passes
</verification>

<success_criteria>
- otelMutationHook error path (hooks.go:28) is covered by a test that verifies span.RecordError via in-memory exporter
- FK constraint violations are tested with non-existent FK references
- All 13 schema types have Edges(), Indexes(), and Annotations() called in tests
- ent/schema coverage >= 65% (up from 47.4% baseline)
</success_criteria>

<output>
After completion, create `.planning/phases/41-schema-minor-package-coverage/41-01-SUMMARY.md`
</output>
