---
milestone: v1.15
milestone_name: "Infrastructure Polish & Schema Hygiene"
audited: "2026-04-18T08:40:00Z"
status: passed
scores:
  requirements: 11/11
  phases: 4/4
  integration: 7/7
  flows: 3/3
phases_audited:
  - 63-schema-hygiene
  - 64-field-level-privacy
  - 65-asymmetric-fly-fleet
  - 66-observability-sqlite3-tooling
gaps: []
tech_debt:
  - phase: 64-field-level-privacy
    items:
      - "UI surface deferred with t.Skip sub-test — ixlan URL is not rendered in /ui/ today; re-enable trigger is adding the field to a templ view"
  - phase: 66-observability-sqlite3-tooling
    items:
      - "Grafana Panel 35 (Peak Heap by Process Group) renders a single aggregate series until `fly.process_group` is forwarded as an OTel resource attribute from `internal/otel/setup.go`. Small wiring, not scoped to Phase 66 per 66-02 SUMMARY."
backlog_added:
  - id: 999.1
    title: "Harden cmd/pdb-schema-generate — preserve hand-added Policy() on poc.go"
    source: "Phase 63 executor deviation #1 (low priority; `go generate ./ent` workaround stable)"
  - id: SEED-003
    title: "Primary HA / hot-standby"
    source: "Phase 65 CONTEXT D-08 folded todo — dormant seed planted 2026-04-17"
---

# Milestone v1.15 — Audit Report

**Milestone:** v1.15 — Infrastructure Polish & Schema Hygiene
**Audited:** 2026-04-18
**Status:** ✅ **passed** (11/11 requirements satisfied)
**Phases:** 4 (63, 64, 65, 66)
**Plans:** 9 total (63-01, 64-01..03, 65-01..02, 66-01..03)

## Definition of Done — Met ✅

All 11 requirements across HYGIENE / VIS / INFRA / OBS / DOC categories satisfied, verified in phase VERIFICATION.md files (60/60 must-haves across phases) and cross-checked by the integration checker.

## Requirements Coverage

| REQ-ID | Description | Phase | Verification | Evidence |
|--------|-------------|-------|--------------|----------|
| HYGIENE-01 | Drop `ixprefix.notes` | 63 | passed | ent schema + pdbcompat + sync + proto + golden fixtures + `migrate.WithDropColumn(true)` at main.go:135; DROP COLUMN executed on LHR + replicated via LiteFS WAL (verified post-deploy 2026-04-18) |
| HYGIENE-02 | Drop `organization.fac_count` | 63 | passed | Same migration pipeline; schema-only drop; `.schema organizations` on primary + NRT replica confirms absence |
| HYGIENE-03 | Drop `organization.net_count` | 63 | passed | Schema-only drop; same verification |
| VIS-08 | Field-level gate pattern | 64 | passed | `internal/privfield.Redact(ctx, visible, value) (string, bool)` with fail-closed semantics; unit tests cover 8 admission matrix cases |
| VIS-09 | ixlan URL exposed auth-only | 64 | passed | ent schema field appended at end of Fields() (proto #14); all 5 serializer surfaces call `privfield.Redact`; 22 E2E sub-tests PASS including `fail-closed-bypass-middleware` |
| INFRA-01 | Process groups | 65 | passed | fly.toml `[processes]` primary+replica; live fleet: 1 primary LHR + 7 replicas in iad/nrt/syd/lax/jnb/sin/gru |
| INFRA-02 | Ephemeral replicas | 65 | passed | `[[mounts]] processes = ["primary"]`; 7 replica volumes destroyed; replicas cold-sync 88MB DB from primary via LiteFS HTTP; IAD replica returns 33,483 organizations |
| INFRA-03 | Operational docs | 65 | passed | DEPLOYMENT.md §Asymmetric fleet, ARCHITECTURE.md §Fleet topology, CLAUDE.md §Deployment, PROJECT.md Key Decisions row, SEED-002 moved to consumed/ |
| OBS-04 | sqlite3 tooling | 66 (ref) | passed | Dockerfile.prod:17 `apk add --no-cache fuse3 sqlite`; shipped via pre-phase quick task 260418-1cn; referenced in CLAUDE.md and DEPLOYMENT.md |
| OBS-05 | Heap threshold monitoring | 66 | passed | `PDBPLUS_HEAP_WARN_MIB` (400) + `PDBPLUS_RSS_WARN_MIB` (384); OTel span attrs `pdbplus.sync.peak_heap_mib` / `pdbplus.sync.peak_rss_mib`; slog.Warn "heap threshold crossed"; Prometheus gauges via `InitMemoryGauges`; 3 Grafana panels under "Sync Memory (SEED-001 watch)" row |
| DOC-04 | Heap watch documented | 66 | passed | CLAUDE.md §Sync observability (new section with env vars + attrs + SEED-001 escalation); docs/DEPLOYMENT.md §Sync memory watch (SEED-001); PROJECT.md Phase 66 Key Decisions row |

## Cross-Phase Integration (7/7 Wires)

| # | Wire | Status |
|---|------|--------|
| 1 | Phase 63 `migrate.WithDropColumn` wiring intact after Phase 66 edits to main.go | ✓ (moved to line 135, comment block preserved) |
| 2 | Phase 64 gqlgen.yml opt-in intact after Phase 65/66 edits | ✓ |
| 3 | Phase 65 asymmetric fly.toml structure | ✓ |
| 4 | Phase 66 `InitMemoryGauges` wired in main.go | ✓ (line 101) |
| 5 | sqlite3 in Dockerfile.prod | ✓ |
| 6 | `internal/privfield` consumed by all 5 surfaces | ✓ (pdbcompat, ConnectRPC, GraphQL, REST middleware; UI deferred) |
| 7 | Privacy tier middleware stamps every request | ✓ |

## E2E Flows (3/3)

1. **Sync cycle on primary populates ixlan URL + emits memory telemetry** — WIRED (upsert path + 3 terminal telemetry call sites)
2. **Anon `/api/ixlan` response omits URL field** — WIRED (golden fixtures confirm key absent, `_visible` still emitted per D-05)
3. **`/readyz` 200 across asymmetric fleet post-Phase-65** — VERIFIED (Plan 65-02 Task 5 + post-Phase-66 spot-check 2026-04-18)

## Shipped commits (v1.14 → v1.15)

Commit range: `c496b72..48a1b8f` (38 commits on main, excluding Phase 66 lifecycle commits).

**Phase 63:** `2646821`, `dbcade1`, `4644da1`, `73a729a`, `ce84323`, `0ce3e3b`, `18f3f0e` (code + verify + backlog)
**Phase 64:** `d521a8c`, `17dc27d`, `905b61e`, `0299f92`, `6e87238`, `0443ebc`, `7a09c33`, `8594649`, `25b0ba6`, `e9e9289`, `ebd032f`, `29d019e`, `729e438`, `22afd7b`, `0ff2706`, `70f80c9` (3 plans)
**Phase 65:** `1767ee4`, `a44e0ba`, `7c651ed`, `6094a54`, `58dbd28`, `584a3b3`, `a7d8453` (config + live migration)
**Phase 66:** `7858fa1`, `a9f509f`, `4e90cfd`, `491dc85`, `4d470e3`, `48a1b8f` (runtime + dashboard + docs)

## Deployments (3 production deploys during this milestone)

1. **Pre-phase quick task 260418-1cn** (commit `4dfc52a`) — sqlite3 in prod image, rolling deploy to all 8 machines.
2. **Post-Phase-63** — new code with `migrate.WithDropColumn` wiring; DROP COLUMN DDL executed on LHR primary startup and propagated via LiteFS WAL.
3. **Phase 65** — asymmetric fleet migration: pre-tag + deploy + scale to target topology + destroy 7 orphaned replica volumes. 3 executor deviations documented (zsh word-splitting, flyctl pre-check workaround, regional correction via scale count). No rollback triggered.
4. **Post-Phase-66** — new runtime code with `InitMemoryGauges` + `emitMemoryTelemetry`. All regions /readyz 200.

## Tech Debt (non-blocking)

- **Phase 64:** ixlan URL not rendered in /ui/ today (t.Skip in E2E). Re-enable trigger: adding the field to a templ view.
- **Phase 66:** Grafana Panel 35 (Peak Heap by Process Group) renders a single aggregate series until `fly.process_group` is forwarded as an OTel resource attribute (one-line change to `internal/otel/setup.go`).

## Backlog Added This Milestone

- **999.1** — Harden `cmd/pdb-schema-generate` to preserve hand-added `Policy()` on `ent/schema/poc.go`. Current workaround: `go generate ./ent` instead of `./...` (documented in CLAUDE.md).

## Seeds

- **SEED-001** (incremental sync evaluation) — dormant; Phase 66 wired its trigger observability.
- **SEED-002** (asymmetric Fly fleet) — ✅ consumed by Phase 65; moved to `.planning/seeds/consumed/`.
- **SEED-003** (primary HA hot-standby) — dormant; planted 2026-04-17 during v1.15 discussion when the decision to keep LHR as sole primary was made.

## Audit Decision

**Status: `passed`.** No blockers, minimal deferred tech debt (2 items, both explicitly deferred per plan), all 11 requirements satisfied, live production verified across all 4 phases, 3 successful production deploys with zero rollbacks.

Proceed to `/gsd-complete-milestone v1.15`.
