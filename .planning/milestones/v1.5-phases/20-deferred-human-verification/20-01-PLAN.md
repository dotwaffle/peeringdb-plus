---
phase: 20-deferred-human-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md
autonomous: false
requirements:
  - VFY-01
  - VFY-02
  - VFY-03
  - VFY-04
must_haves:
  truths:
    - "CI workflow has executed on recent pushes with all 4 jobs passing"
    - "Coverage comment posts on a real PR and deduplicates on subsequent pushes"
    - "pdbcompat-check CLI works with a real PeeringDB API key"
    - "Live integration test passes with API key and invalid key produces WARN without crash"
  artifacts:
    - path: ".planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md"
      provides: "Structured verification report with pass/fail/blocked status for VFY-01 through VFY-04"
      contains: "VFY-01"
  key_links:
    - from: "gh run list"
      to: "CI workflow verification"
      via: "GitHub Actions API"
      pattern: "4 jobs pass"
    - from: "pdbcompat-check --api-key"
      to: "PeeringDB API"
      via: "Authorization Api-Key header"
      pattern: "exit 0"
---

<objective>
Verify CI pipeline (VFY-01, VFY-02) and API key authentication (VFY-03, VFY-04) against live GitHub Actions and PeeringDB API. Create the structured verification report.

Purpose: Confirm that the infrastructure and data layer work correctly before proceeding to browser-based UI verification. Per D-05 and D-06, infrastructure failures block all downstream verification.
Output: `20-VERIFICATION-ITEMS.md` with pass/fail/blocked status for items 1-6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-deferred-human-verification/20-CONTEXT.md
@.planning/phases/20-deferred-human-verification/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI pipeline verification and report scaffold (VFY-01, VFY-02)</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <read_first>
    - .planning/phases/20-deferred-human-verification/20-CONTEXT.md
    - .planning/phases/20-deferred-human-verification/20-RESEARCH.md
    - .github/workflows/ci.yml
    - .github/scripts/coverage-comment.sh
  </read_first>
  <action>
Create the verification report scaffold `20-VERIFICATION-ITEMS.md` per D-01 and D-02 (text only, structured table with pass/fail/blocked status per item, browser/OS noted where applicable).

**VFY-01 verification (CI workflow execution):**
1. Run `gh run list --limit 10` to check recent CI runs on main
2. Verify the latest run shows all 4 jobs: lint (golangci-lint + ent drift + templ drift), test (-race + coverage), build, govulncheck
3. Run `gh run view <latest-run-id>` to confirm all 4 jobs passed
4. Record results in the report: run ID, commit, status of each job

**VFY-02 verification (coverage comment posting and deduplication):**
1. Check if recent pushes already triggered CI by running `gh run list --branch main --limit 5`
2. Create a test PR from the current branch to verify coverage comments:
   - Create a trivial commit (e.g., add a comment to a test file or whitespace change)
   - Push to current branch and create PR with `gh pr create --title "test: verify CI coverage comments" --body "Temporary PR to verify coverage comment posting. Will be closed after verification."`
   - Wait for CI to complete: `gh run list --branch <current-branch> --limit 1`
   - Check PR comments: `gh api repos/{owner}/{repo}/issues/{pr-number}/comments`
   - Verify a comment containing "## Test Coverage" exists from github-actions[bot]
3. For deduplication: push a second trivial commit to the same PR
   - Wait for CI to complete again
   - Check PR comments again: verify there is still only ONE coverage comment (updated, not duplicated)
   - The coverage-comment.sh script searches for existing comments and PATCHes if found
4. Close and clean up the test PR: `gh pr close <pr-number>`
5. Record results in the report

Per D-07: first check if the 12 commits from phases 18-19 already triggered CI before pushing a test commit.

Structure the report with the header format from RESEARCH.md:
```
# Verification Report
**Date:** YYYY-MM-DD
**Browser:** (pending -- will be filled in Plan 02/03)
**Deployment:** https://peeringdb-plus.fly.dev/

## Summary
- Total: 26 items
- Passed: X
- Failed: X
- Blocked: X

## VFY-01: CI Pipeline Execution (v1.2)
| # | Item | Status | Observation |

## VFY-02: Coverage Comments (v1.2)
| # | Item | Status | Observation |

## VFY-03: API Key CLI (v1.3)
(placeholder for Task 2)

## VFY-04: API Key Integration Test (v1.3)
(placeholder for Task 2)

## VFY-05 through VFY-09: Web UI (v1.4)
(placeholder for Plans 02 and 03)
```

IMPORTANT per SEC-2: Never include the actual API key value in the verification report text.
Per D-14/D-15: If CI fails, diagnose and fix inline with atomic commits before continuing.
  </action>
  <verify>
    <automated>gh run list --limit 3 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 20-VERIFICATION-ITEMS.md exists with report header and VFY-01/VFY-02 sections filled in
    - VFY-01 shows all 4 CI jobs with pass/fail status
    - VFY-02 shows coverage comment posting and deduplication results
    - Any CI failures are diagnosed and fixed inline per D-14/D-15
  </acceptance_criteria>
  <done>VFY-01 and VFY-02 have pass/fail/blocked status recorded in the verification report with concrete observations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: API key verification with human-provided key (VFY-03, VFY-04)</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <action>
CHECKPOINT: Human must provide PeeringDB API key and run verification commands.

CI pipeline verification is complete (Task 1). Now the human needs to provide their PeeringDB API key for VFY-03 and VFY-04 verification. The executor will record results but the human must supply the key and run the commands.

**VFY-03: pdbcompat-check CLI with real API key (per D-11):**
1. Run single-type test first (per Pitfall #5 -- rate limiting):
   `go run ./cmd/pdbcompat-check --api-key YOUR_KEY --type net`
   Expected: exits 0, prints comparison results for network type
2. If single-type passes, run all 13 types (~13s with auth):
   `go run ./cmd/pdbcompat-check --api-key YOUR_KEY`
   Expected: exits 0, prints comparison for all types

**VFY-04: Live integration test + invalid key rejection (per D-12, D-13):**
3. Run live integration test:
   `PDBPLUS_PEERINGDB_API_KEY=YOUR_KEY go test -run TestLive -peeringdb-live ./internal/conformance/`
   Expected: test passes
4. Test invalid key rejection:
   `go run ./cmd/pdbcompat-check --api-key invalid-key-here --type net`
   Expected: produces error containing "API key may be invalid" without crashing (no panic, no segfault)

After running these commands, the human provides results so they can be recorded in 20-VERIFICATION-ITEMS.md.

Resume signal: Provide the output/results of each command above, or type "all passed" / describe any failures.
  </action>
  <verify>
    <automated>grep -c "VFY-03\|VFY-04" .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md 2>/dev/null</automated>
  </verify>
  <done>VFY-03 and VFY-04 results recorded in verification report with PASS/FAIL status and observations</done>
</task>

</tasks>

<verification>
- `gh run list --limit 3` shows recent passing CI runs
- 20-VERIFICATION-ITEMS.md exists with VFY-01 through VFY-04 sections populated
- All items have PASS, FAIL, or BLOCKED status with observation text
- No API key values appear in the report (SEC-2)
</verification>

<success_criteria>
- VFY-01: All 4 CI jobs (lint, test, build, govulncheck) confirmed passing on recent push
- VFY-02: Coverage comment confirmed posting on PR and deduplicating on subsequent push
- VFY-03: pdbcompat-check CLI confirmed working with real API key for at least net type
- VFY-04: Live integration test confirmed passing with key; invalid key confirmed producing WARN without crash
- All results recorded in 20-VERIFICATION-ITEMS.md with concrete observations
</success_criteria>

<output>
After completion, create `.planning/phases/20-deferred-human-verification/20-01-SUMMARY.md`
</output>
