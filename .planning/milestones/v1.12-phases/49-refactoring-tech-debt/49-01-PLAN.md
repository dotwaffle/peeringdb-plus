---
phase: 49-refactoring-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/detail.go
  - internal/web/query_network.go
  - internal/web/query_ix.go
  - internal/web/query_facility.go
  - internal/web/query_org.go
  - internal/web/query_campus.go
  - internal/web/query_carrier.go
autonomous: true
requirements: [REFAC-01]

must_haves:
  truths:
    - "No single file in internal/web/ related to detail queries exceeds 300 lines"
    - "All existing detail page routes return the same responses as before the split"
    - "All existing tests in detail_test.go pass without modification"
  artifacts:
    - path: "internal/web/query_network.go"
      provides: "queryNetwork function and network-specific helpers"
      contains: "func (h *Handler) queryNetwork"
    - path: "internal/web/query_ix.go"
      provides: "queryIX function and IX-specific helpers"
      contains: "func (h *Handler) queryIX"
    - path: "internal/web/query_facility.go"
      provides: "queryFacility function and facility-specific helpers"
      contains: "func (h *Handler) queryFacility"
    - path: "internal/web/query_org.go"
      provides: "queryOrg function and org-specific helpers"
      contains: "func (h *Handler) queryOrg"
    - path: "internal/web/query_campus.go"
      provides: "queryCampus function and campus-specific helpers"
      contains: "func (h *Handler) queryCampus"
    - path: "internal/web/query_carrier.go"
      provides: "queryCarrier function and carrier-specific helpers"
      contains: "func (h *Handler) queryCarrier"
    - path: "internal/web/detail.go"
      provides: "handleXxxDetail functions, handleFragment, getFreshness, and fragment handlers"
  key_links:
    - from: "internal/web/detail.go"
      to: "internal/web/query_*.go"
      via: "handleXxxDetail calls queryXxx"
      pattern: "h\\.query(Network|IX|Facility|Org|Campus|Carrier)"
---

<objective>
Split internal/web/detail.go (1427 LOC) into focused per-entity query files so no single file exceeds 300 lines.

Purpose: Improve maintainability by separating entity query logic from handler routing logic.
Output: 6 new query_*.go files + slimmed-down detail.go, all in package web.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-refactoring-tech-debt/49-CONTEXT.md

@internal/web/detail.go
@internal/web/detail_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract query functions into per-entity files</name>
  <files>
    internal/web/query_network.go
    internal/web/query_ix.go
    internal/web/query_facility.go
    internal/web/query_org.go
    internal/web/query_campus.go
    internal/web/query_carrier.go
    internal/web/detail.go
  </files>
  <read_first>
    internal/web/detail.go
  </read_first>
  <action>
Split detail.go by moving each queryXxx function (and any helper functions specific to that entity type) into its own file. Per REFAC-01 from CONTEXT.md decisions:

1. Create `query_network.go` -- move `queryNetwork` (line ~81-204). This is the largest query function.
2. Create `query_ix.go` -- move `queryIX` (line ~238-369).
3. Create `query_facility.go` -- move `queryFacility` (line ~403-514).
4. Create `query_org.go` -- move `queryOrg` (line ~548-707).
5. Create `query_campus.go` -- move `queryCampus` (line ~741-802).
6. Create `query_carrier.go` -- move `queryCarrier` (line ~836-884).

Each new file must:
- Have `package web` declaration
- Import only what that file needs (run goimports after extraction)
- Keep the `(h *Handler)` receiver -- all methods stay on the existing Handler type

What stays in detail.go:
- `getFreshness()` (shared by all handlers)
- All `handleXxxDetail` functions (handleNetworkDetail, handleIXDetail, handleFacilityDetail, handleOrgDetail, handleCampusDetail, handleCarrierDetail)
- `handleFragment` dispatcher and all `handleXxxFragment` functions (handleNetIXLansFragment, handleNetFacilitiesFragment, etc.)
- All imports needed by the remaining functions

Do NOT change any function signatures, parameter types, return types, or logic. This is a pure move refactor.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go build ./internal/web/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/web/...` succeeds with no errors
    - `grep -c "^func" internal/web/query_network.go` returns at least 1
    - `grep -c "^func" internal/web/query_ix.go` returns at least 1
    - `grep -c "^func" internal/web/query_facility.go` returns at least 1
    - `grep -c "^func" internal/web/query_org.go` returns at least 1
    - `grep -c "^func" internal/web/query_campus.go` returns at least 1
    - `grep -c "^func" internal/web/query_carrier.go` returns at least 1
    - `wc -l internal/web/detail.go` is less than 900 (down from 1427)
    - All 7 files have `package web` as their package declaration
  </acceptance_criteria>
  <done>Six query_*.go files created, detail.go reduced, all functions retained, package builds cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Verify no file exceeds 300 lines and all tests pass</name>
  <files>
    internal/web/query_network.go
    internal/web/query_ix.go
    internal/web/query_facility.go
    internal/web/query_org.go
    internal/web/query_campus.go
    internal/web/query_carrier.go
    internal/web/detail.go
  </files>
  <read_first>
    internal/web/detail.go
    internal/web/query_network.go
    internal/web/query_ix.go
  </read_first>
  <action>
Verify and adjust the split to meet the 300-line limit per REFAC-01:

1. Run `wc -l` on each new query_*.go file and detail.go.
2. If any query_*.go file exceeds 300 lines, further split it (e.g., query_network.go may be large due to the extensive queryNetwork function). The CONTEXT.md decision says "No single file should exceed 300 lines after the split" -- this applies to the query files, not necessarily detail.go which retains all handlers and fragments (those are routing logic, not query helpers).
3. If detail.go exceeds 300 lines, that is acceptable per the decisions -- the split strategy separates "query helpers" from "handlers", and detail.go keeps the handler/routing logic. The 300-line limit in the success criteria applies to individual entity query files.
4. Run the full existing test suite to confirm nothing broke.
5. Run `go vet ./internal/web/...` to verify no issues.

If any query file is borderline (250-300 lines), leave it -- it is within spec.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race ./internal/web/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/web/... -count=1` passes with 0 failures
    - `go vet ./internal/web/...` reports no issues
    - `wc -l internal/web/query_network.go` is less than or equal to 300
    - `wc -l internal/web/query_ix.go` is less than or equal to 300
    - `wc -l internal/web/query_facility.go` is less than or equal to 300
    - `wc -l internal/web/query_org.go` is less than or equal to 300
    - `wc -l internal/web/query_campus.go` is less than or equal to 300
    - `wc -l internal/web/query_carrier.go` is less than or equal to 300
  </acceptance_criteria>
  <done>All query files under 300 lines, all existing detail_test.go tests pass, go vet clean</done>
</task>

</tasks>

<verification>
- `go build ./internal/web/...` succeeds
- `go test -race ./internal/web/... -count=1` passes (all existing tests)
- `go vet ./internal/web/...` clean
- No query_*.go file exceeds 300 lines
- `grep -l "func (h \*Handler) query" internal/web/query_*.go | wc -l` equals 6
</verification>

<success_criteria>
detail.go split into focused files. No query file exceeds 300 lines. All existing routes and tests work unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/49-refactoring-tech-debt/49-01-SUMMARY.md`
</output>
