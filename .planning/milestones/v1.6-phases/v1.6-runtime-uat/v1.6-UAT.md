---
status: diagnosed
phase: v1.6-runtime-uat
source: [milestones/v1.6-phases/21-infrastructure/21-VERIFICATION.md, milestones/v1.6-phases/23-connectrpc-services/23-VERIFICATION.md, milestones/v1.6-phases/24-list-filtering/24-VERIFICATION.md]
started: 2026-03-25T04:20:00Z
updated: 2026-03-25T04:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Existing API Surfaces Still Work
expected: Root JSON discovery (with `connectrpc` field), GraphQL playground, REST API, PeeringDB compat API all respond.
result: pass

### 2. ConnectRPC Get RPC via Connect protocol
expected: POST to /peeringdb.v1.NetworkService/GetNetwork with `{"id": 1}` returns a network. Non-existent ID returns NOT_FOUND.
result: pass

### 3. ConnectRPC List RPC with Pagination
expected: ListNetworks with page_size=2 returns 2 networks and a next_page_token. Using that token returns the next page.
result: pass

### 4. ConnectRPC List Filtering
expected: ListNetworks with asn=15169 returns only Google. Invalid asn=-1 returns INVALID_ARGUMENT.
result: pass

### 5. gRPC Server Reflection
expected: grpcurl discovers all 13 PeeringDB services via reflection.
result: issue
reported: "grpcurl fails with: rpc error: code = Internal desc = *middleware.responseWriter does not implement http.Flusher. Reflection uses server-streaming which requires http.Flusher on the response writer. A middleware in the chain wraps the writer without forwarding the Flusher interface."
severity: major

### 6. gRPC Health Check
expected: Health check for peeringdb.v1.NetworkService returns SERVING status.
result: pass

### 7. h2c gRPC Protocol
expected: ConnectRPC endpoints respond through Fly.io edge (TLS -> h2c backend), confirming h2_backend works.
result: pass

### 8. Fly.io fly-replay Header
expected: On a Fly.io replica, POST /sync returns fly-replay header.
result: skipped
reason: Requires SSH into a replica machine to test; all external requests are routed by Fly proxy. Existing unit tests cover this logic.

## Summary

total: 8
passed: 5
issues: 1
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "gRPC server reflection allows grpcurl/grpcui to discover all 13 services"
  status: failed
  reason: "User reported: grpcurl fails with rpc error: code = Internal desc = *middleware.responseWriter does not implement http.Flusher. Reflection uses server-streaming which requires http.Flusher. A middleware wraps the response writer without forwarding Flusher."
  severity: major
  test: 5
  artifacts:
    - internal/middleware/logging.go
    - cmd/peeringdb-plus/main.go
  missing:
    - http.Flusher implementation on middleware response writers
  root_cause: "The logging middleware (or readiness middleware) wraps http.ResponseWriter in a custom responseWriter struct that captures status codes but does not implement http.Flusher. gRPC streaming (used by reflection and health watch) requires Flusher to send incremental responses. The middleware needs to forward Flusher/Hijacker interfaces to the underlying writer."
