---
phase: 24-list-filtering
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - internal/grpcserver/campus.go
  - internal/grpcserver/carrier.go
  - internal/grpcserver/carrierfacility.go
  - internal/grpcserver/facility.go
  - internal/grpcserver/internetexchange.go
  - internal/grpcserver/ixfacility.go
  - internal/grpcserver/ixlan.go
  - internal/grpcserver/ixprefix.go
  - internal/grpcserver/networkfacility.go
  - internal/grpcserver/networkixlan.go
  - internal/grpcserver/organization.go
  - internal/grpcserver/poc.go
  - internal/grpcserver/grpcserver_test.go
autonomous: true
requirements: [API-03]

must_haves:
  truths:
    - "ListFacilities filtered by country=US returns only US facilities"
    - "ListOrganizations filtered by name substring matches case-insensitively"
    - "ListIxPrefixes filtered by protocol=IPv4 returns only IPv4 prefixes"
    - "ListPocs filtered by role=Abuse returns only abuse contacts"
    - "ListNetworkIxLans filtered by net_id returns only that networks peerings"
    - "ListCarrierFacilities filtered by carrier_id returns only that carriers facilities"
    - "Invalid FK ID values return INVALID_ARGUMENT across all services"
    - "All 12 remaining services support their respective filter fields"
  artifacts:
    - path: "internal/grpcserver/facility.go"
      provides: "Filter predicate application in ListFacilities"
      contains: "req.Country != nil"
    - path: "internal/grpcserver/organization.go"
      provides: "Filter predicate application in ListOrganizations"
      contains: "req.Name != nil"
    - path: "internal/grpcserver/poc.go"
      provides: "Filter predicate application in ListPocs"
      contains: "req.Role != nil"
    - path: "internal/grpcserver/ixprefix.go"
      provides: "Filter predicate application in ListIxPrefixes"
      contains: "req.Protocol != nil"
    - path: "internal/grpcserver/grpcserver_test.go"
      provides: "Filter tests for representative entity types"
      contains: "TestListFacilitiesFilters"
  key_links:
    - from: "internal/grpcserver/facility.go"
      to: "ent/facility/where.go"
      via: "ent predicate functions"
      pattern: "facility\\.CountryEQ"
    - from: "internal/grpcserver/poc.go"
      to: "ent/poc/where.go"
      via: "ent predicate functions"
      pattern: "poc\\.RoleEQ"
---

<objective>
Apply the filter predicate pattern (established in Plan 01 for Network) to all 12 remaining service handlers, with tests for representative entity types covering all filter field categories.

Purpose: Completes API-03 by ensuring every List RPC across all 13 PeeringDB types supports typed filtering.
Output: All 12 remaining handlers have filter logic, tests cover the key filter categories (geographic, FK, string code, name substring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-list-filtering/24-CONTEXT.md
@.planning/phases/24-list-filtering/24-RESEARCH.md
@.planning/phases/24-list-filtering/24-01-SUMMARY.md
@internal/grpcserver/network.go
@proto/peeringdb/v1/services.proto
</context>

<interfaces>
<!-- The executor needs the Network filter pattern from Plan 01 as the template. -->
<!-- Pattern: nil-check optional pointer field -> validate if numeric -> append ent predicate -> Where(entity.And(predicates...)) -->

From internal/grpcserver/network.go (after Plan 01):
```go
// Build filter predicates from optional fields.
var predicates []predicate.Network
if req.Asn != nil {
    if *req.Asn <= 0 {
        return nil, connect.NewError(connect.CodeInvalidArgument,
            fmt.Errorf("invalid filter: asn must be positive"))
    }
    predicates = append(predicates, network.AsnEQ(int(*req.Asn)))
}
// ... more filters ...
query := s.Client.Network.Query().
    Order(ent.Asc(network.FieldID)).
    Limit(pageSize + 1).
    Offset(offset)
if len(predicates) > 0 {
    query = query.Where(network.And(predicates...))
}
```

Entity predicate packages and functions (all verified to exist):
- campus: NameContainsFold, CountryEQ, CityContainsFold, StatusEQ, OrgIDEQ
- carrier: NameContainsFold, StatusEQ, OrgIDEQ
- carrierfacility: CarrierIDEQ, FacIDEQ, StatusEQ
- facility: NameContainsFold, CountryEQ, CityContainsFold, StatusEQ, OrgIDEQ
- internetexchange: NameContainsFold, CountryEQ, CityContainsFold, StatusEQ, OrgIDEQ
- ixfacility: IxIDEQ, FacIDEQ, CountryEQ, CityContainsFold, StatusEQ
- ixlan: IxIDEQ, NameContainsFold, StatusEQ
- ixprefix: IxlanIDEQ, ProtocolEQ, StatusEQ
- networkfacility: NetIDEQ, FacIDEQ, CountryEQ, CityContainsFold, StatusEQ
- networkixlan: NetIDEQ, IxlanIDEQ, AsnEQ, NameContainsFold, StatusEQ
- organization: NameContainsFold, CountryEQ, CityContainsFold, StatusEQ
- poc: NetIDEQ, RoleEQ, NameContainsFold, StatusEQ
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add filter logic to all 12 remaining List handlers</name>
  <files>
    internal/grpcserver/campus.go,
    internal/grpcserver/carrier.go,
    internal/grpcserver/carrierfacility.go,
    internal/grpcserver/facility.go,
    internal/grpcserver/internetexchange.go,
    internal/grpcserver/ixfacility.go,
    internal/grpcserver/ixlan.go,
    internal/grpcserver/ixprefix.go,
    internal/grpcserver/networkfacility.go,
    internal/grpcserver/networkixlan.go,
    internal/grpcserver/organization.go,
    internal/grpcserver/poc.go
  </files>
  <read_first>
    - internal/grpcserver/network.go (reference pattern from Plan 01)
    - internal/grpcserver/campus.go
    - internal/grpcserver/carrier.go
    - internal/grpcserver/carrierfacility.go
    - internal/grpcserver/facility.go
    - internal/grpcserver/internetexchange.go
    - internal/grpcserver/ixfacility.go
    - internal/grpcserver/ixlan.go
    - internal/grpcserver/ixprefix.go
    - internal/grpcserver/networkfacility.go
    - internal/grpcserver/networkixlan.go
    - internal/grpcserver/organization.go
    - internal/grpcserver/poc.go
    - proto/peeringdb/v1/services.proto (to see what filter fields exist per message)
  </read_first>
  <action>
Apply the same filter predicate accumulation pattern from NetworkService (Plan 01) to each of the 12 remaining List handlers. For each handler:

1. Add `"github.com/dotwaffle/peeringdb-plus/ent/predicate"` to imports
2. Insert filter predicate accumulation block between `decodePageToken` and the query builder
3. Modify the query to apply predicates via `.Where(entity.And(predicates...))` when non-empty

**Filter fields and validation per handler:**

**CampusService.ListCampuses** -- predicates type: `[]predicate.Campus`
- `req.Name != nil` -> `campus.NameContainsFold(*req.Name)`
- `req.Country != nil` -> `campus.CountryEQ(*req.Country)`
- `req.City != nil` -> `campus.CityContainsFold(*req.City)`
- `req.Status != nil` -> `campus.StatusEQ(*req.Status)`
- `req.OrgId != nil` -> validate `*req.OrgId > 0`, then `campus.OrgIDEQ(int(*req.OrgId))`
  Error: `"invalid filter: org_id must be positive"`

**CarrierService.ListCarriers** -- predicates type: `[]predicate.Carrier`
- `req.Name != nil` -> `carrier.NameContainsFold(*req.Name)`
- `req.Status != nil` -> `carrier.StatusEQ(*req.Status)`
- `req.OrgId != nil` -> validate `*req.OrgId > 0`, then `carrier.OrgIDEQ(int(*req.OrgId))`
  Error: `"invalid filter: org_id must be positive"`

**CarrierFacilityService.ListCarrierFacilities** -- predicates type: `[]predicate.CarrierFacility`
- `req.CarrierId != nil` -> validate `*req.CarrierId > 0`, then `carrierfacility.CarrierIDEQ(int(*req.CarrierId))`
  Error: `"invalid filter: carrier_id must be positive"`
- `req.FacId != nil` -> validate `*req.FacId > 0`, then `carrierfacility.FacIDEQ(int(*req.FacId))`
  Error: `"invalid filter: fac_id must be positive"`
- `req.Status != nil` -> `carrierfacility.StatusEQ(*req.Status)`

**FacilityService.ListFacilities** -- predicates type: `[]predicate.Facility`
- `req.Name != nil` -> `facility.NameContainsFold(*req.Name)`
- `req.Country != nil` -> `facility.CountryEQ(*req.Country)`
- `req.City != nil` -> `facility.CityContainsFold(*req.City)`
- `req.Status != nil` -> `facility.StatusEQ(*req.Status)`
- `req.OrgId != nil` -> validate `*req.OrgId > 0`, then `facility.OrgIDEQ(int(*req.OrgId))`
  Error: `"invalid filter: org_id must be positive"`

**InternetExchangeService.ListInternetExchanges** -- predicates type: `[]predicate.InternetExchange`
- `req.Name != nil` -> `internetexchange.NameContainsFold(*req.Name)`
- `req.Country != nil` -> `internetexchange.CountryEQ(*req.Country)`
- `req.City != nil` -> `internetexchange.CityContainsFold(*req.City)`
- `req.Status != nil` -> `internetexchange.StatusEQ(*req.Status)`
- `req.OrgId != nil` -> validate `*req.OrgId > 0`, then `internetexchange.OrgIDEQ(int(*req.OrgId))`
  Error: `"invalid filter: org_id must be positive"`

**IxFacilityService.ListIxFacilities** -- predicates type: `[]predicate.IxFacility`
- `req.IxId != nil` -> validate `*req.IxId > 0`, then `ixfacility.IxIDEQ(int(*req.IxId))`
  Error: `"invalid filter: ix_id must be positive"`
- `req.FacId != nil` -> validate `*req.FacId > 0`, then `ixfacility.FacIDEQ(int(*req.FacId))`
  Error: `"invalid filter: fac_id must be positive"`
- `req.Country != nil` -> `ixfacility.CountryEQ(*req.Country)`
- `req.City != nil` -> `ixfacility.CityContainsFold(*req.City)`
- `req.Status != nil` -> `ixfacility.StatusEQ(*req.Status)`

**IxLanService.ListIxLans** -- predicates type: `[]predicate.IxLan`
- `req.IxId != nil` -> validate `*req.IxId > 0`, then `ixlan.IxIDEQ(int(*req.IxId))`
  Error: `"invalid filter: ix_id must be positive"`
- `req.Name != nil` -> `ixlan.NameContainsFold(*req.Name)`
- `req.Status != nil` -> `ixlan.StatusEQ(*req.Status)`

**IxPrefixService.ListIxPrefixes** -- predicates type: `[]predicate.IxPrefix`
- `req.IxlanId != nil` -> validate `*req.IxlanId > 0`, then `ixprefix.IxlanIDEQ(int(*req.IxlanId))`
  Error: `"invalid filter: ixlan_id must be positive"`
- `req.Protocol != nil` -> `ixprefix.ProtocolEQ(*req.Protocol)`
- `req.Status != nil` -> `ixprefix.StatusEQ(*req.Status)`

**NetworkFacilityService.ListNetworkFacilities** -- predicates type: `[]predicate.NetworkFacility`
- `req.NetId != nil` -> validate `*req.NetId > 0`, then `networkfacility.NetIDEQ(int(*req.NetId))`
  Error: `"invalid filter: net_id must be positive"`
- `req.FacId != nil` -> validate `*req.FacId > 0`, then `networkfacility.FacIDEQ(int(*req.FacId))`
  Error: `"invalid filter: fac_id must be positive"`
- `req.Country != nil` -> `networkfacility.CountryEQ(*req.Country)`
- `req.City != nil` -> `networkfacility.CityContainsFold(*req.City)`
- `req.Status != nil` -> `networkfacility.StatusEQ(*req.Status)`

**NetworkIxLanService.ListNetworkIxLans** -- predicates type: `[]predicate.NetworkIxLan`
- `req.NetId != nil` -> validate `*req.NetId > 0`, then `networkixlan.NetIDEQ(int(*req.NetId))`
  Error: `"invalid filter: net_id must be positive"`
- `req.IxlanId != nil` -> validate `*req.IxlanId > 0`, then `networkixlan.IxlanIDEQ(int(*req.IxlanId))`
  Error: `"invalid filter: ixlan_id must be positive"`
- `req.Asn != nil` -> validate `*req.Asn > 0`, then `networkixlan.AsnEQ(int(*req.Asn))`
  Error: `"invalid filter: asn must be positive"`
- `req.Name != nil` -> `networkixlan.NameContainsFold(*req.Name)`
- `req.Status != nil` -> `networkixlan.StatusEQ(*req.Status)`

**OrganizationService.ListOrganizations** -- predicates type: `[]predicate.Organization`
- `req.Name != nil` -> `organization.NameContainsFold(*req.Name)`
- `req.Country != nil` -> `organization.CountryEQ(*req.Country)`
- `req.City != nil` -> `organization.CityContainsFold(*req.City)`
- `req.Status != nil` -> `organization.StatusEQ(*req.Status)`

**PocService.ListPocs** -- predicates type: `[]predicate.Poc`
- `req.NetId != nil` -> validate `*req.NetId > 0`, then `poc.NetIDEQ(int(*req.NetId))`
  Error: `"invalid filter: net_id must be positive"`
- `req.Role != nil` -> `poc.RoleEQ(*req.Role)`
- `req.Name != nil` -> `poc.NameContainsFold(*req.Name)`
- `req.Status != nil` -> `poc.StatusEQ(*req.Status)`

**String filter rules (per locked decisions):**
- `NameContainsFold` for name fields (case-insensitive substring LIKE)
- `CityContainsFold` for city fields (case-insensitive substring LIKE)
- `CountryEQ` for country codes (exact match)
- `StatusEQ` for status codes (exact match)
- `RoleEQ` for role codes (exact match)
- `ProtocolEQ` for protocol codes (exact match)

**Numeric validation rule:** All int64 FK/ID filter fields must be validated as `> 0` before use. Return `connect.NewError(connect.CodeInvalidArgument, fmt.Errorf("invalid filter: {field_name} must be positive"))`.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go build ./... && go vet ./internal/grpcserver/</automated>
  </verify>
  <acceptance_criteria>
    - campus.go contains `req.Name != nil` and `campus.NameContainsFold`
    - campus.go contains `campus.CountryEQ` and `campus.CityContainsFold`
    - carrier.go contains `req.OrgId != nil` and `carrier.OrgIDEQ`
    - carrierfacility.go contains `req.CarrierId != nil` and `carrierfacility.CarrierIDEQ`
    - facility.go contains `req.Country != nil` and `facility.CountryEQ`
    - internetexchange.go contains `internetexchange.CityContainsFold`
    - ixfacility.go contains `req.IxId != nil` and `ixfacility.IxIDEQ`
    - ixlan.go contains `req.IxId != nil` and `ixlan.IxIDEQ`
    - ixprefix.go contains `req.Protocol != nil` and `ixprefix.ProtocolEQ`
    - networkfacility.go contains `req.NetId != nil` and `networkfacility.NetIDEQ`
    - networkixlan.go contains `req.Asn != nil` and `networkixlan.AsnEQ`
    - organization.go contains `req.Country != nil` and `organization.CountryEQ`
    - poc.go contains `req.Role != nil` and `poc.RoleEQ`
    - All 12 handlers contain `invalid filter:` error messages for numeric validation
    - `go build ./...` succeeds
    - `go vet ./internal/grpcserver/` passes
  </acceptance_criteria>
  <done>All 12 remaining List handlers apply entity-specific filter predicates with proper validation, and the codebase compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add filter tests for representative entity types</name>
  <files>internal/grpcserver/grpcserver_test.go</files>
  <read_first>
    - internal/grpcserver/grpcserver_test.go (for existing test patterns including TestListNetworksFilters from Plan 01)
    - internal/grpcserver/facility.go (to verify filter field names)
    - internal/grpcserver/organization.go
    - internal/grpcserver/poc.go
    - internal/grpcserver/ixprefix.go
    - internal/grpcserver/networkixlan.go
    - internal/grpcserver/carrierfacility.go
  </read_first>
  <behavior>
    - Test Facility: filter by country="US" returns only US facilities (tests geographic exact match)
    - Test Facility: filter by city="Dallas" matches case-insensitively (tests city substring)
    - Test Facility: filter by name+country combined (tests AND logic on different entity)
    - Test Facility: invalid org_id returns INVALID_ARGUMENT
    - Test Organization: filter by name substring matches case-insensitively
    - Test Organization: filter by status="ok" excludes deleted
    - Test Poc: filter by role="Abuse" returns only abuse contacts (tests role exact match)
    - Test Poc: filter by net_id returns only that networks contacts (tests FK filter)
    - Test Poc: invalid net_id returns INVALID_ARGUMENT
    - Test IxPrefix: filter by protocol="IPv4" (tests protocol exact match)
    - Test NetworkIxLan: filter by asn returns matching records (tests ASN on junction entity)
    - Test CarrierFacility: filter by carrier_id (tests FK-only entity filtering)
  </behavior>
  <action>
Add test functions to `grpcserver_test.go` for 6 representative entity types that collectively cover all filter field categories. Use the same patterns as `TestListNetworksFilters` from Plan 01.

**TestListFacilitiesFilters**: Seed 3 facilities with distinct country/city/name/status. Required fields per facility: `SetName(v).SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by country="US" (exact match)
- Filter by city="dallas" (case-insensitive substring -- seeds "Dallas")
- Filter by name+country combined (AND)
- Invalid org_id=-1 returns INVALID_ARGUMENT

**TestListOrganizationsFilters**: Seed 3 orgs. Required fields: `SetName(v).SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by name substring
- Filter by status="ok" excludes deleted

**TestListPocsFilters**: Seed 3 POCs with different roles. Required fields: `SetRole(v).SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by role="Abuse" (exact match)
- Filter by net_id (FK filter)
- Invalid net_id=0 returns INVALID_ARGUMENT

**TestListIxPrefixesFilters**: Seed 3 prefixes. Required fields: `SetPrefix(v).SetInDfz(true).SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by protocol="IPv4" (exact match)
- Filter by status

**TestListNetworkIxLansFilters**: Seed 3 network IX LANs. Required fields: `SetAsn(v).SetSpeed(v).SetBfdSupport(false).SetIsRsPeer(false).SetOperational(false).SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by asn (validates ASN filter on junction entity)
- Filter by status

**TestListCarrierFacilitiesFilters**: Seed 2 carrier facilities. Required fields: `SetCreated(now).SetUpdated(now).SetStatus(v)`. Test:
- Filter by carrier_id (FK filter on FK-only entity)
- Invalid carrier_id returns INVALID_ARGUMENT

Import `"google.golang.org/protobuf/proto"` for `proto.Int64()` and `proto.String()`.

Each test function: `t.Parallel()`, table-driven, uses `testutil.SetupClient(t)` for isolated test database.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && go test ./internal/grpcserver/ -run "TestList.+Filters" -race -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grpcserver_test.go contains `func TestListFacilitiesFilters`
    - grpcserver_test.go contains `func TestListOrganizationsFilters`
    - grpcserver_test.go contains `func TestListPocsFilters`
    - grpcserver_test.go contains `func TestListIxPrefixesFilters`
    - grpcserver_test.go contains `func TestListNetworkIxLansFilters`
    - grpcserver_test.go contains `func TestListCarrierFacilitiesFilters`
    - grpcserver_test.go contains `proto.String("US")` for country filter test
    - grpcserver_test.go contains `poc.RoleEQ` or `proto.String("Abuse")` for role test
    - grpcserver_test.go contains `proto.String("IPv4")` for protocol test
    - All TestList*Filters tests pass with -race
    - `go test ./internal/grpcserver/ -race -count=1` all pass (no regressions)
  </acceptance_criteria>
  <done>Filter tests cover all filter field categories (geographic exact match, city substring, name substring, FK ID, role code, protocol code, status code, ASN, combined AND, invalid value validation) across 6 representative entity types. All pass with -race.</done>
</task>

</tasks>

<verification>
```bash
# All filter tests
go test ./internal/grpcserver/ -run "TestList.+Filters" -race -count=1 -v

# Full grpcserver test suite (includes Plan 01 tests)
go test ./internal/grpcserver/ -race -count=1

# Full project suite
go test -race ./...

# Verify all 13 handlers have filter logic
grep -l "predicates\|req\.\w\+ != nil" internal/grpcserver/*.go | wc -l
# Expected: 13 (all handler files)
```
</verification>

<success_criteria>
1. All 12 remaining List handlers apply entity-specific filter predicates
2. Numeric FK/ID filter values validated as positive with descriptive INVALID_ARGUMENT errors
3. String matching follows locked decisions: ContainsFold for names/cities, EQ for codes
4. Filter tests cover all field categories across 6 representative entities
5. Full test suite passes with -race
6. API-03 requirement fully satisfied: all 13 entity types support typed List filtering
</success_criteria>

<output>
After completion, create `.planning/phases/24-list-filtering/24-02-SUMMARY.md`
</output>
