---
phase: 33-grpc-dedup-filter-parity
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - internal/grpcserver/grpcserver_test.go
  - internal/grpcserver/generic_test.go
autonomous: true
requirements: [QUAL-01, QUAL-03, ARCH-02]

must_haves:
  truths:
    - "grpcserver package test coverage reaches 60%+"
    - "Generic ListEntities and StreamEntities helpers are tested directly"
    - "All 13 entity types have at least one List and one Stream test"
    - "New filter parity fields are exercised in tests"
  artifacts:
    - path: "internal/grpcserver/generic_test.go"
      provides: "Direct tests for ListEntities and StreamEntities generic helpers"
      min_lines: 150
    - path: "internal/grpcserver/grpcserver_test.go"
      provides: "Comprehensive per-entity integration tests"
      contains: "TestListCampuses"
  key_links:
    - from: "internal/grpcserver/generic_test.go"
      to: "internal/grpcserver/generic.go"
      via: "Direct calls to ListEntities and StreamEntities"
      pattern: "ListEntities|StreamEntities"
    - from: "internal/grpcserver/grpcserver_test.go"
      to: "internal/testutil"
      via: "testutil.SetupClient(t) for in-memory SQLite"
      pattern: "testutil.SetupClient"
---

<objective>
Add comprehensive tests to bring grpcserver package coverage to 60%+, covering the generic helpers directly, all 13 entity types, and new filter parity fields.

Purpose: QUAL-03 requires grpcserver coverage at 60%+ (currently 22.2%). QUAL-01 requires the generic helpers to be tested. ARCH-02 requires the new filter fields to be exercised. Current tests cover 7 of 13 entity types -- need tests for Campus, Carrier, InternetExchange, IxFacility, IxLan, NetworkFacility.

Output: generic_test.go with direct helper tests, expanded grpcserver_test.go covering all 13 entity types and new filter fields. Coverage at 60%+.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-grpc-dedup-filter-parity/33-CONTEXT.md
@.planning/phases/33-grpc-dedup-filter-parity/33-RESEARCH.md
@.planning/phases/33-grpc-dedup-filter-parity/33-01-SUMMARY.md
@.planning/phases/33-grpc-dedup-filter-parity/33-02-SUMMARY.md

@internal/grpcserver/generic.go
@internal/grpcserver/grpcserver_test.go
@internal/grpcserver/pagination_test.go
@internal/testutil/

<interfaces>
<!-- From internal/grpcserver/generic.go (created in Plan 02): -->
<!-- func ListEntities[E any, P any](ctx context.Context, params ListParams[E, P]) ([]*P, string, error) -->
<!-- func StreamEntities[E any, P any](ctx context.Context, params StreamParams[E, P], stream *connect.ServerStream[P]) error -->
<!-- type ListParams[E any, P any] struct { EntityName, PageSize, PageToken, ApplyFilters, Query, Convert } -->
<!-- type StreamParams[E any, P any] struct { EntityName, Timeout, SinceID, UpdatedSince, ApplyFilters, Count, QueryBatch, Convert, GetID } -->

<!-- From internal/testutil: -->
<!-- func SetupClient(t *testing.T) *ent.Client  -- creates in-memory SQLite with auto-migration -->

<!-- From existing grpcserver_test.go: -->
<!-- Tests exist for: Network, Facility, Organization, Poc, IxPrefix, NetworkIxLan, CarrierFacility -->
<!-- Missing tests for: Campus, Carrier, InternetExchange, IxFacility, IxLan, NetworkFacility -->
<!-- Pattern: Create service struct, seed data, call method, assert response -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct tests for generic ListEntities and StreamEntities helpers</name>
  <files>internal/grpcserver/generic_test.go</files>
  <read_first>
    - internal/grpcserver/generic.go (the functions being tested -- ListParams, StreamParams, ListEntities, StreamEntities)
    - internal/grpcserver/grpcserver_test.go (first 100 lines for import patterns, test setup style)
    - internal/grpcserver/pagination.go (encodePageToken/decodePageToken used in assertions)
    - internal/grpcserver/pagination_test.go (existing pagination test patterns)
  </read_first>
  <action>
Create `internal/grpcserver/generic_test.go` with tests that exercise the generic helpers directly using mock callbacks. This provides coverage of the generic code independent of specific entity types.

The tests use simple mock types (not ent entities) to test the generic logic:

```go
type mockEntity struct {
    ID   int
    Name string
}
type mockProto struct {
    Id   int64
    Name string
}
```

**TestListEntities** (table-driven per T-1):
Test cases:
1. `"empty result"` -- Query returns no items -> returns empty slice, no next token
2. `"single page"` -- Query returns 3 items (pageSize=5) -> returns 3 items, no next token
3. `"pagination"` -- Query returns 6 items (pageSize=5) -> returns 5 items, next token present
4. `"invalid page token"` -- PageToken is invalid base64 -> returns CodeInvalidArgument error
5. `"filter error"` -- ApplyFilters returns connect error -> returns that error
6. `"query error"` -- Query returns error -> returns CodeInternal error
7. `"default page size"` -- PageSize=0 -> uses defaultPageSize (100)
8. `"max page size"` -- PageSize=5000 -> caps at maxPageSize (1000)

For each test case, construct `ListParams[mockEntity, mockProto]` with appropriate callback implementations. Verify: item count, next page token presence/absence, error code.

**TestStreamEntities** (table-driven):
Test cases:
1. `"empty stream"` -- QueryBatch returns empty -> stream receives nothing, returns nil
2. `"single batch"` -- QueryBatch returns 3 items (streamBatchSize=500) -> stream receives 3 items
3. `"filter error"` -- ApplyFilters returns error -> returns that error
4. `"count error"` -- Count returns error -> returns CodeInternal error
5. `"since_id"` -- SinceID set -> verify lastID starts from SinceID value (check QueryBatch receives correct afterID)

For stream tests, create a mock `*connect.ServerStream[mockProto]`. Since connect.ServerStream is a concrete struct (not an interface), we need to test via the actual service methods instead. Alternative: test StreamEntities by calling it through a real ConnectRPC server setup like the existing tests do.

Actually, the cleaner approach for StreamEntities: create a real HTTP test server with a handler that calls StreamEntities, then use a ConnectRPC client to consume the stream. But this is complex.

Simpler approach: test the stream functionality through per-entity service tests (Task 2). For generic_test.go, focus on ListEntities which doesn't need a stream mock.

For StreamEntities coverage, add a helper test that verifies the timeout and predicate construction logic by calling the real service methods (covered in Task 2).

So generic_test.go focuses on: TestListEntities (comprehensive, 8 cases) + TestCastPredicates (verify type conversion works).

**TestCastPredicates:**
- Create `[]func(*sql.Selector)` with 3 functions
- Cast to a typed predicate type
- Verify length and each function identity

All tests must use `t.Parallel()`.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -run 'TestListEntities|TestCastPredicates' ./internal/grpcserver/ -v</automated>
  </verify>
  <acceptance_criteria>
    - File internal/grpcserver/generic_test.go exists
    - grep 'TestListEntities' internal/grpcserver/generic_test.go returns at least 1 match
    - grep 'TestCastPredicates' internal/grpcserver/generic_test.go returns at least 1 match
    - grep 't.Parallel' internal/grpcserver/generic_test.go returns at least 2 matches
    - go test -race -run 'TestListEntities|TestCastPredicates' ./internal/grpcserver/ passes
    - Test cases cover: empty result, pagination, invalid token, filter error, query error, page size normalization
  </acceptance_criteria>
  <done>Generic helper tests pass covering ListEntities pagination logic, error handling, and castPredicates type conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for missing entity types and new filter fields</name>
  <files>internal/grpcserver/grpcserver_test.go</files>
  <read_first>
    - internal/grpcserver/grpcserver_test.go (existing test patterns -- TestGetNetwork, TestListNetworks, etc.)
    - internal/grpcserver/generic.go (understand what the service methods now delegate to)
    - internal/grpcserver/campus.go (refactored handler -- see applyFilters function for filter fields)
    - internal/grpcserver/carrier.go (refactored handler)
    - internal/grpcserver/internetexchange.go (refactored handler)
    - internal/grpcserver/ixfacility.go (refactored handler)
    - internal/grpcserver/ixlan.go (refactored handler)
    - internal/grpcserver/networkfacility.go (refactored handler)
    - internal/grpcserver/network.go (refactored handler -- see new filter fields for parity testing)
  </read_first>
  <action>
Expand grpcserver_test.go with tests for the 6 missing entity types AND add filter parity tests for existing entity types that now have new filter fields.

**Add tests for 6 missing entity types** (follow the exact pattern of existing tests like TestGetNetwork, TestListNetworks):

For each missing entity type, add Get + List tests:

1. **Campus:** TestGetCampus, TestListCampuses
   - Create: `client.Campus.Create().SetID(1).SetName("Test Campus").SetCountry("US").SetCity("Ashburn").SetCreated(now).SetUpdated(now).SetStatus("ok").SaveX(ctx)`
   - Get: verify returned campus matches
   - List: verify pagination, filter by name, filter by country

2. **Carrier:** TestGetCarrier, TestListCarriers
   - Create: `client.Carrier.Create().SetID(1).SetName("Test Carrier").SetCreated(now).SetUpdated(now).SetStatus("ok").SaveX(ctx)`
   - Get: verify returned carrier matches
   - List: verify pagination, filter by name, filter by status

3. **InternetExchange:** TestGetInternetExchange, TestListInternetExchanges
   - Create: `client.InternetExchange.Create().SetID(1).SetName("Test IX").SetCountry("US").SetCity("Ashburn").SetCreated(now).SetUpdated(now).SetStatus("ok").SaveX(ctx)`
   - Get: verify returned IX matches
   - List: verify pagination, filter by name, filter by country

4. **IxFacility:** TestGetIxFacility, TestListIxFacilities
   - Create: requires an IX and a Facility first (foreign key). Create IX id=1, Facility id=1, then IxFacility with IxID=1, FacID=1
   - Get: verify returned ixfac matches
   - List: verify filter by ix_id, filter by fac_id

5. **IxLan:** TestGetIxLan, TestListIxLans
   - Create: requires an IX first. `client.IxLan.Create().SetID(1).SetIxID(1).SetName("Test IxLan").SetCreated(now).SetUpdated(now).SetStatus("ok").SaveX(ctx)`
   - Get: verify returned ixlan matches
   - List: verify filter by ix_id, filter by name

6. **NetworkFacility:** TestGetNetworkFacility, TestListNetworkFacilities
   - Create: requires Network and Facility. `client.NetworkFacility.Create().SetID(1).SetNetID(1).SetFacID(1).SetName("Test NetFac").SetCountry("US").SetCity("Ashburn").SetCreated(now).SetUpdated(now).SetStatus("ok").SaveX(ctx)`
   - Get: verify returned netfac matches
   - List: verify filter by net_id, filter by fac_id

**Add filter parity tests for new fields on existing entities:**

7. **TestListNetworksFilters** -- Test new filter fields added for parity:
   - Create 3 networks with different info_type values ("Content", "NSP", "Enterprise")
   - Filter by info_type="Content" -> verify only 1 result returned
   - Filter by info_unicast=true -> verify correct subset
   - Filter by asn (existing) + info_type (new) combined -> verify AND logic

8. **TestListOrganizationsFilters** -- Test new filter fields:
   - Create 2 orgs with different aka values
   - Filter by aka -> verify correct result

9. **TestStreamNetworksWithFilters** -- Test streaming with new filter fields:
   - Use httptest.NewServer + ConnectRPC client pattern (check how existing stream tests work, if any)
   - If no stream test pattern exists: create minimal ConnectRPC test server, call StreamNetworks with info_type filter, verify only matching records streamed

All tests MUST:
- Use `t.Parallel()` per T-3
- Use `testutil.SetupClient(t)` for database setup
- Use table-driven patterns where multiple cases exist per T-1
- Use `proto.Int64()`, `proto.String()`, `proto.Bool()` for optional field construction (matching existing test patterns)

After adding all tests, verify coverage:
```bash
TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/grpcserver/...
```
Target: 60%+ coverage.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/grpcserver/...</automated>
  </verify>
  <acceptance_criteria>
    - grep 'TestGetCampus' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestGetCarrier' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestGetInternetExchange' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestGetIxFacility' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestGetIxLan' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestGetNetworkFacility' internal/grpcserver/grpcserver_test.go returns at least 1 match
    - grep 'TestListNetworksFilters' internal/grpcserver/grpcserver_test.go returns at least 1 match (new parity filter test)
    - grep 'info_type\|InfoType' internal/grpcserver/grpcserver_test.go returns at least 1 match (parity field exercised)
    - go test -race -cover ./internal/grpcserver/... shows coverage >= 60%
    - go test -race ./internal/grpcserver/... passes all tests
  </acceptance_criteria>
  <done>All 13 entity types have Get and List tests. New filter parity fields are exercised. grpcserver package coverage reaches 60%+. All tests pass with -race.</done>
</task>

</tasks>

<verification>
- `TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/grpcserver/...` passes with 60%+ coverage
- `TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/middleware/...` passes with 60%+ coverage (from Plan 01)
- All 13 entity types have tests
- Generic helpers have direct tests
</verification>

<success_criteria>
1. grpcserver package coverage >= 60% (was 22.2%)
2. All 13 entity types covered by at least Get + List tests
3. Generic ListEntities helper tested directly with mock callbacks
4. New filter parity fields exercised in at least one integration test
5. All tests pass with -race flag
</success_criteria>

<output>
After completion, create `.planning/phases/33-grpc-dedup-filter-parity/33-03-SUMMARY.md`
</output>
