---
phase: 62
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified: []
autonomous: false
requirements: [SYNC-01]
must_haves:
  truths:
    - "Production Fly.io app `peeringdb-plus` has PDBPLUS_PEERINGDB_API_KEY set as a fly secret"
    - "fly logs --app peeringdb-plus shows the phase-61 classification line reporting auth=authenticated"
    - "Anonymous smoke test against the deployed app returns a Public-only POC list"
    - "/about on the deployed app renders auth=authenticated, public_tier=public"
  artifacts: []
  key_links:
    - from: "fly secret PDBPLUS_PEERINGDB_API_KEY"
      to: "peeringdb-plus sync worker on next machine start"
      via: "fly rolling deploy triggered by fly secrets set"
      pattern: "auth=authenticated"
---

<objective>
Apply the v1.14 production rollout: operator sets the PeeringDB API key as a Fly secret on app `peeringdb-plus`, then verifies via `fly logs`, `fly secrets list`, and two curl smoke tests that authenticated sync is in effect and the anonymous surface still returns `Public`-only data. Zero code, zero doc edits; this plan is four operator checkpoints. Closes SYNC-01 and success criterion #1 of Phase 62.

Purpose: SYNC-01 — Fly secret is the one operator action that flips production from anonymous sync to authenticated sync. Plan 01's docs describe this workflow; plan 02 executes it.

Output: phase summary capturing operator-observed outputs (fly logs excerpt, fly secrets list excerpt, curl output) so the rollout is reproducible the next time the fleet is rebuilt (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-api-key-default-docs/62-CONTEXT.md
@docs/DEPLOYMENT.md

<interfaces>
<!-- Operator-facing observability signals — all added in phase 61. -->
<!-- Executor should expect these exact field names when inspecting fly logs. -->

Startup classification log (phase 61 plan 01):
- Log line emitted once at startup with key `sync.classification`.
- Fields: `auth=authenticated` (when PDBPLUS_PEERINGDB_API_KEY is set), `auth=anonymous` (when empty).

Privacy override WARN (phase 61 plan 01):
- Log line `privacy.override.active` fires ONLY when `PDBPLUS_PUBLIC_TIER=users`.
- Production does NOT set this, so the WARN line should be absent in phase 62 smoke tests.

/about endpoint (phase 61 plan 02):
- Both `/about` (HTML) and `/ui/about` (terminal) render a "Privacy & Sync" section.
- Expected values after rollout: `auth=authenticated`, `public_tier=public`.

OTel attribute (phase 61 plan 03):
- Read-path spans carry `pdbplus.privacy.tier=public`.

Deployed app URL:
- https://peeringdb-plus.fly.dev
</interfaces>

<conventions>
- All `fly` commands run from the operator's workstation with `FLY_API_TOKEN` already authenticated; the plan does not handle auth setup.
- This plan is `autonomous: false`. Every task is `checkpoint:human-action` because only the operator has the PeeringDB API key and the Fly credentials. Do NOT attempt to automate `fly secrets set` — the key is not available to Claude.
- Record operator-observed output verbatim in the phase summary (D-13). Mask the key itself; the logged digest from `fly secrets list` is expected output and safe to paste.
- Because this plan is entirely operator-driven, `<verify>` blocks describe the expected operator-observed output rather than an automated command. No `<automated>` commands are listed — the verification is the operator pasting the relevant output.
</conventions>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set PDBPLUS_PEERINGDB_API_KEY as a Fly secret</name>
  <files>(operator-driven; no files in repo)</files>
  <action>
  Operator action — Claude cannot execute this because the key is not in the repo and should not be.

  1. Obtain a PeeringDB API key: sign in at <https://www.peeringdb.com/profile>, go to the "API Keys" tab, and generate a key if you do not already have one. The key is the long token shown once at creation.
  2. From the operator workstation (with `fly auth login` already in effect), run:

     ```bash
     fly secrets set PDBPLUS_PEERINGDB_API_KEY=<paste-key> --app peeringdb-plus
     ```

  3. Fly responds with a staging message and kicks off a rolling deploy. Wait until it reports "Release complete" (typically 30-90s on the tuned production topology described in `fly.toml`).

  Paste the operator's full `fly secrets set` output (release version, per-machine progress lines) into the phase summary. The command output does not include the secret value; it is safe to paste verbatim.
  </action>
  <verify>
  Operator-observed: `fly secrets set` exits 0 with output similar to `Release v<N> created` followed by per-machine progress lines. No errors from Fly or from the app during rollout. Rolling deploy completes within ~90s. No `<automated>` command — this is a human-gated checkpoint.

  Resume signal: operator types "done" once the rolling deploy completes, or pastes the error for diagnosis.
  </verify>
  <done>PDBPLUS_PEERINGDB_API_KEY is set as a Fly secret on `peeringdb-plus` and the rolling deploy has finished.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Verify the secret is present via fly secrets list</name>
  <files>(operator-driven; no files in repo)</files>
  <action>
  Confirm Fly is storing the secret. This is the check called out in ROADMAP Phase 62 success criterion #1 ("presence verified via `fly secrets list`") and D-04.

  ```bash
  fly secrets list --app peeringdb-plus
  ```

  Paste the table into the phase summary. Fly displays only the digest of the secret value, not the value itself — this is expected and safe to commit. `PDBPLUS_SYNC_TOKEN` should also still appear in the listing from earlier deployments.
  </action>
  <verify>
  Operator-observed: output is a table whose rows include a `PDBPLUS_PEERINGDB_API_KEY` row. The digest column is non-empty; the value column is masked (that is the expected behaviour — Fly never re-displays a secret's value). No `<automated>` command — human-gated checkpoint.

  Resume signal: operator pastes the table (digests are safe to paste), or "missing" if the row is absent (in which case Task 1 did not land).
  </verify>
  <done>Operator confirms `PDBPLUS_PEERINGDB_API_KEY` appears in `fly secrets list` output for the `peeringdb-plus` app.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Confirm the classification log reports auth=authenticated</name>
  <files>(operator-driven; no files in repo)</files>
  <action>
  Verify the running app picked up the new secret. Phase 61 plan 01 added a single startup classification log line (`sync.classification`); with the key set, it must now report `auth=authenticated`. This is D-03.

  ```bash
  # Tail logs until at least one machine has printed the classification line.
  fly logs --app peeringdb-plus | grep -m1 sync.classification
  ```

  The grep exits as soon as one machine's startup line appears. If the rolling deploy is still in flight, some machines may not have cycled yet — one line from any cycled machine is sufficient.

  Paste the matched log line into the phase summary.
  </action>
  <verify>
  Operator-observed: one structured slog line containing `sync.classification` and `auth=authenticated`. Example shape:

  ```
  {"time":"...","level":"INFO","msg":"sync.classification","auth":"authenticated","public_tier":"public"}
  ```

  Additionally: no `privacy.override.active` WARN should appear (production uses the default `PDBPLUS_PUBLIC_TIER=public`). No `<automated>` command — human-gated checkpoint.

  Resume signal: operator pastes the matched log line, or "not seen" if the grep returned nothing after ~60s (in which case diagnose via `fly machines list` and `fly logs` unfiltered).
  </verify>
  <done>Operator confirms at least one machine's startup log reports `auth=authenticated` and `public_tier=public`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Smoke-test /api/poc and /about on the deployed app</name>
  <files>(operator-driven; no files in repo)</files>
  <action>
  End-to-end check that the anonymous read surface still returns `Public`-only POCs (privacy policy correctly filtering `Users`-tier rows that now exist in the DB) and that `/about` renders the new Privacy & Sync section with the expected values. This is D-12.

  ```bash
  # 1. Anonymous POC list count — should be the Public-only count.
  curl -sS https://peeringdb-plus.fly.dev/api/poc | jq '.data | length'

  # 2. /about — browser-style UA to get HTML; grep the two fields phase 61 plan 02 added.
  curl -sS -H 'User-Agent: Mozilla/5.0' https://peeringdb-plus.fly.dev/about \
    | grep -E 'auth|public_tier|Privacy'
  ```

  Note: the exact POC count fluctuates with upstream and with which `Users`-tier rows were admitted on the last sync; what matters is that the endpoint returns a 200 with a positive count. The phase-60 integration tests already prove no `Users`-tier contact can leak through in code — this is a live production smoke test.

  Paste both the integer count and the `/about` grep output into the phase summary.
  </action>
  <verify>
  Operator-observed:

  1. `curl ... /api/poc | jq '.data | length'` returns a positive integer; HTTP 200 is implied by `jq` not erroring on the JSON.
  2. `curl ... /about` returns HTML containing the phrases "Privacy" (section heading), "auth=authenticated" (or the rendered equivalent — phase 61 plan 02 owns the exact wording), and "public_tier=public".

  No `<automated>` command — human-gated checkpoint.

  Resume signal: operator pastes POC count and `/about` grep output, or "fail" with the error for diagnosis.
  </verify>
  <done>Both curl smoke tests succeed: `/api/poc` returns a positive POC count, `/about` renders the Privacy & Sync fields with `auth=authenticated` and `public_tier=public`.</done>
</task>

</tasks>

<verification>
Evidence requirements (all come from operator-observed output; no automation):

- SYNC-01 satisfied: `fly secrets list --app peeringdb-plus` output (Task 2) shows `PDBPLUS_PEERINGDB_API_KEY`.
- ROADMAP Phase 62 success criterion #1: ditto Task 2.
- D-03 verification: `fly logs` classification line (Task 3) contains `auth=authenticated`.
- D-04 verification: `fly secrets list` digest visible (Task 2).
- D-12 smoke tests: Task 4 produces non-empty `/api/poc` list and `/about` shows the Privacy & Sync section.

This plan has no `<automated>` commands because the entire dependency chain is operator action against live Fly.io infrastructure using credentials Claude does not have. The plan is `autonomous: false` for that reason.
</verification>

<success_criteria>
- `fly secrets list --app peeringdb-plus` contains a `PDBPLUS_PEERINGDB_API_KEY` row (SYNC-01).
- Deployed app logs `sync.classification auth=authenticated` on startup (D-03).
- Deployed app's `/about` renders `auth=authenticated, public_tier=public` (D-12).
- `/api/poc` returns a 200 with a positive POC count against the deployed app (D-12).
- All four observations captured in `62-02-SUMMARY.md` (D-13) so the rollout is reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/62-api-key-default-docs/62-02-SUMMARY.md` containing:
- Timestamp of the rollout and Fly release version from Task 1.
- Pasted `fly secrets list` excerpt from Task 2 (digest is safe; key value must stay out).
- Pasted `sync.classification` log line from Task 3.
- Pasted POC count integer and `/about` grep output from Task 4.
- Any follow-up items (e.g. replicas that needed manual cycling) — none expected on the current topology.
</output>
