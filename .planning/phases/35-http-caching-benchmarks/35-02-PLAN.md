---
phase: 35-http-caching-benchmarks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/search_bench_test.go
  - internal/pdbcompat/projection_bench_test.go
  - internal/grpcserver/list_bench_test.go
  - internal/sync/upsert_bench_test.go
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "Search benchmark runs successfully and produces stable ns/op output"
    - "Field projection benchmark runs successfully with reflect-based projection"
    - "Generic ListEntities benchmark runs successfully with mock callbacks"
    - "Sync upsert benchmark runs successfully with in-memory SQLite"
    - "All benchmarks use b.Loop() pattern (Go 1.26)"
    - "Benchmark results are comparable via benchstat (no external I/O, deterministic)"
  artifacts:
    - path: "internal/web/search_bench_test.go"
      provides: "Search benchmark across 6 entity types"
      contains: "BenchmarkSearch"
    - path: "internal/pdbcompat/projection_bench_test.go"
      provides: "Field projection benchmark with pre-built struct data"
      contains: "BenchmarkApplyFieldProjection"
    - path: "internal/grpcserver/list_bench_test.go"
      provides: "Generic ListEntities benchmark with mock callbacks"
      contains: "BenchmarkListEntities"
    - path: "internal/sync/upsert_bench_test.go"
      provides: "Bulk upsert benchmark with in-memory SQLite"
      contains: "BenchmarkUpsert"
  key_links:
    - from: "internal/web/search_bench_test.go"
      to: "internal/web/search.go"
      via: "NewSearchService + Search method"
      pattern: "NewSearchService|svc\\.Search"
    - from: "internal/pdbcompat/projection_bench_test.go"
      to: "internal/pdbcompat/search.go"
      via: "applyFieldProjection function"
      pattern: "applyFieldProjection"
    - from: "internal/grpcserver/list_bench_test.go"
      to: "internal/grpcserver/generic.go"
      via: "ListEntities generic function"
      pattern: "ListEntities"
    - from: "internal/sync/upsert_bench_test.go"
      to: "internal/sync/upsert.go"
      via: "upsertOrganizations function"
      pattern: "upsertOrganizations"
---

<objective>
Create a benchmark suite covering four hot paths: search queries, field projection, gRPC list entity conversion, and sync upsert operations. Establishes performance baselines on the optimized code.

Purpose: Enable performance regression detection and data-driven optimization (PERF-04).
Output: Four `*_bench_test.go` files in their respective packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-http-caching-benchmarks/35-RESEARCH.md

@internal/web/search.go
@internal/web/search_test.go
@internal/pdbcompat/search.go
@internal/pdbcompat/search_test.go
@internal/grpcserver/generic.go
@internal/grpcserver/generic_test.go
@internal/sync/upsert.go
@internal/sync/worker_test.go
@internal/testutil/testutil.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/web/search.go:
```go
type SearchService struct { client *ent.Client }
func NewSearchService(client *ent.Client) *SearchService
func (s *SearchService) Search(ctx context.Context, query string) ([]TypeResult, error)
```

From internal/pdbcompat/search.go:
```go
func applyFieldProjection(data []any, fields []string) []any  // unexported, same package
```

From internal/grpcserver/generic.go:
```go
type ListParams[E any, P any] struct {
    EntityName   string
    PageSize     int32
    PageToken    string
    ApplyFilters func() ([]func(*sql.Selector), error)
    Query        func(ctx context.Context, predicates []func(*sql.Selector), limit, offset int) ([]*E, error)
    Convert      func(*E) *P
}
func ListEntities[E any, P any](ctx context.Context, params ListParams[E, P]) ([]*P, string, error)
```

From internal/sync/upsert.go:
```go
func upsertOrganizations(ctx context.Context, tx *ent.Tx, orgs []peeringdb.Organization) ([]int, error)  // unexported
const batchSize = 500
```

From internal/testutil/testutil.go:
```go
func SetupClient(t *testing.T) *ent.Client
func SetupClientWithDB(t *testing.T) (*ent.Client, *sql.DB)
```

From internal/sync/worker_test.go (TestMain pattern for OTel metrics init):
```go
func TestMain(m *testing.M) {
    mp := sdkmetric.NewMeterProvider()
    otel.SetMeterProvider(mp)
    if err := pdbotel.InitMetrics(); err != nil { panic(...) }
    os.Exit(m.Run())
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search and field projection benchmarks</name>
  <files>internal/web/search_bench_test.go, internal/pdbcompat/projection_bench_test.go</files>
  <read_first>internal/web/search.go, internal/web/search_test.go, internal/pdbcompat/search.go, internal/pdbcompat/search_test.go, internal/testutil/testutil.go</read_first>
  <action>
    Create `internal/web/search_bench_test.go`:

    1. Package `web` (same as search.go -- internal, can access unexported).
    2. Create `seedBenchSearchData(b *testing.B) *SearchService` helper that:
       - Calls `testutil.SetupClient(b)` for in-memory SQLite (note: testutil.SetupClient accepts `*testing.T` -- for benchmarks, use the approach from search_test.go but adapted for `*testing.B`; since `SetupClient` takes `*testing.T`, create a local helper that opens enttest directly with `b` as the testing.TB, or use `b` since `*testing.B` embeds `testing.TB` which satisfies the `*testing.T` parameter -- actually `SetupClient` takes `*testing.T` not an interface, so create a local setup using `enttest.Open(b, dialect.SQLite, dsn)` with the same pattern as testutil but accepting `*testing.B`)
       - Seeds 120+ entities per type (networks, IXPs, facilities, orgs, campuses, carriers) with realistic names containing searchable terms like "Cloud", "Equinix", "DE-CIX"
       - Returns a `*SearchService` ready for benchmarking
    3. Create `BenchmarkSearch` with sub-benchmarks:
       - `b.Run("broad_match", ...)` -- query "Cloud" (matches networks, orgs)
       - `b.Run("narrow_match", ...)` -- query "DE-CIX" (matches only IXPs)
       - `b.Run("no_match", ...)` -- query "zzzznonexistent"
    4. Each sub-benchmark: seed data outside loop, call `b.ResetTimer()`, use `for b.Loop()` pattern, call `svc.Search(ctx, query)`, fatal on error.

    Create `internal/pdbcompat/projection_bench_test.go`:

    1. Package `pdbcompat` (same package -- can access unexported `applyFieldProjection`).
    2. Create `BenchmarkApplyFieldProjection` with sub-benchmarks:
       - `b.Run("3_fields_100_items", ...)` -- project 3 fields from 100 struct items
       - `b.Run("10_fields_100_items", ...)` -- project 10 fields from 100 struct items
       - `b.Run("no_projection", ...)` -- empty fields (passthrough, baseline)
    3. Pre-build a slice of 100 `testEntity` structs (same type as in search_test.go: `testEntity{ID, Name, Value, Internal, Untagged, OrgSet}`) cast to `[]any`.
    4. Use `for b.Loop()` pattern. Reset timer after setup.
    5. Use `fields := []string{"name", "value", "org_set"}` for 3-field test.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -bench=BenchmarkSearch -benchtime=100ms -count=1 ./internal/web/ && TMPDIR=/tmp/claude-1000 go test -bench=BenchmarkApplyFieldProjection -benchtime=100ms -count=1 ./internal/pdbcompat/</automated>
  </verify>
  <acceptance_criteria>
    - `grep "func BenchmarkSearch" internal/web/search_bench_test.go` finds the benchmark function
    - `grep "b.Loop()" internal/web/search_bench_test.go` confirms Go 1.26 pattern
    - `grep "b.ResetTimer()" internal/web/search_bench_test.go` confirms timer reset after setup
    - `grep "func BenchmarkApplyFieldProjection" internal/pdbcompat/projection_bench_test.go` finds the benchmark function
    - `grep "b.Loop()" internal/pdbcompat/projection_bench_test.go` confirms Go 1.26 pattern
    - `go test -bench=BenchmarkSearch -benchtime=100ms ./internal/web/` produces ns/op output
    - `go test -bench=BenchmarkApplyFieldProjection -benchtime=100ms ./internal/pdbcompat/` produces ns/op output
  </acceptance_criteria>
  <done>Search benchmark exercises 3 query patterns across 120+ seeded entities. Field projection benchmark covers 3 projection sizes. Both use b.Loop() and produce stable ns/op.</done>
</task>

<task type="auto">
  <name>Task 2: gRPC list and sync upsert benchmarks</name>
  <files>internal/grpcserver/list_bench_test.go, internal/sync/upsert_bench_test.go</files>
  <read_first>internal/grpcserver/generic.go, internal/grpcserver/generic_test.go, internal/sync/upsert.go, internal/sync/worker_test.go, internal/testutil/testutil.go</read_first>
  <action>
    Create `internal/grpcserver/list_bench_test.go`:

    1. Package `grpcserver` (same package -- can access unexported helpers).
    2. Follow the mock pattern from `generic_test.go`: define local `mockEntity` and `mockProto` structs, `convert` function, `makeMockData` helper.
    3. Create `BenchmarkListEntities` with sub-benchmarks:
       - `b.Run("100_items_page50", ...)` -- 100 mock entities, page size 50
       - `b.Run("1000_items_page100", ...)` -- 1000 mock entities, page size 100
       - `b.Run("empty_result", ...)` -- returns nil (baseline)
    4. Each sub-benchmark: pre-build mock data, create `ListParams` with mock `Query` callback that slices the pre-built data, use `for b.Loop()` pattern, call `ListEntities(ctx, params)`, fatal on error.
    5. The mock Query function should simulate real behavior: respect limit and offset parameters by slicing the pre-built data (same pattern as generic_test.go).
    6. No database needed -- purely in-memory mock callbacks.

    Create `internal/sync/upsert_bench_test.go`:

    1. Package `sync` (same package -- can access unexported `upsertOrganizations`).
    2. IMPORTANT: The sync package has a `TestMain` in `worker_test.go` that initializes OTel metrics. Since `upsert_bench_test.go` is in the same package, `TestMain` already runs. Do NOT add another `TestMain`.
    3. Create `generateBenchOrgs(n int) []peeringdb.Organization` helper that generates `n` organizations with realistic field values (ID, Name, Website, Country, State, City, Address1, Zipcode, Created, Updated).
    4. Create `BenchmarkUpsertOrganizations` with sub-benchmarks:
       - `b.Run("100_orgs", ...)` -- upsert 100 organizations
       - `b.Run("500_orgs", ...)` -- upsert 500 organizations (at batch limit)
    5. Each sub-benchmark:
       - Use `testutil.SetupClient(b)` -- note this takes `*testing.T` so create a local enttest.Open setup that accepts `*testing.B` (same pattern as the search bench). Use `enttest.Open(b, dialect.SQLite, dsn)` with in-memory DSN per testutil pattern.
       - Pre-generate organizations outside the loop
       - `b.ResetTimer()`
       - In `for b.Loop()`: start transaction, call `upsertOrganizations(ctx, tx, orgs)`, commit transaction
       - Fatal on any error
    6. Import `peeringdb` package for Organization type. Import `enttest` and `dialect` for direct client setup.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -bench=BenchmarkListEntities -benchtime=100ms -count=1 ./internal/grpcserver/ && TMPDIR=/tmp/claude-1000 go test -bench=BenchmarkUpsert -benchtime=100ms -count=1 ./internal/sync/</automated>
  </verify>
  <acceptance_criteria>
    - `grep "func BenchmarkListEntities" internal/grpcserver/list_bench_test.go` finds the benchmark function
    - `grep "b.Loop()" internal/grpcserver/list_bench_test.go` confirms Go 1.26 pattern
    - `grep "func BenchmarkUpsertOrganizations" internal/sync/upsert_bench_test.go` finds the benchmark function
    - `grep "b.Loop()" internal/sync/upsert_bench_test.go` confirms Go 1.26 pattern
    - `grep "b.ResetTimer()" internal/sync/upsert_bench_test.go` confirms timer reset
    - `go test -bench=BenchmarkListEntities -benchtime=100ms ./internal/grpcserver/` produces ns/op output
    - `go test -bench=BenchmarkUpsert -benchtime=100ms ./internal/sync/` produces ns/op output
    - No `func TestMain` in upsert_bench_test.go (it exists in worker_test.go already)
  </acceptance_criteria>
  <done>ListEntities benchmark exercises pagination with 100 and 1000 mock items. Upsert benchmark exercises bulk organization insertion at 100 and 500 item scales. Both use b.Loop() and in-memory data only.</done>
</task>

</tasks>

<verification>
- `go test -bench=. -benchtime=100ms -count=1 ./internal/web/` -- search benchmarks run
- `go test -bench=. -benchtime=100ms -count=1 ./internal/pdbcompat/` -- projection benchmarks run
- `go test -bench=. -benchtime=100ms -count=1 ./internal/grpcserver/` -- list benchmarks run
- `go test -bench=. -benchtime=100ms -count=1 ./internal/sync/` -- upsert benchmarks run
- `go test -race ./internal/web/... ./internal/pdbcompat/... ./internal/grpcserver/... ./internal/sync/...` -- all tests still pass
- All benchmark output includes `ns/op` metric (stable, no external I/O)
</verification>

<success_criteria>
- Four benchmark files exist in their respective packages
- All benchmarks use `b.Loop()` (Go 1.26 pattern, not `b.N`)
- All benchmarks use `b.ResetTimer()` after data seeding
- Search benchmark seeds 120+ entities and tests 3 query patterns
- Projection benchmark tests 3 field counts
- List benchmark tests 2 data sizes with mock callbacks
- Upsert benchmark tests 2 batch sizes with in-memory SQLite
- `go test -bench=. -benchtime=1s -count=6 ./internal/...` can produce benchstat-compatible output
</success_criteria>

<output>
After completion, create `.planning/phases/35-http-caching-benchmarks/35-02-SUMMARY.md`
</output>
