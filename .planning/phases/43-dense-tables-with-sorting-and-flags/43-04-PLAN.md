---
phase: 43-dense-tables-with-sorting-and-flags
plan: 04
type: execute
wave: 1
depends_on: ["43-03"]
files_modified:
  - internal/web/detail_test.go
autonomous: true
gap_closure: true
requirements: [DENS-01, DENS-02, DENS-03, SORT-01, SORT-02, SORT-03, FLAG-01]

must_haves:
  truths:
    - "All 16 fragment test cases assert table HTML structure, not just entity names"
    - "Multi-column sortable fragments assert data-sortable presence"
    - "Single-column fragments assert data-sortable absence"
    - "Fragments with country flags assert fi fi- class presence"
    - "All 9 IX/net/fac fragment tests reject old card layout pattern"
  artifacts:
    - path: "internal/web/detail_test.go"
      provides: "Table structure assertions for IX/net/fac fragments"
      contains: "data-sortable"
  key_links:
    - from: "internal/web/detail_test.go"
      to: "internal/web/templates/detail_ix.templ"
      via: "Fragment test assertions match template output"
      pattern: "<table.*data-sortable"
    - from: "internal/web/detail_test.go"
      to: "internal/web/templates/detail_net.templ"
      via: "Fragment test assertions match template output"
      pattern: "<table.*data-sortable"
    - from: "internal/web/detail_test.go"
      to: "internal/web/templates/detail_fac.templ"
      via: "Fragment test assertions match template output"
      pattern: "<table.*data-sortable"
---

<objective>
Close the single verification gap from Phase 43: add table structure assertions to the 9 IX/net/fac fragment test cases in detail_test.go.

Purpose: Plan 02 converted 9 templates (IX: 3, net: 3, fac: 3) to table layout but had no test update task. Plan 03 added test assertions only for its own 7 templates (org/campus/carrier) due to parallel worktree execution. This leaves 9 fragment tests asserting only entity names without verifying table HTML structure -- a future regression to card layout would go undetected.

Output: Updated detail_test.go with table structure assertions on all 16 fragment test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-dense-tables-with-sorting-and-flags/43-VERIFICATION.md
@.planning/phases/43-dense-tables-with-sorting-and-flags/43-03-SUMMARY.md

<interfaces>
<!-- Pattern already established by Plan 03 on lines 412-416 of detail_test.go -->
<!-- Executor MUST match this exact pattern for the 9 IX/net/fac test cases -->

From internal/web/detail_test.go (lines 412-416, the reference pattern):
```go
// Multi-column sortable with flags (e.g., org facilities):
{"org facilities", "/ui/fragment/org/1/facilities", http.StatusOK, []string{"Equinix", "<table", "data-sortable", "fi fi-", "data-sort-value"}, []string{"<!doctype", "px-4 py-3 hover:bg-neutral-800/50"}},

// Multi-column sortable without flags (e.g., org networks):
{"org networks", "/ui/fragment/org/1/networks", http.StatusOK, []string{"Cloudflare", "<table", "data-sortable", "data-sort-value"}, []string{"<!doctype", "px-4 py-3 hover:bg-neutral-800/50"}},

// Single-column not sortable (e.g., org ixps, carrier facilities):
{"org ixps", "/ui/fragment/org/1/ixps", http.StatusOK, []string{"DE-CIX", "<table"}, []string{"<!doctype", "data-sortable", "px-4 py-3 hover:bg-neutral-800/50"}},
```

<!-- Template attribute inventory (verified by grep): -->
<!-- IX detail_ix.templ: -->
<!--   IXParticipantsList: sortable, no flags, has data-sort-value -->
<!--   IXFacilitiesList: sortable, HAS CountryFlag, has data-sort-value -->
<!--   IXPrefixesList: sortable, no flags, has data-sort-value -->
<!-- Net detail_net.templ: -->
<!--   NetworkIXLansList: sortable, no flags, has data-sort-value -->
<!--   NetworkFacilitiesList: sortable, HAS CountryFlag, has data-sort-value -->
<!--   NetworkContactsList: sortable, no flags, has data-sort-value -->
<!-- Fac detail_fac.templ: -->
<!--   FacNetworksList: sortable, HAS CountryFlag, has data-sort-value -->
<!--   FacIXPsList: NOT sortable (single-column), no flags, no data-sort-value -->
<!--   FacCarriersList: NOT sortable (single-column), no flags, no data-sort-value -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add table structure assertions to IX/net/fac fragment tests</name>
  <files>internal/web/detail_test.go</files>
  <read_first>
    - internal/web/detail_test.go (lines 392-443, TestFragments_AllTypes function)
    - internal/web/templates/detail_ix.templ (confirm sortable/flag attributes)
    - internal/web/templates/detail_net.templ (confirm sortable/flag attributes)
    - internal/web/templates/detail_fac.templ (confirm sortable/flag attributes)
  </read_first>
  <action>
Update the 9 IX/net/fac test cases in TestFragments_AllTypes (lines 403-411) to add table structure assertions matching the pattern established by Plan 03 on lines 412-416.

For each test case, update wantBody and noBody arrays as follows:

**7 multi-column sortable fragments -- add to wantBody: `"<table"`, `"data-sortable"`, `"data-sort-value"`; add to noBody: `"px-4 py-3 hover:bg-neutral-800/50"`:**
1. `"net ixlans"` -- wantBody: add `"<table"`, `"data-sortable"`, `"data-sort-value"` (no flags -- IX presences have no country column)
2. `"net facilities"` -- wantBody: add `"<table"`, `"data-sortable"`, `"fi fi-"`, `"data-sort-value"` (HAS CountryFlag)
3. `"net contacts"` -- wantBody: add `"<table"`, `"data-sortable"`, `"data-sort-value"` (no flags -- contacts have no country)
4. `"ix participants"` -- wantBody: add `"<table"`, `"data-sortable"`, `"data-sort-value"` (no flags -- participants have no country column)
5. `"ix facilities"` -- wantBody: add `"<table"`, `"data-sortable"`, `"fi fi-"`, `"data-sort-value"` (HAS CountryFlag)
6. `"ix prefixes"` -- wantBody: add `"<table"`, `"data-sortable"`, `"data-sort-value"` (no flags -- prefixes have no country)
7. `"fac networks"` -- wantBody: add `"<table"`, `"data-sortable"`, `"fi fi-"`, `"data-sort-value"` (HAS CountryFlag)

**2 single-column non-sortable fragments -- add to wantBody: `"<table"`; add to noBody: `"data-sortable"`, `"px-4 py-3 hover:bg-neutral-800/50"`:**
8. `"fac ixps"` -- wantBody: add `"<table"` ; noBody: add `"data-sortable"`, `"px-4 py-3 hover:bg-neutral-800/50"`
9. `"fac carriers"` -- wantBody: add `"<table"` ; noBody: add `"data-sortable"`, `"px-4 py-3 hover:bg-neutral-800/50"`

Keep all existing assertions (entity names, `"<!doctype"` in noBody). Only ADD new assertions to existing arrays.

Do NOT modify lines 412-416 (org/campus/carrier assertions from Plan 03) or any other test functions.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test ./internal/web/ -run TestFragments_AllTypes -race -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - All 14 subtests in TestFragments_AllTypes pass
    - All 9 IX/net/fac test cases assert `"<table"` in wantBody
    - 7 multi-column sortable test cases assert `"data-sortable"` and `"data-sort-value"` in wantBody
    - 3 country-flag test cases (net facilities, ix facilities, fac networks) assert `"fi fi-"` in wantBody
    - 2 single-column test cases (fac ixps, fac carriers) assert `"data-sortable"` in noBody
    - All 9 test cases assert `"px-4 py-3 hover:bg-neutral-800/50"` in noBody (old card pattern absent)
    - Full web test suite passes: `go test ./internal/web/... -race -count=1`
  </acceptance_criteria>
  <done>All 16 fragment test cases in TestFragments_AllTypes have table structure assertions. No fragment test would pass if its template reverted to the old card layout.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/web/ -run TestFragments_AllTypes -race -count=1 -v` -- all 14 subtests pass
2. `go test ./internal/web/... -race -count=1` -- full web test suite passes
3. Grep detail_test.go lines 403-416: every test case includes `"<table"` in wantBody and `"px-4 py-3 hover:bg-neutral-800/50"` in noBody
</verification>

<success_criteria>
- All 16 fragment test cases assert table HTML structure (not just entity names)
- Full web test suite passes with -race
- The verification gap from 43-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/43-dense-tables-with-sorting-and-flags/43-04-SUMMARY.md`
</output>
