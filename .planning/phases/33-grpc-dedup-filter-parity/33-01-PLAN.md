---
phase: 33-grpc-dedup-filter-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/peeringdb/v1/services.proto
  - gen/peeringdb/v1/v1.pb.go
  - gen/peeringdb/v1/peeringdbv1connect/v1.connect.go
  - internal/middleware/logging_test.go
  - internal/middleware/recovery_test.go
autonomous: true
requirements: [ARCH-02, QUAL-03]

must_haves:
  truths:
    - "Every filterable field in the pdbcompat Registry has a corresponding optional field on the ConnectRPC List and Stream request messages"
    - "Middleware package test coverage reaches 60%+"
  artifacts:
    - path: "proto/peeringdb/v1/services.proto"
      provides: "Proto request messages with full filter parity"
      contains: "optional string info_type"
    - path: "internal/middleware/logging_test.go"
      provides: "Logging middleware test coverage"
      min_lines: 80
    - path: "internal/middleware/recovery_test.go"
      provides: "Recovery middleware test coverage"
      min_lines: 60
  key_links:
    - from: "proto/peeringdb/v1/services.proto"
      to: "gen/peeringdb/v1/"
      via: "buf generate"
      pattern: "buf generate"
---

<objective>
Add all missing filter fields to ConnectRPC proto request messages for full parity with the PeeringDB compat layer, and add middleware test coverage to reach 60%+.

Purpose: ARCH-02 requires every filterable field from pdbcompat to exist on the corresponding ConnectRPC List/Stream request message. QUAL-03 requires middleware coverage at 60%+. These are independent work streams that can be done in Wave 1 before the generic helper refactor.

Output: Updated services.proto with ~96 new optional fields across 26 request messages, regenerated Go types, and middleware tests for logging.go and recovery.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-grpc-dedup-filter-parity/33-CONTEXT.md
@.planning/phases/33-grpc-dedup-filter-parity/33-RESEARCH.md

@proto/peeringdb/v1/services.proto
@internal/pdbcompat/registry.go
@internal/middleware/logging.go
@internal/middleware/recovery.go
@internal/middleware/cors_test.go

<interfaces>
<!-- Source of truth: pdbcompat Registry defines all filterable fields per entity type -->
<!-- From internal/pdbcompat/registry.go: Registry map[string]TypeConfig with Fields map[string]FieldType -->

<!-- Current proto filter fields per List/Stream message (to be expanded): -->
<!-- Network: asn, name, status, org_id (4 fields; pdbcompat has 28+) -->
<!-- Organization: name, country, city, status (4 fields; pdbcompat has 17+) -->
<!-- Facility: name, country, city, status, org_id (5 fields; pdbcompat has 27+) -->
<!-- See RESEARCH.md Filter Parity Gap Analysis for full per-entity breakdown -->

<!-- Middleware pattern from cors_test.go: uses httptest.NewRecorder + httptest.NewRequest -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter parity fields to services.proto and regenerate</name>
  <files>proto/peeringdb/v1/services.proto, gen/peeringdb/v1/v1.pb.go, gen/peeringdb/v1/peeringdbv1connect/v1.connect.go</files>
  <read_first>
    - proto/peeringdb/v1/services.proto (current proto definitions -- see highest field numbers per message)
    - internal/pdbcompat/registry.go (source of truth for all filterable fields per entity type)
    - .planning/phases/33-grpc-dedup-filter-parity/33-RESEARCH.md (Filter Parity Gap Analysis section)
  </read_first>
  <action>
Add all missing optional filter fields to every List and Stream request message in services.proto so that every non-time, non-float field in the pdbcompat Registry has a corresponding proto field. Field numbering: continue sequentially after the highest existing field number in each message.

Per CONTEXT.md locked decisions:
- Use proper proto types: `optional int64` for IDs and integer fields, `optional string` for string fields, `optional bool` for boolean fields
- Do NOT add latitude/longitude (FieldFloat -- low utility per RESEARCH.md)
- Do NOT add created/updated FieldTime fields (use updated_since on Stream messages instead)
- Do NOT change service definitions (Get/List/Stream RPCs) -- only request message fields

Entity-by-entity additions (field name -> proto type, for BOTH List and Stream request messages):

**Organization** (ListOrganizationsRequest fields 3-6 exist; add after 6; StreamOrganizationsRequest fields 1-6 exist; add after 6):
- `id` -> `optional int64` (List only, Stream uses since_id)
- `aka` -> `optional string`
- `name_long` -> `optional string`
- `website` -> `optional string`
- `notes` -> `optional string`
- `logo` -> `optional string`
- `address1` -> `optional string`
- `address2` -> `optional string`
- `state` -> `optional string`
- `zipcode` -> `optional string`
- `suite` -> `optional string`
- `floor` -> `optional string`

**Network** (ListNetworksRequest fields 3-6 exist; StreamNetworksRequest fields 1-6 exist):
- `id` -> `optional int64` (List only)
- `aka` -> `optional string`
- `name_long` -> `optional string`
- `website` -> `optional string`
- `looking_glass` -> `optional string`
- `route_server` -> `optional string`
- `irr_as_set` -> `optional string`
- `info_type` -> `optional string`
- `info_prefixes4` -> `optional int64`
- `info_prefixes6` -> `optional int64`
- `info_traffic` -> `optional string`
- `info_ratio` -> `optional string`
- `info_scope` -> `optional string`
- `info_unicast` -> `optional bool`
- `info_multicast` -> `optional bool`
- `info_ipv6` -> `optional bool`
- `info_never_via_route_servers` -> `optional bool`
- `notes` -> `optional string`
- `policy_url` -> `optional string`
- `policy_general` -> `optional string`
- `policy_locations` -> `optional string`
- `policy_ratio` -> `optional bool`
- `policy_contracts` -> `optional string`
- `allow_ixp_update` -> `optional bool`
- `status_dashboard` -> `optional string`
- `rir_status` -> `optional string`
- `logo` -> `optional string`

**Facility** (ListFacilitiesRequest fields 3-7 exist; StreamFacilitiesRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `aka` -> `optional string`
- `name_long` -> `optional string`
- `website` -> `optional string`
- `clli` -> `optional string`
- `rencode` -> `optional string`
- `npanxx` -> `optional string`
- `tech_email` -> `optional string`
- `tech_phone` -> `optional string`
- `sales_email` -> `optional string`
- `sales_phone` -> `optional string`
- `property` -> `optional string`
- `diverse_serving_substations` -> `optional bool`
- `notes` -> `optional string`
- `region_continent` -> `optional string`
- `status_dashboard` -> `optional string`
- `logo` -> `optional string`
- `address1` -> `optional string`
- `address2` -> `optional string`
- `state` -> `optional string`
- `zipcode` -> `optional string`
- `suite` -> `optional string`
- `floor` -> `optional string`
- `campus_id` -> `optional int64`
- `org_name` -> `optional string`

**InternetExchange** (ListInternetExchangesRequest fields 3-7 exist; StreamInternetExchangesRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `aka` -> `optional string`
- `name_long` -> `optional string`
- `region_continent` -> `optional string`
- `media` -> `optional string`
- `notes` -> `optional string`
- `proto_unicast` -> `optional bool`
- `proto_multicast` -> `optional bool`
- `proto_ipv6` -> `optional bool`
- `website` -> `optional string`
- `url_stats` -> `optional string`
- `tech_email` -> `optional string`
- `tech_phone` -> `optional string`
- `policy_email` -> `optional string`
- `policy_phone` -> `optional string`
- `sales_email` -> `optional string`
- `sales_phone` -> `optional string`
- `service_level` -> `optional string`
- `terms` -> `optional string`
- `status_dashboard` -> `optional string`
- `logo` -> `optional string`

**Poc** (ListPocsRequest fields 3-6 exist; StreamPocsRequest fields 1-6 exist):
- `id` -> `optional int64` (List only)
- `visible` -> `optional string`
- `phone` -> `optional string`
- `email` -> `optional string`
- `url` -> `optional string`

**IxLan** (ListIxLansRequest fields 3-5 exist; StreamIxLansRequest fields 1-5 exist):
- `id` -> `optional int64` (List only)
- `descr` -> `optional string`
- `mtu` -> `optional int64`
- `dot1q_support` -> `optional bool`
- `rs_asn` -> `optional int64`
- `arp_sponge` -> `optional string`
- `ixf_ixp_member_list_url_visible` -> `optional string`
- `ixf_ixp_import_enabled` -> `optional bool`

**IxPrefix** (ListIxPrefixesRequest fields 3-5 exist; StreamIxPrefixesRequest fields 1-5 exist):
- `id` -> `optional int64` (List only)
- `prefix` -> `optional string`
- `in_dfz` -> `optional bool`
- `notes` -> `optional string`

**NetworkIxLan** (ListNetworkIxLansRequest fields 3-7 exist; StreamNetworkIxLansRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `ix_id` -> `optional int64`
- `speed` -> `optional int64`
- `ipaddr4` -> `optional string`
- `ipaddr6` -> `optional string`
- `is_rs_peer` -> `optional bool`
- `bfd_support` -> `optional bool`
- `operational` -> `optional bool`
- `notes` -> `optional string`
- `net_side_id` -> `optional int64`
- `ix_side_id` -> `optional int64`

**NetworkFacility** (ListNetworkFacilitiesRequest fields 3-7 exist; StreamNetworkFacilitiesRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `name` -> `optional string`
- `local_asn` -> `optional int64`

**IxFacility** (ListIxFacilitiesRequest fields 3-7 exist; StreamIxFacilitiesRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `name` -> `optional string`

**Carrier** (ListCarriersRequest fields 3-5 exist; StreamCarriersRequest fields 1-5 exist):
- `id` -> `optional int64` (List only)
- `aka` -> `optional string`
- `name_long` -> `optional string`
- `website` -> `optional string`
- `notes` -> `optional string`
- `org_name` -> `optional string`
- `logo` -> `optional string`

**CarrierFacility** (ListCarrierFacilitiesRequest fields 3-5 exist; StreamCarrierFacilitiesRequest fields 1-5 exist):
- `id` -> `optional int64` (List only)
- `name` -> `optional string`

**Campus** (ListCampusesRequest fields 3-7 exist; StreamCampusesRequest fields 1-7 exist):
- `id` -> `optional int64` (List only)
- `org_name` -> `optional string`
- `name_long` -> `optional string`
- `aka` -> `optional string`
- `website` -> `optional string`
- `notes` -> `optional string`
- `state` -> `optional string`
- `zipcode` -> `optional string`
- `logo` -> `optional string`

After editing services.proto, run:
```bash
TMPDIR=/tmp/claude-1000 buf generate
```

Then verify the generated Go code compiles:
```bash
TMPDIR=/tmp/claude-1000 go build ./...
```
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 buf lint proto/ && TMPDIR=/tmp/claude-1000 go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'optional' proto/peeringdb/v1/services.proto returns a number > 150 (was ~70, adding ~96 new fields)
    - grep 'optional string info_type' proto/peeringdb/v1/services.proto matches in both ListNetworksRequest and StreamNetworksRequest
    - grep 'optional bool in_dfz' proto/peeringdb/v1/services.proto matches in both ListIxPrefixesRequest and StreamIxPrefixesRequest
    - grep 'optional int64 local_asn' proto/peeringdb/v1/services.proto matches in both ListNetworkFacilitiesRequest and StreamNetworkFacilitiesRequest
    - grep 'optional string aka' proto/peeringdb/v1/services.proto matches for Network, Organization, Facility, InternetExchange, Carrier, Campus (12+ occurrences across List+Stream)
    - buf lint proto/ passes with no errors
    - go build ./... succeeds after buf generate
  </acceptance_criteria>
  <done>Every filterable field from pdbcompat Registry (excluding FieldTime and FieldFloat types) exists as an optional field on the corresponding ConnectRPC List and Stream request messages. Generated Go types compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add middleware logging and recovery tests for 60%+ coverage</name>
  <files>internal/middleware/logging_test.go, internal/middleware/recovery_test.go</files>
  <read_first>
    - internal/middleware/logging.go (63 lines -- full implementation to test)
    - internal/middleware/recovery.go (28 lines -- full implementation to test)
    - internal/middleware/cors_test.go (existing test patterns -- httptest.NewRecorder, httptest.NewRequest)
  </read_first>
  <action>
Create two new test files following the established pattern from cors_test.go (table-driven, t.Parallel, httptest).

**internal/middleware/logging_test.go:**

Test the `Logging` middleware with table-driven tests covering:

1. `TestLogging_StatusCapture` -- Verify responseWriter captures status codes:
   - Handler writes 200: verify logged status is 200
   - Handler writes 404: verify logged status is 404
   - Handler writes 500: verify logged status is 500
   - Handler writes nothing (default): verify logged status is 200

2. `TestLogging_Attributes` -- Verify correct slog attributes are emitted:
   - Create a `slog.Logger` with a custom `slog.Handler` that captures log records into a slice
   - Send a GET request to /test/path
   - Verify the log record contains: method="GET", path="/test/path", status=200, and duration > 0
   - Use `slog.NewTextHandler` writing to a `bytes.Buffer`, then check buffer contains expected key=value pairs

3. `TestLogging_Flush` -- Verify responseWriter delegates Flush to underlying writer:
   - Create a mock ResponseWriter that implements http.Flusher
   - Wrap it, call Flush(), verify delegation

4. `TestLogging_Unwrap` -- Verify Unwrap returns underlying writer:
   - Create responseWriter, call Unwrap(), assert identity

Use a custom slog handler for attribute verification:
```go
type captureHandler struct {
    records []slog.Record
    mu      sync.Mutex
}
func (h *captureHandler) Handle(_ context.Context, r slog.Record) error {
    h.mu.Lock()
    defer h.mu.Unlock()
    h.records = append(h.records, r)
    return nil
}
func (h *captureHandler) Enabled(_ context.Context, _ slog.Level) bool { return true }
func (h *captureHandler) WithAttrs(_ []slog.Attr) slog.Handler { return h }
func (h *captureHandler) WithGroup(_ string) slog.Handler { return h }
```

**internal/middleware/recovery_test.go:**

Test the `Recovery` middleware:

1. `TestRecovery_NoPanic` -- Normal handler completes, no 500:
   - Handler writes "ok" with 200
   - Verify response body is "ok", status 200

2. `TestRecovery_PanicString` -- Handler panics with string:
   - Handler does `panic("test panic")`
   - Verify response status is 500
   - Verify response body contains `"internal server error"`
   - Verify slog captured an Error-level record with "panic recovered" message

3. `TestRecovery_PanicError` -- Handler panics with error value:
   - Handler does `panic(fmt.Errorf("error panic"))`
   - Verify response status is 500

4. `TestRecovery_LogAttributes` -- Verify panic log includes method, path, stack:
   - Use captureHandler to inspect log record
   - Verify attributes include: panic value, stack trace, method, path

All tests must use t.Parallel() per T-3, table-driven per T-1.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/middleware/...</automated>
  </verify>
  <acceptance_criteria>
    - File internal/middleware/logging_test.go exists with TestLogging in function names
    - File internal/middleware/recovery_test.go exists with TestRecovery in function names
    - go test -race -cover ./internal/middleware/... shows coverage >= 60%
    - grep 't.Parallel' internal/middleware/logging_test.go returns at least 2 matches
    - grep 't.Parallel' internal/middleware/recovery_test.go returns at least 2 matches
    - grep 'captureHandler' internal/middleware/logging_test.go returns at least 1 match (custom slog handler for verification)
  </acceptance_criteria>
  <done>Middleware package coverage reaches 60%+ with comprehensive table-driven tests for logging and recovery middleware. All tests pass with -race.</done>
</task>

</tasks>

<verification>
- `buf lint proto/` passes
- `TMPDIR=/tmp/claude-1000 go build ./...` passes
- `TMPDIR=/tmp/claude-1000 go test -race -cover ./internal/middleware/...` passes with 60%+ coverage
- `grep -c 'optional' proto/peeringdb/v1/services.proto` returns > 150
</verification>

<success_criteria>
1. Proto request messages contain all filterable fields from pdbcompat Registry (excluding FieldTime/FieldFloat)
2. Generated Go code compiles
3. Middleware package coverage >= 60%
4. All tests pass with -race flag
</success_criteria>

<output>
After completion, create `.planning/phases/33-grpc-dedup-filter-parity/33-01-SUMMARY.md`
</output>
