---
phase: 58-visibility-schema-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/visbaseline/schema_alignment_test.go
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements: [VIS-03]
must_haves:
  truths:
    - "The Phase 57 diff.json is mechanically asserted to contain no auth-gated schema surfaces beyond those already covered by ent (poc row-level visibility + ixlan.ixf_ixp_member_list_url)"
    - "The existing ent schema is confirmed unchanged: poc.visible (field.String, default Public) and ixlan.ixf_ixp_member_list_url_visible (field.String, default Private) remain the only visibility-bearing fields"
    - "PROJECT.md Key Decisions table carries a v1.14 entry documenting that Phase 58 surfaced no new auth-gated fields, the <field>_visible naming convention, and the NULL-treats-as-default-on-upgrade rule (D-07)"
    - "CLAUDE.md ent conventions document the <field>_visible pattern and the NULL handling rule so future maintainers recognise the convention"
    - "go generate ./... leaves the working tree clean (no drift in ent/, gen/, graph/, internal/web/templates/)"
  artifacts:
    - path: "internal/visbaseline/schema_alignment_test.go"
      provides: "TestSchemaAlignmentWithPhase57Diff — parses testdata/visibility-baseline/diff.json and asserts the only auth-gated field surfaces are poc row-level + ixlan.ixf_ixp_member_list_url"
      contains: "TestSchemaAlignmentWithPhase57Diff"
    - path: ".planning/PROJECT.md"
      provides: "v1.14 Key Decision entry documenting the Phase 58 empirical finding"
      contains: "Phase 58"
    - path: "CLAUDE.md"
      provides: "Schema & Visibility convention note under Conventions"
      contains: "_visible"
  key_links:
    - from: "internal/visbaseline/schema_alignment_test.go"
      to: "testdata/visibility-baseline/diff.json"
      via: "os.ReadFile + json.Unmarshal into visbaseline.Report"
      pattern: "testdata/visibility-baseline/diff.json"
    - from: "internal/visbaseline/schema_alignment_test.go"
      to: "ent/schema/poc.go + ent/schema/ixlan.go"
      via: "expected-fields allowlist encoded as test data — assertion fails if diff.json surfaces a type or field not present in the allowlist"
      pattern: "expectedAuthGated|allowedAuthGated"
---

<objective>
Phase 58 collapses to a confirmation + documentation phase. The Phase 57 DIFF proves that only two auth-gated schema surfaces exist across all 13 PeeringDB types, and both are already covered by existing ent fields:

  1. `poc` — 460 rows are auth-only (row-level visibility); `poc.visible` (ent/schema/poc.go:53) already classifies rows as `Public` vs `Users`.
  2. `ixlan.ixf_ixp_member_list_url` — 18 rows have this field auth-only; `ixlan.ixf_ixp_member_list_url_visible` (ent/schema/ixlan.go:45) already exists with default `"Private"`.

All other 11 types show zero field-level deltas. **No new ent fields are required.**

Purpose: Satisfy VIS-03 by empirically confirming the existing schema is sufficient, locking the finding into an automated test that will break if the next Phase 57 re-capture surfaces a new auth-gated field, and persisting the `<field>_visible` + NULL-handling conventions so Phase 59 (privacy policy) and later maintainers have a durable reference.

Output:
- Regression test `TestSchemaAlignmentWithPhase57Diff` in `internal/visbaseline/`
- v1.14 entry in PROJECT.md Key Decisions (per D-08)
- Schema & Visibility note in CLAUDE.md Conventions (per D-09)
- Confirmation that `go generate ./...` has no drift (SC #3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-visibility-schema-alignment/58-CONTEXT.md
@testdata/visibility-baseline/DIFF.md
@testdata/visibility-baseline/diff.json
@ent/schema/poc.go
@ent/schema/ixlan.go
@ent/entc.go
@CLAUDE.md
@internal/visbaseline/diff.go

<interfaces>
<!-- Key types the test will reuse. Extracted from internal/visbaseline/diff.go. -->
<!-- The test unmarshals testdata/visibility-baseline/diff.json directly into these -->
<!-- types — do NOT re-declare parallel structs. -->

From internal/visbaseline/diff.go:
```go
const ReportSchemaVersion = 1

type Report struct {
    SchemaVersion int                   `json:"schema_version"`
    GeneratedAt   time.Time             `json:"generated"`
    Targets       []string              `json:"targets"`
    Types         map[string]TypeReport `json:"types"`
}

type TypeReport struct {
    AnonRowCount      int          `json:"anon_row_count"`
    AuthRowCount      int          `json:"auth_row_count"`
    AuthOnlyRowCount  int          `json:"auth_only_row_count"`
    VisibleValuesAnon []string     `json:"visible_values_anon,omitempty"`
    VisibleValuesAuth []string     `json:"visible_values_auth,omitempty"`
    Fields            []FieldDelta `json:"fields"`
}

type FieldDelta struct {
    Name          string `json:"name"`
    AuthOnly      bool   `json:"auth_only"`
    Placeholder   string `json:"placeholder,omitempty"`
    RowsAdded     int    `json:"rows_added,omitempty"`
    ValueSetDrift bool   `json:"value_set_drift,omitempty"`
    IsPII         bool   `json:"is_pii,omitempty"`
}
```

From ent/schema/poc.go:53-57 (existing visibility-bearing field — reference template):
```go
field.String("visible").
    Optional().
    Default("Public").
    Annotations(entrest.WithFilter(entrest.FilterGroupEqual | entrest.FilterGroupArray)).
    Comment("Visibility level"),
```

From ent/schema/ixlan.go:45-48 (existing per-field visibility — precedent for <field>_visible convention):
```go
field.String("ixf_ixp_member_list_url_visible").
    Optional().
    Default("Private").
    Comment("IXF member list URL visibility"),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema-alignment regression test against Phase 57 diff.json</name>
  <files>internal/visbaseline/schema_alignment_test.go</files>
  <read_first>
    - internal/visbaseline/diff.go (for Report / TypeReport / FieldDelta types — reuse, do not redeclare)
    - internal/visbaseline/capture.go (for the canonical list of all 13 PeeringDB types if exported; otherwise hardcode — see action)
    - testdata/visibility-baseline/diff.json (the artifact under test)
    - ent/schema/poc.go and ent/schema/ixlan.go (to confirm the two allowlisted surfaces)
    - testdata/visibility-baseline/DIFF.md (human-readable summary of what the test must accept)
  </read_first>
  <behavior>
    - Test 1 (happy path): Parses testdata/visibility-baseline/diff.json into visbaseline.Report — unmarshalling must succeed and SchemaVersion must equal visbaseline.ReportSchemaVersion.
    - Test 2 (coverage): All 13 PeeringDB types appear in report.Types under the "beta/" prefix: campus, carrier, carrierfac, fac, ix, ixfac, ixlan, ixpfx, net, netfac, netixlan, org, poc. Missing type → test fails with clear name.
    - Test 3 (the core assertion): For every type in report.Types, iterate report.Types[t].Fields and assert — for every FieldDelta with AuthOnly==true — that (typeName, fieldName) is in the expected-allowlist:
        * "beta/poc" allows: created, email, id, name, net_id, phone, role, status, updated, url  (all 10 per-row fields that appear when a previously-hidden Users-tier row enters the result set — not new schema fields, just row visibility)
        * "beta/ixlan" allows: ixf_ixp_member_list_url  (already covered by ixlan.ixf_ixp_member_list_url_visible)
        * All other types: allowlist is empty — any AuthOnly field is a failure
      Any AuthOnly field outside the allowlist fails the test with "unexpected auth-gated field <type>.<name> — Phase 58 did not plan schema work for this; re-run planning."
    - Test 4 (poc row-level sanity): For "beta/poc", VisibleValuesAnon == []string{"Public"} and VisibleValuesAuth contains both "Public" and "Users". Confirms poc.visible is the row-discriminator.
    - Test 5 (non-AuthOnly visible field on poc): The "visible" field entry on poc (if present in Fields) has AuthOnly==false and ValueSetDrift may be true — document in a comment but do not assert ValueSetDrift since the flag is optional.
  </behavior>
  <action>
    Create `internal/visbaseline/schema_alignment_test.go` in package `visbaseline` (white-box — reuse Report/TypeReport/FieldDelta directly).

    Table-driven test per GO-T-1. Single top-level test `TestSchemaAlignmentWithPhase57Diff` with sub-tests via `t.Run` for each assertion (Test 1-5 above). Mark sub-tests `t.Parallel()` per GO-T-3 where safe (parsing sub-test is the dependency; downstream sub-tests reuse the parsed Report via a closure-captured variable, so skip t.Parallel on the parse sub-test and its children).

    Expected allowlist shape (hardcode in the test file):
    ```go
    var allowedAuthGated = map[string]map[string]struct{}{
        "beta/poc": {
            "created": {}, "email": {}, "id": {}, "name": {}, "net_id": {},
            "phone": {}, "role": {}, "status": {}, "updated": {}, "url": {},
        },
        "beta/ixlan": {
            "ixf_ixp_member_list_url": {},
        },
    }
    ```
    These are the fields that appeared as auth-only in the committed DIFF. They do NOT represent new ent fields — the poc entries are row-level leakage (all fields on a previously-hidden poc row), and ixf_ixp_member_list_url is already gated by ixlan.ixf_ixp_member_list_url_visible.

    Expected 13 types (hardcode — canonical list mirrors cmd/pdbcompat-check/main.go AllTypes per Phase 57 D-113):
    ```go
    var expectedTypes = []string{
        "beta/campus", "beta/carrier", "beta/carrierfac", "beta/fac",
        "beta/ix", "beta/ixfac", "beta/ixlan", "beta/ixpfx",
        "beta/net", "beta/netfac", "beta/netixlan", "beta/org", "beta/poc",
    }
    ```

    Path to diff.json from test: compute via filepath.Join with a relative walk up to repo root — simplest is:
    ```go
    diffPath := filepath.Join("..", "..", "testdata", "visibility-baseline", "diff.json")
    ```
    Tests run from `internal/visbaseline/` so two `..` reaches repo root. Use `t.Helper()` + `os.ReadFile`; on error call `t.Fatalf("read %s: %v", diffPath, err)`.

    Use `errors.Is`/`errors.As` only for real error-wrapping cases — here a bare `t.Fatalf` after `json.Unmarshal` is fine per GO-ERR-1 intent (test-only error context).

    Failure messages must be actionable: include the offending (type, field) pair and instruct re-running Phase 58 planning. Example:
    ```
    unexpected auth-gated field "beta/net.policy_general" in testdata/visibility-baseline/diff.json —
    Phase 58 planned no schema work for this. Re-run /gsd-plan-phase 58 after updating 58-CONTEXT.md,
    or add an ent <field>_visible field per the <field>_visible convention.
    ```

    Do NOT add a new allowlist entry without re-opening planning — the test failing is the intended signal.

    No external deps. stdlib only: testing, encoding/json, os, path/filepath.
  </action>
  <verify>
    <automated>TMPDIR=/tmp/claude-1000 go test -race -run TestSchemaAlignmentWithPhase57Diff ./internal/visbaseline/...</automated>
  </verify>
  <acceptance_criteria>
    - [ ] File `internal/visbaseline/schema_alignment_test.go` exists in package `visbaseline`
    - [ ] Test `TestSchemaAlignmentWithPhase57Diff` present (verify: `grep -n "func TestSchemaAlignmentWithPhase57Diff" internal/visbaseline/schema_alignment_test.go`)
    - [ ] Test passes against the committed diff.json (verify: `go test -race -run TestSchemaAlignmentWithPhase57Diff ./internal/visbaseline/...` exits 0)
    - [ ] Test reuses `Report`, `TypeReport`, `FieldDelta` from diff.go (verify: no local redeclaration — `grep -n "type Report\|type TypeReport\|type FieldDelta" internal/visbaseline/schema_alignment_test.go` returns empty)
    - [ ] Allowlist covers exactly poc (10 fields) + ixlan (1 field) — nothing else (verify: `grep -c "^\s*\"beta/" internal/visbaseline/schema_alignment_test.go` shows only those two type keys in the allowlist map)
    - [ ] Failure message includes instructions to re-run `/gsd-plan-phase 58` (verify: `grep -n "gsd-plan-phase 58" internal/visbaseline/schema_alignment_test.go`)
    - [ ] Failure message references the `<field>_visible` convention (verify: `grep -n "_visible" internal/visbaseline/schema_alignment_test.go`)
    - [ ] `golangci-lint run ./internal/visbaseline/...` exits 0
    - [ ] `go vet ./internal/visbaseline/...` exits 0
  </acceptance_criteria>
  <done>The regression test is committed and green on the current diff.json. Any future re-capture that surfaces a new auth-gated field will fail this test and force Phase 58 to be re-opened with planning, per the Phase 58 → Phase 59 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 58 findings in PROJECT.md Key Decisions (D-08)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 141-197 — Key Decisions table; last entry is v1.8 "Server-side completion search...")
    - .planning/phases/58-visibility-schema-alignment/58-CONTEXT.md (D-01 through D-09)
    - testdata/visibility-baseline/DIFF.md
  </read_first>
  <action>
    Append four new rows to the Key Decisions table in `.planning/PROJECT.md` (the table starts at line 143 with the header `| Decision | Rationale | Outcome |`). Keep the existing single-line-per-row format and pipe alignment.

    The four rows (copy verbatim — decision text is normative per D-08):

    ```
    | Phase 58 schema alignment: existing ent fields sufficient | Phase 57 empirical diff surfaced only two auth-gated surfaces — poc row-level visibility (already covered by poc.visible per D-01/D-02) and ixlan.ixf_ixp_member_list_url (already covered by ixlan.ixf_ixp_member_list_url_visible). All other 11 types show zero field-level deltas. No new ent fields required | ✓ Validated Phase 58 |
    | <field>_visible naming convention for per-field visibility | Established by ixlan.ixf_ixp_member_list_url_visible (pre-existing) and confirmed in Phase 58 as the canonical pattern for any future auth-gated field additions. field.String (not Enum) per D-02 — avoids entgql/entrest/entproto codegen churn; validation happens in the privacy policy layer | ✓ Validated Phase 58 |
    | Privacy policy treats NULL <field>_visible as schema default | D-07: ent auto-migrate adds new *_visible columns with declared defaults, but existing rows synced before the column existed will have NULL until the next sync rewrites them. The Phase 59 privacy policy MUST treat NULL as the column default (`Public` per the upstream-default rule) rather than as `Users`, to prevent a post-upgrade flood of suddenly-hidden rows | ✓ Validated Phase 58 |
    | Phase 58 regression test guards the empirical assumption | internal/visbaseline/schema_alignment_test.go asserts diff.json contains only the two known auth-gated surfaces. If a future Phase 57 re-capture surfaces a new auth-gated field, the test fails and forces Phase 58 to be re-opened before Phase 59's privacy policy ships against a stale assumption | ✓ Validated Phase 58 |
    ```

    Insert these as the last four rows of the Key Decisions table (after the "Server-side completion search returning IDs only" row at line 196). Do NOT disturb the Evolution section below it.

    Also update the "Last updated" line at the bottom of PROJECT.md:
    - Find: `*Last updated: 2026-04-16 — milestone v1.14 Authenticated Sync & Visibility Layer started (v1.13 Security & Sync Hardening shipped 2026-04-11)*`
    - Replace with: `*Last updated: 2026-04-16 — Phase 58 visibility schema alignment validated existing schema sufficient (v1.14 in progress, v1.13 shipped 2026-04-11)*`
  </action>
  <verify>
    <automated>grep -c "Phase 58" .planning/PROJECT.md | awk '{ if ($1 >= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Four new rows appear in the Key Decisions table, each ending with `| ✓ Validated Phase 58 |` (verify: `grep -c "Validated Phase 58" .planning/PROJECT.md` returns 4)
    - [ ] Row 1 documents that no new fields were needed (verify: `grep -n "existing ent fields sufficient" .planning/PROJECT.md`)
    - [ ] Row 2 documents the `<field>_visible` convention (verify: `grep -n "<field>_visible naming convention" .planning/PROJECT.md`)
    - [ ] Row 3 documents the NULL-treats-as-default rule per D-07 (verify: `grep -n "NULL <field>_visible" .planning/PROJECT.md`)
    - [ ] Row 4 references the regression test (verify: `grep -n "schema_alignment_test.go" .planning/PROJECT.md`)
    - [ ] The "Last updated" footer reflects Phase 58 (verify: `grep -n "Phase 58 visibility schema alignment validated" .planning/PROJECT.md`)
    - [ ] Table formatting is preserved (verify: `awk -F '|' 'NF && /^\|/ { if (NF != 5) print NR": "NF" cols" }' .planning/PROJECT.md` returns nothing)
    - [ ] Evolution section below the table is untouched (verify: `grep -n "^## Evolution" .planning/PROJECT.md` returns exactly one match)
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions table carries the Phase 58 empirical finding, the `<field>_visible` convention, the NULL rule, and the regression-test safeguard. Future maintainers can trace the why of the privacy-policy design to a single table row and its originating phase.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md Conventions with Schema & Visibility note (D-09)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 53-132 — the Conventions section; the last subsection is "Deployment" at line 128)
    - .planning/phases/58-visibility-schema-alignment/58-CONTEXT.md (D-07, D-09)
  </read_first>
  <action>
    Add a new `### Schema & Visibility` subsection under `## Conventions` in `CLAUDE.md`. Place it immediately after the existing `### Code Generation` subsection (which ends around line 65, just before `### Middleware`). This keeps schema-authoring guidance next to the codegen pipeline it interacts with.

    Content to insert (keep it short — this is prompt context, not a tutorial):

    ```
    ### Schema & Visibility

    Two ent fields carry upstream PeeringDB visibility signals (Phase 58):

    - `poc.visible` (`ent/schema/poc.go`) — row-level: `Public` or `Users`. Rows with non-`Public` values are filtered from anonymous responses by the ent Privacy policy. This is the only entity where a whole row can be hidden.
    - `ixlan.ixf_ixp_member_list_url_visible` (`ent/schema/ixlan.go`) — per-field: `Public`, `Users`, or `Private`. Gates the sibling `ixf_ixp_member_list_url` field; the privacy policy nulls the value field when the visibility field is not `Public`.

    When a future Phase 57 re-capture surfaces a new auth-gated field (the `internal/visbaseline/schema_alignment_test.go` regression test will flag this), add a sibling `<field>_visible` field using `field.String` (not `Enum` — avoids entgql/entrest/entproto codegen churn per the v1.14 Key Decision). Default to the upstream default when known; default to `"Public"` otherwise and document the assumption in the Key Decisions table.

    **NULL handling:** ent auto-migrate adds `*_visible` columns with their declared defaults, but rows synced before the column existed will have NULL until the next sync rewrites them. The privacy policy treats NULL as the column default (`Public` for upstream-public fields), never as `Users`. This prevents a post-upgrade flood of suddenly-hidden rows.
    ```

    Do not modify any other section. Preserve existing formatting.
  </action>
  <verify>
    <automated>grep -q "^### Schema & Visibility$" CLAUDE.md && grep -q "_visible" CLAUDE.md && grep -q "NULL as the column default" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] New subsection `### Schema & Visibility` exists under `## Conventions` (verify: `grep -n "^### Schema & Visibility$" CLAUDE.md`)
    - [ ] Subsection appears between `### Code Generation` and `### Middleware` (verify: `awk '/^### Code Generation$/{cg=NR} /^### Schema & Visibility$/{sv=NR} /^### Middleware$/{mw=NR} END{ if (cg<sv && sv<mw) print "ok" }' CLAUDE.md` prints "ok")
    - [ ] References both `poc.visible` and `ixlan.ixf_ixp_member_list_url_visible` (verify: `grep -c "poc.visible\|ixf_ixp_member_list_url_visible" CLAUDE.md` ≥ 2)
    - [ ] Documents the `<field>_visible` convention using `field.String` (verify: `grep -n "field.String" CLAUDE.md | grep -v go.mod`)
    - [ ] Documents the NULL-treats-as-default rule (verify: `grep -n "NULL as the column default" CLAUDE.md`)
    - [ ] References the regression test by path (verify: `grep -n "schema_alignment_test.go" CLAUDE.md`)
    - [ ] No other section is modified (verify by reviewing the diff: only additions between `### Code Generation` and `### Middleware`)
  </acceptance_criteria>
  <done>CLAUDE.md carries the `<field>_visible` + NULL-handling conventions inline with the other schema-adjacent conventions, so future Claude sessions loading CLAUDE.md as project memory will have the rule available without needing to rediscover Phase 58.</done>
</task>

<task type="auto">
  <name>Task 4: Confirm `go generate ./...` produces zero drift</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Code Generation" (lines 55-65 — codegen pipeline order)
    - CLAUDE.md §"Go Module" (line 124 — TMPDIR + GONOSUMCHECK env vars)
  </read_first>
  <action>
    This task verifies SC #3 from the ROADMAP Phase 58 success criteria: "`go generate ./...` regenerates ent cleanly ... committed `ent/` files are byte-identical with what the generator produces."

    Run the full codegen pipeline and assert the working tree is clean afterward. Commands (run from repo root):

    1. Ensure tree is clean before starting — `git status --porcelain` should show only the Task 1-3 additions. Note those in the Summary.
    2. Stash or commit Task 1-3 changes first (recommended: commit Task 1-3 together, then run codegen verification on the post-commit tree). If the executor prefers to verify codegen drift BEFORE committing, `git stash -u` is also acceptable.
    3. Run: `TMPDIR=/tmp/claude-1000 go generate ./...`
    4. Run: `git status --porcelain -- ent/ gen/ graph/ internal/web/templates/`
    5. Expected output: empty. Any diff in those directories means Phase 58 unexpectedly changed generated code — investigate before proceeding.

    If stashed in step 2: `git stash pop` after verification.

    This is a pure verification task — no files are modified. The executor records the result in the SUMMARY (clean or drift-detected).

    **Note for executor:** Per CLAUDE.md §"Go Module", sandbox runs may need `GONOSUMCHECK=* GONOSUMDB=*` prepended if the Go sum database is read-only. Also note from CLAUDE.md §"CI": CI runs this same check as a "Generated code drift check" job — so a green local run strongly predicts a green CI run.
  </action>
  <verify>
    <automated>TMPDIR=/tmp/claude-1000 go generate ./... && test -z "$(git status --porcelain -- ent/ gen/ graph/ internal/web/templates/)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `go generate ./...` exits 0 (no codegen errors)
    - [ ] `git status --porcelain -- ent/ gen/ graph/ internal/web/templates/` is empty after codegen (no drift)
    - [ ] SUMMARY records the outcome — either "clean: no drift" or — if drift is detected — a full listing of drifted files for operator review (which would indicate a latent bug unrelated to Phase 58's intent)
    - [ ] Any pre-existing uncommitted work (Task 1-3) is preserved (not accidentally overwritten by codegen)
  </acceptance_criteria>
  <done>ROADMAP Phase 58 SC #3 is satisfied: the committed ent/gen/graph/templates tree is byte-identical with what the generator produces, confirming Phase 58 introduced no schema changes. CI's "Generated code drift check" job will pass on the PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → committed fixture | `schema_alignment_test.go` reads `testdata/visibility-baseline/diff.json` — a committed artifact, not a runtime input. Trust boundary is the git review process (humans reviewed the Phase 57 diff on commit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | testdata/visibility-baseline/diff.json | accept | The file is a committed artifact reviewed at Phase 57 merge. An attacker with commit access could tamper with both the diff AND the test allowlist — out of scope for this phase (relies on git review discipline, not a runtime defence). |
| T-58-02 | Information Disclosure | schema_alignment_test.go failure messages | mitigate | Failure messages name the offending `(type, field)` pair but do not leak actual field values — the Phase 57 diff.json already enforces placeholder-only contents (see threat T-57-02). The test surfaces only field *names* which are public PeeringDB schema, not values. |
| T-58-03 | Denial of Service | Phase 59 privacy policy regressing to pre-Phase-58 state | mitigate | Regression test `TestSchemaAlignmentWithPhase57Diff` fires in every `go test ./...` run (including CI) and fails loudly if a future re-capture surfaces a new auth-gated field not in the allowlist. Forces Phase 58 re-opening before Phase 59 ships against a stale assumption. |
| T-58-04 | Elevation of Privilege | NULL `*_visible` treated as `Users` on upgrade | mitigate | D-07 rule documented in PROJECT.md Key Decisions (Task 2) AND CLAUDE.md Conventions (Task 3). Phase 59's privacy policy will encode the rule as policy logic; this phase locks the rule into both human-reviewable locations so that encoding is grounded in an explicit written decision. |
</threat_model>

<verification>
All four tasks must succeed independently:

1. `TMPDIR=/tmp/claude-1000 go test -race -run TestSchemaAlignmentWithPhase57Diff ./internal/visbaseline/...` — exit 0
2. `grep -c "Validated Phase 58" .planning/PROJECT.md` — ≥ 4
3. `grep -q "^### Schema & Visibility$" CLAUDE.md` — exit 0
4. `TMPDIR=/tmp/claude-1000 go generate ./... && test -z "$(git status --porcelain -- ent/ gen/ graph/ internal/web/templates/)"` — exit 0

Full-phase smoke:
```bash
TMPDIR=/tmp/claude-1000 go test -race ./internal/visbaseline/...
TMPDIR=/tmp/claude-1000 golangci-lint run ./internal/visbaseline/...
TMPDIR=/tmp/claude-1000 go vet ./...
```
All must pass.

**Negative verification (sanity check — optional):** Temporarily edit `testdata/visibility-baseline/diff.json` in a scratch copy to add an auth-only field on `beta/net`, then re-run the test. It MUST fail with the actionable message. Revert the scratch edit — do not commit the scratch.
</verification>

<success_criteria>
Phase 58 is complete when:

1. **VIS-03 satisfied (empirically):** `internal/visbaseline/schema_alignment_test.go` asserts and passes that the committed Phase 57 diff.json contains only the two pre-known auth-gated surfaces.
2. **SC #1 (Phase 58 ROADMAP):** `poc.visible` confirmed sufficient for POCs — diff.json shows the anon/auth `visible` value-set drift, the test asserts on it, and no additional POC-level visibility field is needed.
3. **SC #2 (Phase 58 ROADMAP):** No additional auth-gated fields surfaced — the diff shows zero field-level deltas across 11 of 13 types, and the two non-zero types (poc row-level, ixlan.ixf_ixp_member_list_url) are already covered.
4. **SC #3 (Phase 58 ROADMAP):** `go generate ./...` leaves the tree clean (Task 4).
5. **SC #4 (Phase 58 ROADMAP):** PROJECT.md Key Decisions carries four new Phase-58-validated rows (Task 2); CLAUDE.md carries the `<field>_visible` convention (Task 3).
6. **Phase 59 unblocked:** Phase 59 can proceed to wire the ent Privacy policy against `poc.visible` and `ixlan.ixf_ixp_member_list_url_visible` with documented confidence that no other fields need gating.
</success_criteria>

<output>
After completion, create `.planning/phases/58-visibility-schema-alignment/58-01-SUMMARY.md` covering:
- Files modified (the three text files; ent/gen/graph explicitly unchanged)
- Test result for `TestSchemaAlignmentWithPhase57Diff`
- `go generate ./...` drift-check outcome
- The four PROJECT.md Key Decisions rows added, verbatim
- Explicit note: **"No ent schema changes made. No `go generate` output committed. Phase 58 was a confirmation + documentation phase."**
- Readiness signal for Phase 59 with pointer to the two visibility-bearing fields Phase 59 will read.
</output>
