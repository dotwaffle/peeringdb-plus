---
phase: 69-unicode-operator-in-robustness
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - CHANGELOG.md
  - docs/API.md
  - CLAUDE.md
autonomous: true
requirements:
  - UNICODE-01
  - UNICODE-02
  - UNICODE-03
  - IN-01
  - IN-02
tags:
  - docs
  - changelog
  - req-audit
must_haves:
  truths:
    - "CHANGELOG.md v1.16 [Unreleased] entry extended with Phase 69 behavioural deltas under Added (UNICODE folding + operator coercion + __in robustness) and Known-issues (ASCII-only divergence window until first post-deploy sync cycle)"
    - "docs/API.md § Known Divergences table gains a Phase 69 row for the brief ASCII-only window (rows created before v1.16 deploy have `_fold = ''` until re-sync)"
    - "CLAUDE.md § Conventions gains a Phase 69 subsection (`Shadow-column folding` or similar) documenting the `internal/unifold` + `_fold` column + filter-routing pattern for future phases that add new searchable text fields"
    - "REQ-ID audit: every one of UNICODE-01, UNICODE-02, UNICODE-03, IN-01, IN-02 points to at least one grep-verifiable test artifact in the repo"
    - "Phase 69 frontmatter audit: each of Plans 01-05 lists the correct REQ-IDs in its `requirements:` field; no REQ-ID is unreferenced"
    - "No `fly deploy` commands emitted — ship timing is coordinated with 67-71 per CONTEXT.md § Coordination notes"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 69 entries under Added + known-issues note"
      contains: "UNICODE-01"
    - path: "docs/API.md"
      provides: "Known Divergences row for ASCII-only window"
      contains: "name_fold"
    - path: "CLAUDE.md"
      provides: "Phase 69 shadow-column folding convention subsection"
      contains: "internal/unifold"
  key_links:
    - from: "REQ-ID audit (manual grep in SUMMARY)"
      to: "test artifacts in internal/unifold / internal/pdbcompat / internal/sync"
      via: "grep matching each REQ-ID to a test name"
      pattern: "UNICODE-|IN-"
---

<objective>
Close Phase 69 with a docs-and-audit plan that is bit-identical in structure to Phase 68 Plan 04:

1. Extend the existing v1.16 [Unreleased] CHANGELOG.md section with Phase 69's behavioural deltas (ADDITIVE — do not rewrite Phase 67/68 entries).
2. Append a Phase 69 row to `docs/API.md § Known Divergences` documenting the brief ASCII-only window between v1.16 deploy and the first post-deploy sync cycle (per D-03).
3. Add a CLAUDE.md convention subsection so future phases adding searchable text fields know how to extend the shadow-column + unifold + filter-routing pattern.
4. Perform a REQ-ID audit across Plans 01-05: every UNICODE-xx and IN-xx must have a grep-verifiable test artifact.

No `fly deploy` commands — Phase 69 ships as part of the coordinated 67-71 release window per CONTEXT.md § Coordination notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-unicode-operator-in-robustness/CONTEXT.md
@CHANGELOG.md
@CLAUDE.md

<interfaces>
<!-- CHANGELOG.md current v1.16 [Unreleased] shape (Keep-a-Changelog 1.1.0): -->
## [Unreleased] — v1.16
<preamble paragraph>
### Breaking
- Removed PDBPLUS_INCLUDE_DELETED ...
### Added
- pdbcompat status × since matrix ...
- pdbcompat ?limit=0 semantics ...
- pdbcompat cross-surface default ordering ...
### Changed
- Sync now soft-deletes ...
### Deprecated
- PDBPLUS_INCLUDE_DELETED ...
### Fixed
- ?limit=0 on pdbcompat list endpoints previously fell back to DefaultLimit ...

<!-- docs/API.md § Known Divergences — Phase 68 already seeded it. Phase 69
     appends one row. -->

<!-- CLAUDE.md § Conventions already documents Phase 58 visibility,
     Phase 63 schema hygiene, Phase 64 field privacy, Phase 68
     soft-delete tombstones. Phase 69 adds a sibling subsection. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CHANGELOG.md v1.16 entry with Phase 69 deltas</name>
  <read_first>
    - CHANGELOG.md full file (currently ~80 lines)
    - .planning/phases/69-unicode-operator-in-robustness/CONTEXT.md
    - .planning/phases/69-unicode-operator-in-robustness/69-0{1..5}-SUMMARY.md (to pull actual implementation phrasing)
  </read_first>
  <action>
    Edit CHANGELOG.md in place. Touch ONLY the `## [Unreleased] — v1.16` block — do not rearrange Phase 67/68 entries. Two edits:

    **A) Under `### Added`**, append three new bullets AFTER the existing 3 Phase 67/68 bullets:

    ```markdown
    - **pdbcompat Unicode folding** for diacritic-insensitive matching on
      searchable text fields. `?name__contains=Zurich` now matches a
      DB row where `name="Zürich"`. Implementation uses shadow columns
      (`<field>_fold`) populated at sync time by a new `internal/unifold`
      package (NFKD decomposition + a small hand-rolled ligature map for
      ß/æ/ø/ł/þ/đ). 16 shadow columns across 6 entity types (network,
      facility, ix, organization, campus, carrier). Matches upstream
      `peeringdb_server/rest.py:576` (`unidecode.unidecode(v)`).

    - **pdbcompat operator coercion**: `__contains` is now equivalent to
      `__icontains` (case-insensitive) and `__startswith` is equivalent
      to `__istartswith`, per upstream `rest.py:638-641`. All other
      operators (`__exact`, `__gt`, `__lt`, `__gte`, `__lte`, `__in`)
      are unchanged.

    - **pdbcompat `__in` large-list support**: `?<field>__in=` now
      accepts arbitrarily-large comma-separated lists via a SQLite
      `json_each` single-bind rewrite, bypassing the 999-variable
      parameter limit. Empty `__in` (e.g. `?asn__in=`) returns
      `{"data":[], "meta":{"count":0}}` with no SQL executed, matching
      Django ORM semantics.
    ```

    **B) Under `### Fixed`** (or a new `### Known Issues` sub-section if preferred by Keep-a-Changelog 1.1.0 taste), add:

    ```markdown
    - **One-time ASCII-only window** for diacritic-insensitive matching.
      Between v1.16 deploy and the first post-deploy sync cycle (within
      the 1h sync interval), rows have `_fold = ''` and return no match
      for non-ASCII queries. ASCII queries continue to work via the
      existing non-folded columns throughout. No manual backfill is
      required — the next standard sync cycle populates every affected
      row. See `docs/API.md` § Known Divergences for the detailed window.
    ```

    Keep bullet formatting consistent with the existing Phase 67/68 entries (Oxford commas, backticks around identifiers, full sentences).
  </action>
  <verify>
    <automated>grep -c 'Unicode folding\|operator coercion\|json_each\|ASCII-only window' /home/dotwaffle/Code/pdb/peeringdb-plus/CHANGELOG.md</automated>
    <notes>Expected ≥4 (one hit per canonical phrase). If zero, the edit was reverted.</notes>
  </verify>
  <done>
    CHANGELOG.md v1.16 [Unreleased] block extended with 3 Added bullets + 1 Known-issue bullet. Phase 67/68 entries byte-identical to pre-edit state. `git diff CHANGELOG.md` shows only additive lines within the [Unreleased] block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 69 row to docs/API.md § Known Divergences</name>
  <read_first>
    - docs/API.md § Known Divergences section (find via grep — seeded by Phase 68 Plan 04)
  </read_first>
  <action>
    Locate the `## Known Divergences` (or `### Known Divergences`) section in `docs/API.md`. Phase 68 Plan 04 seeded it with two rows (D-07 silent override + D-03 one-time gap citing rest.py:700-712/725). Append one row to the same table:

    ```markdown
    | Area | Upstream behaviour | peeringdb-plus behaviour | Since | Rationale |
    |------|--------------------|--------------------------|-------|-----------|
    | `__contains` / `__startswith` / `__iexact` on searchable text fields | Folds via `unidecode.unidecode(v)` on both sides at query time | Folds via `internal/unifold.Fold` populated to `<field>_fold` shadow columns at sync time; same semantic match, differently staged | v1.16 (Phase 69) | Shadow columns let SQLite use a single indexable comparison path; a brief ASCII-only window exists between v1.16 deploy and the first post-deploy sync cycle (≤1h with default `PDBPLUS_SYNC_INTERVAL=1h`) during which rows have `_fold = ''` and return no match for non-ASCII queries. ASCII queries continue to work via the non-folded columns throughout the window. |
    ```

    Adapt the table column headers to whatever Phase 68 used — do not rewrite the header row. If the existing table has different columns (Upstream / peeringdb-plus / Reason / Since-version), match that exactly.

    Bonus: if `docs/API.md` also has a `## Validation Notes` or similar section for operator-actionable knobs (reading the existing structure will confirm), add a one-line reference: "Phase 69 shadow columns are populated by `internal/sync.upsert*` via `internal/unifold.Fold`; operators upgrading to v1.16 should not run any manual backfill — the next automatic sync covers it."
  </action>
  <verify>
    <automated>grep -c 'name_fold\|internal/unifold\|ASCII-only window' /home/dotwaffle/Code/pdb/peeringdb-plus/docs/API.md</automated>
  </verify>
  <done>
    `docs/API.md` gains one Phase 69 row in the Known Divergences table citing upstream's unidecode approach, our shadow-column approach, and the one-hour window. Existing Phase 68 rows unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 69 convention subsection to CLAUDE.md</name>
  <read_first>
    - CLAUDE.md § Conventions (lines ~80-250 — find the Phase 68 Soft-delete tombstones subsection as an insertion landmark)
  </read_first>
  <action>
    Insert a new `### Shadow-column folding (Phase 69)` subsection into `CLAUDE.md`. Insertion point: IMMEDIATELY AFTER the `### Soft-delete tombstones (Phase 68)` subsection, BEFORE the next existing subsection. Mirror the voice and density of Phase 68's block.

    ```markdown
    ### Shadow-column folding (Phase 69)

    Searchable text fields on 6 entity types (network, facility, internetexchange, organization, campus, carrier) have companion `<field>_fold` columns populated at sync time via `internal/unifold.Fold`. The `pdbcompat` filter layer routes `__contains` / `__startswith` / `__iexact` to the `_fold` column with `unifold.Fold(query_value)` on the RHS, producing diacritic-insensitive matches against upstream's `unidecode.unidecode(v)` behaviour (`rest.py:576`).

    **Adding a new searchable text field on one of the 6 folded entities (e.g. a future `network.tagline_fold`):**

    1. Add `field.String("tagline_fold").Optional().Default("").Comment(...)` to `ent/schema/network.go`. No entrest/entgql/entproto annotations — shadow columns are internal plumbing; leaking them via REST/GraphQL/Proto would expose `tagline_fold` as a filterable surface field (meaningless to callers).
    2. Run `go generate ./...` to regenerate the ent builder surface (`SetTaglineFold` accessor). Proto stays frozen via `entproto.SkipGenFile`.
    3. Extend `internal/sync/upsert.go` `upsertNetworks` closure with `.SetTaglineFold(unifold.Fold(n.Tagline))`. The `OnConflict().UpdateNewValues()` path rewrites `_fold` on every re-sync — no one-shot backfill needed.
    4. Add `"tagline": true` to the entity's `FoldedFields` map in `internal/pdbcompat/registry.go`. The filter layer reads this map to decide whether to route to the shadow column.
    5. Extend `internal/pdbcompat/filter_test.go` with a round-trip test proving `?tagline__contains=<ascii>` matches a `tagline="<diacritic>"` row.

    **Adding shadow columns on a 7th entity** (e.g. future ixlan `descr_fold`): steps 1-5 above plus add the entity to the `FoldedFields` map in `registry.go`. The other 6 upsert functions stay untouched — per-entity surgery is the convention.

    **Do NOT:**
    - Add `entrest.WithFilter` on `_fold` fields (leaks internal plumbing)
    - Run `go generate ./schema` after adding `_fold` fields (`cmd/pdb-schema-generate` strips entproto annotations and would need re-patching; not worth it for internal-only columns)
    - Use `_fold` columns in non-pdbcompat surfaces — entrest/entgql/grpcserver already use ent-level `FieldContainsFold`/NOCASE semantics and do not need shadow columns for their current scope.

    **Convention for `_fold` column indexes:** Phase 69 benchmarked adding `@index(*_fold)` at 10k rows. [Decision recorded in `.planning/phases/69-unicode-operator-in-robustness/69-05-SUMMARY.md`.] New `_fold` fields added in future phases should follow that decision's precedent — if indexes were added in Phase 69, match the pattern; if deferred, match the deferral and re-benchmark only when a specific query shape surfaces a regression.
    ```

    Note: the bracketed sentence about Phase 69-05's decision will be literal text after that plan's SUMMARY is written — leave the bracketed reference as "see `.planning/phases/69-unicode-operator-in-robustness/69-05-SUMMARY.md`" if the index decision is deferred; replace with "indexes added to all 6 entities per 69-05-SUMMARY" if Option A was taken. Resolve during Task 4 REQ-audit by reading the 69-05-SUMMARY.md that exists by the time this plan runs.
  </action>
  <verify>
    <automated>grep -c 'Shadow-column folding (Phase 69)\|internal/unifold\|FoldedFields' /home/dotwaffle/Code/pdb/peeringdb-plus/CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md has a new `### Shadow-column folding (Phase 69)` subsection sitting directly after the Phase 68 Soft-delete subsection, mirroring voice/density, with the "add a new folded field" checklist and the "do not" list. All prior subsections byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 4: REQ-ID audit — every Phase 69 REQ must point to a grep-verifiable test artifact</name>
  <read_first>
    - .planning/phases/69-unicode-operator-in-robustness/69-01-SUMMARY.md (once Plan 01 has shipped)
    - .planning/phases/69-unicode-operator-in-robustness/69-02-SUMMARY.md
    - .planning/phases/69-unicode-operator-in-robustness/69-03-SUMMARY.md
    - .planning/phases/69-unicode-operator-in-robustness/69-04-SUMMARY.md
    - .planning/phases/69-unicode-operator-in-robustness/69-05-SUMMARY.md
    - internal/unifold/unifold_test.go
    - internal/pdbcompat/filter_test.go
    - internal/pdbcompat/fuzz_test.go
    - internal/sync/upsert_test.go
  </read_first>
  <action>
    Produce a REQ-ID audit table in the 69-06-SUMMARY.md matching this structure:

    | REQ-ID | Phase 69 plan | Test artifact (file:test-name) | grep anchor |
    |--------|--------------|-------------------------------|-------------|
    | UNICODE-01 | 01 + 02 + 03 + 04 | `internal/unifold/unifold_test.go:TestFold` (fold contract) + `internal/sync/upsert_test.go:TestUpsertPopulatesFoldColumns` (data-path) + `internal/pdbcompat/filter_test.go:TestShadowRouting_Network_NameFold` (query routing) | `zurich` + `name_fold` + `unifold.Fold` |
    | UNICODE-02 | 04 | `internal/pdbcompat/filter_test.go:TestCoerce_Contains_CaseInsensitive` + `:TestCoerce_StartsWith_CaseInsensitive` + `:TestCoerce_OnlyContainsAndStartswith_Untouched` | `coerceToCaseInsensitive` |
    | UNICODE-03 | 05 | `internal/pdbcompat/fuzz_test.go:FuzzFilterParser` extended seeds + local 500k exec log in 69-05-SUMMARY | `f.Add.*Zürich\|f.Add.*日本語\|f.Add.*70_000` |
    | IN-01 | 04 + 05 | `internal/pdbcompat/filter_test.go:TestIn_Large_Bypasses_SQLite_Limit` + `:TestExplainQueryPlan_JsonEach_SingleBind` | `json_each` |
    | IN-02 | 04 | `internal/pdbcompat/filter_test.go:TestIn_EmptyString_ReturnsEmpty` | `EmptyResult\|errEmptyIn` |

    For each row, grep the repository to confirm the test name actually exists:

    ```bash
    grep -rn 'TestFold\b' internal/unifold/
    grep -rn 'TestUpsertPopulatesFoldColumns\b' internal/sync/
    grep -rn 'TestShadowRouting_Network_NameFold\b\|TestCoerce_Contains_CaseInsensitive\b\|TestCoerce_StartsWith_CaseInsensitive\b\|TestCoerce_OnlyContainsAndStartswith_Untouched\b\|TestIn_Large_Bypasses_SQLite_Limit\b\|TestIn_EmptyString_ReturnsEmpty\b\|TestExplainQueryPlan_JsonEach_SingleBind\b' internal/pdbcompat/
    grep -rn 'FuzzFilterParser' internal/pdbcompat/
    ```

    If ANY grep returns zero hits, the test was not landed OR was named differently than the plan anticipated. Fix options:

    1. Add a minimal test harness in this plan that closes the gap (unusual — usually fix upstream of this plan).
    2. Update the audit row to cite the actual test name used by the implementing plan (Plans 01-05 SUMMARYs should be trusted).
    3. If a REQ-ID genuinely lacks ANY test artifact, STOP and return a PHASE GAP signal to the orchestrator — do NOT silently pass the audit.

    Also verify the ROADMAP.md check-mark state:

    ```bash
    grep -n 'IN-01\|IN-02\|UNICODE-01\|UNICODE-02\|UNICODE-03' /home/dotwaffle/Code/pdb/peeringdb-plus/.planning/REQUIREMENTS.md
    ```

    Flip each from `pending` to `complete (69-0N)` — mirror Phase 68 Plan 04's update pattern. Record the checkbox transitions in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && grep -c 'complete (69-' .planning/REQUIREMENTS.md</automated>
    <notes>Expected ≥5 (one per Phase 69 REQ-ID).</notes>
  </verify>
  <done>
    69-06-SUMMARY.md contains the REQ-ID audit table with every row pointing to a grep-verifiable test artifact. REQUIREMENTS.md has IN-01, IN-02, UNICODE-01, UNICODE-02, UNICODE-03 flipped from `pending` to `complete (69-0N)` per the plan column. Zero rows stranded at `pending`.
  </done>
</task>

</tasks>

<verification>
- CHANGELOG.md [Unreleased] block has ≥3 new Added bullets + 1 known-issue bullet for Phase 69
- docs/API.md Known Divergences has exactly 1 new Phase 69 row (Phase 68 rows unchanged)
- CLAUDE.md has a new `### Shadow-column folding (Phase 69)` subsection
- REQUIREMENTS.md has all 5 Phase 69 REQ-IDs marked complete
- `grep -rn 'fly deploy' .planning/phases/69-unicode-operator-in-robustness/` returns zero hits (coordination window preserved)
- `go test -race ./...` still green (no functional change from docs; but worth a final confirmation)
- `go vet ./...` + `golangci-lint run` clean
</verification>

<success_criteria>
- Every one of the 5 Phase 69 REQ-IDs (UNICODE-01, UNICODE-02, UNICODE-03, IN-01, IN-02) is documented, tested, and marked complete in REQUIREMENTS.md
- Operators reading CHANGELOG.md get the full Phase 69 delta in the v1.16 release notes
- Operators reading docs/API.md understand the brief ASCII-only window and know no manual backfill is required
- Future phases adding searchable text fields have a documented pattern to follow in CLAUDE.md
- Phase 69 is closed with zero `fly deploy` commands — coordinated release window preserved
</success_criteria>

<output>
Create `.planning/phases/69-unicode-operator-in-robustness/69-06-SUMMARY.md` with:
- REQ-ID audit table (5 rows, all grep-verifiable)
- REQUIREMENTS.md check-mark flip summary
- The final `### Shadow-column folding (Phase 69)` block that landed in CLAUDE.md (for quick reference in downstream phase plans)
- Confirmation that Phase 69 is CLOSED pending the coordinated 67-71 deploy window
</output>
