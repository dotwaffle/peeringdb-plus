---
phase: 74-test-ci-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/visbaseline/redactcli.go
  - internal/visbaseline/reportcli.go
  - internal/visbaseline/checkpoint.go
autonomous: true
requirements:
  - TEST-03
tags:
  - lint
  - hygiene
  - visbaseline

must_haves:
  truths:
    - "All three gosec G304 sites in internal/visbaseline use filepath.Clean(path) on the operator-supplied path before the os.ReadFile / os.OpenFile call"
    - "All three gosec G304 sites carry the canonical nolint directive: //nolint:gosec // visbaseline is a CLI tool — paths are operator-supplied by contract"
    - "Any pre-existing nolintlint findings (stale or unused //nolint:gosec directives) are removed"
    - "The exhaustive switch on shape in reportcli.go covers shapeUnknown explicitly (no default-only fallthrough for the iota zero-value)"
    - "golangci-lint run ./internal/visbaseline/... returns 0 issues. on a clean tree"
    - "golangci-lint run ./... (whole repo) returns 0 issues. — no regression elsewhere"
    - "go test ./internal/visbaseline/... passes"
  artifacts:
    - path: "internal/visbaseline/redactcli.go"
      provides: "filepath.Clean'd anonPath at the os.ReadFile call site (line ~99) with canonical nolint reason"
      contains: "visbaseline is a CLI tool — paths are operator-supplied by contract"
    - path: "internal/visbaseline/reportcli.go"
      provides: "filepath.Clean'd jsonPath and p at the two os.OpenFile call sites (lines ~426 and ~442) with canonical nolint reason; shapeUnknown case in switch"
      contains: "visbaseline is a CLI tool — paths are operator-supplied by contract"
  key_links:
    - from: "internal/visbaseline/redactcli.go::os.ReadFile(anonPath)"
      to: "filepath.Clean"
      via: "direct call before ReadFile"
      pattern: "filepath.Clean\\(anonPath\\)"
    - from: "internal/visbaseline/reportcli.go::os.OpenFile(jsonPath ...)"
      to: "filepath.Clean"
      via: "direct call before OpenFile"
      pattern: "filepath.Clean\\(jsonPath\\)"
---

<objective>
Resolve the remaining `internal/visbaseline` lint hygiene per Phase 74 D-03. The package is operator-driven CLI tooling that legitimately reads / writes operator-supplied paths — `gosec G304` is correct in principle but inappropriate to the tool's contract. Per D-03 the right disposition is mixed: apply `filepath.Clean()` to handle the genuine `..` traversal sub-class of G304 risk, then nolint the residual finding with a canonical reason that documents the operator-supplied-by-contract design. The other two findings cited in CONTEXT.md plan-hints (`exhaustive` shapeUnknown, `nolintlint` stale directive at :387) appear already resolved in current main; this plan re-verifies them and rewords the existing nolint directives to canonical form.

Purpose: Make the package's lint disposition explicit, honest, and uniformly worded so future readers understand WHY each `os.{Read,Open}File` is suppression-correct.

Output: Three G304 call sites updated with `filepath.Clean()` + canonical nolint reason; whole-package + whole-repo lint stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-test-ci-debt/CONTEXT.md

@internal/visbaseline/redactcli.go
@internal/visbaseline/reportcli.go

<call_sites>
The three call sites that must change (per Phase 74 D-03 plan-hints, verified against current source):

1. `internal/visbaseline/redactcli.go:99` — `os.ReadFile(anonPath)` where `anonPath` is `filepath.Join(cfg.AnonDir, "api", typeName, fmt.Sprintf("page-%d.json", page))`. Current state: carries `//nolint:gosec // G304: path derived from CLI caller.` (vague wording). Action: apply `filepath.Clean(anonPath)` to the local before the ReadFile, reword the nolint to canonical text.

2. `internal/visbaseline/reportcli.go:426` — `os.OpenFile(jsonPath, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600)` where `jsonPath` is `filepath.Join(outDir, jsonName)`. Current state: carries `//nolint:gosec // G304: path derived from CLI caller.` (vague). Action: apply `filepath.Clean(jsonPath)`, reword nolint.

3. `internal/visbaseline/reportcli.go:442` — `os.OpenFile(p, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600)` where `p` is `filepath.Join(outDir, name)`. Current state: carries `//nolint:gosec // G304: path derived from CLI caller.` (vague). Action: apply `filepath.Clean(p)`, reword nolint.

A FOURTH related site exists at `redactcli.go:105` (`os.ReadFile(path)` for the auth path — the auth-side equivalent of site 1). It was not in the original CONTEXT.md plan-hint list because the lint findings predated the auth-side refactor, but it carries the same vague nolint wording and the same operator-supplied-by-contract disposition. Reword it for symmetry (do not need filepath.Clean since `path` arrives directly from `filepath.Walk` which already returns cleaned paths — but apply Clean anyway for uniform discipline).

A FIFTH site at `checkpoint.go:117` (`os.ReadFile(path)`) carries similar nolint wording. Inspect during execution and reword to canonical form for symmetry.

Per Phase 74 D-03 plan-hints, two additional findings were anticipated:
- `reportcli.go:70` exhaustive missing `shapeUnknown` case — verify; current source already has the case (line 75). No action required if confirmed clean.
- `reportcli.go:387` stale `//nolint:gosec` directive — verify; current source line 387 is comment text, not a nolint directive. No action required if confirmed clean.

If verification at execution time reveals either of those findings is in fact still present (perhaps `golangci-lint` config drifted or the file shifted), fix it inline within this plan's Task 1 — D-03's per-finding disposition is explicit on both.
</call_sites>

<canonical_nolint_text>
The exact text to use on every visbaseline gosec G304 nolint directive in this plan:

```go
//nolint:gosec // visbaseline is a CLI tool — paths are operator-supplied by contract
```

Note: literal em-dash (`—`, U+2014). The build environment is UTF-8; current repo already contains many em-dashes. Do not substitute `--`.
</canonical_nolint_text>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply filepath.Clean + canonical nolint to all three G304 sites</name>
  <files>internal/visbaseline/redactcli.go, internal/visbaseline/reportcli.go</files>
  <read_first>
    - internal/visbaseline/redactcli.go (entire file — confirm import block has path/filepath; locate all os.ReadFile and os.OpenFile call sites; record current line numbers in case file has shifted)
    - internal/visbaseline/reportcli.go (entire file — same)
    - internal/visbaseline/checkpoint.go lines 110-125 (confirm checkpoint.go:117 site shape)
    - .planning/phases/74-test-ci-debt/CONTEXT.md (D-03 in full — confirm exact nolint reason text)
  </read_first>
  <action>
For each of the FIVE call sites listed below, perform the same edit pattern:

1. **`internal/visbaseline/redactcli.go:99`** (`os.ReadFile(anonPath)`):
   - BEFORE: `anonBytes, err := os.ReadFile(anonPath) //nolint:gosec // G304: path derived from CLI caller.`
   - AFTER:
     ```go
     anonPath = filepath.Clean(anonPath)
     anonBytes, err := os.ReadFile(anonPath) //nolint:gosec // visbaseline is a CLI tool — paths are operator-supplied by contract
     ```
   (Adding the `filepath.Clean` re-assignment on the line above; `path/filepath` is already imported in this file.)

2. **`internal/visbaseline/redactcli.go:105`** (`os.ReadFile(path)` for auth side):
   - Apply the same pattern: `path = filepath.Clean(path)` immediately before the ReadFile call, then update the nolint to the canonical text.

3. **`internal/visbaseline/reportcli.go:426`** (`os.OpenFile(jsonPath, ...)`):
   - BEFORE: `f, err := os.OpenFile(jsonPath, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600) //nolint:gosec // G304: path derived from CLI caller.`
   - AFTER:
     ```go
     jsonPath = filepath.Clean(jsonPath)
     f, err := os.OpenFile(jsonPath, os.O_WRONLY|os.O_CREATE|os.O_TRUNC, 0o600) //nolint:gosec // visbaseline is a CLI tool — paths are operator-supplied by contract
     ```

4. **`internal/visbaseline/reportcli.go:442`** (`os.OpenFile(p, ...)`):
   - Apply the same pattern: `p = filepath.Clean(p)` immediately before the OpenFile call, then update the nolint.

5. **`internal/visbaseline/checkpoint.go:117`** (`os.ReadFile(path)`):
   - Apply the same pattern: `path = filepath.Clean(path)` immediately before the ReadFile call, then update the nolint to the canonical text. (Required for symmetry — leaving one site with vague wording reintroduces the same drift this plan eliminates.)

Then verify the existing assumed-clean findings:

6. Confirm `reportcli.go` line ~70 switch on `shape` covers `shapeUnknown` (current line 75 — `case shapeUnknown:` is already present per pre-plan verification). Run `golangci-lint run --enable-only=exhaustive ./internal/visbaseline/...` to confirm. If any exhaustive finding remains, add the missing case with `return fmt.Errorf("BuildReport: shape not detected ...")` (matching the existing `case shapeUnknown:` semantics already at line 75).

7. Confirm `reportcli.go` no longer carries any STALE / UNUSED `//nolint:gosec` directives. After the rewordings above, run `golangci-lint run --enable-only=nolintlint ./internal/visbaseline/...`. Any unused-nolint finding must be resolved by either restoring the suppressed code path's actual lint trigger OR removing the directive. Per D-03, removal is the correct fix for stale directives.

DO NOT touch any other line in either file. The package is shipped tooling — touch only what's needed per CONTEXT.md "Out of scope: Refactoring `internal/visbaseline` beyond the lint findings".

DO NOT change comment prose elsewhere in the file (the human-readable rationale comments above each call site like `// anonPath is derived from cfg.AnonDir ...` stay as-is — they remain accurate).

DO NOT use `--` in place of the em-dash; the canonical text uses U+2014 `—`.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; golangci-lint run ./internal/visbaseline/... &amp;&amp; go test -count=1 ./internal/visbaseline/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'visbaseline is a CLI tool — paths are operator-supplied by contract' internal/visbaseline/redactcli.go` returns at least `2` (sites at lines ~99 and ~105)
    - `grep -c 'visbaseline is a CLI tool — paths are operator-supplied by contract' internal/visbaseline/reportcli.go` returns at least `2` (sites at lines ~426 and ~442)
    - `grep -c 'visbaseline is a CLI tool — paths are operator-supplied by contract' internal/visbaseline/checkpoint.go` returns at least `1` (site at line ~117)
    - `grep -c 'G304: path derived from CLI caller' internal/visbaseline/redactcli.go` returns `0` (vague wording removed)
    - `grep -c 'G304: path derived from CLI caller' internal/visbaseline/reportcli.go` returns `0`
    - `grep -c 'G304: path is a deliberate CLI input' internal/visbaseline/checkpoint.go` returns `0` (or whatever the current vague wording was — confirmed during read)
    - `grep -c 'filepath.Clean(anonPath)' internal/visbaseline/redactcli.go` returns at least `1`
    - `grep -c 'filepath.Clean(jsonPath)' internal/visbaseline/reportcli.go` returns at least `1`
    - `grep -c 'filepath.Clean(p)' internal/visbaseline/reportcli.go` returns at least `1`
    - `golangci-lint run ./internal/visbaseline/...` outputs the literal string `0 issues.`
    - `go vet ./internal/visbaseline/...` exits 0
    - `go test -count=1 ./internal/visbaseline/...` exits 0
  </acceptance_criteria>
  <done>All five visbaseline G304 call sites carry filepath.Clean + canonical nolint reason; package lint clean; tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm whole-repo lint stays green</name>
  <files></files>
  <read_first>
    - .golangci.yml (confirm linter set hasn't changed unexpectedly during execution)
  </read_first>
  <action>
Run the whole-repo lint to verify Task 1's edits did not regress any other package (e.g. an em-dash encoding mishap, an unused import accidentally added):

```bash
cd /home/dotwaffle/Code/pdb/peeringdb-plus && golangci-lint run ./...
```

Expected output: the literal string `0 issues.` on the last line.

If any new finding appears, treat it as a Task 1 regression — do NOT add new nolint directives to silence it. Fix at the source per the same disposition philosophy as Task 1 (filepath.Clean if path-related; proper resolution otherwise).

Then run the full test suite scoped to the affected package + its callers to confirm no behavioral regression:

```bash
cd /home/dotwaffle/Code/pdb/peeringdb-plus && go test -count=1 -race ./internal/visbaseline/... ./cmd/...
```

The `cmd/...` scope catches any visbaseline CLI tool callers (the package is consumed by the visbaseline CLI commands; if any of those import the changed functions, race-mode test of cmd/ surfaces breakage).
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; golangci-lint run ./... &amp;&amp; go test -count=1 -race ./internal/visbaseline/... ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `golangci-lint run ./...` outputs `0 issues.` as the final line
    - `go test -count=1 -race ./internal/visbaseline/...` exits 0
    - `go test -count=1 -race ./cmd/...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>Whole-repo lint and tests stay green after Task 1 edits — no regression elsewhere; CI drift gate will pass.</done>
</task>

</tasks>

<verification>
- `golangci-lint run ./internal/visbaseline/...` returns `0 issues.`
- `golangci-lint run ./...` returns `0 issues.` (whole-repo)
- `go test -count=1 -race ./internal/visbaseline/...` passes
- `go vet ./...` clean
- All three CONTEXT.md D-03 dispositions satisfied:
  - exhaustive shapeUnknown: confirmed present (no edit needed in current source)
  - gosec G304 × 3 (×5 with the auth-side and checkpoint.go symmetry sites): filepath.Clean + canonical nolint applied
  - nolintlint stale directive: confirmed not present in current source (no edit needed)
</verification>

<success_criteria>
- TEST-03 fully closed per Phase 74 D-03: every G304 nolint in the package uses canonical reason text + has filepath.Clean as defense-in-depth against `..` traversal
- Future readers see uniform, honest documentation of why visbaseline suppresses G304
- Whole-repo CI drift + lint gates remain green
</success_criteria>

<output>
After completion, create `.planning/phases/74-test-ci-debt/74-03-SUMMARY.md` summarising:
- The five call sites updated (the three from CONTEXT.md plan-hints + the two symmetry sites)
- Confirmation that the exhaustive + nolintlint findings cited in plan-hints were already resolved in current source
- Final lint output (whole-repo) for the record
- Any additional G304 sites discovered that were not in the plan-hints (with disposition)
</output>
</content>
</invoke>