---
phase: 68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/sync/worker.go
  - cmd/peeringdb-plus/main.go
  - docs/TESTING.md
  - docs/CONFIGURATION.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - STATUS-05
must_haves:
  truths:
    - "Setting PDBPLUS_INCLUDE_DELETED at startup emits a slog.Warn naming the variable as deprecated and does not fail Load()"
    - "Config.IncludeDeleted and WorkerConfig.IncludeDeleted no longer exist; package compiles without them"
    - "syncIncremental[E] has no includeDeleted parameter; dispatchScratchChunk no longer reads w.config.IncludeDeleted"
    - "filterByStatus helper and its test file are removed from internal/sync"
    - "docs/CONFIGURATION.md, README.md, CLAUDE.md, docs/TESTING.md no longer reference PDBPLUS_INCLUDE_DELETED as active config"
  artifacts:
    - path: "internal/config/config.go"
      provides: "Deprecation WARN for PDBPLUS_INCLUDE_DELETED; no IncludeDeleted field on Config"
      contains: "PDBPLUS_INCLUDE_DELETED is deprecated"
    - path: "internal/config/config_test.go"
      provides: "TestLoad_IncludeDeleted_Deprecated with slog capture"
      contains: "TestLoad_IncludeDeleted_Deprecated"
    - path: "internal/sync/worker.go"
      provides: "syncIncremental without includeDeleted; dispatchScratchChunk without w.config.IncludeDeleted; WorkerConfig without IncludeDeleted field"
    - path: "cmd/peeringdb-plus/main.go"
      provides: "NewWorker call without IncludeDeleted field"
  key_links:
    - from: "internal/config/config.go Load()"
      to: "slog.Warn default logger"
      via: "package-level slog.Warn call"
      pattern: "slog\\.Warn.*PDBPLUS_INCLUDE_DELETED"
    - from: "internal/sync/worker.go dispatchScratchChunk"
      to: "syncIncremental[E]"
      via: "13 case branches, no includeDeleted arg"
      pattern: "return syncIncremental\\(ctx, tx, syncIncrementalInput"
---

<objective>
Remove the `PDBPLUS_INCLUDE_DELETED` wiring across `internal/config`, `internal/sync`, and `cmd/peeringdb-plus`, replacing it with a startup deprecation WARN. After this plan, deleted upstream rows flow through the upsert path unconditionally, but the sync delete pass STILL hard-deletes (soft-delete flip is Plan 68-02). This is a safe intermediate state: hard-deleting a row on cycle N is equivalent to never having upserted it, so net behaviour is unchanged from today for the one-cycle window before 68-02 lands.

Purpose: Establishes the pre-conditions for 68-02 (soft-delete flip) and 68-03 (status matrix) without behavioural regression. Delivers STATUS-05 per D-01 — the env var is gone from Config; sync no longer cares about it.

Output: `internal/config/config.go` without `IncludeDeleted` field, with a WARN-and-ignore deprecation block. `internal/sync/worker.go` `WorkerConfig` without `IncludeDeleted` field and `syncIncremental[E]` without the `includeDeleted` parameter. `filterByStatus` helper and its 244-line test file deleted. All test fixtures stripped of `IncludeDeleted: ...` initializers. Docs table entries removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-status-since-matrix/CONTEXT.md
@.planning/phases/68-status-since-matrix/68-RESEARCH.md
@.planning/phases/68-status-since-matrix/68-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Key signatures the executor needs. Do NOT re-read to discover these. -->

Current `internal/config/config.go:179-183` — REMOVE:
```go
includeDeleted, err := parseBool("PDBPLUS_INCLUDE_DELETED", true)
if err != nil {
    return nil, fmt.Errorf("parsing PDBPLUS_INCLUDE_DELETED: %w", err)
}
cfg.IncludeDeleted = includeDeleted
```

Current `internal/config/config.go:66-67` — REMOVE the field from the Config struct:
```go
// IncludeDeleted controls whether objects with status=deleted are synced.
IncludeDeleted bool
```

Current `internal/sync/worker.go:50` — REMOVE the field from WorkerConfig:
```go
type WorkerConfig struct {
    IncludeDeleted bool        // <-- DELETE
    IsPrimary      func() bool
    ...
}
```

Current `internal/sync/worker.go:911-919` — REMOVE the entire generic helper:
```go
func filterByStatus[E any](items []E, getStatus func(E) string) []E {
    ...
}
```

Current `internal/sync/worker.go:969-998` — REMOVE the `includeDeleted bool` parameter and the `if !includeDeleted { items = filterByStatus(...) }` branch inside `syncIncremental[E]`.

Current `internal/sync/worker.go:1021-1199` — in `dispatchScratchChunk`, REMOVE line `includeDeleted := w.config.IncludeDeleted` and strip the trailing `includeDeleted` arg from all 13 `syncIncremental(...)` calls. Keep the `getStatus` field on the `syncIncrementalInput[E]` literal untouched in THIS plan (the field becomes unused but removing it ripples the struct; defer to a follow-up if desired — research allows either).

Current `cmd/peeringdb-plus/main.go:233`:
```go
IncludeDeleted: cfg.IncludeDeleted,    // <-- REMOVE line
```

Current `docs/TESTING.md:183` and `.planning/quick/260324-lc5-*/*.md` (planning docs — leave planning artefacts alone). ONLY update `docs/TESTING.md`.

`TestSyncIncludeDeleted` in `internal/sync/integration_test.go:363-408` — do NOT rewrite here; Plan 68-02 handles the semantic flip to `TestSync_SoftDeleteMarksRows`. In THIS plan, remove only the `IncludeDeleted: true` literal on line 379 and update the comment so the file still compiles. The test body keeps asserting 3 orgs (now the default behaviour) — rename to `TestSyncPersistsDeletedRowsUnconditional` and change the assertion comment to reflect "default behaviour persists status=deleted rows". Plan 68-02 will replace this test entirely with the soft-delete flip test.

Pattern for slog capture in tests (NEW in this package — stdlib):
```go
var buf bytes.Buffer
prev := slog.Default()
slog.SetDefault(slog.New(slog.NewTextHandler(&buf, &slog.HandlerOptions{Level: slog.LevelWarn})))
t.Cleanup(func() { slog.SetDefault(prev) })
// ... run Load() ...
if !strings.Contains(buf.String(), "PDBPLUS_INCLUDE_DELETED is deprecated") {
    t.Fatalf("expected deprecation WARN; got %q", buf.String())
}
```

Subtests must NOT use `t.Parallel()` — `slog.SetDefault` is process-global.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove IncludeDeleted from Config + emit deprecation WARN</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go (lines 60-220 for struct + Load() body)
    - internal/config/config_test.go (lines 220-280 for existing TestLoad_IncludeDeleted)
    - .planning/phases/68-status-since-matrix/68-PATTERNS.md § `internal/config/config.go` — deprecation WARN (config-struct) AND § `internal/config/config_test.go` — TestLoad_IncludeDeleted_Deprecated
  </read_first>
  <action>
1. Edit `internal/config/config.go`:
   - DELETE the `IncludeDeleted bool` field and its comment from the `Config` struct (lines 66-67).
   - In `Load()`, REPLACE the block at lines 179-183:
     ```go
     includeDeleted, err := parseBool("PDBPLUS_INCLUDE_DELETED", true)
     if err != nil {
         return nil, fmt.Errorf("parsing PDBPLUS_INCLUDE_DELETED: %w", err)
     }
     cfg.IncludeDeleted = includeDeleted
     ```
     with:
     ```go
     // PDBPLUS_INCLUDE_DELETED was removed in v1.16 Phase 68 (D-01). Sync now
     // always persists deleted rows as tombstones (Phase 68 Plan 02 lands the
     // soft-delete flip); pdbcompat applies the upstream status × since matrix
     // regardless of any legacy gate. During the v1.16 → v1.17 grace period,
     // the variable is logged and ignored. Flipping to fail-fast in v1.17 is a
     // one-line swap (slog.Warn → return nil, errors.New(...)).
     if v := os.Getenv("PDBPLUS_INCLUDE_DELETED"); v != "" {
         slog.Warn("PDBPLUS_INCLUDE_DELETED is deprecated and ignored; remove it from your environment. This will be a startup error in v1.17.",
             slog.String("value", v),
         )
     }
     ```
   - Add `"log/slog"` to the import block if not already present.
   - Do NOT remove `parseBool` — it is still used by `PDBPLUS_CSP_ENFORCE` (grep confirms).

2. Edit `internal/config/config_test.go`:
   - REPLACE the entire `TestLoad_IncludeDeleted` (lines 235-274) with `TestLoad_IncludeDeleted_Deprecated` following the exact shape shown in 68-PATTERNS.md § `TestLoad_IncludeDeleted_Deprecated`. The test has TWO subtests (`env_set_warns` and `env_unset_no_warn`); neither calls `t.Parallel()` because `slog.SetDefault` is process-global.
   - The new test must assert:
     - When `PDBPLUS_INCLUDE_DELETED=true` is set, `Load()` returns no error AND the log buffer contains the string `"PDBPLUS_INCLUDE_DELETED is deprecated"`.
     - When the env var is unset, `Load()` returns no error AND the log buffer does NOT contain `"PDBPLUS_INCLUDE_DELETED"`.
   - Add `"bytes"`, `"log/slog"`, `"strings"` imports as needed (strings is already present per research).
   - Do NOT reference `cfg.IncludeDeleted` — the field is gone.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test ./internal/config -race -count=1 -run 'TestLoad_IncludeDeleted_Deprecated|TestLoad$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "IncludeDeleted bool" internal/config/config.go` returns NO matches.
    - `grep -n "IncludeDeleted" internal/config/config.go` returns NO matches.
    - `grep -n "cfg.IncludeDeleted" internal/config/` returns NO matches.
    - `grep -n "TestLoad_IncludeDeleted_Deprecated" internal/config/config_test.go` returns at least one match.
    - `grep -n "PDBPLUS_INCLUDE_DELETED is deprecated" internal/config/config.go` returns exactly one match.
    - `go test ./internal/config -race -count=1 -run 'TestLoad_IncludeDeleted_Deprecated'` passes both subtests.
    - `go build ./internal/config/...` succeeds.
  </acceptance_criteria>
  <done>
    Config package compiles and tests pass; setting the env var triggers a WARN and Load() returns nil error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove IncludeDeleted wiring from sync worker + delete filterByStatus helper</name>
  <files>internal/sync/worker.go, internal/sync/filter_test.go, cmd/peeringdb-plus/main.go</files>
  <read_first>
    - internal/sync/worker.go (lines 40-80 for WorkerConfig; lines 900-1000 for filterByStatus + syncIncremental; lines 1020-1200 for dispatchScratchChunk)
    - internal/sync/filter_test.go (full file — being deleted)
    - cmd/peeringdb-plus/main.go (lines 225-245 for NewWorker call)
    - .planning/phases/68-status-since-matrix/68-PATTERNS.md § `internal/sync/worker.go` — three related edits
  </read_first>
  <action>
1. Edit `internal/sync/worker.go`:
   - DELETE the `IncludeDeleted bool` field (line 50) from `WorkerConfig`. Keep the surrounding fields untouched.
   - DELETE the entire `filterByStatus[E any]` generic helper at lines 911-919. Its only caller is the `if !includeDeleted` branch about to be removed.
   - In `syncIncremental[E]` (line 969), REMOVE the `includeDeleted bool` parameter. Update the function signature to:
     ```go
     func syncIncremental[E any](ctx context.Context, tx *ent.Tx, in syncIncrementalInput[E], rows []scratchRow) (int, error) {
     ```
   - In `syncIncremental[E]` body, DELETE the entire branch:
     ```go
     if !includeDeleted {
         items = filterByStatus(items, in.getStatus)
     }
     ```
   - Leave `in.fkFilter` branch and all other logic untouched.
   - In `dispatchScratchChunk` (line 1021), DELETE line `includeDeleted := w.config.IncludeDeleted`. In each of the 13 `return syncIncremental(ctx, tx, syncIncrementalInput[...]{...}, rows, includeDeleted)` call sites (~lines 1030, 1046, 1062, 1075, 1088, 1101, 1114, 1127, 1140, 1154, 1167, 1180, 1195 — verify exact by grepping `syncIncremental(`), drop the trailing `, includeDeleted` argument. Result per call:
     ```go
     return syncIncremental(ctx, tx, syncIncrementalInput[peeringdb.Organization]{...}, rows)
     ```
   - Do NOT remove `getStatus` from `syncIncrementalInput[E]` in this plan — keeping it preserves a defensive accessor and avoids a 13-site refactor that Plan 68-02 does not require. Research OK with either; choose keep-in-place.

2. DELETE `internal/sync/filter_test.go` entirely (244 lines). It references `filterByStatus` which no longer exists. The assertion at line 244 (`t.Fatalf("filter.go must not exist ...")`) is satisfied by the file's own removal.

3. Edit `cmd/peeringdb-plus/main.go`:
   - At line 233 (inside the `sync.NewWorker(..., sync.WorkerConfig{...}, ...)` call), DELETE the line `IncludeDeleted: cfg.IncludeDeleted,`.

4. Verify no stray references:
   - `grep -rn "IncludeDeleted" internal/sync/ cmd/peeringdb-plus/` should return ONLY hits inside test fixtures (handled in Task 3) — zero hits in non-test files.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go build ./internal/sync/... ./cmd/peeringdb-plus/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "IncludeDeleted bool" internal/sync/worker.go` returns NO matches.
    - `grep -n "filterByStatus" internal/sync/worker.go` returns NO matches.
    - `grep -n "includeDeleted" internal/sync/worker.go` returns NO matches (check lowercase local var too).
    - `grep -rn "filterByStatus" internal/sync/` returns NO matches outside of removed files.
    - `test -f internal/sync/filter_test.go && echo EXISTS || echo ABSENT` prints `ABSENT`.
    - `grep -n "IncludeDeleted" cmd/peeringdb-plus/main.go` returns NO matches.
    - `go build ./internal/sync/... ./cmd/peeringdb-plus/...` succeeds without errors.
    - `go vet ./internal/sync/...` passes.
  </acceptance_criteria>
  <done>
    Sync package compiles; the generic filterByStatus helper and its tests are gone; `syncIncremental[E]` no longer has the `includeDeleted` gate; main.go NewWorker call site is clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip IncludeDeleted fixtures from sync test files + intermediate rename</name>
  <files>internal/sync/integration_test.go, internal/sync/worker_test.go, internal/sync/nokey_sync_test.go, internal/sync/replay_snapshot_test.go, internal/sync/worker_bench_test.go, docs/TESTING.md, docs/CONFIGURATION.md, README.md, CLAUDE.md</files>
  <read_first>
    - internal/sync/integration_test.go (lines 120-145, 225-240, 360-410, 420-435, 565-580 for IncludeDeleted literals)
    - internal/sync/worker_test.go (search: `grep -n "IncludeDeleted" internal/sync/worker_test.go`)
    - internal/sync/nokey_sync_test.go (line ~174)
    - internal/sync/replay_snapshot_test.go (line ~71)
    - internal/sync/worker_bench_test.go (line ~627)
    - docs/TESTING.md (line 183)
    - docs/CONFIGURATION.md (lines 40-50 for env var table entry)
    - README.md (line ~83)
    - CLAUDE.md (line ~133 for env var row)
  </read_first>
  <action>
1. Go test files — `grep -rn "IncludeDeleted" internal/sync/` and for EACH match, DELETE the `IncludeDeleted: true,` or `IncludeDeleted: false,` line from the `sync.WorkerConfig{...}` struct literal. Do NOT edit logic, only remove the field initializer. After this pass, all test WorkerConfig literals use the field-less defaults.

2. `internal/sync/integration_test.go` — the `TestSyncIncludeDeleted` function at lines 363-408 requires TWO edits in this plan (full semantic flip is Plan 68-02):
   - RENAME the function to `TestSyncPersistsDeletedRowsUnconditional`.
   - UPDATE the leading comment to: `// TestSyncPersistsDeletedRowsUnconditional verifies that deleted upstream rows are persisted by default after Phase 68 D-01 (PDBPLUS_INCLUDE_DELETED removed). Plan 68-02 rewrites this to TestSync_SoftDeleteMarksRows once the soft-delete flip lands.`
   - The body already asserts 3 orgs present with org 3 `status="deleted"`. After the `IncludeDeleted: true` line is stripped (per step 1), that becomes the default behaviour — the assertions still pass because the fixture still serves 3 orgs with org 3 as `status=deleted`.
   - Update the inline comment at line 388 from `// With IncludeDeleted=true, all 3 orgs should be present.` to `// With PDBPLUS_INCLUDE_DELETED removed (Phase 68 D-01), all 3 orgs are persisted by default.`
   - Update the error message at line 394 from `"expected 3 orgs with IncludeDeleted=true, got %d"` to `"expected 3 orgs (deleted persisted by default), got %d"`.

3. `docs/TESTING.md` line 183 — this is a doc example showing `IncludeDeleted: false,` in a WorkerConfig literal. Remove that line from the example. Leave surrounding context intact.

4. `docs/CONFIGURATION.md` lines 40-50:
   - DELETE the `PDBPLUS_INCLUDE_DELETED` row from the "Sync Worker" env var table.
   - ADD a new "### Removed in v1.16" section (after the active env var tables, before any "Legacy" or "Deprecated" sections — if none, append at end of "Environment Variables" area):
     ```markdown
     ### Removed in v1.16

     | Variable | Removed in | Replacement | Migration |
     |----------|------------|-------------|-----------|
     | `PDBPLUS_INCLUDE_DELETED` | v1.16 (Phase 68, D-01) | None — deleted rows are always persisted as tombstones (soft-delete, Plan 68-02) and exposed via `?since=N` / pk lookup per upstream `rest.py:694-727` status × since matrix. | Remove from your environment. During the v1.16 → v1.17 grace period a startup WARN is emitted; v1.17 upgrades this to a fatal startup error. |
     ```

5. `README.md` (~line 83) — DELETE the `PDBPLUS_INCLUDE_DELETED` table row entirely. Leave surrounding rows intact.

6. `CLAUDE.md` (~line 133, inside the env var table under "Environment Variables") — DELETE the `PDBPLUS_INCLUDE_DELETED` row.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && TMPDIR=/tmp/claude-1000 go test ./internal/sync -race -count=1 -run 'TestSyncPersistsDeletedRowsUnconditional'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "IncludeDeleted" internal/sync/` returns NO matches.
    - `grep -n "TestSyncIncludeDeleted" internal/sync/integration_test.go` returns NO matches.
    - `grep -n "TestSyncPersistsDeletedRowsUnconditional" internal/sync/integration_test.go` returns exactly one match.
    - `grep -n "PDBPLUS_INCLUDE_DELETED" README.md CLAUDE.md docs/CONFIGURATION.md docs/TESTING.md` returns matches ONLY in docs/CONFIGURATION.md's "Removed in v1.16" block (at least one match, no active-variable table row).
    - `grep -n "Removed in v1.16" docs/CONFIGURATION.md` returns at least one match.
    - `go test ./internal/sync -race -count=1 -run 'TestSyncPersistsDeletedRowsUnconditional'` passes.
    - `go test -race ./...` passes (full suite green; sync soft-delete flip is Plan 68-02, but pre-flip the delete pass still hard-deletes and the assertion set is unchanged from pre-phase except the renamed test).
  </acceptance_criteria>
  <done>
    All test fixtures compile without `IncludeDeleted`; the renamed integration test passes; docs no longer list the env var as active; grace-period note is recorded in `docs/CONFIGURATION.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→process env | Operator may still set `PDBPLUS_INCLUDE_DELETED` — we MUST log it and continue, not crash. |
| fixture→test | Test WorkerConfig literals previously controlled sync filtering; post-plan they have no knob. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | Tampering | `internal/config/config.go` env var parsing | mitigate | Deprecation WARN is informational only; Load() still returns a valid Config and never errors on the legacy variable. v1.17 flip is a one-line change, documented in the replacement comment. |
| T-68-01-02 | Denial of Service | startup path | accept | Operator setting the old variable does not fail startup; WARN-and-ignore is intentional (CONTEXT.md D-01). Operators on hot-standby upgrades are not locked out by config drift. |
| T-68-01-03 | Information disclosure | slog.Warn output | accept | `slog.String("value", v)` logs the env var's raw string value. The variable is a boolean flag (expected "true"/"false") with no PII or credential content. Logging is safe. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test -race ./internal/config ./internal/sync` passes.
- `go vet ./...` passes.
- `golangci-lint run` passes (no unused imports from removed code).
- No non-test file contains the identifier `IncludeDeleted` (grep clean).
</verification>

<success_criteria>
- STATUS-05 delivered at the config-side: `PDBPLUS_INCLUDE_DELETED` is no longer a knob.
- Sync package no longer branches on `IncludeDeleted`; deleted upstream rows will be upserted whenever the upsert path runs.
- Net behaviour is unchanged from today for the one-cycle window before Plan 68-02 ships: hard-delete pass still removes rows, so there is no observable regression.
- Intermediate test (`TestSyncPersistsDeletedRowsUnconditional`) documents the behavioural expectation for code review, then Plan 68-02 replaces it.
</success_criteria>

<output>
After completion, create `.planning/phases/68-status-since-matrix/68-01-SUMMARY.md` following the standard summary template.
</output>
