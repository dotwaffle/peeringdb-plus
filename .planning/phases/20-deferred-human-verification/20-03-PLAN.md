---
phase: 20-deferred-human-verification
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md
autonomous: false
requirements:
  - VFY-08
  - VFY-09
must_haves:
  truths:
    - "ASN comparison shows shared IXPs/facilities between two networks"
    - "View toggle switches between shared-only and full side-by-side display"
    - "Compare with... button on network detail page navigates to comparison form"
    - "Dark mode toggle works and persists across page reload"
    - "Keyboard navigation moves focus through search results"
    - "CSS animations render smoothly on search results"
    - "Loading indicators appear during htmx requests"
    - "404 error page displays styled page with search box"
    - "About page shows data freshness indicator"
  artifacts:
    - path: ".planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md"
      provides: "Complete verification report with all 26 items having pass/fail/blocked status"
      contains: "VFY-09"
  key_links:
    - from: "/ui/compare/13335/15169"
      to: "comparison results"
      via: "compare handler"
      pattern: "shared IXPs"
    - from: "dark mode toggle"
      to: "localStorage"
      via: "darkMode key"
      pattern: "class dark"
---

<objective>
Verify ASN comparison (VFY-08) and polish items (VFY-09) against the live Fly.io deployment in Chrome browser. Finalize the complete verification report with summary counts.

Purpose: Complete the final verification items covering comparison workflow and UI polish (dark mode, keyboard nav, animations, error pages, about page). Produce the final verification report with all 26 items accounted for.
Output: Completed `20-VERIFICATION-ITEMS.md` with all VFY-01 through VFY-09 filled in and summary counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-deferred-human-verification/20-CONTEXT.md
@.planning/phases/20-deferred-human-verification/20-RESEARCH.md
@.planning/phases/20-deferred-human-verification/20-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser verification of comparison and polish (VFY-08, VFY-09)</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <action>
CHECKPOINT: Human verifies comparison and polish features in Chrome browser against live deployment.

Plans 01 and 02 verified infrastructure (CI, API key) and core UI (foundation, search, detail pages). The remaining items are comparison workflow and polish features.

**All verification must target: https://peeringdb-plus.fly.dev/**
**Use Chrome only (per D-03).**

**VFY-08: ASN Comparison (3 sub-items)**

8a. Multi-step compare flow:
   - Navigate to https://peeringdb-plus.fly.dev/ui/asn/13335 (Cloudflare)
   - Find and click the "Compare with..." button
   - Verify: navigates to /ui/compare with ASN 13335 pre-filled (or /ui/compare/13335)
   - Report: PASS/FAIL

8b. Comparison results layout:
   - Enter second ASN: 15169 (Google) and submit
   - Verify: results page at /ui/compare/13335/15169 shows shared IXPs and/or facilities
   - Verify: results are readable with clear labels
   - Report: PASS/FAIL with description of what you see

8c. View toggle (shared-only vs full):
   - On the comparison results page, find the view toggle
   - Click "Full View" (or equivalent toggle)
   - Verify: URL changes to include ?view=full and all items display (not just shared)
   - Click "Shared Only" (or equivalent)
   - Verify: returns to shared-only view
   - Report: PASS/FAIL

**VFY-09: Polish (7 sub-items)**

9a. Dark mode toggle and system preference (per Pitfall #6):
   - First: Open DevTools Console and run localStorage.removeItem('darkMode') then reload
   - Verify: page respects system preference (if system is dark, page should be dark; if light, should be light)
   - Click the dark mode toggle (sun/moon icon in nav)
   - Verify: theme switches
   - Reload the page
   - Verify: chosen theme persists (localStorage)
   - Report: PASS/FAIL for each step

9b. Keyboard navigation of search results:
   - Navigate to /ui/ and type a search query (e.g., "cloud")
   - Wait for results to appear
   - Press Down Arrow key, verify focus moves to first result
   - Press Down Arrow again, verify focus moves to second result
   - Press Enter on a focused result, verify navigation to detail page
   - Report: PASS/FAIL

9c. CSS animations (fadeIn on search results):
   - Navigate to /ui/ and type a search query
   - Observe: do results fade in smoothly (not just appear abruptly)?
   - Look for the animate-fade-in CSS class in DevTools Elements panel
   - Report: PASS/FAIL

9d. Loading indicators (htmx loading bar):
   - On any page that uses htmx (e.g., expand a collapsible section on a detail page)
   - Watch for a green loading bar at the top of the page during the request
   - Optional: throttle network in DevTools (Network then Slow 3G) to make the indicator more visible
   - Report: PASS/FAIL

9e. Styled 404 error page:
   - Navigate to https://peeringdb-plus.fly.dev/ui/nonexistent-page-xyz
   - Verify: styled 404 page appears (not a raw text error)
   - Verify: page includes a search box and/or "Back to home" link
   - Report: PASS/FAIL with description

9f. Styled 500 error page:
   - Cannot be triggered safely in production (per research Open Question #2)
   - Mark as BLOCKED with justification: "Cannot trigger 500 safely against production. Code review confirms ServerErrorPage template exists with red 500 styling and home link."
   - Report: BLOCKED

9g. About page data freshness:
   - Navigate to https://peeringdb-plus.fly.dev/ui/about
   - Verify: page shows "Last synced: [timestamp]" or similar freshness indicator
   - Verify: page shows "[X] ago" relative time
   - Verify: API surface links are present
   - Report: PASS/FAIL with description

Per D-14: If any item FAILS, describe the failure so it can be fixed inline.

Resume signal: Report results for each sub-item (8a, 8b, 8c, 9a, 9b, 9c, 9d, 9e, 9f, 9g) as PASS/FAIL/BLOCKED with brief observations.
  </action>
  <verify>
    <automated>grep -c "VFY-08\|VFY-09" .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md 2>/dev/null</automated>
  </verify>
  <done>VFY-08 and VFY-09 results recorded in verification report with PASS/FAIL/BLOCKED status and observations for all sub-items</done>
</task>

<task type="auto">
  <name>Task 2: Finalize verification report with summary and fix any failures</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <read_first>
    - .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md
    - .planning/phases/20-deferred-human-verification/20-CONTEXT.md
  </read_first>
  <action>
Based on the human's verification results from Task 1:

1. **Record all VFY-08 and VFY-09 results** in 20-VERIFICATION-ITEMS.md with the exact observations reported by the human.

2. **Fix any failures inline** per D-14 and D-15:
   - For each FAIL item: diagnose the issue, implement a fix in the relevant source file
   - Commit each fix atomically with message: `fix(web): [what failed] -- [what was fixed]`
   - Re-verify the fix (run tests if code changed: `go test -race ./...`)
   - Update the verification report with the fix commit hash

3. **Finalize the summary section** at the top of the report:
   - Count total PASS, FAIL (fixed), FAIL (unfixed), and BLOCKED items
   - Add browser/OS information as reported by the human
   - Ensure all 26 items have a status

4. **Cross-check completeness:**
   - VFY-01: CI pipeline (from Plan 01)
   - VFY-02: Coverage comments (from Plan 01)
   - VFY-03: CLI API key (from Plan 01)
   - VFY-04: Integration test (from Plan 01)
   - VFY-05: Foundation (from Plan 02) -- 3 sub-items including BLOCKED syncing page
   - VFY-06: Search (from Plan 02) -- 3 sub-items
   - VFY-07: Detail pages (from Plan 02) -- 4 sub-items
   - VFY-08: Comparison (this plan) -- 3 sub-items
   - VFY-09: Polish (this plan) -- 7 sub-items including BLOCKED 500 page
   - Total should be 26 items across all groups

Per D-02: text only, no screenshots. Per SEC-2: no API key values in the report.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL\|BLOCKED" .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All 26 verification items have PASS, FAIL, or BLOCKED status
    - Summary section shows final counts
    - Any FAIL items have been fixed inline with atomic commits per D-15
    - Report is text-only per D-02 with no API key values per SEC-2
    - Browser/OS information is recorded per D-01
  </acceptance_criteria>
  <done>Complete verification report with all 26 items accounted for, summary counts finalized, and all fixable failures resolved</done>
</task>

</tasks>

<verification>
- 20-VERIFICATION-ITEMS.md contains all 26 items with status
- Summary section at top has accurate pass/fail/blocked counts
- BLOCKED items have clear justification (syncing page, 500 error page)
- Any inline fixes are committed per D-15 and test suite still passes
- `go test -race ./...` passes (if any code changes were made)
</verification>

<success_criteria>
- VFY-08: Compare flow works from detail page through results with view toggle
- VFY-09: Dark mode, keyboard nav, animations, loading indicators, 404 page, and about page all verified; 500 page verified by code review
- All 26 items have final status in the verification report
- Phase 20 deliverable (20-VERIFICATION-ITEMS.md) is complete and committed
</success_criteria>

<output>
After completion, create `.planning/phases/20-deferred-human-verification/20-03-SUMMARY.md`
</output>
