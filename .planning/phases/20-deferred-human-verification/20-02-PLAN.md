---
phase: 20-deferred-human-verification
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md
autonomous: false
requirements:
  - VFY-05
  - VFY-06
  - VFY-07
must_haves:
  truths:
    - "Content negotiation correctly returns HTML redirect for browsers and JSON for API clients"
    - "Responsive layout adapts correctly at mobile (375px), tablet (768px), and desktop (1024px+) breakpoints"
    - "Live search returns results within 300ms with colored type badges"
    - "ASN numeric input redirects to correct detail page on Enter"
    - "Detail page collapsible sections expand/collapse with lazy loading on first expand only"
    - "Summary stats are visible in detail page headers"
    - "Cross-links navigate correctly between record types"
  artifacts:
    - path: ".planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md"
      provides: "Updated verification report with VFY-05, VFY-06, VFY-07 results"
      contains: "VFY-05"
  key_links:
    - from: "curl -H Accept text/html"
      to: "https://peeringdb-plus.fly.dev/"
      via: "HTTP 302 redirect"
      pattern: "302 Location ui"
    - from: "search input"
      to: "/ui/search"
      via: "htmx hx-get with 300ms debounce"
      pattern: "search results appear"
---

<objective>
Verify web UI foundation, live search, and detail pages (VFY-05, VFY-06, VFY-07) against the live Fly.io deployment in Chrome browser. Run automated curl checks first, then guide human through browser verification.

Purpose: Confirm the core web UI flow works: content negotiation -> responsive layout -> search -> detail pages. Per D-05, these must pass before comparison and polish items can be verified.
Output: Updated `20-VERIFICATION-ITEMS.md` with VFY-05, VFY-06, VFY-07 results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-deferred-human-verification/20-CONTEXT.md
@.planning/phases/20-deferred-human-verification/20-RESEARCH.md
@.planning/phases/20-deferred-human-verification/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated content negotiation and endpoint checks (VFY-05 partial)</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <read_first>
    - .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md
    - .planning/phases/20-deferred-human-verification/20-RESEARCH.md
    - cmd/peeringdb-plus/main.go
    - internal/web/templates/layout.templ
  </read_first>
  <action>
Run automated checks that can be done without a browser and record results in the VFY-05 section of 20-VERIFICATION-ITEMS.md.

**Content negotiation (per research, already confirmed during research phase):**
1. `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" -H 'Accept: text/html' https://peeringdb-plus.fly.dev/`
   - Expected: 302 redirect to /ui/
2. `curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/`
   - Expected: 200 with JSON body (API discovery document)
3. `curl -s https://peeringdb-plus.fly.dev/ | head -5`
   - Expected: JSON object (not HTML)
4. `curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/ui/`
   - Expected: 200

**Endpoint availability checks:**
5. `curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/ui/about`
   - Expected: 200
6. `curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/ui/compare`
   - Expected: 200
7. `curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/ui/asn/13335`
   - Expected: 200

**Syncing page verification (per Pitfall #3 in research):**
The syncing page only shows before the first sync completes. The live deployment has already synced, so this cannot be verified non-destructively. Verify by code review instead:
- Read `cmd/peeringdb-plus/main.go` readiness middleware (lines ~296-321) to confirm the logic is correct
- Read `internal/web/templates/syncing.templ` to confirm the pulse animation template exists
- Mark this sub-item as BLOCKED with justification: "Requires fresh deployment with empty DB; verified correct by code review"

Update the VFY-05 section of 20-VERIFICATION-ITEMS.md with results from automated checks. Leave responsive layout sub-items as "PENDING -- requires browser verification in Task 2."
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://peeringdb-plus.fly.dev/ui/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Content negotiation curl tests recorded with HTTP status codes
    - Endpoint availability checks recorded
    - Syncing page marked BLOCKED with code review justification
    - VFY-05 section partially filled in 20-VERIFICATION-ITEMS.md
  </acceptance_criteria>
  <done>All automatable VFY-05 checks recorded in verification report; browser-dependent items marked pending</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of foundation, search, and detail pages (VFY-05, VFY-06, VFY-07)</name>
  <files>.planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md</files>
  <action>
CHECKPOINT: Human verifies UI in Chrome browser against live deployment.

Automated content negotiation checks are complete. The following items require Chrome browser verification against the live deployment per D-03 and D-04.

**All verification must target: https://peeringdb-plus.fly.dev/**
**Use Chrome only (per D-03). Use DevTools device toolbar for responsive testing (per D-04).**

**VFY-05: Foundation (3 sub-items)**

5a. Responsive layout at breakpoints (per D-04):
   - Open https://peeringdb-plus.fly.dev/ui/ in Chrome
   - Open DevTools (F12) then Toggle device toolbar (Ctrl+Shift+M)
   - Test at 375px width (mobile): Does nav collapse to hamburger menu? Do API cards stack vertically?
   - Test at 768px width (tablet): Does layout adapt? Is content readable?
   - Test at 1024px+ width (desktop): Are API cards in 3-column grid? Is nav fully expanded?
   - Report: PASS/FAIL for each breakpoint with brief description

5b. Neon green on dark theme:
   - Toggle dark mode (sun/moon icon in nav)
   - Verify accent colors (green/emerald) are visible and readable against dark background
   - Report: PASS/FAIL with description

5c. Syncing page: Already marked BLOCKED (code review verified in Task 1)

**VFY-06: Live Search (3 sub-items)**

6a. Search latency under 300ms:
   - Open https://peeringdb-plus.fly.dev/ui/
   - Open DevTools Network tab, filter by XHR
   - Type "cloudflare" in search box
   - Observe: results should appear after 300ms debounce + server response
   - Check Network tab: XHR request to /ui/search should complete quickly
   - Report: PASS/FAIL with observed timing

6b. Type badge colors:
   - In search results for "cloudflare", verify colored badges appear
   - Networks should show emerald/green badge, IXPs sky/blue, others distinct colors
   - Report: PASS/FAIL with description of what you see

6c. ASN redirect on Enter:
   - Clear search box, type "13335" (numeric ASN for Cloudflare), press Enter
   - Verify: browser navigates to /ui/asn/13335 showing Cloudflare network detail
   - Report: PASS/FAIL

**VFY-07: Detail Pages (4 sub-items)**

7a. Collapsible sections:
   - On /ui/asn/13335 (Cloudflare), click section headers to expand/collapse
   - Report: PASS/FAIL -- do sections expand/collapse smoothly?

7b. Lazy loading (first expand only):
   - Open DevTools Network tab, expand "IX Presences" section
   - Observe: network request fires (htmx GET to /ui/fragment/...)
   - Collapse and re-expand: verify NO second network request (htmx `once` trigger)
   - Report: PASS/FAIL

7c. Summary stats in header:
   - On /ui/asn/13335, look for stat badges near the top (e.g., "IXPs: N", "Facilities: N")
   - Report: PASS/FAIL with what stats you see

7d. Cross-links navigate between types:
   - In the expanded IX Presences list, click an IX name
   - Verify: navigates to /ui/ix/{id} showing that IX's detail page
   - Report: PASS/FAIL

Per D-14: If any item FAILS, describe the failure so it can be fixed inline before proceeding to Plan 03.

Resume signal: Report results for each sub-item (5a, 5b, 6a, 6b, 6c, 7a, 7b, 7c, 7d) as PASS/FAIL with brief observations, or describe failures that need fixing.
  </action>
  <verify>
    <automated>grep -c "VFY-05\|VFY-06\|VFY-07" .planning/phases/20-deferred-human-verification/20-VERIFICATION-ITEMS.md 2>/dev/null</automated>
  </verify>
  <done>VFY-05, VFY-06, and VFY-07 results recorded in verification report with PASS/FAIL/BLOCKED status and observations for all sub-items</done>
</task>

</tasks>

<verification>
- All VFY-05 sub-items have status in verification report (content negotiation, responsive layout, syncing page)
- All VFY-06 sub-items have status (search latency, badges, ASN redirect)
- All VFY-07 sub-items have status (collapsible, lazy load, stats, cross-links)
- Any failures fixed inline per D-14/D-15
- 20-VERIFICATION-ITEMS.md updated with all results
</verification>

<success_criteria>
- VFY-05: Content negotiation returns 302 for HTML Accept, 200 JSON otherwise; responsive layout works at 3 breakpoints; syncing page verified by code review
- VFY-06: Search results appear after typing with under 300ms server response; type badges show distinct colors; ASN redirect works on Enter
- VFY-07: Sections expand/collapse; lazy loading fires once only; stats visible in header; cross-links navigate correctly
</success_criteria>

<output>
After completion, create `.planning/phases/20-deferred-human-verification/20-02-SUMMARY.md`
</output>
