---
phase: 59-ent-privacy-policy-sync-bypass
verified: 2026-04-16T12:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 59: ent Privacy policy + sync bypass — Verification Report

**Phase Goal:** Install the read-path privacy floor — anonymous queries cannot return rows whose upstream visibility is `Users`-only, while the sync worker retains full read/write access.
**Verified:** 2026-04-16T12:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | `gen.FeaturePrivacy` enabled in `ent/entc.go` + `ent/privacy/privacy.go` generated | VERIFIED | `ent/entc.go:101` contains `entc.FeatureNames("sql/upsert", "sql/execquery", "privacy")`. `ent/privacy/privacy.go` exists (15896 bytes, header `// Code generated by ent, DO NOT EDIT.`) with `Allow`/`Deny`/`Skip` re-exports, `DecisionContext` at :44, and `PocQueryRuleFunc` typed adapter at :403–406. |
| 2 | `ent/schema/poc.go` has `Policy()` method using `privctx.TierFrom(ctx)` + NULL defense | VERIFIED | `ent/schema/poc.go:135` `func (Poc) Policy() ent.Policy` with `privctx.TierFrom(ctx) == privctx.TierUsers` early-skip (:139), and `q.Where(poc.Or(poc.VisibleEQ("Public"), poc.VisibleIsNil()))` (:142–145) for NULL-safe predicate per Pitfall 2. |
| 3 | `internal/sync/worker.go` `Sync(ctx)` has `privacy.DecisionContext(ctx, privacy.Allow)` as its first line | VERIFIED | `internal/sync/worker.go:259–261`: function opens, next statement is `ctx = privacy.DecisionContext(ctx, privacy.Allow) // VIS-05 bypass — sole call site (D-08/D-09)`, followed by `w.running.CompareAndSwap` CAS. Bypass precedes CAS, otel span, and any ent call as required. |
| 4 | `PDBPLUS_PUBLIC_TIER` env var parsed in `internal/config/config.go` with fail-fast validator | VERIFIED | `internal/config/config.go:98` `PublicTier privctx.Tier` field; `:188` `publicTier, err := parsePublicTier("PDBPLUS_PUBLIC_TIER", privctx.TierPublic)` in `Load()` with `%w` wrap; `:373` `func parsePublicTier(...)` is a strict switch: empty → default, `"public"` / `"users"` → Tier, anything else → `fmt.Errorf(... "must be 'public' or 'users'")`. `TestLoad_PublicTier*` tests pass. |
| 5 | `internal/middleware/privacy_tier.go` middleware wired in chain between Logging and Readiness | VERIFIED | `internal/middleware/privacy_tier.go:52` `func PrivacyTier(in PrivacyTierInput)` stamps `privctx.WithTier(r.Context(), tier)`. `cmd/peeringdb-plus/main.go:620` wraps `middleware.PrivacyTier(middleware.PrivacyTierInput{DefaultTier: cc.DefaultTier})(h)` between `readinessMiddleware` (:619) and `middleware.Logging` (:621). `chainConfig.DefaultTier` populated from `cfg.PublicTier` at :453. Regression-locked by `middleware_chain_test.go:63` which asserts `"middleware.PrivacyTier("` at its exact position in `wantOrder`. |
| 6 | D-15 end-to-end test `TestE2E_AnonymousCannotSeeUsersPoc` covering all 5 surfaces | VERIFIED | `cmd/peeringdb-plus/e2e_privacy_test.go:340` `TestE2E_AnonymousCannotSeeUsersPoc` and `:548` `TestE2E_PublicTierUsersAdmitsRow`. 19 `t.Run` sub-tests (TierPublic: pdbcompat detail+list, entrest detail+list, ConnectRPC Get+List, GraphQL node+list, UI contacts fragment; TierUsers: same positive assertions). Test uses real `buildMiddlewareChain` (`grep "buildMiddlewareChain"` → 3 matches) and seeds via `privacy.DecisionContext` (exempt from audit). Both tests pass under `-race`. |
| 7 | Single-bypass-site audit test passes (including the WR-01 broadening to catch aliasing) | VERIFIED | `internal/sync/bypass_audit_test.go:40` `bypassCallRE` for `privacy.DecisionContext(..., privacy.Allow)` call shape; `:54` `bypassRefRE` = `regexp.MustCompile(\`privacy\.Allow\b\`)` added per WR-01 to catch aliasing evasions (`allow := privacy.Allow`, `var bypass = privacy.Allow`). `assertSingleHit` closure (:153) asserts both regexes yield exactly 1 hit in `internal/sync/worker.go`. `scanDirs` extended from {internal, cmd, ent/schema} to include `graph/` per IN-01 bundled fix (:94). Test skips `*_test.go`, generated `ent/`, and `graph/generated.go`. Passes under `-race`. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `ent/entc.go` | `"privacy"` in FeatureNames | VERIFIED | Line 101 contains `entc.FeatureNames("sql/upsert", "sql/execquery", "privacy")` |
| `ent/privacy/privacy.go` | Generated privacy package with `PocQueryRuleFunc` + `DecisionContext` | VERIFIED | Generated file present (15896 bytes); both symbols exported |
| `ent/schema/poc.go` | `Policy()` method with NULL-safe predicate | VERIFIED | Policy at :135; `privacy.PocQueryRuleFunc`, `poc.VisibleEQ("Public")`, `poc.VisibleIsNil()`, `privctx.TierFrom`, `privctx.TierUsers` all present |
| `internal/privctx/privctx.go` | `Tier`, `WithTier`, `TierFrom` exported | VERIFIED | 53 lines, stdlib-only (imports `context`), `tierCtxKey struct{}` private key, `TierPublic = iota` (fail-safe default) |
| `internal/config/config.go` | `PublicTier` field + `parsePublicTier` helper | VERIFIED | Field at :98, helper at :373, wired in `Load()` at :188 |
| `internal/middleware/privacy_tier.go` | `PrivacyTier` factory + `PrivacyTierInput` | VERIFIED | 61 lines, uses `privctx.WithTier`, does NOT call `privacy.DecisionContext` (D-07) |
| `cmd/peeringdb-plus/main.go` | `middleware.PrivacyTier` wrapped between Readiness and Logging | VERIFIED | Wrap at :620; `chainConfig.DefaultTier` at :582; populated from `cfg.PublicTier` at :453 |
| `cmd/peeringdb-plus/middleware_chain_test.go` | Regression lock for PrivacyTier position | VERIFIED | `"middleware.PrivacyTier("` at :63 in `wantOrder` slice |
| `internal/sync/worker.go` | `privacy.DecisionContext(ctx, privacy.Allow)` as first statement of `Sync` | VERIFIED | :260, immediately after function opening brace and before CAS guard |
| `internal/sync/bypass_audit_test.go` | Single-call-site + WR-01 alias audit | VERIFIED | Two-regex audit with `scanDirs` including `graph/`; both regexes assert exactly-1 hit in `internal/sync/worker.go` |
| `cmd/peeringdb-plus/e2e_privacy_test.go` | D-15 end-to-end test covering all 5 surfaces × 2 tiers | VERIFIED | 2 top-level tests, 19 sub-tests; real `buildMiddlewareChain`; passes under `-race` |
| `internal/sync/policy_test.go` | VIS-04 behaviour tests | VERIFIED | 5 tests (FiltersUsersTier, AdmitsPublicToTierPublic, AdmitsAllToTierUsers, AdmitsNullVisibleToTierPublic, EdgeTraversalFilters); all green under `-race` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `cmd/peeringdb-plus/main.go buildMiddlewareChain` | `middleware.PrivacyTier` | wrap between readinessMiddleware and Logging | WIRED | :620 wrap line sits between :619 (`readinessMiddleware`) and :621 (`middleware.Logging`); `TestMiddlewareChain_Order` regression-locks |
| `middleware.PrivacyTier` | `privctx.WithTier` | request ctx stamp | WIRED | `privacy_tier.go:56` `ctx := privctx.WithTier(r.Context(), tier)` then `next.ServeHTTP(w, r.WithContext(ctx))` |
| `ent/schema/poc.go Policy` | `privctx.TierFrom` + `poc.Or(VisibleEQ, VisibleIsNil)` | query rule predicate | WIRED | :139 reads tier; :142–145 applies NULL-safe predicate |
| `internal/sync/worker.go Sync` | `ent/privacy DecisionContext` | first statement rebinds ctx | WIRED | :260 is first non-brace statement of `Sync`; audit test confirms sole call site |
| `internal/config.Load` | `parsePublicTier` | strict-switch validator | WIRED | :188 call site; error wrapped with `%w` so `main.go`'s `os.Exit(1)` fires fast on misconfig |
| `chainConfig.DefaultTier` | `cfg.PublicTier` | main populates from config | WIRED | :453 `DefaultTier: cfg.PublicTier` in buildMiddlewareChain construction |
| `e2e_privacy_test.go` | `buildMiddlewareChain` + `privacy.DecisionContext` | real chain + bypass seeding | WIRED | 3 `buildMiddlewareChain` references; 1 `privacy.DecisionContext` call (test-file exempt from audit) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `PrivacyTier` middleware | `tier` (stamped on ctx) | `in.DefaultTier` captured from `chainConfig.DefaultTier` ← `cfg.PublicTier` ← env `PDBPLUS_PUBLIC_TIER` | Yes — flows env → config → chainConfig → middleware → request ctx | FLOWING |
| POC Policy query rule | `ctx` tier + `q.Where(...)` | `privctx.TierFrom(ctx)` reads what middleware stamped; `q` is the materialised `*pdbent.PocQuery` from all 5 surfaces | Yes — E2E test empirically proves all 5 surfaces route through this rule | FLOWING |
| Sync worker bypass | `ctx` decision | `privacy.DecisionContext(ctx, privacy.Allow)` stamped on worker entry; every downstream ent call + child goroutine inherits via standard context parent-chain | Yes — `TestWorkerSync_BypassesPrivacy` + `TestWorkerSync_ChildGoroutineInheritsBypass` prove flow | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| privctx roundtrip + defaults | `go test -race -run 'TestTier' ./internal/privctx/...` | ok (all tests green) | PASS |
| Config PDBPLUS_PUBLIC_TIER parser | `go test -race -run 'TestLoad_PublicTier' ./internal/config/...` | ok | PASS |
| PrivacyTier middleware stamps ctx | `go test -race -run 'TestPrivacyTier' ./internal/middleware/...` | ok | PASS |
| Chain order locked | `go test -race -run 'TestMiddlewareChain_Order' ./cmd/peeringdb-plus/...` | ok | PASS |
| VIS-04 POC policy matrix | `go test -race -run 'TestPocPolicy' ./internal/sync/...` | ok | PASS |
| VIS-05 sync bypass behaviour | `go test -race -run 'TestWorkerSync_Bypass\|TestWorkerSync_Child\|TestWorkerSync_HasBypassCall' ./internal/sync/...` | ok | PASS |
| Single-call-site audit (incl. WR-01 alias regex) | `go test -race -run 'TestSyncBypass_SingleCallSite' ./internal/sync/...` | ok | PASS |
| D-15 end-to-end (5 surfaces × 2 tiers) | `go test -race -run 'TestE2E_' ./cmd/peeringdb-plus/...` | ok (19 sub-tests) | PASS |
| `go build ./...` | `go build ./...` | clean | PASS |
| `go vet ./...` | `go vet ./...` | clean | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| VIS-04 | 59-04 | ent Privacy query policy filters non-`Public` rows from anonymous responses on POC; loaded via `entgo.io/ent/privacy` and wired in `ent/entc.go` | SATISFIED | `ent/entc.go:101` enables `"privacy"`; `ent/schema/poc.go:135` installs `Policy()`; `internal/sync/policy_test.go` 5 tests green; E2E test confirms filtering on all 5 surfaces |
| VIS-05 | 59-05 | Sync worker bypasses the privacy policy via `privacy.DecisionContext(ctx, privacy.Allow)`; tests assert bypass active for sync-context goroutines and absent everywhere else | SATISFIED | `internal/sync/worker.go:260` sole call site; `TestWorkerSync_BypassesPrivacy`, `TestWorkerSync_BypassDoesNotLeak`, `TestWorkerSync_ChildGoroutineInheritsBypass` all green; `TestSyncBypass_SingleCallSite` enforces exactly-one production bypass |
| SYNC-03 | 59-01/02/03/06 | `PDBPLUS_PUBLIC_TIER` env var (default `public`, accepts `users`) elevates anonymous callers to Users-tier for private-instance deployments; tests cover both values | SATISFIED | `internal/config/config.go:98,188,373` parses env; `middleware.PrivacyTier` stamps tier; `TestE2E_PublicTierUsersAdmitsRow` proves TierUsers flip returns Users rows on all 5 surfaces; `TestE2E_AnonymousCannotSeeUsersPoc` proves default hides them |

No orphaned requirements detected — REQUIREMENTS.md maps VIS-04, VIS-05, SYNC-03 to Phase 59 and all three appear in plan frontmatter.

### Anti-Patterns Found

None. Anti-pattern scan of phase 59 key files (`internal/privctx/privctx.go`, `internal/middleware/privacy_tier.go`, `ent/schema/poc.go`, `internal/sync/worker.go` new bypass line, `internal/config/config.go` PublicTier path, `cmd/peeringdb-plus/main.go` DefaultTier wiring, `cmd/peeringdb-plus/e2e_privacy_test.go`) shows:

- No TODO/FIXME/XXX/HACK placeholder markers in new code paths.
- No empty `return nil`/`return {}` on policy-sensitive paths — the Policy rule returns `privacy.Skip` (correct ent dispatch semantics) after applying the predicate.
- No hardcoded empty data on render paths — all 19 E2E sub-tests receive real rows via the real chain.
- No orphan code — every new artifact is imported and exercised by tests.
- `TestWorkerSync_LineBudget` (pre-existing REFAC-03 invariant) reports Sync body at 100 lines = budget, preserving the line-budget contract.

### Human Verification Required

None. All seven must-haves are programmatically verifiable:

- File existence and content patterns verified via Grep/Read.
- Wiring verified via chain-order test + middleware test + E2E test.
- Behavioural contract verified via VIS-04 policy matrix + VIS-05 bypass tests + D-15 E2E across all 5 surfaces × 2 tiers.
- Audit invariant verified via `TestSyncBypass_SingleCallSite` including the WR-01 alias-reference regex (proven load-bearing by the REVIEW-FIX iteration).
- Full `go test -race -count=1 ./...` (subset of phase-59 test names) green; `go vet ./...` + `go build ./...` clean.

The phase 59 REVIEW already captured one warning (WR-01) and 3 deferred info-level findings (IN-02: CAS-vs-bypass ordering cosmetic; IN-03: `visible` filter exposure — design choice deferred; IN-04: CLAUDE.md doc drift — gated by separate workflow). The warning was fixed in REVIEW-FIX iteration 1 (commit `c10266c`) and is verified live in `internal/sync/bypass_audit_test.go:54` + `:94`. No open items block goal achievement.

### Gaps Summary

No gaps. Every must-have on the list resolves to verifiable artifacts + passing tests. The phase delivers on its goal: anonymous HTTP requests cannot read `visible="Users"` POC rows on any of the 5 surfaces (pdbcompat, entrest, ConnectRPC, GraphQL, UI) under default `PDBPLUS_PUBLIC_TIER=public`, flipping to `users` admits them, and the sync worker retains full read/write via a single audited bypass point that is regression-locked by a source-scanning audit test whose alias-evasion channel was closed in REVIEW-FIX iteration 1.

---

_Verified: 2026-04-16T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
