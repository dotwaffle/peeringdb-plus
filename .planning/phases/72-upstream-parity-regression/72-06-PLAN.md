---
phase: 72-upstream-parity-regression
plan: 06
type: execute
wave: 6
depends_on:
  - 72-05
files_modified:
  - docs/API.md
  - CHANGELOG.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - PARITY-01
  - PARITY-02
tags:
  - parity
  - docs
  - milestone-close
  - traceability

must_haves:
  truths:
    - "docs/API.md § Known Divergences is extended with the Phase 72-confirmed divergence entries (at minimum: limit=0 count-only-is-wrong, depth-on-list silent-drop per LIMIT-02, DEFER-70-verifier-01 fac-via-ixlan-ix-fac-count, DEFER-70-06-01 campus-table) — each with upstream peeringdb/peeringdb SHA ref per D-04"
    - "docs/API.md § Validation Notes (NEW sub-section per D-05) documents the 5 invalid pdbfe claims: (1) net?country=NL not a valid upstream filter, (2) limit=0 is unlimited not count-only, (3) default ordering is (-updated,-created) not id ASC, (4) unicode folding is Python unidecode not MySQL collation, (5) filter surface is prepare_query + queryable_relations not a DRF filterset_class — each with pinned peeringdb/peeringdb SHA ref"
    - "CLAUDE.md gains a § Upstream parity regression (Phase 72) subsection documenting internal/pdbcompat/parity/ conventions, the pdb-fixture-port tool workflow, the fixtures.go SHA-pinning protocol (D-03 advisory quarterly check), and the divergence-registry pointer"
    - "CHANGELOG.md v1.16 [Unreleased] block gains Phase 72 Added bullets (6 parity test files + 3 benchmarks + docs updates) and v1.16 is flipped from [Unreleased] → released (or marked READY — user chooses at close time)"
    - ".planning/REQUIREMENTS.md PARITY-01 + PARITY-02 flipped from `pending` to `complete (72-0N)` with grep-verifiable artefact pointers"
    - ".planning/ROADMAP.md Phase 72 flipped from [ ] to [x] with plan count 6/6 + Progress row updated"
    - "REQ-ID coverage audit: `grep -cE 'ORDER-0[123]|STATUS-0[12345]|LIMIT-0[12]|IN-0[12]|UNICODE-0[123]|TRAVERSAL-0[1234]|MEMORY-0[1234]|PARITY-0[12]'` against REQUIREMENTS.md traceability table shows all 25 v1.16 REQ-IDs in `complete` status"
    - "v1.16 shippability note: CHANGELOG.md coordinated-release block flipped to indicate v1.16 is now fully regression-locked; /gsd-complete-milestone is the next action"
    - "Human verification subtest run confirms docs render correctly (MD tables align, anchor links resolve, no broken citation SHAs) — manual step via checkpoint:human-verify"
  artifacts:
    - path: "docs/API.md"
      provides: "Extended § Known Divergences (existing seed from Phase 68/69/70 preserved; new rows appended with Phase 72 date) + NEW § Validation Notes sub-section with 5 invalid-pdbfe-claims each carrying peeringdb/peeringdb@<sha> refs"
      min_lines: 60
    - path: "CHANGELOG.md"
      provides: "Phase 72 Added entries; v1.16 released/ready flip; coordinated-release note updated to include Phase 72 in the v1.16 bundle"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "§ Upstream parity regression (Phase 72) subsection — conventions for adding to parity/, the pdb-fixture-port SHA-pin protocol, and the divergence-registry authoring pointer"
      min_lines: 30
  key_links:
    - from: "docs/API.md"
      to: "internal/pdbcompat/parity/"
      via: "divergence table rows reference the parity test names (grep-verifiable from docs/API.md back to specific *_test.go sub-tests)"
      pattern: "TestParity_Traversal/divergence_"
    - from: "CHANGELOG.md"
      to: ".planning/REQUIREMENTS.md"
      via: "v1.16 Phase 72 Added bullets cite PARITY-01 and PARITY-02"
      pattern: "PARITY-0[12]"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 72 [x] row reflects 2/2 PARITY requirements complete"
      pattern: "Phase 72.*complete|PARITY.*complete \\(72"
---

<objective>
Close Phase 72 with the docs-and-traceability delta: extend `docs/API.md`
with Phase 72-specific divergence rows and a new § Validation Notes
sub-section documenting the 5 invalid pdbfe claims (per D-04 + D-05);
add a CLAUDE.md convention subsection anchoring the
`internal/pdbcompat/parity/` layout; flip PARITY-01/02 and Phase 72 in
REQUIREMENTS.md + ROADMAP.md; update CHANGELOG.md to mark v1.16 as fully
regression-locked and ready for `/gsd-complete-milestone`.

Purpose: PARITY-02 is a docs-only requirement — it exists because future
conformance auditors reading pdbfe's gotchas doc against our code need a
single registry that distinguishes intentional divergence from
regression. This plan establishes that registry permanently. It also
closes the v1.16 milestone bookkeeping.

Output: 5 files touched (docs/API.md, CHANGELOG.md, CLAUDE.md,
REQUIREMENTS.md, ROADMAP.md); zero code changes; one human-verify
checkpoint on the MD rendering; Phase 72 marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-upstream-parity-regression/CONTEXT.md
@.planning/phases/72-upstream-parity-regression/72-01-SUMMARY.md
@.planning/phases/72-upstream-parity-regression/72-02-SUMMARY.md
@.planning/phases/72-upstream-parity-regression/72-03-SUMMARY.md
@.planning/phases/72-upstream-parity-regression/72-04-SUMMARY.md
@.planning/phases/72-upstream-parity-regression/72-05-SUMMARY.md
@.planning/phases/70-cross-entity-traversal/deferred-items.md

# Docs being extended — must edit in place, not rewrite
@docs/API.md
@CLAUDE.md
@CHANGELOG.md

<interfaces>
<!-- Upstream SHA refs to pin per D-04/D-05. The pdb-fixture-port SHA captured in 72-01 is the
     canonical one — reuse it across all 5 Validation Notes entries. Verify it's still available
     upstream via `gh api repos/peeringdb/peeringdb/commits/<SHA>` before writing. -->

Validation Notes authoritative citations (confirm exact line numbers against the SHA at write time):

1. net?country=NL NOT a valid filter:
   - `peeringdb_server/serializers.py:2938-2992` (NetworkSerializer.prepare_query — no `country` key)
   - `django_peeringdb/models/abstract.py` Network model — no country field (country lives on org)
   Implication: our acceptance of `net?country=*` (if any) is a bug, not a feature; parity test in
   traversal_test.go subtest must assert silent-ignore for this filter key.

2. limit=0 is unlimited:
   - `peeringdb_server/rest.py:494-497` — `if limit == 0: limit = None` (Python None = no LIMIT)
   - Our impl: `internal/pdbcompat/filter.go` ParsePaginationParams — `Limit == 0` sentinel

3. Default ordering is (-updated, -created):
   - `django_handleref/models.py:95-101` — `class Meta: ordering = ('-updated', '-created')`
   - Our impl: Phase 67 per-schema entrest.WithDefaultOrder + grpcserver cursor + pdbcompat

4. Unicode folding is Python unidecode:
   - `peeringdb_server/rest.py:576` — `unidecode.unidecode(v)` call
   - Our impl: Phase 69 internal/unifold (NFKD + ligature map; not byte-compatible with
     Python unidecode for ALL inputs — see divergence row for edge cases)

5. Filter surface is prepare_query + queryable_relations:
   - `peeringdb_server/serializers.py:754-780` — `queryable_relations()` method
   - `peeringdb_server/serializers.py:128-157` — `FILTER_EXCLUDE` list
   - Our impl: Phase 70 allowlist_gen.go (Path A) + Edges map (Path B) + FILTER_EXCLUDE annotations

docs/API.md Known Divergences additions (append to existing table from Phase 68/69/70):

| `?limit=N` with count-only-if-zero claim (invalid pdbfe claim — upstream actually returns unlimited) | covered above | — | See § Validation Notes entry 2 | v1.16 (Phase 72 lock-in) |
| `?depth=N` on list endpoints | Upstream accepts depth on list (see rest.py:744-748) | pdbcompat silently drops depth on list with slog.DebugContext paper trail per Phase 68 D-07 guardrail; functional list+depth deferred indefinitely (Phase 71 budget.go would refuse the resulting response sizes anyway) | Memory envelope on 256 MB replicas (Phase 71 D-06) + DoS protection on 13-entity × 2-depth worst case (ARCHITECTURE.md § Response Memory Envelope) | v1.16 (Phase 72 lock-in) |

Preserve the existing DEFER-70-06-01 and DEFER-70-verifier-01 rows from the current API.md;
the Phase 72 pass adds parity-test-name cross-refs in their Since columns (e.g.
"v1.16 (Phase 70; locked by TestParity_Traversal/divergence_fac_campus_name_500 in Phase 72)").
</interfaces>
</context>

<threat_model>
Docs-only change set; no trust boundaries move. STRIDE register unchanged.
Security-sensitive items (TRAVERSAL-04 silent-ignore, limit=0 memory envelope)
are ALREADY documented in their owning phases' Known Divergences rows; Phase 72
extends the registry without changing the underlying guardrails.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/API.md with Phase 72 divergence rows + NEW § Validation Notes</name>
  <files>docs/API.md</files>
  <action>
Read the current § Known Divergences table (line ~540 onward). Append the 2 new rows
per <interfaces> block. Update existing DEFER-70-06-01 / DEFER-70-verifier-01 rows'
"Since" columns to cross-reference the parity test names from 72-04.

Append a new top-level sub-section immediately AFTER § Known Divergences:

```markdown
## Validation Notes

Future conformance auditors reading pdbfe's gotchas doc against the
PeeringDB Plus codebase may encounter claims about upstream behaviour
that turn out to be wrong. This section documents 5 such invalid
claims identified during v1.16 (Phases 67-72), each with a pinned
`peeringdb/peeringdb@<sha>` reference so the authoritative upstream
source can be re-read without re-research.

| Claim | Verdict | Upstream truth | Our impl |
|-------|---------|----------------|----------|
| `net?country=NL` is a valid filter | **WRONG** | `country` lives on `org`, not `net`. See `peeringdb/peeringdb@<SHA>:src/peeringdb_server/serializers.py:2938-2992` — `NetworkSerializer.prepare_query` has no `country` key. | Filter key silently ignored via TRAVERSAL-04 silent-ignore mechanism (Phase 70 D-04). Parity-locked by `TestParity_Traversal/unknown_field_silently_ignored`. |
| `?limit=0` returns a count-only response | **WRONG** | `limit=0` means unlimited. See `peeringdb/peeringdb@<SHA>:src/peeringdb_server/rest.py:494-497` — `if limit == 0: limit = None` (Python `None` = no SQL `LIMIT`). | Unbounded response via Phase 68 LIMIT-01 + Phase 71 memory budget. Parity-locked by `TestParity_Limit/limit_zero_returns_all_rows_unbounded`. |
| Default ordering is `id ASC` | **WRONG** | Default ordering is `(-updated, -created)` via `django-handleref` base Meta. See `django-handleref@<SHA>:src/django_handleref/models.py:95-101`. | Phase 67 default-order flip across pdbcompat + grpcserver + entrest. Parity-locked by `TestParity_Ordering/default_list_order_updated_desc`. |
| Unicode folding uses MySQL collation | **WRONG** | Folding is Python `unidecode.unidecode(v)` at query time. See `peeringdb/peeringdb@<SHA>:src/peeringdb_server/rest.py:576`. | Phase 69 shadow `_fold` columns + `internal/unifold` NFKD + ligature map. Parity-locked by `TestParity_Unicode/name_contains_diacritic_matches_ascii`. Not byte-compatible with Python `unidecode` for all inputs — see § Known Divergences row if/when a specific gap is surfaced. |
| Filter surface is a DRF `filterset_class` | **WRONG** | Filter surface is a per-serializer `prepare_query(...)` hook plus an auto-`queryable_relations()` mechanism. See `peeringdb/peeringdb@<SHA>:src/peeringdb_server/serializers.py:754-780` (`queryable_relations()`) and `:128-157` (`FILTER_EXCLUDE`). | Phase 70 Path A (`pdbcompat.WithPrepareQueryAllow` annotations → `allowlist_gen.go`) + Path B (ent edge introspection via generated `Edges` map). Parity-locked by `TestParity_Traversal/path_a_1hop_org_name` and `path_b_1hop_via_edges`. |

The `<SHA>` placeholder is the upstream commit pinned by
`cmd/pdb-fixture-port/` and recorded in the header of
`internal/testutil/parity/fixtures.go`. Quarterly re-validation is
advisory only per Phase 72 CONTEXT.md D-03 — a drift alert does not
block merges.
```

Replace `<SHA>` with the actual upstream SHA recorded in
`internal/testutil/parity/fixtures.go` header (read the file first; do not
hand-edit an unverified hash). If the fixtures.go header records a commit
abbreviated to 7-8 chars, expand to the full 40-char SHA via
`gh api repos/peeringdb/peeringdb/commits/<short>` at write time.

Keep existing Phase 68/69/70 rows unchanged EXCEPT for the two "Since"
column updates noted in <interfaces>.

Commit message: `docs(72-06): extend docs/API.md § Known Divergences + add § Validation Notes`
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && grep -c "Validation Notes" docs/API.md && grep -c "peeringdb/peeringdb@" docs/API.md && grep -c "TestParity_" docs/API.md</automated>
  </verify>
  <done>§ Validation Notes present with 5 rows; 5 upstream SHA refs present; ≥5 parity-test-name cross-refs; existing Known Divergences rows preserved byte-identical except for 2 "Since" column updates.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md + CHANGELOG.md; flip REQUIREMENTS.md + ROADMAP.md</name>
  <files>CLAUDE.md, CHANGELOG.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
**CLAUDE.md** — add a new subsection under `## Conventions` titled
`### Upstream parity regression (Phase 72)`:

```markdown
### Upstream parity regression (Phase 72)

`internal/pdbcompat/parity/` locks v1.16 pdbcompat semantics against regression
with 6 category-split test files (`ordering_test.go`, `status_test.go`,
`limit_test.go`, `unicode_test.go`, `in_test.go`, `traversal_test.go`) plus a
shared `harness.go`. Fixtures come from `internal/testutil/parity/fixtures.go`,
ported from upstream `peeringdb_server/management/commands/pdb_api_test.py`
via `cmd/pdb-fixture-port/`.

**Adding a new parity test.** Pick the matching category file; add a sub-test to
`TestParity_<Category>` with `t.Parallel()`, an upstream citation comment
(`// upstream: pdb_api_test.py:<line>`) or a synthesised marker
(`// synthesised: phase-<NN>-<context>`). Seed via `seedFixtures(t, c, parity.<Cat>Fixtures)`
— do NOT reach into `seed.Full` (cross-test contamination per Phase 72 CONTEXT.md plan-hints).

**Porting new fixtures.** Run `go generate ./internal/testutil/parity/` after
`cmd/pdb-fixture-port/` changes. The generator is idempotent; two runs produce
byte-identical output. The header of `fixtures.go` records the upstream
`peeringdb/peeringdb` commit SHA the data was ported from (Phase 72 D-03).
Quarterly drift check: run `cmd/pdb-fixture-port/ --check` — advisory only,
not blocking per Phase 72 D-06.

**Known divergences registry.** `docs/API.md § Known Divergences` is the
single source of truth. Every intentional non-parity outcome is a row there,
and every divergence row has a matching assertion in
`internal/pdbcompat/parity/*_test.go`. `docs/API.md § Validation Notes`
documents 5 invalid-pdbfe-claims with upstream SHA refs so future
conformance audits don't re-research the same gotchas.

**Benchmarks.** `internal/pdbcompat/parity/bench_test.go` covers 3
performance-sensitive parity cases (2-hop traversal, `limit=0` streaming,
5001-element `__in`) with `b.Loop()`-style benchmarks. Run locally via
`go test -run=^$ -bench=BenchmarkParity -count=3 -benchtime=5x ./internal/pdbcompat/parity/ | benchstat -`.
```

**CHANGELOG.md** — append Phase 72 bullets under the v1.16 [Unreleased] heading.
If a prior plan already flipped v1.16 from [Unreleased] → released, DO NOT
revert — add the Phase 72 bullets under the released heading instead.

```markdown
### Added — Phase 72 upstream parity regression (PARITY-01, PARITY-02)

- `internal/pdbcompat/parity/` — 6 category-split regression test files
  (ordering, status, limit, unicode, in, traversal) locking v1.16
  pdbcompat semantics against future regression
- `cmd/pdb-fixture-port/` — fixture porting tool reading upstream
  `pdb_api_test.py` and emitting Go fixture literals into
  `internal/testutil/parity/fixtures.go`, with upstream commit SHA pinned
  in the file header (quarterly drift alert advisory-only)
- `internal/pdbcompat/parity/bench_test.go` — 3 `b.Loop()`-style
  benchmarks covering 2-hop traversal, `limit=0` streaming, 5001-element
  `__in` for benchstat-on-main tracking
- `docs/API.md § Known Divergences` — 2 new rows (invalid-pdbfe limit=0
  claim, depth-on-list silent-drop) plus parity-test cross-refs on the
  existing DEFER-70-06-01 / DEFER-70-verifier-01 rows
- `docs/API.md § Validation Notes` — NEW sub-section documenting 5
  invalid-pdbfe-claims with upstream `peeringdb/peeringdb@<sha>` refs

### Released

v1.16 (Django-compat correctness) is fully regression-locked. Phase 72 ships
independently of Phases 67-71; /gsd-complete-milestone is the next action
to archive the milestone into `.planning/milestones/v1.16-*`.
```

**REQUIREMENTS.md** — flip the 2 pending rows in the Traceability table:

```
| PARITY-01 | 72 | complete (72-01..04; internal/pdbcompat/parity/ 6 test files + internal/testutil/parity/fixtures.go + cmd/pdb-fixture-port/ tool) |
| PARITY-02 | 72 | complete (72-06; docs/API.md § Known Divergences extended + § Validation Notes added with 5 invalid-pdbfe-claims upstream SHA refs) |
```

Flip the PARITY header's `[ ]` checkboxes to `[x]`.

**ROADMAP.md** — flip Phase 72's `- [ ]` to `- [x]`; add `6/6 plans executed`;
update Progress row to `6/6 Complete 2026-04-19`; update the Progress header at
the top of the milestone if applicable.

Commit message: `docs(72-06): close phase 72 — CLAUDE.md + CHANGELOG + REQ/ROADMAP traceability`
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus && grep -c "PARITY-0[12] | 72 | complete" .planning/REQUIREMENTS.md && grep -c "Phase 72.*\[x\]" .planning/ROADMAP.md && grep -c "Phase 72" CHANGELOG.md && grep -c "Upstream parity regression (Phase 72)" CLAUDE.md</automated>
  </verify>
  <done>PARITY-01 + PARITY-02 marked complete in REQUIREMENTS.md; Phase 72 [x] in ROADMAP; Phase 72 section present in CHANGELOG; CLAUDE.md convention subsection present. All 25 v1.16 REQ-IDs reachable via the complete-status grep invariant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 72 docs close:
- docs/API.md has a new § Validation Notes sub-section with 5 invalid-pdbfe-claims each carrying upstream peeringdb/peeringdb SHA refs
- docs/API.md § Known Divergences extended with 2 Phase 72 rows + existing rows cross-referenced to parity test names
- CLAUDE.md § Upstream parity regression (Phase 72) convention subsection added
- CHANGELOG.md v1.16 Phase 72 Added bullets appended; v1.16 released/ready note updated
- .planning/REQUIREMENTS.md PARITY-01 + PARITY-02 flipped complete; all 25 v1.16 REQ-IDs show complete
- .planning/ROADMAP.md Phase 72 flipped [x] 6/6 plans; Progress row updated
  </what-built>
  <how-to-verify>
1. Open `docs/API.md` in a Markdown renderer (VSCode preview, `grip docs/API.md`, or similar):
   - Scroll to § Known Divergences table. Confirm 2 new rows appended (limit=0 count-only invalid claim cross-ref, depth-on-list silent-drop) and the 2 DEFER rows now cross-reference `TestParity_Traversal/divergence_*` names.
   - Scroll to § Validation Notes (new sub-section immediately after § Known Divergences). Confirm 5 rows with a consistent upstream SHA (same value in all 5 rows).
   - Spot-check 1-2 upstream SHA refs by hitting `https://github.com/peeringdb/peeringdb/blob/<SHA>/src/peeringdb_server/rest.py#L494-L497` — the cited code should match the claim.
2. Open `CLAUDE.md`:
   - Search for "Upstream parity regression (Phase 72)". Confirm the subsection exists under `## Conventions` with the 4 labelled blocks (Adding a new parity test / Porting new fixtures / Known divergences registry / Benchmarks).
3. Open `CHANGELOG.md`:
   - Search for "Phase 72 upstream parity regression". Confirm the Added bullets match reality (5 bullets).
   - Confirm v1.16 is marked released or clearly marked READY for milestone close.
4. Run the Traceability audit:
   ```bash
   grep -cE '(ORDER|STATUS|LIMIT|IN|UNICODE|TRAVERSAL|MEMORY|PARITY)-0[1-5] \|.*complete' .planning/REQUIREMENTS.md
   ```
   Expected result: 25 (all v1.16 requirements complete).
5. Open `.planning/ROADMAP.md`:
   - Phase 72 row shows `- [x]` and `6/6 plans executed`
   - Progress row for v1.16 updated to reflect Phase 72 complete

**Expected:** All 5 steps confirm cleanly. If any SHA ref 404s on GitHub, OR any anchor link
in API.md is broken, OR any grep invariant fails → reject and request fix.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 72 complete, OR describe the specific issue to fix</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "Validation Notes" docs/API.md` == 1 (new sub-section)
- `grep -c "peeringdb/peeringdb@" docs/API.md` ≥ 5 (one SHA ref per Validation Notes entry)
- `grep -c "TestParity_" docs/API.md` ≥ 5 (parity test cross-refs)
- `grep -c "Upstream parity regression (Phase 72)" CLAUDE.md` == 1
- `grep -c "Phase 72 upstream parity regression" CHANGELOG.md` == 1
- `grep -cE '(ORDER|STATUS|LIMIT|IN|UNICODE|TRAVERSAL|MEMORY|PARITY)-0[1-5] \|.*complete' .planning/REQUIREMENTS.md` == 25
- `grep -c "Phase 72.*\[x\]" .planning/ROADMAP.md` == 1
- No fly deploy imperatives introduced (per CONTEXT.md coordination-notes: "v1.16 ships after 72 — no fly deploy in plans themselves")
- `go build ./...` + `go test -race ./...` clean (no code edits in this plan, but guardrail sanity)
</verification>

<success_criteria>
Plan 72-06 is DONE when:
- [ ] docs/API.md § Known Divergences extended (2 new rows + 2 Since-column cross-refs)
- [ ] docs/API.md § Validation Notes sub-section added (5 rows, 5 upstream SHA refs)
- [ ] CLAUDE.md § Upstream parity regression (Phase 72) subsection added
- [ ] CHANGELOG.md Phase 72 Added bullets appended; v1.16 release status reflected
- [ ] REQUIREMENTS.md PARITY-01 + PARITY-02 flipped complete; 25/25 v1.16 REQ-IDs show complete
- [ ] ROADMAP.md Phase 72 [x] 6/6 plans; Progress row updated
- [ ] Human verification checkpoint approved (MD renders correctly, SHA refs resolve on GitHub, grep invariants pass)
- [ ] Two atomic commits (one per auto task)
- [ ] Next-action pointer in SUMMARY: `/gsd-complete-milestone` for v1.16 archival
</success_criteria>

<output>
After completion, write `.planning/phases/72-upstream-parity-regression/72-06-SUMMARY.md`
and `.planning/phases/72-upstream-parity-regression/72-SUMMARY.md` (phase-level
summary spanning 72-01..06) with:
- per-plan commit SHAs
- aggregate LOC delta (docs + test + tool)
- fixtures.go size and upstream SHA pinned
- parity test count (6 files × N subtests) + benchmark count (3)
- all 25 v1.16 REQ-IDs complete status
- CHANGELOG entries added per phase
- next action: `/gsd-complete-milestone` to archive v1.16 into `.planning/milestones/v1.16-*`
- any deferred items (e.g. CI benchstat workflow, DEFER-70-06-01 fix for the Campus table name)
</output>
