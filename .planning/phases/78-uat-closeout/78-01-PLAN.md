---
phase: 78-uat-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/peeringdb-plus/security_e2e_test.go
  - .planning/phases/78-uat-closeout/UAT-RESULTS.md
autonomous: true
requirements: [UAT-02]
tags: [security, headers, body-cap, slowloris, uat]

must_haves:
  truths:
    - "curl -I https://peeringdb-plus.fly.dev/ui/ returns Strict-Transport-Security, X-Frame-Options, X-Content-Type-Options headers"
    - "curl -I https://peeringdb-plus.fly.dev/api/net returns Strict-Transport-Security and X-Content-Type-Options headers (X-Frame-Options scoped to browser paths only — must be ABSENT on /api/*)"
    - "curl -I https://peeringdb-plus.fly.dev/rest/v1/networks returns Strict-Transport-Security and X-Content-Type-Options headers (no X-Frame-Options on JSON APIs)"
    - "POST to /api/net with a 1.1 MB body returns HTTP 413 (rejection at the 1 MB cap)"
    - "POST to /api/net with a 0.9 MB body does NOT return HTTP 413 (under-cap acceptance, even if upstream auth/method rejects with 401/405 — the body is not what fails)"
    - "ConnectRPC paths (/peeringdb.v1.*) bypass the body cap — gRPC streams are not truncated at 1 MB (confirmed via existing maxBytesSkipPrefixes table)"
    - "Slowloris-style slow-write probe against /api/net does not exhaust the connection pool — concurrent normal requests still complete; offending connections are reaped within ReadTimeout (30s) or ReadHeaderTimeout (10s)"
  artifacts:
    - path: "cmd/peeringdb-plus/security_e2e_test.go"
      provides: "in-process regression lock for security headers + body cap; mirrors the curl-driven UAT-02 assertions"
      contains: "TestSecurity_HeadersOnUIPath, TestSecurity_HeadersOnAPIPath, TestSecurity_BodyCapEnforced, TestSecurity_GRPCBypassesBodyCap"
    - path: ".planning/phases/78-uat-closeout/UAT-RESULTS.md"
      provides: "captured curl outputs for headers and body-cap; slowloris probe outcome; UAT-02 sign-off block"
      contains: "## UAT-02"
  key_links:
    - from: "internal/middleware/security.go SecurityHeaders"
      to: "cmd/peeringdb-plus/security_e2e_test.go"
      via: "in-process buildMiddlewareChain assertions matching live curl outputs"
      pattern: "Strict-Transport-Security|X-Frame-Options|X-Content-Type-Options"
    - from: "internal/middleware/maxbody.go MaxBytesBody"
      to: "cmd/peeringdb-plus/security_e2e_test.go"
      via: "413 assertion on over-cap body"
      pattern: "MaxBytesBody|maxBytesSkipPrefixes"
---

<objective>
Verify the v1.13 Phase 53 security controls (HSTS / X-Frame-Options / X-Content-Type-Options headers, 1 MB body cap with gRPC skip-list, slowloris bound by `ReadTimeout`) against the live `peeringdb-plus.fly.dev` deployment, capture the evidence in `UAT-RESULTS.md`, and lock the in-process behaviour with a new `security_e2e_test.go` so future regressions fail in CI rather than on next year's UAT.

Purpose: Closes UAT-02. The curl-driven half of CONTEXT.md D-01.
Output: `UAT-02` section of `UAT-RESULTS.md` (live evidence) + `cmd/peeringdb-plus/security_e2e_test.go` (regression lock).
</objective>

<execution_context>
@.claude/skills/

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-uat-closeout/CONTEXT.md
@CLAUDE.md

@internal/middleware/security.go
@internal/middleware/maxbody.go
@cmd/peeringdb-plus/route_tag_e2e_test.go
@cmd/peeringdb-plus/middleware_chain_test.go

<interfaces>
<!-- Key contracts the executor needs. Extracted from current code (NOT 2 MB as CONTEXT.md hint says — actual cap is 1 MB). -->

From `cmd/peeringdb-plus/main.go:53-55`:
```go
// maxRequestBodySize is the maximum allowed request body for POST endpoints (1 MB).
const maxRequestBodySize = 1 << 20
```

From `internal/middleware/maxbody.go`:
```go
var maxBytesSkipPrefixes = []string{
    "/peeringdb.v1.",   // ConnectRPC service routes
    "/grpc.",
    "/grpc.health.v1.",
}
type MaxBytesBodyInput struct{ MaxBytes int64 }
func MaxBytesBody(in MaxBytesBodyInput) func(http.Handler) http.Handler
```

From `internal/middleware/security.go`:
```go
type SecurityHeadersInput struct {
    HSTSMaxAge            time.Duration  // v1.13 default: 180 * 24 * time.Hour
    HSTSIncludeSubDomains bool           // true in main.go
    FrameOptions          string         // "DENY" — applied ONLY to browser paths (/, /ui/*, /graphql)
    ContentTypeOptions    bool           // true → X-Content-Type-Options: nosniff on ALL responses
}
// isBrowserPath: "/", "/ui", "/graphql", "/ui/*", "/graphql/*"
```

From `cmd/peeringdb-plus/main.go:537-538`:
```go
MaxBodyBytes: maxRequestBodySize,        // 1 MB, NOT 2 MB
HSTSMaxAge:   180 * 24 * time.Hour,
```

From `cmd/peeringdb-plus/main.go:792-794`:
```go
ReadHeaderTimeout: 10 * time.Second,  // slowloris header-stall mitigation
ReadTimeout:       30 * time.Second,  // slowloris body-stall mitigation
// WriteTimeout intentionally 0 (long-lived h2c streams).
```

`fly.toml`: `kill_timeout = 30`, `[[services.tcp_checks]] grace_period = "30s"`. `PDBPLUS_DRAIN_TIMEOUT` default 10s.

From `cmd/peeringdb-plus/route_tag_e2e_test.go` (pattern — calls into `package main` so `buildMiddlewareChain`/`buildServer` are reachable):
```go
package main
import "net/http/httptest"
// ... rec := httptest.NewRecorder(); req := httptest.NewRequest(...); chain.ServeHTTP(rec, req)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — security_e2e_test.go with failing assertions for headers, body-cap, and gRPC bypass</name>
  <files>cmd/peeringdb-plus/security_e2e_test.go</files>
  <read_first>
    - cmd/peeringdb-plus/route_tag_e2e_test.go (pattern: in-process chain via httptest.NewRecorder + httptest.NewRequest, package main, reuses buildMiddlewareChain)
    - cmd/peeringdb-plus/middleware_chain_test.go (pattern: source-scan style assertions; not what we want here, included only to confirm the package main test convention)
    - internal/middleware/security.go (full body — SecurityHeadersInput shape, isBrowserPath rules)
    - internal/middleware/maxbody.go (full body — MaxBytesBody signature, maxBytesSkipPrefixes)
    - internal/middleware/security_test.go and internal/middleware/maxbody_test.go (existing unit-level invariants — DO NOT duplicate; we are adding the integrated chain-level lock)
  </read_first>
  <behavior>
    The new `security_e2e_test.go` must contain four sub-tests (or four functions — author's choice; sub-tests under one parent are fine):
    - `TestSecurity_HeadersOnUIPath` — request `GET /ui/` through `buildMiddlewareChain` (or an equivalent harness mirroring the production chain ordering: Recovery → MaxBytesBody → CORS → OTel → Logging → PrivacyTier → Readiness → SecurityHeaders → CSP → Caching → Gzip → RouteTag → mux). Assert ALL THREE headers are present: `Strict-Transport-Security` (matches `^max-age=\d+; includeSubDomains$`, max-age must be 15552000 — 180 days in seconds), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`.
    - `TestSecurity_HeadersOnAPIPath` — request `GET /api/net` through the chain. Assert: `Strict-Transport-Security` PRESENT, `X-Content-Type-Options: nosniff` PRESENT, `X-Frame-Options` ABSENT (per isBrowserPath scoping). This is the v1.13 D-01 invariant — JSON API surfaces must not carry XFO.
    - `TestSecurity_BodyCapEnforced` — POST a 1.1 MB random byte payload to `/api/net` (any pdbcompat write surface that would otherwise read the body — if /api/net is read-only, use whichever pdbcompat path the existing maxbody_test.go targets; the goal is observing 413 from the middleware, not exercising auth). Assert response status `http.StatusRequestEntityTooLarge` (413). Body bytes must be `crypto/rand.Read`-generated, NOT loaded from a file (per threat model — no real-payload exfiltration). Use a deterministic seed via `mathrand.New(mathrand.NewSource(1))` if `crypto/rand` is overkill — but DO NOT embed any real-looking data.
    - `TestSecurity_GRPCBypassesBodyCap` — POST a 1.1 MB body to `/peeringdb.v1.NetworkService/ListNetworks` (any path matching `maxBytesSkipPrefixes`). Assert response status is NOT 413. The handler may return a Connect-protocol error for malformed body — that's fine; the test asserts the body cap did NOT short-circuit at 413.
    Initial commit is RED: write the four tests FIRST, run with `go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/...` and confirm they FAIL (no `security_e2e_test.go` exists yet → compile error is acceptable as RED). If a similar test harness already exists in the package and the new tests compile cleanly but fail on assertion, that is preferable RED.
  </behavior>
  <action>
    Create `cmd/peeringdb-plus/security_e2e_test.go` with `package main` declaration. Mirror the harness pattern from `cmd/peeringdb-plus/route_tag_e2e_test.go`: build the production middleware chain via `buildMiddlewareChain` (or equivalent helper). Wire a stub mux that returns 200 for any path (so non-cap-related routing failures don't mask the test).

    For the body-cap test: use `bytes.NewReader(make([]byte, 1<<20+1024))` (1 MB + 1 KB) as the request body. Do NOT use any production fixture or real payload. Add a comment `// generated random bytes — see threat_model section of 78-01-PLAN.md` at the body-creation site.

    For the gRPC bypass test: send POST to `/peeringdb.v1.NetworkService/ListNetworks` with a 1.1 MB random body. The exact ConnectRPC path doesn't matter for the assertion (we are testing middleware bypass, not RPC dispatch); pick any valid `peeringdb.v1.*` route from `gen/peeringdb/v1/peeringdbv1connect/`.

    Per CONTEXT.md D-01 sub-bullet, the body-cap is on /api/* (REST/pdbcompat). The CONTEXT.md hint claims "2 MB" but the actual constant is `maxRequestBodySize = 1 << 20` (1 MB) — use the constant by name in the test (`if got > maxRequestBodySize { ... }`) so the test is self-updating if the constant ever changes.

    Commit message: `test(78-01): RED — security E2E asserts for headers, body cap, gRPC bypass`
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/... 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/peeringdb-plus/security_e2e_test.go` exists.
    - `grep -c '^func TestSecurity_' cmd/peeringdb-plus/security_e2e_test.go` returns ≥ 4.
    - `grep -c 'maxRequestBodySize' cmd/peeringdb-plus/security_e2e_test.go` returns ≥ 1 (uses the named constant rather than a hardcoded literal).
    - `grep -c '/peeringdb.v1.' cmd/peeringdb-plus/security_e2e_test.go` returns ≥ 1 (gRPC bypass exercised).
    - `grep -E 'Strict-Transport-Security|X-Frame-Options|X-Content-Type-Options' cmd/peeringdb-plus/security_e2e_test.go | wc -l` returns ≥ 3.
    - The test FAILS or fails-to-compile when run (RED state). Recorded in commit message.
    - Threat-model comment present: `grep -c 'threat_model\|random bytes\|no real payload' cmd/peeringdb-plus/security_e2e_test.go` returns ≥ 1.
  </acceptance_criteria>
  <done>RED commit lands; `go test -race -run='^TestSecurity_'` fails with assertion errors (or compile error if the harness needed extending in main_test.go etc.); no production code changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — make all four TestSecurity_ tests pass; capture live curl + slowloris evidence into UAT-RESULTS.md</name>
  <files>
    cmd/peeringdb-plus/security_e2e_test.go
    .planning/phases/78-uat-closeout/UAT-RESULTS.md
  </files>
  <read_first>
    - The Task 1 RED test file (now exists)
    - cmd/peeringdb-plus/main.go (to see exact buildMiddlewareChain signature — DO NOT modify, just call)
    - internal/middleware/security_test.go (for the 180-day == 15552000 second value check pattern)
    - .planning/phases/78-uat-closeout/CONTEXT.md (for the UAT-RESULTS.md output structure expected by Plan 78-02)
  </read_first>
  <behavior>
    GREEN consists of two halves:

    **(A) In-process tests pass.** Adjust `security_e2e_test.go` so all four `TestSecurity_*` tests pass with `go test -race`. NO production code is touched — the SecurityHeaders / MaxBytesBody / chain ordering already work; only the test harness is the work. If a test fails because the chain is wired through a helper that requires a non-trivial config, build a minimal `chainConfig` struct in the test file (mirror what `cmd/peeringdb-plus/main.go` builds for production).

    **(B) Live evidence in UAT-RESULTS.md.** Run live curl probes against `https://peeringdb-plus.fly.dev/` and capture exact outputs. Run a small slowloris probe (inline Go program executed via `go run` from a `_testdata/slowloris/main.go` ephemeral file, OR an inline `bash` here-doc that opens raw TCP sockets via `nc` — author's choice; whichever is cleaner). The probe MUST self-terminate within 60 seconds and MUST NOT be left running.
  </behavior>
  <action>
    Step 1 — fix the test harness so the four tests pass. Likely fixes: import correct packages; build a minimal `chainConfig` matching `cmd/peeringdb-plus/main.go:805-815`; call `buildMiddlewareChain(cc, mux)` against an in-test `http.ServeMux` whose handlers return 200; assert via `rec.Result().Header.Get(...)` and `rec.Result().StatusCode`.

    Step 2 — `go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/...` MUST pass.

    Step 3 — commit GREEN: `feat(78-01): GREEN — security E2E passes against production middleware chain`.

    Step 4 — run the live curl probes (the production deployment is at `https://peeringdb-plus.fly.dev/`, deployed across 8 machines, primary in lhr). Run EXACTLY these three commands and capture stdout:
    ```
    curl -sI https://peeringdb-plus.fly.dev/ui/ 2>&amp;1
    curl -sI https://peeringdb-plus.fly.dev/api/net 2>&amp;1
    curl -sI https://peeringdb-plus.fly.dev/rest/v1/networks 2>&amp;1
    ```

    Step 5 — body-cap probe against production. Send a 1.1 MB random body to `/api/net` (this is a read-only API; expect 405 Method Not Allowed for POST OR 413 — the test of interest is whether 413 fires when the body is over 1 MB). Run:
    ```
    head -c 1100000 /dev/urandom &gt; /tmp/uat02-overcap.bin
    curl -sI -X POST -H 'Content-Type: application/json' --data-binary @/tmp/uat02-overcap.bin https://peeringdb-plus.fly.dev/api/net 2>&amp;1
    ```
    Capture status. Also run with a 900 KB body (under cap) for contrast:
    ```
    head -c 900000 /dev/urandom &gt; /tmp/uat02-undercap.bin
    curl -sI -X POST -H 'Content-Type: application/json' --data-binary @/tmp/uat02-undercap.bin https://peeringdb-plus.fly.dev/api/net 2>&amp;1
    ```
    Then `rm /tmp/uat02-*.bin`. (Per threat model — no real payload, just `/dev/urandom`.)

    Step 6 — slowloris probe. Implement a small Go program that opens 20 TCP connections to `peeringdb-plus.fly.dev:443` and writes 1 byte per second per connection (NOT a full TLS handshake — just raw TCP to demonstrate the slowloris pattern is bounded by ReadHeaderTimeout=10s). PREFERRED IMPLEMENTATION: Go program in `cmd/peeringdb-plus/_testdata/slowloris.go` (the leading `_` keeps the file out of the regular `go test` run; invoke via `go run cmd/peeringdb-plus/_testdata/slowloris.go`). The program MUST:
    - `time.AfterFunc(60*time.Second, ...)` hard kill — never run more than 60 seconds.
    - `ctx, cancel := context.WithTimeout(ctx, 60*time.Second)` on every `net.Dial`.
    - Print "slowloris probe: opened N=20 connections, expected reaping at ReadHeaderTimeout=10s ± grace; observed connections reaped within X.Ys; no connection-pool exhaustion (concurrent baseline curl succeeded)".
    - In parallel, fire a baseline `curl -sI https://peeringdb-plus.fly.dev/healthz` every 2s for the duration; assert all baseline curls return HTTP 200 (proving the connection pool wasn't exhausted by the slow-write probe).

    Step 7 — write UAT-RESULTS.md. Structure exactly:
    ```
    ---
    phase: 78-uat-closeout
    artefact: UAT-RESULTS
    last_updated: &lt;today's date&gt;
    requirements: [UAT-01, UAT-02]
    ---

    # Phase 78 — UAT Results

    ## UAT-02 (Claude-driven, completed &lt;date&gt;)

    ### Headers — `curl -I` outputs

    #### `/ui/`
    ```
    &lt;captured curl -I output verbatim, with any IP, session, or OTel headers redacted to "&lt;redacted&gt;"&gt;
    ```
    Verdict: PASS / FAIL with reason.

    #### `/api/net`
    ```
    &lt;output&gt;
    ```
    Verdict: PASS — Strict-Transport-Security present, X-Content-Type-Options: nosniff present, X-Frame-Options correctly absent (JSON API surface, isBrowserPath() returns false).

    #### `/rest/v1/networks`
    ```
    &lt;output&gt;
    ```
    Verdict: ...

    ### Body cap

    #### Over-cap probe (1.1 MB POST to `/api/net`)
    ```
    &lt;curl output — expect HTTP 413&gt;
    ```
    Verdict: ...

    #### Under-cap probe (0.9 MB POST to `/api/net`)
    ```
    &lt;curl output — expect non-413; method/auth rejection acceptable&gt;
    ```
    Verdict: ...

    #### gRPC bypass (covered by in-process test)
    Locked by `TestSecurity_GRPCBypassesBodyCap` in `cmd/peeringdb-plus/security_e2e_test.go`. Production-side validation deferred to the regression suite — re-running it against prod would require a hand-crafted ConnectRPC frame and is out of scope per CONTEXT.md (the in-process test is the contract).

    ### Slowloris probe

    Probe: `go run cmd/peeringdb-plus/_testdata/slowloris.go` against `peeringdb-plus.fly.dev:443`, 20 concurrent connections writing 1 byte/sec.

    Output:
    ```
    &lt;captured slowloris probe stdout&gt;
    ```

    Baseline curl during probe: 30/30 200 OK (one every 2 seconds for 60 seconds).

    Verdict: PASS — connections reaped within ReadHeaderTimeout=10s; baseline traffic unaffected; no connection-pool exhaustion.

    ### Summary

    | Sub-item | Result |
    |---|---|
    | HSTS / XFO / XCTO present on /ui/ | PASS |
    | HSTS / XCTO present on /api/net (XFO absent) | PASS |
    | HSTS / XCTO present on /rest/v1/networks | PASS |
    | 1 MB body cap rejects oversize POST on /api/* | PASS |
    | gRPC bypass (in-process test) | PASS |
    | Slowloris bounded by ReadHeaderTimeout | PASS |

    UAT-02 status: **CLOSED**. Regression locked by `cmd/peeringdb-plus/security_e2e_test.go`.

    ## UAT-01 (operator-driven)

    To be filled by Plan 78-02. Placeholder section.
    ```

    Step 8 — commit: `docs(78-01): UAT-02 evidence — live headers, body cap, slowloris probe`.

    PII / threat-model rules:
    - REDACT any session token, machine ID, ingress IP, or grafana stack URL that appears in curl output. Replace with literal `&lt;redacted&gt;`. Per memory `feedback_no_pii_in_repo.md`.
    - DO NOT commit `/tmp/uat02-*.bin`. The `rm` step is mandatory.
    - DO NOT commit the slowloris probe output if it captured the operator's IP.
    - The slowloris probe must hard-kill at 60 seconds and MUST NOT be re-runnable as a test (`go test` won't pick it up because `_testdata/` directories are excluded by go's build tooling). If the executor opts for the bash `nc` approach instead of Go, the same 60-second hard-kill rule applies via `timeout 60 ...`.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/... 2>&amp;1 | tail -10 &amp;&amp; test -f .planning/phases/78-uat-closeout/UAT-RESULTS.md &amp;&amp; test "$(grep -c '^## UAT-02' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/...` exits 0.
    - `.planning/phases/78-uat-closeout/UAT-RESULTS.md` exists.
    - File contains exactly one `## UAT-02` heading and exactly one `## UAT-01` heading (placeholder for 78-02).
    - All three live curl outputs are quoted verbatim under `### Headers`.
    - Body-cap section captures both over-cap (expected 413) and under-cap probe results.
    - Slowloris section quotes the probe output and a verdict.
    - `grep -c 'redacted\|REDACTED' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns ≥ 0 — redactions are conditional on what the curl output contained, not mandatory; if no PII appeared, no redactions are needed but the file MUST NOT contain a Grafana stack URL or operator email (manual review).
    - `grep -c 'dotwaffle@gmail.com\|grafana.net' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns 0 (PII guard from memory `feedback_no_pii_in_repo.md`).
    - No leftover `/tmp/uat02-*.bin` files: `ls /tmp/uat02-*.bin 2>/dev/null | wc -l` returns 0.
    - If the slowloris probe was a Go file, it lives at `cmd/peeringdb-plus/_testdata/slowloris.go` (excluded from `go test ./...`) AND has a `time.AfterFunc(60*time.Second, ...)` hard kill: `grep -c 'AfterFunc.*60.*Second\|time.After.*60.*Second' cmd/peeringdb-plus/_testdata/slowloris.go` returns ≥ 1.
    - UAT-02 verdict in UAT-RESULTS.md is "CLOSED" / "PASS" with all six sub-item rows green.
  </acceptance_criteria>
  <done>All four TestSecurity_ tests pass. UAT-RESULTS.md committed with verbatim curl outputs, body-cap evidence, and slowloris probe verdict. No /tmp residue. No PII in committed file. Regression-locked at the in-process layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator workstation → production | UAT-02 commands run from operator's terminal against `peeringdb-plus.fly.dev` (public TLS endpoint). |
| operator workstation → repo | curl outputs and probe results are captured into `.planning/phases/78-uat-closeout/UAT-RESULTS.md` and committed. |
| slowloris probe → production | TCP connections from operator workstation to production port 443 with deliberately slow writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-78-01 | I (Information disclosure) | UAT-RESULTS.md committed file | mitigate | Redact session tokens, ingress IPs, OTel trace IDs, machine IDs from any captured curl output before commit. Hard PII rule from `feedback_no_pii_in_repo.md`: no `grafana.net` URLs, no operator email. Acceptance criterion `grep -c 'dotwaffle@gmail.com\|grafana.net'` returns 0. |
| T-78-02 | I (Information disclosure) | Random body sent to `/api/net` for body-cap probe | mitigate | Body content is `/dev/urandom`-generated, NEVER a real payload. Test harness uses `bytes.NewReader(make([]byte, ...))` (zero-byte slice) to avoid even synthetic-looking data on the wire. `/tmp/uat02-*.bin` deleted post-test. |
| T-78-03 | D (Denial of service) | Slowloris probe accidentally left running against production | mitigate | Hard 60-second `time.AfterFunc` kill in the Go probe; mandatory `timeout 60` wrapper if bash/nc approach. Probe lives in `_testdata/` so `go test ./...` cannot accidentally invoke it. Acceptance criterion checks for the timeout. |
| T-78-04 | D | Slowloris probe count too high → real exhaustion | accept | N=20 connections is well below Fly's per-machine default ulimit (8192 fd). For an 8-machine fleet, even sustained 20 stuck connections on one machine leaves >99.7% of capacity. Probe is short-lived (60s max). |
| T-78-05 | T (Tampering) | In-process test reaches into production secret space | accept | Tests use `httptest.NewRecorder` against an in-process chain — no network I/O, no real config, no `chainConfig` field that holds prod tokens. Test config is synthesized inline. |

## Mitigations enforced by acceptance criteria

- T-78-01: `grep -c 'dotwaffle@gmail.com\|grafana.net' UAT-RESULTS.md` returns 0.
- T-78-02: `ls /tmp/uat02-*.bin 2>/dev/null | wc -l` returns 0; test harness uses `make([]byte, ...)` with no real-payload-shaped content.
- T-78-03: `grep -c 'AfterFunc.*60.*Second\|time.After.*60.*Second' cmd/peeringdb-plus/_testdata/slowloris.go` returns ≥ 1.
</threat_model>

<verification>
- `go test -race -run='^TestSecurity_' ./cmd/peeringdb-plus/...` exits 0.
- `go test -race ./cmd/peeringdb-plus/...` exits 0 (full package — no regression in sibling tests).
- `golangci-lint run ./cmd/peeringdb-plus/... --timeout 2m` reports 0 issues for the new file.
- `.planning/phases/78-uat-closeout/UAT-RESULTS.md` exists with `## UAT-02` section populated and a verdict block.
- No leftover `/tmp/uat02-*.bin`.
- No PII in committed UAT-RESULTS.md.
</verification>

<success_criteria>
1. `cmd/peeringdb-plus/security_e2e_test.go` exists with TestSecurity_HeadersOnUIPath, TestSecurity_HeadersOnAPIPath, TestSecurity_BodyCapEnforced, TestSecurity_GRPCBypassesBodyCap and all four pass under `go test -race`.
2. `.planning/phases/78-uat-closeout/UAT-RESULTS.md` `## UAT-02` section captures verbatim live curl outputs for HSTS/XFO/XCTO across `/ui/`, `/api/net`, `/rest/v1/networks` plus the body-cap and slowloris probe results.
3. UAT-02 status block reads "CLOSED" with all six sub-item rows green.
4. No PII (`grafana.net`, operator email) anywhere in committed files.
5. No residual `/tmp/uat02-*.bin` files.
</success_criteria>

<output>
After completion, create `.planning/phases/78-uat-closeout/78-01-SUMMARY.md` capturing:
- The four TestSecurity_ test names + 1-line behavior
- The captured curl/body-cap/slowloris evidence verdict
- Whether 1 MB cap (vs the CONTEXT.md "2 MB" hint) was confirmed in production
- Any divergence from production behavior (e.g. if `/api/net` returns 401 instead of 405 for POST, that's behavioral noise, not a UAT-02 failure)
</output>
