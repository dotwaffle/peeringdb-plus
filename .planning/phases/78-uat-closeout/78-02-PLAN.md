---
phase: 78-uat-closeout
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - .planning/phases/78-uat-closeout/UAT-RESULTS.md
autonomous: false
requirements: [UAT-01]
tags: [csp, devtools, browser, uat, checkpoint]

must_haves:
  truths:
    - "With PDBPLUS_CSP_ENFORCE=true set as a Fly secret, /ui/, /ui/asn/13335, /ui/compare load in Chrome with zero CSP violations in the DevTools Console panel"
    - "Tailwind v4 JIT runtime stylesheet (CDN-loaded) does not trigger any 'Refused to apply inline style' or 'Refused to load the stylesheet' messages — the original Phase 52 concern"
    - "After the operator pastes the DevTools verdict into UAT-RESULTS.md, the file's UAT-01 section captures violation count (zero) per URL plus the operator's date/time stamp"
    - "After verification, PDBPLUS_CSP_ENFORCE=true is left set as the production default (or rolled back to false if violations were found — operator decides; checkpoint surfaces the choice)"
  artifacts:
    - path: ".planning/phases/78-uat-closeout/UAT-RESULTS.md"
      provides: "operator-supplied DevTools evidence for UAT-01; closes the v1.13 Phase 52 deferred item"
      contains: "## UAT-01"
  key_links:
    - from: ".planning/phases/78-uat-closeout/UAT-RESULTS.md ## UAT-01"
      to: "internal/middleware/csp.go EnforcingMode"
      via: "operator confirmation that EnforcingMode=true does not break the production /ui/ render path"
      pattern: "PDBPLUS_CSP_ENFORCE|EnforcingMode"
---

<objective>
Drive the v1.13 Phase 52 CSP enforcement verification through Chrome DevTools against `https://peeringdb-plus.fly.dev/`, capture the operator's findings into the `## UAT-01` section of `UAT-RESULTS.md`, and decide whether to leave `PDBPLUS_CSP_ENFORCE=true` set as the production default.

Purpose: Closes UAT-01. The operator-driven half of CONTEXT.md D-01. CSP enforcement-mode violation reports require Chrome DevTools Network panel inspection that isn't curl-friendly, so this plan is structured around an operator checkpoint with a precise step-list.
Output: `## UAT-01` section of `UAT-RESULTS.md` populated with operator verdict + `PDBPLUS_CSP_ENFORCE` final state.
</objective>

<execution_context>
@.claude/skills/

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-uat-closeout/CONTEXT.md
@.planning/phases/78-uat-closeout/UAT-RESULTS.md
@CLAUDE.md

@internal/middleware/csp.go

<interfaces>
<!-- CSP middleware behaviour and env-var contract. Extracted from current code. -->

From `internal/middleware/csp.go`:
```go
type CSPInput struct {
    UIPolicy      string  // CSP directive applied to /ui/* — tighter
    GraphQLPolicy string  // CSP directive applied to /graphql — permissive (allows GraphiQL inline-script)
    EnforcingMode bool    // false → "Content-Security-Policy-Report-Only"; true → "Content-Security-Policy"
}
```

From `CLAUDE.md § Environment Variables`:
- `PDBPLUS_CSP_ENFORCE=false` (default) — header is `Content-Security-Policy-Report-Only`
- `PDBPLUS_CSP_ENFORCE=true` — header is `Content-Security-Policy` (BLOCKING — violations break the page)

From `cmd/peeringdb-plus/main.go:530-533`:
```go
CSPInput: middleware.CSPInput{
    UIPolicy:      ...,
    GraphQLPolicy: ...,
    EnforcingMode: cfg.CSPEnforce,  // sourced from PDBPLUS_CSP_ENFORCE
},
```

Production deployment: `https://peeringdb-plus.fly.dev/` (Fly app `peeringdb-plus`, primary lhr + 7 replicas). Set `PDBPLUS_CSP_ENFORCE=true` via `fly secrets set --app peeringdb-plus PDBPLUS_CSP_ENFORCE=true` — the secret update triggers a rolling redeploy across all 8 machines.

The original Phase 52 concern (per CONTEXT.md): Tailwind v4 JIT runtime behaviour. Tailwind v4 CDN was statically verified not to require `unsafe-eval` at v1.13 ship time, but enforcement-mode runtime behaviour was deferred — this UAT-01 closes that gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — write the UAT-01 step-list into UAT-RESULTS.md</name>
  <files>.planning/phases/78-uat-closeout/UAT-RESULTS.md</files>
  <read_first>
    - .planning/phases/78-uat-closeout/UAT-RESULTS.md (exists post-78-01; must NOT clobber the ## UAT-02 section)
    - .planning/phases/78-uat-closeout/CONTEXT.md (D-01 sub-bullet for UAT-01)
    - internal/middleware/csp.go (full body — confirms EnforcingMode toggle is the only behaviour delta)
  </read_first>
  <action>
    Edit `.planning/phases/78-uat-closeout/UAT-RESULTS.md` and replace the `## UAT-01` placeholder section (created by Plan 78-01) with the following step-list. DO NOT touch the `## UAT-02` section; use Edit (not Write) to scope the change.

    The replacement section content:
    ```
    ## UAT-01 (operator-driven, awaiting DevTools confirmation)

    ### Pre-flight

    Set `PDBPLUS_CSP_ENFORCE=true` on the live Fly app:
    ```
    fly secrets set --app peeringdb-plus PDBPLUS_CSP_ENFORCE=true
    fly status --app peeringdb-plus  # wait for rolling deploy to complete (~2 min for 8 machines)
    ```

    Confirm the header flipped from report-only to enforcing:
    ```
    curl -sI https://peeringdb-plus.fly.dev/ui/ | grep -i 'content-security-policy'
    ```
    Expected: header name is `Content-Security-Policy:` (NOT `Content-Security-Policy-Report-Only:`). Capture below.

    Captured:
    ```
    &lt;PASTE curl output here&gt;
    ```

    ### DevTools sweep

    For each URL: open Chrome (or Chromium/Edge), open DevTools (Cmd+Opt+I / Ctrl+Shift+I), switch to the **Console** panel and the **Network** panel. Hard-reload the page (Cmd+Shift+R / Ctrl+Shift+R). Check for any of the following:
    - Console messages matching `Refused to .* because it violates the .* directive` — these are CSP violations.
    - Network panel requests with status `(blocked:csp)` — these are blocked subresources.
    - Tailwind v4 stylesheet (`https://cdn.tailwindcss.com/...` or whatever is wired) loads with no errors.

    Record violation count per URL.

    | URL | Violations | Tailwind v4 OK? | Notes |
    |---|---|---|---|
    | `https://peeringdb-plus.fly.dev/ui/` | &lt;count&gt; | yes / no | ... |
    | `https://peeringdb-plus.fly.dev/ui/asn/13335` | &lt;count&gt; | yes / no | ... |
    | `https://peeringdb-plus.fly.dev/ui/compare` | &lt;count&gt; | yes / no | ... |

    ### Verdict

    - **All zero violations + Tailwind v4 OK** → UAT-01 PASS. Leave `PDBPLUS_CSP_ENFORCE=true` set permanently. Update `CLAUDE.md § Environment Variables` to reflect new default in a follow-up commit (out of scope for this plan).
    - **Any violation** → UAT-01 FAIL. Roll back: `fly secrets unset --app peeringdb-plus PDBPLUS_CSP_ENFORCE` or `fly secrets set --app peeringdb-plus PDBPLUS_CSP_ENFORCE=false`. Capture the violation list verbatim below; this becomes a follow-up phase to fix the policy.

    ### Final operator entry

    Date: &lt;YYYY-MM-DD&gt;
    Operator: &lt;handle, e.g. "matthew" — NO email, NO grafana stack URL&gt;
    Browser: Chrome &lt;version&gt; / Chromium &lt;version&gt;
    Final secret state: `PDBPLUS_CSP_ENFORCE=` &lt;true|false|unset&gt;
    Verdict: PASS / FAIL
    Violations (if any), redacted of any session-identifying data:
    ```
    &lt;paste verbatim Console panel violation lines, redacting any token, IP, or path containing PII&gt;
    ```

    UAT-01 status: **AWAITING OPERATOR**. Update to **CLOSED** after fields above are filled.
    ```

    Commit: `docs(78-02): UAT-01 — DevTools step-list ready for operator`.

    DO NOT change `PDBPLUS_CSP_ENFORCE` yourself or run `fly secrets set` — that is part of the operator checkpoint in Task 2. The step-list is the deliverable here.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; test "$(grep -c '^## UAT-01' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -eq 1 &amp;&amp; test "$(grep -c '^## UAT-02' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -eq 1 &amp;&amp; grep -c 'AWAITING OPERATOR' .planning/phases/78-uat-closeout/UAT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/78-uat-closeout/UAT-RESULTS.md` still contains exactly one `## UAT-02` heading (unchanged from 78-01).
    - File contains exactly one `## UAT-01` heading (replaced from placeholder).
    - `## UAT-01` section contains: a `### Pre-flight` block with `fly secrets set` command; a `### DevTools sweep` block with the 3-row URL table; a `### Verdict` block; a `### Final operator entry` block with field placeholders.
    - `grep -c 'AWAITING OPERATOR' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns ≥ 1.
    - `grep -c 'fly secrets set\|PDBPLUS_CSP_ENFORCE=true' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns ≥ 2 (one in pre-flight, one in verdict-rollback).
    - `grep -c 'dotwaffle@gmail.com\|grafana.net' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns 0 (PII guard).
    - The 3 specific URLs (`/ui/`, `/ui/asn/13335`, `/ui/compare`) all appear in the URL table: `grep -c 'peeringdb-plus.fly.dev/ui/asn/13335\|peeringdb-plus.fly.dev/ui/compare\|peeringdb-plus.fly.dev/ui/' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns ≥ 3.
  </acceptance_criteria>
  <done>UAT-01 section exists with the full operator step-list; commit lands; ready for operator checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator checkpoint — execute UAT-01 DevTools sweep, paste verdict</name>
  <files>.planning/phases/78-uat-closeout/UAT-RESULTS.md</files>
  <read_first>
    - .planning/phases/78-uat-closeout/UAT-RESULTS.md (the step-list seeded by Task 1 — operator follows it)
    - CLAUDE.md § Environment Variables — `PDBPLUS_CSP_ENFORCE` semantics
  </read_first>
  <what-built>
    `.planning/phases/78-uat-closeout/UAT-RESULTS.md` `## UAT-01` section is populated with a step-list (pre-flight `fly secrets set` command, DevTools sweep table for 3 URLs, verdict block). Plan 78-01 already produced the `## UAT-02` evidence and the regression-locking `cmd/peeringdb-plus/security_e2e_test.go`. The middleware code itself (CSP, body cap, security headers) is unchanged — UAT-01 verifies existing v1.13 behaviour, not new behaviour.
  </what-built>
  <action>
    OPERATOR ACTION (Claude pauses; resume only after operator marks done).

    Manual operator steps — Claude cannot run a Chrome DevTools session, hence this human-action checkpoint.

    1. **Set the secret on production:**
       ```
       fly secrets set --app peeringdb-plus PDBPLUS_CSP_ENFORCE=true
       fly status --app peeringdb-plus
       ```
       Wait for the rolling redeploy to finish (`fly status` shows all 8 machines `started` and the latest `version`). Roughly 2 minutes for 8 machines.

    2. **Confirm the header flipped:**
       ```
       curl -sI https://peeringdb-plus.fly.dev/ui/ | grep -i 'content-security-policy'
       ```
       Header name should now be `Content-Security-Policy:` (without `-Report-Only`). If you still see `Content-Security-Policy-Report-Only`, the secret didn't propagate — check `fly secrets list --app peeringdb-plus` and `fly logs --app peeringdb-plus | grep CSP_ENFORCE`. Paste the curl line into the captured block in UAT-RESULTS.md.

    3. **DevTools sweep on the 3 URLs.** Open each in Chrome (or Chromium/Edge) with DevTools open (Console + Network panels), hard-reload (Cmd+Shift+R / Ctrl+Shift+R), and check for:
       - Console panel: any line matching `Refused to (load|apply|execute|connect|frame) .* because it violates`. These are CSP violations.
       - Network panel: any request with `(blocked:csp)` status.
       - Tailwind v4 stylesheet: confirm it loads without error (this is the original Phase 52 concern — Tailwind v4 JIT runtime under enforcing mode).

       URLs:
       - `https://peeringdb-plus.fly.dev/ui/`
       - `https://peeringdb-plus.fly.dev/ui/asn/13335`
       - `https://peeringdb-plus.fly.dev/ui/compare`

       Fill the URL table in UAT-RESULTS.md with violation counts (expected: 0, 0, 0).

    4. **Decide verdict and finalize secret state:**
       - **Zero violations on all 3 URLs + Tailwind v4 OK:** UAT-01 PASS. Leave `PDBPLUS_CSP_ENFORCE=true` set. (Optional follow-up: a future quick task can flip the in-code default to `true` so this is the new project default; out of scope for this plan.)
       - **Any violations:** UAT-01 FAIL. Roll back:
         ```
         fly secrets unset --app peeringdb-plus PDBPLUS_CSP_ENFORCE
         ```
         (or `fly secrets set ... PDBPLUS_CSP_ENFORCE=false`). Paste the violation list verbatim into the `### Final operator entry` block, redacting any session token, ingress IP, OTel trace ID, or path containing PII. The fix becomes a follow-up phase.

    5. **Fill the operator entry block:** date, operator handle (NO email per `feedback_no_pii_in_repo.md`), browser version, final secret state (`true`/`false`/`unset`), verdict (`PASS`/`FAIL`), violations list (if any).

    6. **Update the status line at the bottom of `## UAT-01` from `AWAITING OPERATOR` to `CLOSED`.**

    7. **Commit:** `docs(78-02): UAT-01 — operator DevTools sweep PASS / FAIL` (use whichever verdict applies).

    PII / threat-model rules:
    - Do NOT paste DevTools screenshots into the repo. Text-only.
    - REDACT operator email (`dotwaffle@gmail.com`), Grafana stack URL (`*.grafana.net`), session tokens, ingress IPs, OTel trace IDs from any pasted output.
    - Use a generic operator handle like `matthew` or `mw` for the `Operator:` field — NOT an email address.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; test "$(grep -c 'AWAITING OPERATOR' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -eq 0 &amp;&amp; test "$(grep -c 'CLOSED' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -ge 2 &amp;&amp; test "$(grep -c 'dotwaffle@gmail.com\|grafana.net' .planning/phases/78-uat-closeout/UAT-RESULTS.md)" -eq 0</automated>
  </verify>
  <resume-signal>
    Once UAT-RESULTS.md `## UAT-01` is filled in (verdict PASS or FAIL captured, status line updated to `CLOSED`, commit landed), reply with `done` (or `done — PASS` / `done — FAIL` for clarity). If UAT-01 FAIL, also describe whether a follow-up phase should be created or whether the violations are minor enough to ignore (operator's call).

    If you hit a deployment problem (secret didn't propagate, DevTools shows infrastructure-level error not a CSP violation, Fly is down), reply with the symptom and abort — we'll handle it as a separate issue.
  </resume-signal>
  <acceptance_criteria>
    Post-resume, when the executor checks acceptance:
    - `grep -c 'AWAITING OPERATOR' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns 0 (status flipped).
    - `grep -c 'CLOSED' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns ≥ 2 (one for UAT-01, one for UAT-02 from 78-01).
    - The URL table has all 3 rows populated with non-placeholder values (no remaining `&lt;count&gt;` literals).
    - `grep -c 'dotwaffle@gmail.com\|grafana.net' .planning/phases/78-uat-closeout/UAT-RESULTS.md` returns 0.
    - The `Final operator entry` block has Date, Operator, Browser, Final secret state, Verdict all filled.
    - If verdict is PASS, `fly secrets list --app peeringdb-plus | grep CSP_ENFORCE` shows the secret is set (operator's responsibility to confirm; not a Claude-side check).
    - The Tailwind v4 row in the table has `yes` in the "Tailwind v4 OK?" column (or the verdict explicitly addresses why it's not — original Phase 52 concern is the JIT runtime).
  </acceptance_criteria>
  <done>UAT-RESULTS.md `## UAT-01` filled in with operator's verdict (PASS or FAIL), status line flipped to `CLOSED`, secret state finalized, commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator browser → production | Chrome DevTools session against `https://peeringdb-plus.fly.dev/` UI. |
| operator workstation → repo | DevTools findings pasted into `UAT-RESULTS.md` and committed. |
| operator workstation → Fly platform | `fly secrets set/unset` modifies the live deployment's environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-78-06 | I (Information disclosure) | DevTools Network panel responses pasted into UAT-RESULTS.md | mitigate | Operator step-list explicitly directs redaction of session tokens, ingress IPs, OTel trace IDs, and any URL containing PII. PII guard `grep -c 'dotwaffle@gmail.com\|grafana.net' UAT-RESULTS.md == 0` enforces no email or Grafana stack URL leaks. Acceptance criterion checks. |
| T-78-07 | I | DevTools screenshots committed accidentally | mitigate | Operator step explicitly says "Text-only. Do NOT paste DevTools screenshots." Per `feedback_no_pii_in_repo.md` memory. |
| T-78-08 | D (Denial of service) | `PDBPLUS_CSP_ENFORCE=true` causes production /ui/ to break for real users | mitigate | Service is in tech-demo state with no scheduled traffic source (per planning context — "no public traffic"). The blast radius of an enforcing-mode CSP violation is the operator's own browser session, not user-facing breakage. If FAIL: rollback command (`fly secrets unset`) is in the step-list with the same SLA as the original `set`. |
| T-78-09 | T (Tampering) | Operator forgets to flip header back to `Report-Only` after a FAIL verdict | accept | Operator's responsibility per resume-signal. Worst case is /ui/ remains partially-broken for the operator's next session — acceptable risk for a tech-demo-state service. The next dev session will surface the issue immediately on next page load. |
| T-78-10 | E (Elevation of privilege) | `PDBPLUS_CSP_ENFORCE=true` becomes the new default and silently breaks future Tailwind upgrades | accept | UAT-01 captures the verdict at one point in time. Future Tailwind/CDN upgrades fall under the existing CI drift gates and v1.13 phase 52's regression locks (`internal/middleware/csp_test.go`). Re-running UAT-01 is a future operator's decision when the upgrade lands. |

## Mitigations enforced by acceptance criteria

- T-78-06 / T-78-07: `grep -c 'dotwaffle@gmail.com\|grafana.net' UAT-RESULTS.md` returns 0.
- T-78-08: rollback command embedded in step-list at line where it matters; `fly secrets unset` is documented.
</threat_model>

<verification>
- `.planning/phases/78-uat-closeout/UAT-RESULTS.md` `## UAT-01` section is fully populated (no AWAITING OPERATOR, no `&lt;count&gt;` placeholders).
- Operator verdict (PASS or FAIL) recorded.
- If PASS: `PDBPLUS_CSP_ENFORCE=true` left set in production secrets.
- If FAIL: secret rolled back to default (unset or `false`); follow-up surfaced.
- No PII in committed file.
- Status line says `CLOSED`.
</verification>

<success_criteria>
1. UAT-01 step-list created in UAT-RESULTS.md before the operator checkpoint.
2. Operator checkpoint completes with PASS or FAIL verdict captured.
3. Secret state (`PDBPLUS_CSP_ENFORCE`) reflects the verdict (true if PASS, unset/false if FAIL).
4. UAT-RESULTS.md `## UAT-01` section has all required fields filled, no placeholders, no PII.
5. Status line transitions from `AWAITING OPERATOR` to `CLOSED`.
</success_criteria>

<output>
After completion, create `.planning/phases/78-uat-closeout/78-02-SUMMARY.md` capturing:
- The operator's verdict (PASS or FAIL)
- Per-URL violation counts (the table contents)
- Tailwind v4 JIT runtime confirmation (per the original Phase 52 concern)
- Final state of `PDBPLUS_CSP_ENFORCE` secret
- Whether a follow-up phase is needed (only if FAIL)
</output>
