---
phase: 78-uat-closeout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.5-phases/20-deferred-human-verification
  - .planning/STATE.md
tags: [archive, planning-hygiene, uat]
autonomous: true
requirements: [UAT-03]

must_haves:
  truths:
    - "The directory `.planning/milestones/v1.5-phases/20-deferred-human-verification/` no longer exists at its original path"
    - "An archived sibling exists at `.planning/milestones/v1.5-phases/20-deferred-human-verification.archived/` containing all 7 original files (20-01-PLAN.md, 20-02-PLAN.md, 20-03-PLAN.md, 20-CONTEXT.md, 20-DISCUSSION-LOG.md, 20-RESEARCH.md, 20-VERIFICATION-ITEMS.md)"
    - "The relocation preserves git history (`git log --follow` on any moved file shows the pre-move commits)"
    - "STATE.md `## Outstanding Human Verification` section does not reference v1.5 / Phase 20 (already absent — verify, do not re-add)"
    - "The 26 verification items captured in the original `20-VERIFICATION-ITEMS.md` are NOT re-verified — per CONTEXT.md D-02, trust the 2026-03-24 record"
  artifacts:
    - path: ".planning/milestones/v1.5-phases/20-deferred-human-verification.archived/"
      provides: "archived snapshot of the v1.5 Phase 20 deferred-human-verification dir; closes the stale-pointer issue"
      contains: "20-VERIFICATION-ITEMS.md"
  key_links:
    - from: ".planning/STATE.md ## Outstanding Human Verification"
      to: ".planning/milestones/v1.5-phases/20-deferred-human-verification.archived/"
      via: "absence — STATE.md no longer references v1.5 / Phase 20 anywhere in the outstanding-verification context"
      pattern: "Phase 20|v1.5"
---

<objective>
Mechanically relocate the stale `.planning/milestones/v1.5-phases/20-deferred-human-verification/` pointer dir to a `.archived/` sibling, preserving git history. Verify (do not re-add) that STATE.md `## Outstanding Human Verification` does not list v1.5/Phase 20.

Purpose: Closes UAT-03. CONTEXT.md D-02 is unambiguous: the 26 v1.5 items were verified on 2026-03-24; trust that record. This plan is purely planning-hygiene — no production code, no test changes, no re-verification.
Output: `git mv`-ed directory at the archived path; STATE.md unchanged in content but explicitly verified.
</objective>

<execution_context>
@.claude/skills/

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-uat-closeout/CONTEXT.md

<interfaces>
<!-- Existing dir contents (verified 2026-04-27): -->

```
.planning/milestones/v1.5-phases/20-deferred-human-verification/
├── 20-01-PLAN.md
├── 20-02-PLAN.md
├── 20-03-PLAN.md
├── 20-CONTEXT.md
├── 20-DISCUSSION-LOG.md
├── 20-RESEARCH.md
└── 20-VERIFICATION-ITEMS.md
```

Archive convention (from `.planning/seeds/consumed/`):
- Sibling-style naming with `consumed/` directory holding the consumed file (e.g. `seeds/consumed/SEED-002-fly-asymmetric-fleet.md`).
- For directories, the equivalent is appending `.archived/` to the dir name (per CONTEXT.md D-02 specifying "consumed-style archive location" with the example path `.planning/milestones/v1.5-phases/20-deferred-human-verification.archived/`).

`memory/project_human_verification.md` (the auto-memory file, NOT in repo — at `~/.claude/projects/-home-dotwaffle-Code-pdb-peeringdb-plus/memory/project_human_verification.md`):
- Line 7: "## Phase 20 (v1.5) — COMPLETE / All 26 items from v1.2-v1.4 verified against live deployment on 2026-03-24."
- This record is the authoritative source for "no re-verification needed" per D-02.

`STATE.md ## Outstanding Human Verification` (lines 108-116): currently lists Phase 52 / Phase 53 (under UAT-01/UAT-02) and Phase 20 (under UAT-03). After UAT-03 closes, the Phase 20 line should be removed/updated; the audit trail is the link to `20-deferred-human-verification.archived/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-mv the dir + verify STATE.md does not need re-edit beyond the Phase 20 line</name>
  <files>
    .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (full file — must NOT clobber any unrelated section; only the `## Outstanding Human Verification` section is in scope, and only the Phase 20 line)
    - .planning/phases/78-uat-closeout/CONTEXT.md (D-02 — verify "no re-verification" rule)
    - List the source dir contents to confirm 7 files exist before move:
      `ls .planning/milestones/v1.5-phases/20-deferred-human-verification/`
  </read_first>
  <action>
    Step 1 — verify source dir exists and contains 7 files:
    ```
    test -d .planning/milestones/v1.5-phases/20-deferred-human-verification
    test "$(ls .planning/milestones/v1.5-phases/20-deferred-human-verification | wc -l)" -eq 7
    ```

    Step 2 — `git mv` the directory to the archived path. Use `git mv` (NOT `mv` followed by `git rm -r` + `git add`) so git tracks the rename and `git log --follow` continues to work:
    ```
    git mv .planning/milestones/v1.5-phases/20-deferred-human-verification .planning/milestones/v1.5-phases/20-deferred-human-verification.archived
    ```
    Note: `git mv` of a directory works on git ≥1.7; if the operator's git is older or this fails, fall back to:
    ```
    mkdir -p .planning/milestones/v1.5-phases/20-deferred-human-verification.archived
    git mv .planning/milestones/v1.5-phases/20-deferred-human-verification/*.md .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/
    rmdir .planning/milestones/v1.5-phases/20-deferred-human-verification
    ```

    Step 3 — confirm move succeeded:
    ```
    test ! -d .planning/milestones/v1.5-phases/20-deferred-human-verification
    test -d .planning/milestones/v1.5-phases/20-deferred-human-verification.archived
    test "$(ls .planning/milestones/v1.5-phases/20-deferred-human-verification.archived | wc -l)" -eq 7
    git status -s | grep -c 'renamed: .*20-deferred-human-verification'
    ```

    Step 4 — read the current STATE.md `## Outstanding Human Verification` section (lines 108-116). Confirm it currently includes a Phase 20 / v1.5 reference (line 114 in the snapshot read earlier: `- **Phase 20 (v1.5):** stale deferred-items pointer dir relocation — under UAT-03`). After UAT-03 closes, this line is no longer "outstanding" — it's done.

    Update the section to REMOVE the Phase 20 bullet line, keeping the Phase 52 and Phase 53 lines (those close in plans 78-02 and 78-01 respectively, but their STATE.md disposition is updated by the verifier at end-of-phase, NOT here). Be surgical — Edit, not Write. Pattern:

    Before:
    ```
    - **Phase 52 (v1.13):** Chrome devtools CSP check on `/ui/`, `/ui/asn/13335`, `/ui/compare` — under UAT-01
    - **Phase 53 (v1.13):** curl HSTS / X-Frame-Options / X-Content-Type-Options headers, 2 MB body-cap REST vs gRPC skip-list, slowloris TCP smoke test — under UAT-02
    - **Phase 20 (v1.5):** stale deferred-items pointer dir relocation — under UAT-03
    ```

    After:
    ```
    - **Phase 52 (v1.13):** Chrome devtools CSP check on `/ui/`, `/ui/asn/13335`, `/ui/compare` — under UAT-01
    - **Phase 53 (v1.13):** curl HSTS / X-Frame-Options / X-Content-Type-Options headers, 1 MB body-cap REST vs gRPC skip-list, slowloris TCP smoke test — under UAT-02
    ```

    Note: also fix the "2 MB" to "1 MB" while editing the Phase 53 line — the actual cap is 1 MB (`maxRequestBodySize = 1 << 20`); the "2 MB" in the current STATE.md is a documentation drift bug. This is an opportunistic fix, traceable to the same line being edited.

    Step 5 — also append an "Archived" note immediately below the bullet list, OR a new sub-section, so future readers can find the archived path:
    ```
    Archived (closed by Phase 78):
    - **Phase 20 (v1.5)** — relocated to `.planning/milestones/v1.5-phases/20-deferred-human-verification.archived/` (UAT-03, closed &lt;date&gt;). Original 26 items verified 2026-03-24 per memory `project_human_verification.md`.
    ```

    Step 6 — commit:
    ```
    git add .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/
    git add .planning/STATE.md
    git commit -m "$(cat &lt;&lt;'EOF'
    docs(78-03): UAT-03 — archive v1.5 Phase 20 deferred-human-verification dir

    Per CONTEXT.md D-02, the 26 items were verified 2026-03-24 (memory
    project_human_verification.md) — relocate dir, update STATE.md to point
    at the archived path, fix incidental 2 MB→1 MB body-cap doc drift.

    EOF
    )"
    ```

    DO NOT modify any of the 7 archived files (they are point-in-time records). DO NOT re-verify the 26 items.
  </action>
  <verify>
    <automated>cd /home/dotwaffle/Code/pdb/peeringdb-plus &amp;&amp; test ! -d .planning/milestones/v1.5-phases/20-deferred-human-verification &amp;&amp; test -d .planning/milestones/v1.5-phases/20-deferred-human-verification.archived &amp;&amp; test "$(ls .planning/milestones/v1.5-phases/20-deferred-human-verification.archived | wc -l)" -eq 7 &amp;&amp; git log --follow --format='%H' -- .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/20-VERIFICATION-ITEMS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Source dir gone: `test ! -d .planning/milestones/v1.5-phases/20-deferred-human-verification` returns 0.
    - Archived dir exists with all 7 files: `test "$(ls .planning/milestones/v1.5-phases/20-deferred-human-verification.archived | wc -l)" -eq 7`.
    - All 7 file names match the original (20-01-PLAN.md, 20-02-PLAN.md, 20-03-PLAN.md, 20-CONTEXT.md, 20-DISCUSSION-LOG.md, 20-RESEARCH.md, 20-VERIFICATION-ITEMS.md): `for f in 20-01-PLAN.md 20-02-PLAN.md 20-03-PLAN.md 20-CONTEXT.md 20-DISCUSSION-LOG.md 20-RESEARCH.md 20-VERIFICATION-ITEMS.md; do test -f .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/$f || exit 1; done`.
    - Git rename detected: `git status -s | grep -c 'renamed:.*20-deferred-human-verification'` returns ≥ 1 (or `git diff --cached --stat | grep -c rename` ≥ 1 if already committed).
    - History preserved: `git log --follow --oneline -- .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/20-VERIFICATION-ITEMS.md | wc -l` returns ≥ 2 (at least one pre-move commit, plus the rename commit).
    - STATE.md no longer has the Phase 20 / v1.5 outstanding bullet: `test "$(grep -c 'Phase 20.*v1.5.*stale.*pointer' .planning/STATE.md | grep -v archived)" -eq 0` (i.e. the bullet form is gone — only the new "Archived" reference remains).
    - STATE.md gained an "Archived" reference: `grep -c '20-deferred-human-verification.archived' .planning/STATE.md` returns ≥ 1.
    - 2 MB → 1 MB doc drift fixed: `grep -c '2 MB body-cap' .planning/STATE.md` returns 0; `grep -c '1 MB body-cap' .planning/STATE.md` returns ≥ 1.
    - The 7 files inside the archived dir are byte-identical to pre-move (verify by `git log --oneline -1 .planning/milestones/v1.5-phases/20-deferred-human-verification.archived/20-VERIFICATION-ITEMS.md` showing only the rename, no content edits).
  </acceptance_criteria>
  <done>Dir relocated with git history; STATE.md updated to point at archived location and fix the 2 MB doc drift; commit lands; ready for verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repo working tree → git history | `git mv` operation must preserve rename detection so `git log --follow` works for the 7 files. |
| STATE.md edits → planning-tree consumers | Surgical Edit on `## Outstanding Human Verification` section only; must not clobber `## Recently Shipped` or `## Session Continuity`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-78-11 | T (Tampering) | Accidental content modification of the 7 archived files during the move | mitigate | Acceptance criterion verifies `git log --oneline -1` shows ONLY the rename commit, no content edits. Use `git mv`, NOT `cp + rm + git add`. |
| T-78-12 | T | STATE.md gets clobbered (lose `## Session Continuity` or `## Recently Shipped`) | mitigate | Use the Edit tool with a precise pattern match on the Phase 20 bullet line + an Edit on the section header to add the "Archived" sub-block. NOT Write. Test: post-edit `wc -l .planning/STATE.md` should be roughly the same count (within ±5 lines for the additions). |
| T-78-13 | I (Information disclosure) | Archived files contain operator PII | accept | The 7 files are pre-existing planning artifacts from v1.5 (March-April 2026 timeframe). They have already been committed; archiving them does not create new PII surface. If they contained PII, that's a separate audit task — not in UAT-03 scope per CONTEXT.md D-02. |
| T-78-14 | R (Repudiation) | git history broken so future audits cannot trace the move | mitigate | `git mv` preserves rename; acceptance criterion verifies `git log --follow` returns ≥ 2 entries on a representative file. If `git mv` fails (e.g. older git), fallback in the action uses per-file `git mv` which still triggers rename detection. |

## Mitigations enforced by acceptance criteria

- T-78-11: `git log --oneline -1 ... | wc -l == 1` AND no content edits in the move commit.
- T-78-12: STATE.md line count check; surgical Edit not Write.
- T-78-14: `git log --follow --oneline ... | wc -l >= 2`.
</threat_model>

<verification>
- Source dir absent; archived dir present with all 7 files.
- `git log --follow` returns multi-entry history for any of the moved files.
- STATE.md no longer lists v1.5 / Phase 20 as outstanding.
- STATE.md gained an "Archived" reference pointing at the new path.
- Incidental "2 MB" → "1 MB" body-cap doc drift fixed in the same edit.
- No content edits to the 7 archived files.
</verification>

<success_criteria>
1. Directory relocated from `.planning/milestones/v1.5-phases/20-deferred-human-verification/` to `.planning/milestones/v1.5-phases/20-deferred-human-verification.archived/` via `git mv`.
2. All 7 files present at the new path; byte-identical to pre-move.
3. Git rename detection works: `git status -s` shows `renamed:` for the move; `git log --follow` works on at least one moved file.
4. STATE.md `## Outstanding Human Verification` section no longer lists Phase 20 (v1.5).
5. STATE.md gained an Archived sub-section referencing the new path.
6. STATE.md "2 MB body-cap" doc drift fixed to "1 MB body-cap" (incidental fix).
7. No re-verification of the 26 items; no content edits to the archived files.
</success_criteria>

<output>
After completion, create `.planning/phases/78-uat-closeout/78-03-SUMMARY.md` capturing:
- Source path → destination path (with file count)
- `git mv` rename detected (yes/no)
- STATE.md edits made (Phase 20 bullet removed; Archived sub-block added; 2 MB→1 MB drift fix)
- Confirmation that the 26 items were NOT re-verified per CONTEXT.md D-02
- Any deviations (e.g. if `git mv` of a dir failed and fallback per-file was used)
</output>
